# 验证与审计边界

## 标签验收

任务须为原生 `Passed`、完整 observation、`combatEnded`，且实际初始构筑、遗物顺序、计数、目标、教师与请求一致。`damage_model/observation.py` 逐张核对升级、附魔和持久状态；协议 3 缺少实际状态回读即拒绝。异常、超时、清理期限错误和隔离任务不进入标签。

生命值必须为整数、满血开战，终局 HP 在合法边界内，`netHpLoss = initialHp - finalHp`，死亡标记与 HP=0 一致。生产起始 70/70；战斗回血抵消净掉血。

最大生命改变仅允许已适配的下降模式：目标为单只咬人卷轴的弱/普通编组，或实际初始牌组含至亮之焰；下降须为偶数，或触及最小值 1。这是字段与机制模式校验，尚不是逐事件追踪证明每次最大生命变化的来源。不能据此宣称任意改血机制均已支持。

## 测试范围

Python 测试覆盖来源历史还原、计数与先古合法性、特殊卡状态、队列租约/去重/原子写入、重试隔离、备份、谱系和训练划分。原生烟测和差分用于验证游戏恢复与终局；少量场景通过不代表所有牌、遗物和怪物组合全量正确。

历史验收材料保留在部署副本的 `evidence/`，如 `card-state-20260910/`、`orobas-replacement-20260910/`、`shared-ancients-20260910/`、`source-versions-20260913/`。这些是当次代码、配置和运行环境的证据，不能把旧测试数写作当前分支重新执行的结果。游戏、原生附件及多数 evidence 不随普通 Git checkout 获得。

已纳入仓库的两批固定训练报告，见[训练文档](../train/README.md)。留出误差说明快照内泛化表现，不能证明最优搜索、未见卡牌全覆盖或下一版教师同样准确。

## 运行与数据检查

生产执行目录为 `/data1/pl/ImageTask/wxq/Projects/Sts2Bot`。运行检查应核对部署版本、冻结教师、实际进程与锁、近期有效产出、备份摘要和 SQLite quick_check。正常库状态与历史补采状态分别读取，操作入口见[持续采集](continuous-collection.md)和[补采说明](../tools/one_off/README.md)。

历史 evidence 中的迁移监控或恢复脚本只证明当时的检查范围，不能默认覆盖新增专项库、补采库或不同运行配置。运行正常也不表示历史附件完整；完整标签、去重索引和故障附件需要分别核验。

## 已知历史附件缺口

旧盘故障后，任务 `c45cc81b4ae9f6cdc47d4516f788c0861bd244d514ebda2f08153fe18f5485ac` 的 attempts 62457、62524、62533 仍保留数据库失败记录和请求，但对应独立 `failure.json` / `native.log` 附件缺失。清单位于部署证据 `evidence/reconnect-20260913-1309/missing-native-artifacts.json`。

这些失败不计入标签。保留隔离与缺口记录，不自动重打来替代旧证据，不伪造附件，也不把后续运行正常写成历史完整审计恢复。原始完整审计基线与迁移后的运行检查分别保留。
