# 验证记录（2026-09-05）

在 01 的原版 Windows 游戏 + Wine 11.0 上完成 5 场完整独立对战，原始起始构筑与结果见 `evidence/pilot.json`。

| 场景 | 结果 | runId |
|---|---|---|
| 初始静默、A10、同一弱敌、种子 1 | 70 → 69，掉血 1 | d7d098f4568c42e99626b864b716f765 |
| 同一构筑、种子 2、独立重置 | 70 → 65，掉血 5 | ea0fa6143d9345d0a0166be13a95c5d6 |
| 仅防御牌的死亡样本 | 70 → 0，掉血 70，死亡 true | 67a23fb08f27477382a593237f569b11 |
| 原始卡组加 BOLAS、ABRASIVE、Pen Nib=9、Happy Flower=2 | 70 → 70，计数回读一致 | 17f11ca073ef4125b6607b5decdb427f |
| 保留第一幕先古遗物并增加 TOASTY_MITTENS，独立对战 HIVE / BOWLBUGS_WEAK | 70 → 0，原版第二幕编组、死亡 true | a1a65293b7e14998a3aa7b47b0c929fe |

这证明采集协议、死亡标签、输入恢复以及两个幕的基本入口，不代表所有卡牌、遗物、怪物组合已验收。第二场与后续场次复用隔离进程，完整对战约 5–7 秒；首次启动约 49–58 秒，这些不是总体吞吐保证。

服务器 Python 测试 18 项通过（17 项全测，新增留出集补齐测试另测），包括：采样边界、无色牌 0–5、非法先古组合/计数拒绝、弱样本保留、不完整样本排除、租约失效重领与旧提交拒绝、谱系去重、缺目标禁止剪枝、特征编码、真实数据不足拒绝训练、隔离合成数据训练/保存/加载、运行时 SHA-256 不匹配拒绝、补齐留出谱系。合成数据只在测试临时目录。

CombatSolver Release 编译 0 警告、0 错误；结构检查 `REFACTOR_BOUNDARIES_OK search_files=61`。没有改求解器搜索、人工权重或生产游戏安装。

排查中产生的失败记录保留在 `data/pilot.sqlite`，未进入正式训练库。曾发现并修复：Wine 禁用 mscoree、ActModel 被重复转为可变模型、测试默认只接受胜利、隔离存档教学弹窗阻塞。首次精英场因教学弹窗达到 120 秒超时后，转用小场景验证，没有把超时上限调高。

当前还没有充分真实数据训练完成的评分模型。正式队列与试运行数据库隔离，训练结果要看留出卡组上的误差后再决定是否可用于评分。

首批正式队列：72 个卡组（训练 55、验证 14、测试 3），共 6080 场，均为当前第一幕独立目标，每对 4 个种子。一场正式数据已通过完整校验，掉血 3。余下任务由有界后台批次继续执行；完整采集后尝试训练第一个基线，遇到异常会保留现场并停止。此初始种群仍接近基础卡组，不能用来声称已覆盖 45 张卡的空间；后续应先扩大变异代数再评估泛化。
