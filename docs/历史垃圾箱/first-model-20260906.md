# 首版真实数据模型

2026-09-06 01:21（北京时间），首批 6,080 场有效对战已完成训练和模型一致性验证。服务器模型位于 `checkpoints/v1/model.pt`；`validation.json` 记录指标和训练集拟合的简单基线。模型 SHA-256 为 `cfe1605a96165495300df63d7c3b4dfb901f2b4599ea896316045561b07649c4`。本机同目录保存了文件副本并核对散列。详细证据在 `evidence/first-model-20260906.json`。

数据口径仍为静默猎手、进阶 10、满血开战、无药水、独立原版怪物编组，固定同一个 2 秒短搜索老师；有效死亡结果保留。恢复代码只处理已确认的运行环境崩溃，未替换老师 DLL，也未混入旧老师库。

72 个卡组共 11 个谱系；训练/验证/测试分别为 55/14/3 个卡组、8/2/1 个谱系，4,656/1,160/264 场。每对卡组/目标有 4 个种子。以下 MAE 比较的是每对卡组/目标的平均净掉血，单位为生命值；Brier 衡量死亡概率误差，越小越好。

| 留出集 | 模型平均掉血 MAE | 按目标平均基线 MAE | 死亡概率 Brier |
| --- | ---: | ---: | ---: |
| 验证 | 3.947 | 4.488 | 0.02735 |
| 测试 | 6.147 | 5.933 | 0.06511 |

按目标平均基线只使用训练谱系，忽略卡组与遗物差异。首版模型在验证集略好，测试集比基线差约 0.214 HP，不能据此宣称已学会可靠评价构筑。测试集只有 3 个卡组且同属 1 个谱系，结论还很不稳定。逐场 MAE 为验证 5.330 HP、测试 7.314 HP，其中包括种子造成的战斗随机性。

当前覆盖第一幕两个地图的 42 个怪物编组：OVERGROWTH 22 个、UNDERDOCKS 20 个。初始样本仅 11–15 张牌，合计出现 34 种卡牌和 13 种遗物；尚未覆盖第二/三幕、大卡组和大部分卡牌/遗物组合。该版本用于确认数据、训练、验证和推理流程，暂不调整人工评分。

首轮后控制器自动从完整评估的父代生成了 20,288 场新任务，8 worker 已开始并持续产出；首版文件和全部已有样本保留。每完成一轮后在累计数据上另存新模型，不覆盖首版。首版一致性检查通过且第二批已有成功结果后，按用户要求将本机巡检改为每小时一次；服务器控制器仍每 15 秒检查本地状态。
