# 服务器维护与本地体验

租用机已不再使用。当前采集在 01 上运行，说明见 [切回01](return-to-01.md) 与 [8 秒搜索](search-8s.md)。原 VANTOM finalizer 任务已在租用机第 3 次尝试获得原生 Passed，完整终局为 70 → 0；原失败证据保留。

以下为 2026-09-06 迁到租用机之前的历史记录，不能按这些资源条件自动重启。

2026-09-06 11:30 左右按用户要求改为 **4 worker / 4 GiB 预留 / 每 worker 2 GiB 启动预算**，代码 `daa52ae`，服务器 20 项控制器测试通过。四路池已真实启动并由持续控制器接管，但四个游戏都在初始化 `game_startup` 阶段达到原有 worker 截止时间；没有进入战斗，没有新增有效标签。全机 memory PSI full avg60 约 50%，表明严重内存等待。池和控制器已按故障保护退出；仍保留全部 7,052 个原成功任务及不变的首版模型，四个 Timeout 作为失败证据保留，不能声称现在仍在采集。见 `evidence/four-worker-startup-20260906.json`，参数及命令见服务器 `data/resource-profile.json`。

本次四个超时已经核对当前 runId 的日志阶段仅为 game_startup。既有巡检保持每 10 分钟；可用内存至少 12 GiB 且 memory PSI full avg60 低于 10，需在间隔至少 60 秒的两次检查都满足，再重新诊断并允许一次受控恢复。保留原 attempts，不超过每任务总计 3 次，不扩大到未知战斗超时、不修改搜索老师。原 finalizer 任务目前是第 2 次尝试在启动阶段超时，仍未完成原生复跑验证。恢复产出并核对该任务后才重新计算连续稳定 60 分钟；相同压力等待不重复通知。此段为当前状态，下方 8 worker / 32 GiB 为历史记录。

第二批累计 7,052 场时，`VANTOM_BOSS` 的 runId `25f4ef640f074212aac6efff936a7db6` 在本请求清理阶段触发 `GodotObject.Dispose/Finalize` 的 `0xC0000005`，没有结果协议。代码 `9c87e08` 加入严格阶段/调用栈匹配的有限重试，并增加 attempts 时间范围与任务索引；17 项定向测试通过，近期统计测得约 1.8 秒。原失败尝试和 7,052 个有效样本全部保留，该任务已排回队列，尝试次数仍为 1，尚待原生重跑验证。

随后恢复启动因服务器可用内存降至约 15 GiB 被保护机制阻止。代码 `4efe50f` 让持续控制器在已确认的内存保护退出后保持 `waiting_memory`，每 15 秒检查，8 worker 启动要求 56 GiB 可用；12 项控制器测试通过，包括等待后继续、拒绝掩盖失败样本/孤立租约及陈旧 PID。控制器已在真实服务器进入等待状态，首版模型散列保持不变。当前采集暂停，不能把“控制器活着”当作“仍在产出”。巡检恢复为每 10 分钟；资源恢复且待重跑任务完成后，再观察至少 60 分钟稳定产出才改回每小时。证据见 `evidence/finalizer-cleanup-recovery.json`，运行通知状态见服务器 `data/monitor-milestones.json`。

2026-09-06 凌晨巡检在 4,092 个有效样本时发现原生菜单清理断言导致池停止。失败目标 `NIBBITS_NORMAL`，runId `f6fc350ec4014f02b6d8bc2f08d4e70d`；战斗结束后的输入设置空数组越界，结果协议尚未完成。Python 恢复代码 `268484b` 新增严格限定当前请求清理阶段的识别与最多 3 次尝试，11 项定向测试通过。保留原失败尝试，重排同一任务后第 2 次原生对战成功：runId `40e2b2b25ecb4c0e89b5990dc71e0d5d`，70 → 32 HP，净掉血 38，未死亡；老师 DLL 仍为 `9394e5c3…`。8 worker 与持续控制器均已恢复，全部原有 4,092 个成功任务保持不变。完整证据见 `evidence/menu-cleanup-recovery.json`。

用户随后要求首批 6,080 场训练之后继续整晚采集。新增服务器常驻接续控制器；检查先维持 10 分钟，首版模型通过验证、下一批已产出成功样本后改为每小时。运行状态、模型版本和停止方式见 [continuous-collection.md](continuous-collection.md)。

2026-09-05 设置本任务每 10 分钟检查一次 01 上的采集与训练。使用 `data/active-collection.json` 解析活动库，检查真实进程与最近产出，正常时静默；中断、重要修复、需要用户处理或训练完成时通知。监控不操作本地体验局。该定期检查通过本机应用执行，需要应用能够运行任务且 SSH 01 可达。

工作目录清理释放 1,038,776,896 字节（约 1.04 GB）。删除已解压的 Wine 安装包、`.local` 旧传输暂存文件、已解压源码压缩包和已由 `SlayTheSpire2.slim.exe` 替代的原始带调试信息 EXE。删除前核对全部 8 个运行配置引用 slim EXE，所有目标都解析到项目根以内，并检查进程打开文件与映射没有引用目标。保留所有数据库、日志、证据、当前源码、原版 PCK/原生 DLL、Wine 运行库、活动 prefix 和扩容模板。完整原始 EXE 仍在用户本地游戏目录。清单见 `evidence/cleanup-20260905.json`。

本地 `D:/sljt2_108814/sljt2_108814/mods/CombatSolver` 已安装包含 `33babbe` 行为修复的 DLL 和 NOTICE，原 DLL 备份在 `D:/Sts2DamageModelRuntime/visible-tools-20260905/mod-backup`。未更改用户原存档。独立体验局位于同一目录的 `roaming/SlayTheSpire2`：静默 A10 对骇鳗，初始卡组加必备工具，原生满血 70，2 秒短搜。可见启动请求 `visible-tools-20260905` 成功停在初始搜索结果，随后画面及原生日志观察到必备工具出牌和多次自动弃牌。该体验局不进入训练集。

维护期间捕获一例 Wine 原生页错误：`PHANTASMAL_GARDENERS_ELITE`，runId `3ca17087ffbe4afdb9c7af4b204fd3cc`，没有终局结果，采集在 841 个有效样本时停止。已扩展窄故障恢复，只在确认进程退出并观察到 Wine 页错误签名时重启，仍限制每任务 3 次尝试；未知退出不重试。保留原失败尝试并重排该任务，841 个标签保留。专属进程组的清理也覆盖已崩溃的父进程，避免遗留 Wine 调试器。定向测试 6 项通过，覆盖签名判断/无签名拒绝、重试上限、复用进程与旧日志隔离、退出信号与停止边界。证据见 `evidence/wine-crash-recovery.json`。
