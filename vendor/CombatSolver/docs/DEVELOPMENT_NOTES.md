# CombatSolver 开发笔记与未来构想

## 搜索末尾的回合开始选牌（2026-09-05，采集任务分支）

静默 A10 采集在同一个带必备工具的卡组上发生 8 次超时，涉及骇鳗、灵魂异鱼和瀑布巨人。最后一个 `EndTurn` 已包含下一回合的弃牌选择，但 `TryGetPlannedTurnSetupChoices` 要求下一回合还有缓存 continuation，导致原生页面无人接管。现在先重放已经计算好的选择，进入 Play 后由原有逻辑识别 continuation 缺失并重新搜索；人工评分和搜索预算未改。UI snapshot 同时支持只有开始选择、没有后续出牌路线的末尾边界，战损显示未知。

01 原生 Wine 最小边界观察 `horizon-fixed-boundary-20260905`：第 6 回合原生必备工具选中了计划的打击，随后 `TURN_SETUP_PLAN_REPLAYED` 和 `SEARCH_REUSE_MISS reason=continuation_missing` 连续出现。观察到边界后结束专用进程，未生成训练标签，不冒充完整战斗 Passed。第一次只移除 Runtime 限制的中间版本暴露 UI 对完整路线的相同假设，未保留为最终实现。最终 Release 0 警告/错误，结构门禁通过；可见 UI 未验收。

恢复正式采集时优先重跑原失败的 8 场，全部获得原生 Passed 和完整训练观察，真实死亡净掉血均为 70，终局第 11–25 回合。代表骇鳗 runId `363bdc4eba624f12a4c13b36f6af7a8c`、灵魂异鱼 `cbc112d49dcf46b1bb3c69f42888b93a`、瀑布巨人 `647febabea094eabac089b5a6bb54d65`。这是部署衔接与有效终局验证，不声称整场零重算或搜索最优。新 DLL 的标签使用独立数据库，旧 918 场保留。

## 独立掉血模型采集协议（2026-09-05，任务分支）

新增 `TrainingCollection / TrainingActId`，供独立静默 A10 数据采集器使用：在原版开战前构造完整卡组、按顺序获得遗物并恢复计数，满血开战，保留指定幕的原版遭遇编组、房间类型与敌人生命。禁止训练请求混入手牌、生命、敌方行动、Power 或快照覆盖。常规无人测试默认行为保持原样。

`Training.cs` 在原版开战前记录实际卡组、遗物序列化状态与指定计数，在战斗结束、返回菜单前记录最终生命、最大生命、净掉血与死亡；`Writer` 输出观察值。训练模式接受真实死亡终局，未结束或异常不生成完整训练标签。搜索、人工分数和预测引擎未改。

01 上使用复制的原版 Windows 0.111.0 游戏、Wine 11.0、RitsuLib 0.5.13 验证：初始静默两种种子分别掉 1 / 5 HP；死亡夹具掉 70 HP；无色 BOLAS、能力 ABRASIVE、Pen Nib=9、Happy Flower=2 的夹具掉 0 HP；HIVE/BOWLBUGS_WEAK 加第二幕先古遗物夹具掉 70 HP。具体 runId 见测试矩阵。Release 0 警告/错误，结构边界检查通过。完整卡池/遗物池/所有幕目标未验收；未发布。

> 当前开发版本：`0.30.0`（玩家更优世界线策略迭代）；创意工坊稳定版仍为 `0.28.3`
> 本文是项目的持续记录。每次改变搜索语义、评分、模拟覆盖、性能策略、UI 或测试方式时，都应同步更新对应章节和文末变更记录。  
> “未来构想”均不是已经实现的功能。

## 0.30.0（开发中）：2026-09-05 玩家更优世界线策略迭代

- 建立 [玩家更优世界线监控清单](strategy/player-worldlines-20260905.md)：收录当天下载包的 460 条战损对照，按有玩家备注、原始减损降序分批；低于 5 HP 的 148 条暂不进入修复队列。清单额外记录优化后相对人工战损和是否严格更优，未回放条目保持待验证。
- 第一批引入能力引擎收益上下文：从当前格挡、可达格挡牌量和平均格挡量估计狂怒、坚定不移和下回合格挡；壁垒按当前已有格挡并结合入场威胁计入跨回合防御；计策、计划妥当按一次最佳可用牌机会计入跨回合牌权。潜在能力牌仍沿用既有通用估值，避免把尚未验证的未来组合收益重复计入搜索。
- 第 3 条实验体包在玩家完成首轮操作后的第 2 回合状态下，High/DOP1 预计剩余战损 `0 HP`，runId `d538dcebac0543f4b93ed28ff0e95704`。这只验证人工操作后的续局，手操前仍待验收；撤回原先“优于人工 +3”的整体策略结论。
- 第 1 条永世沙漏包补齐回放 Power 动态变量恢复后，手操后第 3 回合 High/Force 预计剩余战损 `3 HP`，runId `5d768646dbb641ea967de0349a18c9ef`。`RAGE+` 按卡牌实际 Power 值乘可达攻击次数量化，`PROLONG+` 按当前格挡量量化下回合防御收益。手操前第 2 回合仍待验收，原先“优于人工 +3”的整体策略结论撤回。
- 第 2 条永世沙漏包修正回放 Power 对账顺序并恢复附魔字段，手操后第 7 回合 High/Force 预计剩余战损 `0 HP`，runId `2a65b5cb43b846809f31838c8e71f7ed`。原报告 `55 HP` 含既有战损，不能直接与续局 `0 HP` 相减；撤回“优于人工 +55”，手操前仍待验收。
- 第 4 条永世沙漏包在手操后第 4 回合续局，High/Force 为 `2 HP`，VeryHigh/Force 为 `0 HP`；VeryHigh 结果 runId `e91590c716aa40548e02eb7931c96d17`，High 结果 runId `fbc9fe2a11a7416abee9d1a542c15931`。整场策略尚未验收，按用户要求跳过。
- 第 5 条实验体包无法建立公平的同状态 Baseline：`current/replay-state/003-search_request_AutoTurnStart` 在导入阶段因缺少本回合卡牌历史失败，runId `06b9c267486a4d1f9ce9618a22d52839`。按证据不足规则暂跳过，清单保留两个对人工字段为待验证。
- 第 6 条机甲骑士包中，跑局快照和回放都记录槽 1 为空，但测试建局残留了攻击药水。按 replay-state 逐槽恢复库存后，第 4 回合 High/Disabled 续局战损 `0 HP`，runId `7d6aa2ce419040b4a3d1533568c8d4dd`；这只证明后续回合可接续，手操前用药决策仍待验收。
- 第 7 条女王包手操后第 2 回合 High/RequireAtLeastOne 续局预计战损 `5 HP`，runId `0155a51343524f769b82068df0c0d857`。该结果没有验证手操前的灵动步法选择；撤回以此排除能力评分缺口的结论。
- 第 8 条永世沙漏包的原 `20 HP` 路线在节点上限终止，敌人仍剩 `420 HP`，缺少完整胜利基线。当前手操后 High/Disabled 已找到完整胜利、战损 `52 HP`，runId `fe655950b7844083868ab9274f712197`；不能把两者相减作为策略差距，该包暂跳过。
- 第 9 条女王包原 `74 → 30` 均含此前 `21 HP`，T3 手操前应比较剩余 `53 → 9`。默认通关优先得到无药 `23 HP`（runId `0067c85bdac14cc9bfb311020cfe4dfe`），但终局 Boss 政策排除了药水，不能算同目标策略缺口。无人协议和两端入口新增两类 Boss 策略覆盖，批量减战损验收显式使用 MinimizeHpLoss；纠正后的完整搜索在 `120 s` 超时（runId `4e804297417641c18019805dd29d9e13`），保持未验证。
- 新增开战快照恢复：在原版开战效果和首次抽牌之前恢复 `000-combat_start`，由原版继续准备首回合。恢复牌堆不再触发生成卡牌效果；可用独立首次出牌前快照校验整段推进。第 3 条从开战恢复到首次求解的完整状态与 `ContinuationStamp` 均通过，runId `0f6926e7b3e0467daa93e31dfe4899e5`。清单第 1、2、3、6、7 条此前续局验证不能证明原始策略已追平，已撤回整体验收标记，改从开战重新验收。
- 第 1 条开战基线为 VeryHigh/Smart、减战损优先预测 `94 HP` / `1` 药，runId `564d0352af0243ed87ae31dfeb7a2a53`。根因之一是 Smart 在第一个达标用药层提前停止；现在继续搜索成本允许的有限层，保留每瓶 `9 HP` 的无药基线门槛，并按战损和用药成本比较已完成候选。相同 VeryHigh 档位下，当前 Smart 预测完整胜利 `8 HP` / `3` 药；四药层的 `1 HP` 只再省 `7 HP`，按额外用药成本保留三药结果。该口径与原包强制四药分开记录。
- 第 1 条首次完整部署发现“坚定不移”把“风的女儿”的遗物格挡计作卡牌格挡，导致第 7 回合预测 `8`、实机 `15` 格挡并触发一次重算。现在从 GainBlock 的 CardPlay 和 ValueProp 记录真实卡牌格挡事件数，在完成出牌后累加到现有回合历史；遗物额外格挡不消耗卡牌翻倍次数。最小差分由预测 `11` / 实机 `16` 修为一致，runId `203972a50aae4aeb8278447d4010f414`。
- 第 1 条最终按原包逐槽强制四药政策、VeryHigh/DOP4 从开战完整部署，战损 `3 HP` / `4` 药，人工 `6 HP` / `4` 药，相对人工 `+3`；T7 胜利、6 次续用、零非预期重算，runId `ff728e5661394c9e8a77c7b03a404a48`。无人协议及两端入口新增逐槽药水指令覆盖，支持严格复现该政策。清单的 `+3` 使用这次整场证据；此前续局结论仍然撤回。
- 第 2 条从开战按原包通关优先、仅稳定血清强制使用，VeryHigh/DOP4 完整部署 `50 HP` / `1` 药，人工 `55 HP` / `1` 药，相对人工 `+5`；T10 胜利、9 次续用、零非预期重算，runId `beb01c29aa57492db5c55ce103a593c2`。搜索源码仍为上一批 `0cb5bcf`；整手保留、狂怒时限和资源通道的组合试验在相同政策下退化至预测 `70 HP`，已撤回。另用减战损优先时原基线仅死亡路线，扩大两倍 Beam 也未改善，不修改生产预设。
- 批量工具默认读取并记录原包 Boss 策略及逐槽药水指令，显式参数可单测另一类 Boss 政策；避免把设置差异误认为策略回归。中途回放不再生成相对整场人工的差值。
- 版本元数据同步到 `0.30.0`。第一批只提交能力引擎策略和对应记录，后续按清单顺序继续处理；每批独立验证并推送一次。

## 0.29.1（定版准备）：2026-09-04 19 点后问题包硬逻辑修复

- 重新整理 Edge 下载目录中 5 批问题包，共 173 条记录；其中 32 条来自 `0.29.0`，26 条带玩家备注。逐条证据、分类口径和未验证边界记录在 [ISSUE_TRIAGE_20260904_AFTER_1900.md](ISSUE_TRIAGE_20260904_AFTER_1900.md)。
- 回合准备统一按原版顺序先执行 Power、再执行玩家遗物，并在能量重置后保持同一顺序；烘焙手套、工具箱、必备工具、选择悖论、暴政等回合开始效果不再因监听器顺序错位而改写手牌或能量。工具箱触发的 Sly 自动出牌如果继续产生原生选择，模拟会保留该待处理选择并在同一回合准备计划中消费。`FIX-141700-TURN-START-CHOICE-FIXED` 和 Toasty 回归均通过。
- 知识恶魔的诅咒二选一在结束回合后继续由同一个原生选牌会话驱动，玩家备注中的“不会自动选知识恶魔选项”已修复；当前 Debug 复跑观测到 `MIND_ROT_POWER`，runId `8ed16a06900c48ddb048fa89d2fe6fe6`。
- 卡牌动作身份键与状态指纹统一排除 `CalculatedVar` 派生缓存。牌堆、力量、Osty、历史或预览更新不会再把同一张 `NO_ESCAPE`、`UNLEASH` 或 `COMET` 误判为不同卡牌；三份问题包已完成根因修复，专用原包复跑尚未完成。
- 对带失效动态变量集合的 `Spite`，重复攻击次数按该卡原版固定公式和升级等级恢复，避免 `Repeat` 缺失把整次搜索中止；帝王蟹和幽暗问题包中的 `KeyNotFoundException` 已完成根因修复，专用原包复跑尚未完成。
- 根监听器捕获会排除空监听项，避免 Queen 问题包在敌方攻击钩子中触发 `NullReferenceException`；该问题包已完成根因修复，专用原包复跑尚未完成。
- 部署前发现药水槽位为空或槽位内容已换成其他药水时，清理旧续接路线并按 `DeploymentDrift` 重新捕获当前战斗；不会再把可恢复的实时槽位变化记成自动执行失败。
- 版本仅同步到 `0.29.1`；创意工坊上传、GitHub 推送和版本标签不在本次请求范围内。

## 0.29.0（历史开发记录）：2026-09-03 问题包硬逻辑修复

- 第一批修复 MANGLE + SLITHER 在分支隔离克隆后的抽牌钩子身份错位：同一次抽牌中其他监听器使卡牌生成 COW 预览后，SLITHER 仍按抽牌开始时的卡牌身份触发随机费用，增量回放与完整回放保持相同的费用 RNG 和牌面状态。
- MANGLE 的 `OnPlay` 进入显式 Mirror 注册，避免依赖未登记类型的推断路径；出牌后直接清理费用和星能临时状态时同步失效牌堆/选牌缓存；回放找牌失败日志补充回合、动作序号、状态键和当前手牌，便于继续定位硬逻辑问题。
- 当前批次无人验证：问题包 `ISSUE-20260903-MANGLE-COW-FIX` 在 `VerifyIncrementalSearch` 下通过，runId `f25dee83d9ba4b039318a4cdef20fbe3`；MANGLE 单卡严格差分通过，runId `b7e5d32dff794ef695c93dbc3123b689`。两项均使用塔 2 `0.111.0`、当前 Debug DLL、DOP1。
- 第四批修复原生选牌会话边界：知识恶魔诅咒只由敌方回合解析器消费，不再被结束回合部署游标重复等待；原生选牌计划与当前页面候选、卡牌节点或页面生命周期不一致时会先关闭当前页面并按 `DeploymentDrift` 重新捕获，确认按钮则等待页面完成布局后再提交，超时和未知页面错误仍明确失败。已有 `PR29-KNOWLEDGE-CURSOR` 结构回归覆盖游标过滤；原生漂移重捕获和关闭确认按钮的可见页面夹具待补，不能把旧问题包的页面异常写成当前版本已通过。
- 第五批把原生选牌计划提前结束或收到计划外请求也归入同一漂移边界：部署动作会取消旧页面、清理会话后重新捕获当前根；页面要求数量变化同样按计划漂移处理，重复计划仍保留为明确的内部失败。`SCULPTING-STRIKE-CHOICE-151` 在当前 Debug DLL、严格增量回放和第 2 回合复用下通过，runId `a35708eb3bae4aa49ef7769230d59bfa`；默认知识恶魔整战夹具只有死亡路线并超时，仍不计为通过。
- 第六批把部署途中失去玩家回合所有权归入实时漂移：清理续接路线并按 `DeploymentDrift` 重捕获当前战斗，避免把回合切换边界误报成不可恢复的自动执行失败；药水槽位、目标和其他真正执行异常仍保留显式失败。
- 第七批修复洗牌 Hook 的待处理选牌边界：一个监听器挂起原生选择后暂停同一 Hook 的后续监听器，分支回放消费该选择后再继续遍历，避免 `STRATAGEM_POWER` 与后续选择同时写入单槽位而触发“已有待处理选牌”。
- 第九批把原生选牌页面/确认按钮等待超时标记为原生页面漂移：关闭当前页面并重新捕获战斗状态；部署延迟和回合准备整体超时仍保持明确失败。

## 0.28.3（已发布，2026-09-03）：战损停止与路线信息

- 设置 > 常规新增“可接受战损上限（HP）”，默认 `0`。完整胜利路线的预计本局战损小于等于该值时，搜索在当前回合层结束后停止继续遍历；死亡、未完成或超过阈值的路线不会触发。阈值随搜索请求快照传入，不改变战斗模拟和最终路线质量排序。headless 控制器/UI/搜索生命周期已验证，runId `aca83616becd422e99558a0d07b970d0`。
- 部署阶段找不到普通计划手牌时按实时状态漂移处理：清理旧续接路线、重新捕获当前战斗并进入 `DeploymentDrift` 搜索，不再把可恢复的手牌消失直接记为自动执行中止；真正的卡牌不可用、药水槽位不一致和原生页面事务错误继续显式失败。现有 Fork/部署身份边界通过，专用实时改牌夹具待补。
- 最终路线回放现在从实际伤害事件记录击杀来源。卡牌、药水、毒、荆棘、能力、遗物和球会显示各自来源；无法由模拟事件确定的直接移除仍明确标为“未知效果”，不再把所有回合末击杀写成“直接结束”。
- 复跑 `0.28.2` Queen 的两类遗物标注差异样本均通过（HP mismatch runId `22f3789304284eb49662d378361940e2`、Haze 状态 mismatch runId `5bfc15213c444b449161c1b216ab256d`）；当前 Spite 失血后重复攻击严格差分 13/13 通过，runId `cd97d0160dec44d3b35cae9b05ca328f`。
- 生成新敌人时先固定现有敌人快照，再计算随机初始生命，避免生成路径在集合变动边界上持有可变枚举器；`THE_OBSCURA_NORMAL` 原生遭遇短搜覆盖 8 回合、3 次洗牌并通过，runId `faeec097dee647af8453f9aa00f2c6ab`。
- 修复召唤或重建敌人击杀时，最终标注错误依赖初始敌人列表而触发 `Sequence contains no matching element`；击杀事件现在同时保留发生时的目标名称。
- 修复路线行只在没有普通动作时显示结束动作的 UI 筛选；有动作的回合显示“结束回合”，只有空过回合显示“直接结束”，回合结算或其他中间结算造成击杀时会在对应动作之后显示带来源的完整击杀胶囊。
- 修复新增敌人未进入初始显示索引时目标名回退为 CombatId 数字的问题；名称现在始终从怪物本地化标题取得，同类型敌人同时在场时才追加从 `1` 开始的编号。
- 修复后台搜索线程回退读取怪物本地化标题导致 SmartFormat 对象池并发修改并使搜索失败的问题；所有怪物标题现在在主线程捕获，worker 只读取不可变文本。
- 保钱策略在敌人携带被盗资源、但当前没有攻击威胁（例如胖地精刚生成的醒来回合）时，优先保留针对资源携带者的有效动作分支，避免分支筛选先保留卑鄙地精而错过追回金币的路线；资源检查覆盖新增的 `HeistPower/SwipePower` 实例。
- `0.28.3` 玩家更新日志见 `docs/0.28.3-RELEASE_NOTES.md`。

## 0.28.2（已发布，2026-09-03）：搜索速度与内存效率

- 合并 [PR #37](https://github.com/Torch1230/CombatSolver/pull/37)：缓存固定的模型查询和 Mirror 类型解析，并移除攻击结算、状态统计、遗物回合开始等搜索热路径上的枚举器与临时数组。贡献者固定机甲根 A/B 保持相同工作量、路线、评分和战损，Windows 搜索耗时 `29.52/29.47 s → 26.21/26.92 s`，worker 分配 `7.40 GB → 6.20 GB`；macOS M4 耗时 `48.13/49.20 s → 43.20/43.53 s`，分配 `10.40 GB → 9.44 GB`。
- 合并 [PR #38](https://github.com/Torch1230/CombatSolver/pull/38)：NoGC 因平台或区域尺寸限制无法建立时继续使用玩家配置的并行度，只有真实系统余量不足才限制为两个 lane；区域尺寸不被 CLR 接受时逐级缩小到 `512 MB`，并行 wave 超出内存预约后改为容量减半。贡献者 macOS M4 固定工作量 A/B 保持结果不变，实际并发 `2 → 8`、耗时 `37.2 s → 27.2 s`；Windows 正常建立 NoGC 的路径保持原行为。
- 感谢時穂（Natsuki，[@s1f102500012](https://github.com/s1f102500012)）贡献上述两项性能优化。

## 0.28.1（已发布，2026-09-03）：药水策略与系统内存释放

- 修复 `0.28.0` 合并后的 Smart 药水“口渴”：新的胜利/战损/回合词典序曾在跨梯度采用与动态展示时抢先于药水成本，使付费药只省 `1 HP` 或仅缩短回合也会覆盖无药路线。现在普通战斗恢复每瓶至少省 `9 HP`，第一、二幕末按战后回复折算为 `45 HP`，终局“能过就行”时无药获胜便不再为提速交药；梯度重新按无药战损限制最大药量，并在第一层达标后停止。无药无法获胜、免费/可再生药、明确强制用药及保护被盗资源仍按各自策略处理。
- 主界面内存预览条右侧新增“强制释放内存”按钮：等待活动搜索退出后先压缩托管堆并修剪游戏进程工作集，再通过 UAC 启动短命辅助程序，以系统内存列表命令清空全部工作集与待机列表。该操作只由玩家手动触发；不清空会集中写盘的修改页列表，其他程序随后重新载入被修剪页面时可能短暂卡顿。

## 0.28.0（已发布，2026-09-03）：更好的小循环、无限与跨回合决策

- 求解器现在能更好地识别小循环、无限与接近无限的资源闭环，并愿意为启动循环保留必要的铺垫路线，不再只盯着眼前伤害或固定重复次数。
- 跨回合收益会综合过牌、回收、能量、星能、持久成长、延迟伤害和特殊战斗目标，让需要多个回合兑现的组合更容易进入最终路线。
- 卖血决策会比较实际战损与后续收益：值得的生命投资可以换取更强的循环或更优终局，同时更快但掉血更多的路线不会覆盖低损路线。
- 感谢 [洛天琉璃依（@ltlly）](https://github.com/ltlly) 在 [PR #35](https://github.com/Torch1230/CombatSolver/pull/35) 中贡献本次核心实现。
- 星能增加现在由内嵌模拟的通用 `GainStars` 命令在状态变更后统一分发原版 `AfterStarsGained` Hook；黑洞不再依赖少数卡牌、Power、药水或遗物入口各自手工补偿。此前直接获得星能的发光、大爆炸、聚集光芒、闪耀打击/太阳打击、击倒猛击、封印王座与神圣命运会漏掉黑洞伤害；统一分发后，只有星能实际获准增加且战斗未结束时才触发一次。黑洞 5 层配合发光的最小严格差分显式验证获得 1 星、敌人恰好失去 5 HP，最终 runId `b4c281152e5b4468b59f10c665d68d`。
- PR #28 已撤回，其“固定重复 `16` 次后停止，再按少数牌、遗物或能力名称放宽”的做法不再是当前实现。生产搜索不读取任何特定卡牌、Power、遗物或敌人 ID 来判定循环、启动收益、跨回合价值或最终路线；结构门禁会拒绝重新引入这些专有例外。
- 同回合搜索从模拟快照构造不含线性收益标量的控制形状，并把牌/药动作的实例、状态、目标、槽位、重放和所有选择写入有序动作键。周期识别最多回看 `32` 个动作；一次同形状复现只形成待探测候选，相邻窗口的动作序列和转移增量一致后才得到更强证据。证据只用于有界调度，不能证明数学上的无限循环；进入 frontier 的每条边仍由模拟器精确执行一次，不使用宏动作或直接倍增伤害。
- 暂时没有即时进展的周期可获得有界探测租约。租约只能沿完全相同的动作相位传播，按最低生命风险带和有限生命投资带各保留至多 `2` 个族、总计至多 `4` 个族。基础探测动作预算由 `MaxExpandedNodes / 16` 限制在 `32–256` 个动作并按周期长度换算重复数；已观察到更好出口的族只获得一次有上限的扩展。每个周期相位同时比较非周期出口；通用收益改善时，最多继续 `8` 个动作、跨 `2` 次回合精确探测。
- 跨回合无进展基础观察期为 `max(16, 两个完整牌堆周期所需回合)`。每个生命风险带只保留一条跨回合探针，优先续接已在途族，没有在途族才启动新族；总计至多两条。直接结束回合的分支构成同一起点的精确 stand-pat 基线，分支相对基线的完整语义状态变化可以把探针延长到最多 `64` 次回合转移；最近一回合确有通用进展改善时，上限最多扩到 `128`。这里的 `16` 不是回合内固定重复次数，也不是整场回合上限。
- 通用进展比较逐敌人耐久与击杀、输出/延迟伤害、持久或战略收益、未来资源、回复/格挡、能量/星能、控制、追回资源、牌库净化和特殊战斗目标。隐藏状态可以通过精确 stand-pat 差异获得额外观察期，但不会仅因普通动作改变完整状态就无限续期。
- 最终路线按完整胜利、真实/战略生命损失、结束回合和资源政策等严格词典序比较；损失相同才偏好更早结束。Smart 会在共享请求期限内比较所有已完成的精确用药层，并保留全局更优路线，不再把第一条满足旧门槛的路线当作最终答案。周期与跨回合保路只影响中间候选，不能让更高战损路线覆盖更低战损路线。
- 合并上游 `0.27.1` 的动态 NoGC 区域和 Smart 层间回收后，层间整理使用零基进度区间，避免第二个药水梯度开始重复累计无药层节点与计时；搜索期限在整理期间到期时仍记下已经完成的 GC 工作，并返回此前完成层中的最佳路线。若玩家请求的 NoGC 因系统余量、平台能力或 CLR 拒绝等原因无法建立或重建，本次搜索回退 CLR 常规 GC，但只建立两个 lane，父节点、动作与选择微批也最多并发 2；玩家主动关闭 NoGC 时继续按其 DOP 设置运行。
- 该机制仍受 Beam、动作候选、时间、节点、最多 `32` 动作的周期识别窗口、探测族数和出口视野约束，是近似搜索而非最优性或无限循环的完备判定。周期超过 `32` 个动作、收益晚于当前有界探测窗口、尚未进入通用进展表达的隐藏状态，或早期路线未进入 Beam，仍可能漏解；后续只应扩充通用状态/收益表达和回归夹具，不能增加内容特判。
- `0.27.2` 最终定向证据：隐藏三相位 DOP1/DOP2 均为 `6/12` 展开/转移、`6` 次洗牌、第 `1` 回合结束（runId `1ae74fcaf14648a19a773e9be602fa34` / `28d3a383cb3c4fd4944ffcc923f76e7a`）；隐藏跨回合缓冲 DOP1/DOP2 均为 `513/770`、第 `17` 回合结束（`e1aac2f411fa4d58b91466022f5edde7` / `d38be6387ddd4aadb13a1adcdc3864cf`）；动态成长循环 DOP1/DOP2 均为 `2400/5354/344` 展开/转移/选牌、`32` 动作、第 `1` 回合结束且动作路线完全一致，DOP2 搜索与动作重放的最大并发均为 `2`（`2d33c3a4eaee44d5b06004758e8cefb4` / `5100ca05213f455ba6f1dd57f0916fef`）。
- 终局与 Smart 最终证据：同为零战损时，Smart 选择第 `1` 回合结束的一药路线，请求累计 `31/74`、选中层 `7/22` 展开/转移（runId `02c81917a3284c269a90e2fec5b65d4e`）；三层 Smart 完成无药、恰好一药和恰好两药，请求累计 `52/140`、选中层 `7/19`，两次层间整理后仍选择零战损 T1 的一药路线，Gen0/1/2 均为 `4` 次（`8e01068bacc049b89acacd66e218f73f`）。控制器生命周期同时执行转置表循环租约策略断言，runId `328dbe4322a54815a83f3946563d73b6` 通过。
- 七个当前规则场景均只由通用状态、动作和最终质量排序求解：单张掠夺的自动转移内连锁为 `1/2`、T1、零损（`aed2647646d645abb18e1ef94bf53283`）；有限双剑柄打击为 `6/7`、T2、战损 `11`（`eb6084633d3744399a3e3422e13e2e8a`）；卖血双剑柄质量场为 `186/464`、T2、战损 `3`，优于 T1 战损 `6`（`b57b545ed9c7415cb6644af2df7e89cc`）；无声猎手抽弃链为 `301/837/261`、T1、零损（`1991d55b153f44c8b722546214e57f0a`）；故障机器人取回链与储君粒子墙链都在 `2400` 节点上限内找到 T1 零损路线（`a7a3fda4781e4f329a00b39aecdf079b` / `5859c64bb16d4a0c8bf1134f45d23861`），因此只证明预算内有解，不称穷尽；封印王座/黑洞为 `10/20`、T1、零损（`3bb92f0684d7414d9d0660f83edb992b`）。
- INFESTED 同根最终候选在 VeryHigh、Smart、DOP8、NoGC `16 GB` 下完整结束配置搜索：runId `1e9c735a5c9e42889ab46c6389a66b16` 请求累计 `13,516/80,477/33,664` 展开/转移/选牌分支，选中层 `4,437/23,810/8,337`；搜索墙钟 `10,548.215 ms`，累计分配 `4,920,306,312 B`，`VmHWM=3,715,840 kB`（约 `3.54 GiB`）。结果为 `43 HP`、预计战损 `17`、第 `5` 回合结束并使用 `2` 瓶药，比旧 `42 HP`、战损 `18`、第 `7` 回合同时减少战损和回合；两次 Smart 层间整理均成功退出、回收并重建 NoGC，Gen0/1/2 均为 `4` 次，GC 暂停累计/最大 `640.203/404.340 ms`。这是配置预算内完成的搜索结果，不声称数学全局最优。
- Silent 396 长线同根 NoGC A/B 保持精确相同的 `27,905/173,477/74,998` 请求工作量、`8,687/59,910/26,619` 选中层工作量、战损 `9`、`56 HP`、T9 与动作路线。4 GB NoGC 为 `72,151.169 ms`、`VmHWM=4,396,804 kB`（约 `4.19 GiB`），Gen0/1/2 均 `30`、GC 暂停 `4,822.405 ms`；关闭 NoGC 为 `97,927.457 ms`、`VmHWM=2,745,000 kB`（约 `2.62 GiB`），Gen0/1/2 为 `3522/1695/88`、GC 暂停 `29,712.935 ms`。NoGC 快约 `26.3%`、GC 暂停少约 `83.8%`，代价是峰值内存增加约 `1.57 GiB`；13 个搜索检查点和 2 次 Smart 层间整理都成功回收并重建，无回退。runId `5f75b1cd2b604f34874be9e6243590db` / `6c187605b7a8451bbd163a800d9f25c4`。

## 0.27.2（已发布，2026-09-02）：动态战损展示修正

- 动态路线尚未结束战斗时，整场预计战损显示为“未知”，不再把当前累计掉血 `0` 当成整场零战损。只有完整胜利路线才显示数值；同一次搜索中，更高战损的完整路线不会覆盖已展示路线，预计战损只会保持或下降。逐回合实际掉血仍按已完成回合展示。

## 0.27.1（已发布，2026-09-02）：内存监控与搜索流畅度

- 主界面右下角新增随底栏剩余空间动态伸缩的内存占用条，进度条加粗为 `8 px`。搜索到达检查点时显示“正在整理…”，释放可重建缓存并执行一次完整 Gen2 回收，然后重建 NoGC 区域继续搜索。Smart 梯度之间继续主动清理；最终梯度或普通搜索正常结束后保留战斗级区域，战斗结束时等待引用释放并延时 `3–5 秒` 清理，避免路线刚算完就产生一次可感知停顿。关闭 NoGC 时显示“自动管理”。
- Smart 药水梯度在无药、单药及多药搜索之间建立内存边界：上一梯度完成后只保留最佳动作路线和统计，丢弃 Beam、模拟状态与分支缓存，主动回收后以新的 NoGC 预算开始下一梯度，避免后续用药搜索从已满的 GC 墙继续运行。
- 新安装和恢复默认的主界面尺寸改为 `1200 × 700`，透明度改为 `65%`；已有玩家明确保存的尺寸和透明度继续优先。
- 搜索因当前性能设置的时间上限或节点上限停止时，标题栏下方固定显示不可关闭的警告，说明计算尚未彻底穷尽、当前只返回已找到的最佳路线，并指向性能设置中的对应上限。每次命中上限都会重新显示；正常结束、手动停止和其他边界不触发。
- 内存诊断新增低频状态日志：后台清理以同一编号记录请求、启动、NoGC 区域退出、Gen2 开始和完成；内存条只在显示状态变化或搜索负荷跨过新的 `10%` 档位时记录。两组日志同时包含进程工作集/私有内存、托管存活量/堆大小/碎片、CLR 内存负载、搜索分配检查点、各代 GC 次数和延迟模式，用于实机校准显示口径与清理时序。
- 实机日志确认固定 `16 GB` NoGC 申请在系统只剩约 `4.2 GB` 安全余量时仍被 CLR 接受，导致内存条显示 `20%`、实际系统压力约 `96%`，随后 NoGC 意外退出并产生密集常规 GC。现在配置值作为上限，实际区域按 CLR 高内存阈值 `95%` 以内的当前余量动态缩小；搜索保留容量和回收检查点同时取分配额度与预测系统压力中的较大值，在 CLR 强制退出前主动整理。系统余量不足 `512 MB` 时直接使用 CLR 默认模式，不进入持续低延迟回退。用药梯度整理与压力整理使用不同的进度文案。
- 内存条改为实时物理内存分段：灰色为系统和其他程序占用，彩色为整个游戏进程工作集，空白为当前余量；文字显示“当前内存占用 X GB / 搜索总可用 Y GB”，不再重复列出系统占用数值。搜索总可用按 CLR 安全总量减去系统占用动态计算，搜索整理和战斗结束后的后台清理期间持续刷新。

## 0.27.0（已发布，2026-09-02）：更好的 UI 与搜索体验

- 合并 PR #31：计算中直接显示已经完成回合的候选路线及逐回合对敌伤害；“采用当前路线”和“执行本回合”分离，独立停止按钮会保留停止时路线。设置新增持久化的“自动计算”开关，关闭后进入战斗和新回合均等待玩家手动开始。
- 药水策略改为主面板右侧的独立窄浮层，每瓶药以紧凑按钮在智能、保护和强制使用间切换；主面板缩放不再把药水浮层宽度写入自身尺寸。
- 主覆盖层保留右边缘、底边缘和右下角三向缩放及三斜线抓手，吸收 PR #31 的 `16 ms` 拖动重排节流和内容最小尺寸约束。位置与尺寸在松手时一次性保存；收起采用紧凑宽高，展开恢复玩家尺寸，较小分辨率只钳制当前显示。
- Release 编译零警告，结构门禁通过。DOP4 控制器/UI/药水生命周期 runId `75070ce99c1b48ef9c9608205ac57e19`，烘焙手套开局候选路线采用 runId `e6cb54b4de4f4110a996c2563c7f96ea`；均使用合并后部署 DLL。未做可见界面观感检查。
- 设置 > 常规新增两项互不影响的幕末 Boss 血量取舍：第一、二幕与最终 Boss 均可在“通关优先”和“最低战损”间切换，默认继续采用通关优先。前者对第一、二幕按战后回复 80% 折算血量，对最终 Boss 只保留存活边界；后者恢复普通战斗的完整 HP 权重、`9 HP/瓶` 智能药水门槛和普通 Boss 卖血阈值。两类 Boss 各自显示当前策略提示，也各自持久记忆关闭状态。DOP4 设置/UI/阈值生命周期 runId `e52f2ec763ac4361a9a09992ab8ae7d5`。
- 合并 PR #32：搜索每约 `100 ms` 从当前仍持有的候选中刷新动态演化路线，并把“当前回合可执行预览”与“可能继续变化或回跳的推演路线”分开；采用路线和停止计算绑定玩家屏幕上最近一次完整展示的推演候选。终局排序在胜负、战损、药水、长线资源和敌方状态均相同后，优先更早结束战斗，不以回合数越过资源质量。合并态 Release 编译零警告；按用户要求未继续运行行为测试。
- 修复活体雾生成的毒气弹自爆后仍被回合收尾推进 AI、因 `EXPLODE_MOVE` 合法地没有后继状态而令整次搜索失败的问题。下一回合准备现在只推进仍在活动阵容且仍可行动的怪物；终局行动的 AI/静态快照继续保留到分支生命周期结束。最小生命周期夹具通过，runId `0450d8534bce46e0b329a22f562d95a5`。
- 合并 PR #33：逐回合对敌伤害改为累计怪物实际失血，避免投影生命与复活/召唤导致显示偏小；药水补查的局部预览、全局最优结果和采用种子同步晋升，较差用药路线不再覆盖已显示的无药好路线。合并态 Release 编译零警告。

## 0.26.0（已发布，2026-09-02）：长线搜索性能与 Boss 资源策略

- 采纳 PR #21 的 Boss 战后血量分型，并按实际 80% 跨幕回复修正：第一、二幕 Boss 的普通药 Smart 门槛由每瓶 `9` 提高为 `45 HP`，多药按 `45 × 瓶数` 开层；卖血硬阈值由 `15` 提高为 `75 HP`，两者仍以存活为硬边界。第三幕第二 Boss 归为 `RunEnding`，完全忽略剩余血量差，只比较是否存活及后续质量；第三幕第一 Boss 仍按正常血量处理。DOP4 定向 runId `444c43b31b3745ef9d94758b2ed79d96`、`4cb4e5936f9b45a09b1ea4ba3091913d`、`da035809ed9243cc850b97085415b3af`。
- 合并 PR #30：虚空形态的手牌机会价值按本回合剩余免费格计量，只给最值得减免费用的有限张牌计价，不再把整手牌都当作免费；X 费牌不获得虚空形态费用减免，因此不计入减免收益。一个/两个免费格的定向断言通过，runId `aaaa1a2b47924f8b8774a1b6df0b017c`；玩家报告中的具体选牌路线未稳定复现，不将其写作实战通过。
- 合并 PR #29：强制结束回合的出牌动作不再把知识恶魔诅咒放入卡牌选牌游标；诅咒继续由敌方回合从原始计划直接消费，普通动作和回合选择仍执行完整消费校验。定向 Fork 夹具 runId `bb84ece61ac7453e8befa2bb37220f86`。
- 合并 PR #27 到 `0.25.3` 之后的开发线；冲突处理保留知识恶魔无效选牌分支淘汰、复活 Power、自动出牌历史及回合末球死亡召唤语义。合并态 Release 编译、结构门禁和 Fork 代表夹具通过，runId `a1747125352741efa72ec01a2ae64c4a`；未重复运行贡献者的长线性能 A/B 与可见 Steam 验收。
- `CombatSolver-INFESTED_PRISMS_ELITE-20260901-210228-701.zip` 只作为不受信任的问题证据读取。最新上游 `0.25.1` 在相同 VeryHigh、Smart、DOP8 根上，三层全搜索阶段为 `112798.755 ms`；低分配最终候选为 `9846.963 ms`，加速 `11.46×`，结束采样工作集 `11,204,886,528 B`。请求累计 `24109` 展开、`157893` 转移、`69006` 选牌分支，选中层 `5861/31244/12125`，仍为 `42 HP`、预计战损 `18`、第 `7` 回合和同一动作路线，runId `80921c75b7224f4b887e096f07505739`。节点数随上游选择修复略有变化；验收以胜负、战损、回合和动作路线不退化为准，不把旧版固定节点数当语义要求。
- 最新上游对照的 Silent 396、Necrobinder、Mecha、Queen 长线阶段分别为 `120464.516/120399.236/23853.522/67789.142 ms`；候选为 `55984.737/29898.891/8034.665/29062.616 ms`，即 `2.15×/4.03×/2.97×/2.33×`。16 GB NoGC 的进程峰值工作集为 `12.82/12.34/5.10/12.47 GB`；Silent 改用用户设置的 `4 GB` 仍为 `53440.887 ms`、同路线和同工作量，峰值降到 `4.60 GB`，证明预算保持独立的速度/内存权衡而非暗中钳制。
- 设置 > 性能新增独立的“NoGC 区域”开关，并继续原样保存 `1–256 GB` 用户预算。本性能版本有意默认启用并沿用 `16 GB`，升级后可手动关闭以恢复上游 `0.25.1` 的 CLR 常规分代 GC；不提升设置迁移版本，也不重置已有预设。开启时使用战斗级 NoGC、搜索内安全检查点和引用释放后的压力回收；关闭时不登记自动补账压力。手动 GC 在两种模式下都等待搜索退出或下一个安全内存检查点；不能与活动搜索形成互等，也不能清掉仍被战斗引用持有的后续补账义务。
- EventPipe CPU/GC/AllocationTick、TraceEvent 调用栈和精确分配计数定位了动作局部历史的 O(depth²) prefix 重扫、无合格永久玩家 Buff 时仍构建 `StrategicEffectContext`、死亡后 roster 反复过滤、Fork 先构造再覆盖四个空容器、牌堆/分支集合接口枚举装箱，以及状态工厂捕获闭包等热点。历史现使用冻结半开区间的零 LINQ tail range；相同 Vital Spark 污损值跳过写入，DynamicVars 合并扫描，战略投影按需建立；模拟 roster 直接消费已移除成员的 COW 视图，Fork 构造时直接接收已分叉容器；牌堆与 Forkable 集合提供 concrete struct enumerator，常用状态工厂改为 static argument factory。Fork 边界 runId `5004871b37f94cbeaf6986556fd53533` 通过。
- 帧恢复从固定阈值改为最近 `31` 个非搜索帧的中位数基线，压力线为 `max(33 ms, baseline × 1.5)`；headless 无显示服务时旁路该等待。请求级 `SearchRequestWorkTotals` 对正常、失败和取消 solver 精确记账一次，并单独记录 Smart 药水层间的 coordinator 内存整理，避免多层、取消工作或主动 GC 从吞吐统计中消失。
- 父节点 wave 不再因五张以上手牌强制退化为 singleton。NoGC 下外层从 2 开始，只有实际完成且未超预约的 multi-parent wave 才扩为 `4/8/DOP`；每个并发父节点都按全搜索最坏分配高水位的 `1.5×` 完整预约，宽度受内部 allocation limit 钳制，never-fit 父节点退回纯串行。这个最终安全准入替换了开发期“额外 lane 只预留 `96 MiB`”的激进实验，避免 DOP16 同时持有多个昂贵 raw snapshot。自然 singleton 的 card action 初始 probe 仍可借用固定 lane；direct-primary 的有限下游配额按独立分支并行，只有 primary 前 finite pending 共享配额或 NoGC 余量不足时保持原序串行；所有结果仍按 parent/action/branch 原序提交。
- 三个玩家根在低分配最终候选上完成复测。默认 `16 GB` 下，INFESTED 为 `24,109 / 157,893 / 69,006`、`9.847 s / 5.730 GB` 累计分配、42 HP/T7；PHANTASMAL 为 `24,526 / 477,315 / 353,923`、`74.328 s / 42.860 GB`、4 HP/T7，较最初 `79.593 s / 53.604 GB` 再降 `6.6% / 20.0%`；EXOSKELETONS 为同一 `5,686 / 199,522 / 175,150`、`42.150 s / 22.180 GB`、96 HP/T4。结束采样工作集约 `11.20/11.87/7.66 GB`；这是进程驻留页而非 NoGC 预算或累计分配。runId `80921c75b7224f4b887e096f07505739`、`6be970228d0b477d8da0fa2748523819`、`a97129a4cc514665ba7d222169ac1aef`。
- EXOSKELETONS 同一最终 DLL 把 NoGC 从 `16 GB` 提到 `32 GB` 后，节点、选择分支、评分、96 HP/T4 路线完全不变，墙钟 `42.150 → 28.242 s`，吞吐提高 `49.2%`；结束工作集 `7.66 → 12.83 GB`，private `18.60 → 35.64 GB`。此前 `24/32/40 GB` 诊断表明 `32 → 40 GB` 仅再快约 `2.5%`，因此默认仍为 `16 GB`，`32 GB` 只建议至少 48 GB 物理内存且当前内存充足的机器。PHANTASMAL 的 `16/32 GB` 为 `74.328/75.273 s`，32 GB 工作集约 `21.90 GB`，证明高预算只对被内存准入压窄的场景有效。
- Thread-time trace 中 BaseLib 未加载，真实锁慢路径仅占搜索样本约 `0.063%`。7 个后台 lane 约 `53.54%` 容量处于显式等待；coordinator barrier 等待期间平均只有 `2.34/7` 个 lane 活跃，全部 7 个同时活跃仅 `2.7%`。EXOSKELETONS 主要受安全内存准入限制，PHANTASMAL 则是固定父 wave 的长尾不均；提高 CPU 利用率仍有调度空间，但不能把增加 DOP 当成线性加速承诺。
- Power 数量在同一非零身份间变化时，Hook listener、run listener 与 Power 投影现在保持原缓存；只有 `0 ↔ 非 0`、实例替换或拓扑变化才失效。listener 重建改用小集合引用扫描去重，不再为每次重建创建 `HashSet<PowerModel>`，Power 投影使用精确长度数组。最终 Fork 夹具覆盖 `1→2` 缓存身份、`2→0→1` 失效、父子缓存只含子 Power 以及新增 Power 唯一/顺序，runId `962946a034004fd88cf7bec055c5a04f`。三场结果、节点与路线不变：INFESTED `9.945 s / 5.474 GB`，PHANTASMAL `75.122 s / 40.015 GB`，EXOSKELETONS `42.257 s / 20.261 GB`；相对上一最终低分配根，累计分配分别再降约 `4.5% / 6.6% / 8.7%`，单次墙钟差异在 `+0.1%` 到 `+1.3%`，因此只认定为减分配、耗时中性。runId `5371edccb4c74f9dab68d85a51daa641`、`71606a471f5a44d9b1315af133c5987c`、`9b811499b12d4d0188f0d8db12e0ee4d`。
- 最终 EXOSKELETONS DOP4/8/12/16 同结果复测为 `44.896 / 44.608 / 43.157 / 43.082 s`。从 4 提至 16 只快 `4.0%`，12→16 仅 `0.2%`；高分配父节点受安全内存准入和原序提交约束，低 CPU 占用不能仅靠增加线程线性消除。因此设置上限开放到 16 供用户选择，新安装仍默认 DOP4。
- AEONGLASS 的独立诊断已定位为搜索质量而非单位模拟性能：全死局终局排序在同战损时把少卖血放在敌方剩余生命前，Smart 药水层又只接受直接获胜，因而会丢掉更深、更接近生存的候选。该诊断转入独立搜索质量分支；最终安全 admission 未重复跑三层长测，不把之前激进候选的墙钟或节点写成当前性能验收。
- GC/DOP/取消工作量完整门禁 runId `df0ab7f8f52c41a2b856aea39c411f49` 通过：覆盖 NoGC 双向切换、关闭模式下手动回收排队、搜索检查点吸收手动回收、生命周期补账、`1 → 2 GB` 重建、DOP1/DOP2 全字段等价和真实并发。关闭模式端到端 runId `6473c714239b4f63a8735b9291d47629` 同时报告 configured `false / 17 GB` 与 actual `false / 0 B / Interactive`；设置页与控制器生命周期 runId `50b51af7a92f42948862686001b1b2cb` 通过。可见 Steam 门已尝试三次，最近一次仍未在 `60 s` 内启动游戏；脚本均恢复协议文件，因此可见帧 p95/p99/max 仍明确记为未验证，不用 headless 墙钟替代。

## 0.25.3（已发布，2026-09-02）：计划外重算收尾

- `20260902-after0930` 批次按用户确定的范围收尾；第三方内容不适配，其余未纳入本版的低频报告停止继续挖掘。
- 敌方回合行动名单在回合开始效果结算前冻结；被回合开始毒伤等效果击杀后才生成的怪物不会在出生当回合行动或提前推进初始行动。新生怪若原生状态机已经选出初始行动，分支快照不再重复登记。1 HP 斧兵毒杀复生场第 2 回合精确复用且计划外重算为 `0`，runId `e33244c904394b19a5e85d78eba5dccf`；千足虫复活边界回归 runId `eee444019fd440a887f22d4fe7b35de7`。
- 爆破的早期自动出牌检查先于乱战等抽牌自动打出效果；乱战本轮刚把爆破送入消耗区时不会再打第二次。虔诚雕刻师实包第 2 回合根在 DOP4 下于第 3 回合精确复用，计划外重算 `0`，runId `e4f7b25918a74f819d0f3ef5705dcaa9`。
- 回合末虚无牌统计读取当前完整关键词，包含 HexPower 等动态赋予的虚无；纸钱计数、阈值抽牌与后续手牌不再漂移。三骑士实包第 4 回合根在 DOP4 下于第 5 回合精确复用，计划外重算 `0`，runId `6a26ff90d1de4c7e8238b82d6b335285`。
- 卡牌补偿层产生的格挡保留当前 CardPlay 身份，佩尔士兵能在拳斗、挑衅、武装、铁斩波等牌完成后正确启动冷却。斧兵实包第 1 回合根在 DOP4 下于第 2 回合精确复用，计划外重算 `0`，runId `0a6b24215996448f9204b84c3fc193da`。
- 卡牌 OnPlay 的通用 Power 效果与专项补偿现在从同一卡牌作用域执行；躁动之灯会先翻倍鞭打施加的 Doom，并在整张牌结算后结束，不再把后续弱化之触错误翻倍。女王实包第 4 回合根在 DOP4 下于第 5 回合精确复用，计划外重算 `0`，runId `018bc519e6204a42be24ed6e92788eda`。
- 卡牌、药水、遗物增加轨道槽位时统一遵守原版容量上限 `10`；`9 + 2` 与满槽继续增加的 Fork 边界回归通过，runId `2eeb4d75036a4f1a8245275efbbcf31f`。永劫之镜问题包的完整回放曾卡在等待玩家回合，未作为通过证据。
- 自动出牌统一应用本回合卡牌历史；坚不可摧会计入能力生效前已经获得的卡牌格挡，不再错误翻倍倾泻等效果随后自动打出的格挡牌。
- 每个回合末充能球被动结算后立即完成敌人死亡与召唤；异蛙寄生虫被闪电球击杀后先生成四只扭动虫，后续玻璃球会按原版顺序命中它们。两项合并定向回归 runId `a2e032db9bf641b89d6d295fa1106f2d`。

## 0.25.2（已发布，2026-09-02）：问题包批量稳定性修复

- `20260902-after0930` 批次中，`78` 份 `0.25.1` 报告有 `20` 份因上一场额外药水槽的持久化策略越过当前槽位而初始化失败。战斗开始时现按当前槽位范围与药水身份清理旧策略，搜索策略捕获也只读取现存槽位。
- Smart 梯度上限按当前允许主动使用的可搜索药水计算；两瓶中禁用一瓶时最大只进入一药层。双药设置/搜索生命周期从失败基线 `c0d4e5eda2c846cb84107b973f4a4374` 修复为通过 `fb708ae2ba4b474aa9c8cf11d6c9a35e`。
- 战乱等自动打牌遇到“必须从空候选中选择”的牌时直接结算其空选择语义，不再构造一个候选数为零的选牌请求；两组顺序夹具通过，runId `d59b5f0128e34e058a2cdef78d6f1bf4`。
- 知识恶魔的计划选牌分支回放失败时仅淘汰该候选，其他候选继续参与搜索；实包根状态在 DOP4、30 秒短搜内拿到可执行路线，runId `5bbd0920aabe4a4ca69e51d4d821867e`。
- Power 卡牌状态归一化在根物化时冻结已有牌；生命火花、流电等效果现在会覆盖第一张新生成牌。感染棱柱实包全自动实际打出“发现”并结束战斗，runId `a33c599436d74965afbada4582739aab`。
- 搜索根会导入压制保存的施法者与原始升级状态，击杀施法者后恢复到原等级；三骑士第 5 回合实包根在 DOP4 下返回跨死亡路线，runId `98d1af7fd9284e6698eb7deb2f37c51e`。
- 首回合抽牌前模拟受祝鹿角随机插入的三张晕眩，花粉核心额外抽牌与筹码选牌因此使用同一牌序。假商人实包的筹码页只搜索/选择一次并在首回合结束战斗，runId `8a140d914a4848d096b00651ba4f438a`。
- 复活中的生物拒绝新挂 Power，实验体重生回合不再保留实机不存在的弱化；Fork 边界回归 runId `daf83b4f2c614f008facdd5f9126ab23`。
- Fatal 资格在伤害与死亡 Power 清理前冻结；狂宴击杀 `MINION_POWER` 随从不再错误增加 4 点最大生命。女王最小夹具 runId `f80ec3725924407a8603741a1e5d78ce`。
- 搜索不再调用实机 `MonsterMoveStateMachine.RollMove`；怪物初始行动改为从分支快照中的状态机与 RNG 独立解析，避免外部行动预测补丁在并行搜索中改坏共享集合。Search/Prediction 目录已无 `RollMove` 调用，Fork 边界回归 runId `78f1a80afe664d1cbc97a80b70e131ed`。
- 强制用药搜索的中间路线先核对指定槽位与药水身份，只有满足全部强制指令的完整胜利线才会展示并允许“采用当前路线”；不再因采用零药中间线而在最终校验时报策略未满足。DOP4 控制器回归 runId `ce7406b333034a61b75c75ee4a5dac75`。
- 玩家在回合准备的 `Start` 阶段、普通搜索尚未启动前点击“执行本回合”时，控制器按当前回合排队执行请求；进入 `Play` 后同回合搜索直接携带执行意图，不再提示“当前不是玩家出牌阶段”。永世沙漏备注包确认了旧拒绝路径，本项完成 Release 编译与控制流验证，未伪造真人点击回放结论。

## 0.25.1（已发布，2026-09-01）：重算、选牌与搜索边界修复

- 主界面底栏移除“手动 GC”，该操作移到“设置 > 性能”的内存维护项；设置页生命周期验证通过，runId `530e502cf9a744c4995c2a56af57a954`。
- `213617` 批次：自动触发药水不再计入 Smart 主动用药梯度；全局超时路线先结算到回合末；木乃伊手按本地费用修正后的候选集推进 RNG。定向验收 runId `7b2a2cf1e7e34dd7813ec0248733ee8b`、`4e42216ff3d145d5a8a19b8dca0c857f`。
- `combatsolver-reports-20260901-204140.zip` 含两份实验体报告。最新 `0.25.0` 首根在 Medium、DOP4、`120 s` 内把 `1,560` 个父节点扩成 `99,804` 次转移和 `68,518` 个选牌分支，累计分配 `8.9 GB`，总预算耗尽时只完成两回合；路线随后精确复用到第 2 回合，没有模拟、部署或重算偏差。升级早有准备的 `Glam` 附魔会在同一动作内重复触发弃两张，原搜索把每次选择分别计算分支上限，且回合内长连锁可以占满全部请求时间。
- 多次选牌现在按整张牌共用分支预算，并在弃牌组合中保留弃牌触发、状态/诅咒清理、保留牌和牌堆取舍代表。深化搜索同时为后续回合层保留时间；Boss 至少按八个回合层分配剩余预算，层预算到达后从真实结束回合候选继续，不限制合法动作数。相同首根、DOP4、`30 s` 从 `4` 回合推进到 `8` 回合，转移 `131,808 → 127,890`、分配 `12,410,879,432 → 12,246,683,840 B`，runId `116f9e03b1fd4181b7412792a9e8277e`。头槌选牌在第 2 回合精确复用且计划外重算 `0`，runId `cb66e56414814c0eba21b176f5de3ce9`。
- 洗牌 Hook 中的回合选牌冻结请求时刻候选，避免后续监听器生成卡牌后污染分支；探寻打击改在攻击触发全部结算后生成候选，并用 RNG 计数和候选集合标识每次随机选择。盗窃跳虫搜索不再失败，runId `b8ca4dd0641840c59b7cee9a8c7a393e`；青蛙骑士同回合多次原生选牌后第 2 回合精确复用，计划外重算 `0`，runId `17cfabb9045a4935b2dcacb0b1ece959`。

## 0.25.0（准备发布，2026-09-01）：药水策略持久化与补查收束

- `CombatSolver-SLUMBERING_BEETLE_NORMAL-20260901-173206-221.zip` 来自 `0.24.1`，只作为不受信任的问题证据读取。包内只有一次 `SEARCH_REQUEST`，重算审计为 `searches=1` 且全部重算原因是 `0`；玩家看到的反复计算来自同一次 Smart 请求依次进入药水免费基线、固定前缀和开局药水后验，展开数从 `3936 → 22`、随后 `3684 → 1`。该包没有证明控制器反复发起搜索。
- Smart 搜索不再先跑可自由用药的主搜索，再追加无药、逐瓶固定前缀与开局后验。现在无药搜索是唯一基线，随后按“恰好 `N` 瓶”逐层搜索；同层全部药水在一个 Beam 中竞争，第一层满足门槛即停止。无药存活时每 `9 HP` 战损开放一瓶普通药，无药死亡时逐层找到第一条生还路线；石化蟾蜍生成的石头药等零成本药不占 `9 HP` 额度。各层共享请求时间余量并累计耗时、世界线和进度。
- 逐瓶药水策略从战斗会话移到持久化设置，只保存非“智能”的槽位 + 药水 ID 覆盖项。同一瓶药跨战斗保留强制/保护策略；药水被使用或槽位换成新药时清理旧覆盖，新获得的药保持智能。
- 控制器生命周期夹具完成逐瓶策略 JSON 往返、同槽换药默认智能、恢复智能后清除覆盖、Smart `1.2 s` 请求级预算和进度单调断言，runId `0911df45b8b34a04b761d9239f530e9f`。
- 合并 Moeblack 的 PR #25：求解器默认不再建立 No-GC 区域，也不再在搜索检查点或战斗结束主动触发补账回收，交由 CLR 正常分代回收；主界面新增“手动 GC”，只在玩家明确点击时执行完整回收。贡献者已完成实机可行性和内存占用验证，本轮不重复跑性能基准；合并时把手动回收后的内存日志改为只读，避免在既有两次回收后额外触发第三次。合并态药水/控制器夹具通过，runId `ffe6bad16592496ea1b02fbc6715930a`。
- 药水策略侧栏从 `300 px` 双列宽面板改为约 `184 px` 单列瘦卡片；主面板继续按原宽度计算，标题栏在侧栏区域单独留空，因此“药水策略 / 设置 / 收起”保持在原主面板右缘，不随侧栏向右移动。结构与生命周期夹具通过，runId `7c0d24e7f3b9448da0e596651d853e15`；按用户要求未做可见观感检查。
- 战损提示保留前往性能页提高预设的文字建议，但点击行为改为永久关闭该提示，不再自动跳转设置页；关闭状态写入玩家设置。世界线统计区显示当前搜索的无药或恰好 `N` 瓶药梯度；固定用药政策仍显示具体药名。
- 最新实机日志中的一次 Smart 请求实际启动了主搜索、无药复核和逐瓶固定路线共 `5` 个 solver，墙钟 `42.2 s`，最终仍选无药。梯度改造后，三瓶阈值与精确药量夹具通过，runId `406220b4b3b7482a97ebef4a16a330e9`；`1 HP` 必死局按无药→恰好一瓶进入格挡药路线并零战损生还，零计划外重算，runId `aa7e15b86b3a412c9c8abdea72d6b375`。
- Smart 搜索在单个 solver 每完成一层可执行回合路线时发布当前结果，而不再等整个 solver 或药水梯度结束。展示结果按胜负、被盗资源、战损加药水战略成本、原始战损、用药数和战斗进度严格单调改善；多花一瓶普通药却少掉不足 `9 HP` 不会覆盖旧路线。玩家点击“采用当前路线”后，solver 在安全检查点正常收束为这条路线，不以取消异常回退。普通搜索控制器通过，runId `c273a77c45f446d59d435a98f9a1e8dd`。
- 中间路线发布与采纳信号同时接入普通 Play 搜索、烘焙手套等首次回合准备搜索和原生选牌页手动刷新搜索。烘焙手套夹具在首次 solver 完成前显示当前路线、接受采纳并返回 `23` 动作计划，runId `0c29c9807cfb477f9e9e36f69a8cb626`。
- 搜索状态区改为与完成摘要一致的双行高度：第一行保留回合、耗时和搜索阶段，第二行以正常字号显示当前用药/战损与世界线数量，避免在主面板右缘被裁掉。控制器结构夹具通过，runId `f26539fb24a040c09705fb9f72947198`。
- 当前路线只发布玩家存活且敌人全灭的完整胜利线；睡眠 Boss 等尚未结束战斗的回合边界继续参与搜索，但不再把阶段性累计战损显示成整场预计战损。普通搜索/控制器夹具与烘焙手套开局搜索分别通过，runId `646a6ebe134e4253a9693983ae398240`、`3307d4fe09c544d3a4373b5339fa2991`。

## 0.24.3（已发布，2026-09-01）：性能引导与药水策略界面

- 新增一次性设置迁移：所有玩家在 `0.24.3` 首次加载时统一改为 Medium 和 `16 GB`；之后性能预设与搜索内存预算分别保存，切换 Low/Medium/High/VeryHigh 不再改写内存。新安装默认 Medium、`16 GB`，内存输入上限提高到 `256 GB`。Medium 迁移、恢复默认和 High + `17 GB` 独立保存均由定向夹具通过，runId `e93ec85ff4de49eea28dfeb5892de013`、`4d12cac9501248c6b108761450f098e8`。
- 搜索进度新增当前战斗累计世界线计数；审计补查与多阶段搜索共用累计口径，完成后显示总耗时与总查阅数，复用路线不重复计数。PR #20 仅作为累计数据链参考，未采用其排版。
- 推荐路线存在战损时，主界面顶部显示可点击提示，引导玩家进入“设置 > 性能”把预设改为 High 或 VeryHigh 后重试；零战损、搜索中和非结果页面隐藏提示。
- 主界面药水策略移到右侧，药水改为自适应多列竖向卡片；按钮字体统一为“执行本回合”使用的主字体，药水策略、设置、收起、重新计算、全自动和状态详情使用一致的按钮样式，全自动状态显示为开/关。
- `0.24.3` 玩家更新日志见 `docs/0.24.3-RELEASE_NOTES.md`。本轮按用户要求不做可见界面观感检查。

## 0.24.2（已发布，2026-09-01）：智能药水补查稳定性

- Smart 审计把后续回合的赌徒特酿路线改造成开局全弃重抽候选时，原先沿用了原动作的回合号和路线派生状态，导致从第 `1` 回合根回放第 `5` 回合动作。现在按当前搜索根重建开局药水动作并重新生成选择；鸟人精英同一首根由固定前缀异常改为正常返回预计用药 `1`、省血 `10/9` 的路线，runId `af0cd37576444983987eeb8e91be1dc3`。
- Smart 的“资源牌→药水”补查内部使用至少一瓶政策寻找反事实；某个可选后验没有合格路线时，现在只标记该候选不成立并继续比较其他路线。玩家逐瓶均为智能时不再显示“本场要求至少使用一瓶”；玩家明确强制用药的主搜索仍保持原失败语义。知识恶魔同一首根 `1 s`、DOP4 正常返回 Smart 无药路线，runId `df63187c89f748df8829271c8e333560`；原报告 `300 s` 整段未复跑。

## 0.24.1（已发布，2026-09-01）：回合开始选项稳定性

- `0.24.0` 实机日志中，烘焙手套首次搜索尚未发布计划时点击“执行”或“重新计算”，请求落入普通 Play 阶段校验并提示“当前不是玩家出牌阶段”。回合准备协调器现在以已观测且尚未解决的原生选择为边界：首次搜索中也可排队执行/全自动；执行后再重算会取消部署意图，等实际选牌完成后从 Play 根重新搜索。地精佣兵定向用例按“执行→重算”顺序得到新的第 `1` 回合 `28` 动作路线，runId `f5b7c0a4749c4baba471d58f0c5fb676`。
- 同一日志在手套选择仍等待时返回主菜单，原版 `NPlayerHand._ExitTree` 完成选择任务后继续调用 `AfterCardsSelected`，对正在拆除的战斗节点执行 `move_child/get_path`。现在返回主菜单前先取消协调器 token，并在战斗场景仍在树内时结束本地手牌选择；目标 `NPlayerHand.SelectCards` 异常栈不再出现。定向用例记录一次退出取消并通过，runId `1868ed8d9b3a4de19715438060db287f`。用例结束后 RitsuLib 设置页焦点链仍会记录独立的离树节点错误，不计作本修复覆盖。
- 烘焙手套选择分支可能移除智能药水后验固定路线所需的手牌；此前该不相容分支会使整次回合准备计算失败，随后界面只显示“当前不是玩家出牌阶段”。搜索现在淘汰不相容的选择分支，保留可执行分支继续求解；回合准备失败同时写入完整根异常与调用栈。同一报告状态修复前报 `找不到手牌 PIERCING_WAIL`，修复后在 `3 s`、DOP4 下生成预计战损 `1` 的路线并等待玩家确认，runId `8e44bc14c2f44574b36ac59a2dd402a2`。

## 0.24.0（已发布，2026-09-01）：长线策略重大更新

- 将后台筛选出的 `58` 份有玩家备注的“找到更优世界线”报告作为策略质量语料；最终验收按报告原搜索预算、同根状态与同 RNG 比较整场战损。无人测试现可组合同检查点的 `run-state` 与 schema 1 `replay-state`，恢复跑局身份、RNG、有序牌堆、逐牌状态、怪物行动和当前资源，并以完整 `ContinuationStamp` fail-fast 核对；逐样例的策略认识、改动和战损对比统一维护在 `docs/STRATEGY_OPTIMIZATION_LOG.md`。目前完成两份代表的严格导入与策略验收，不宣称已跑完全部 `58` 份。
- 单节点卡牌分支不再只按即时总分截取。展开阶段先保留即时防御、即时输出、过牌与资源、持续成长、控制、目标移除和生命投资各自的代表，再用总分填满分支预算；选牌边界和复活窗口继续保持既有强制保路。
- 智能药水审计新增主动干预：无药主路线死亡或预计战损达到保留阈值时，即使主路线没有主动喝药，也会在同预算下搜索一条最多使用一瓶药的强制反事实。只有它改变胜负、保护资源或实际省血达到药水保留成本时才采用。
- Smart 后验统一按每瓶 `9 HP` 重新核算药水成本，强制用药搜索返回的“至少一瓶”附加成本不再被误当成 Smart 门槛；鬼祟珊瑚群两瓶药仅省 `9 < 18` 时保留 `10` 战损无药路线，永世雕像一瓶迅捷药省 `10 >= 9` 时保持 `6` 战损。批量回放默认使用 VeryHigh、Smart、DOP4，普通样例单请求上限 `120 s`，Boss 专项为 `300 s`；High 仅作专项诊断。
- 三张单节点分支预算的过牌组合夹具以升级熟练与升级子弹时间验证资源路线能在即时攻击之外进入搜索，首动作稳定为熟练，runId `d2edebb4a7094d2fbce5787e02cc849c`。四只花园幽灵鳗短搜中，无药路线不能存活，主动药水反事实找到格挡药路线并确认省血 `12/9`，runId `fb7b2287a75e442cad01e1ca32f14417`。
- 持续能力不再以按卡名写死的 `12/8/5` 等常量参与保路。搜索根据剩余敌方生命、预计来袭伤害和命中数、可达攻击/技能/能力/消耗牌、牌的实际费用及预计剩余回合，分别计算伤害、防伤、资源、牌访问和成长潜力；逐次出牌、受击、回合开始、减费、生成牌、施加负面状态、消耗牌与球引擎分别使用自身真实触发结构。
- 普通/精英/Boss 的既有卖血值改为策略阈值，不再是绝对剪枝线。超过阈值的路线只有在同回合、同起点、同药水槽位的最低损失路线之外，实际换来击杀进度、延迟伤害、资源、牌访问、成长、控制或牌堆净化，并且仍能活到下一威胁时，才进入单独的生命投资保路；最终选择继续按整场真实战损比较。
- 逐次出牌获得格挡的能力与五张 0 费攻击牌组成的低分支夹具，首动作使用能力牌、整场预计战损 `0`，runId `35b7698ab74d462bb40182008bf6cd82`。一张支付 `6 HP` 并提供能量和抽牌的牌超过普通战斗阈值时，至少一条产生实际进度的投资分支得到保护，而最终路线仍选择更低战损结果，runId `02b131c187d1433c94ea558e485c591d`。包含资源、控制和多种攻击的固定 `250` 节点夹具通过 DOP1/DOP2 全字段等价并形成真实并发，runId `64414648aaae4406a570a0ff59ef1f17`。
- 同回合资源前验新增“当前能量/星能下可打出的整手牌价值”和零费可执行牌数。减费、过牌和禁止继续抽牌的顺序因此能在兑现伤害/格挡前进入独立战术启动通道；后续真实出牌结果继续作为后验，最终排序不直接读取该启发式。
- 用药保路除已用数量外，再按具体药水身份保留有上限的代表，避免即时收益更高的药水抹掉另一瓶药的后期窗口。默认性能档的 Beam 与选择分支整体上移一档：Low 使用原 Medium 容量，Medium 使用原 High 容量，High 使用原 VeryHigh 容量，VeryHigh 再扩大 `1.5` 倍；各档节点、时间和 No-GC 预算保持不变。默认 Medium 为 `24/60` Beam、`20/32` 卡牌分支、`10/18` 牌堆选择分支和 `12/24` 手牌选择分支。
- 已击杀候选按药水数量分别保留最低累计战损路线，避免治疗后的当前生命掩盖此前掉血；缩放、资源、控制和刚获得未来资源的路线增加真实结束回合后验。奥斯蒂当前/最大生命及下回合召唤按可兑现的奥斯蒂攻击参与资源比较，`ApplyRetain` 留牌选择进入持续选牌分支通道。蜂群术士严格首根在 VeryHigh、禁用药水、DOP4 下从报告原战损 `57` 降为 `0`，追平人工，runId `34d0742df3c74ee6a9a5006eabf0ece2`。
- 会消耗且只在致命时获得长期奖励的卡牌，现在按丢失奖励计算非致命使用的机会成本。感染棱晶样例 High、Smart、DOP4 从 `29` 降到 `21`，前三回合与人工路线一致；致命使用仍正确获得长期资源。
- 生成牌药水的开局审计增加一个按抽牌、能量和星能选择的资源后验；达到零战损后停止同药量的后续组合审计。直飞产卵虫样例 High、Smart、DOP4 选择无色药水生成急躁，从 `2` 降到 `0`。
- 智能药水补查只有在严格降低战损或保护被偷资源时才能覆盖主路线；同战损保留药水。蔓生伏地虫样例不再让 `14` 战损敏捷药路线覆盖 `4` 战损无药路线。
- 智能药水补查增加“资源牌→药水”的联合入口，并分别保留即时防御、持续设置和各目标首攻代表。残杀千足虫样例由未完成战斗的 `48` 战损降至可获胜的 `35`，人工为 `31`；硬锁全程集火会丢失路线，未采用。
- 开局资源入口按攻击、技能和能力类型各保留一个代表，并可连接即时防御后验。灵魂枢纽样例因此保留燃烧契约→火焰屏障→好勇斗狠路线，High、Smart、DOP4 从 `19` 降到 `12`，优于人工 `17`。
- 长期资源不再作为未兑现机会成本压低整个 Beam。候选发生长期资源分叉时，搜索额外保留一套同宽、独立排名的长期资源通道，与即时战损主通道共同展开，终局仍先比较整场战损。虱虫祖先样例保持 `16` 战损，感染棱晶样例从 `21` 降到 `19`，两场均在 High、Smart、DOP4 下追平人工。
- schema 1 旧 replay-state 没有 BaseLib modifier 字段时，仅将当前明确为空的 `/baselib=-` 视为等价；非空 modifier 状态仍参与严格 continuation 比较。机甲骑士旧包恢复到首回合根通过，runId `17d735ddcc0f463d95cc4fca1e06253a`。
- 主界面右侧新增药水策略折叠入口，按当前药水槽实时显示图标与官方名称。每瓶药可独立选择智能使用、强制使用或禁用保护；覆盖项绑定槽位和药水 ID，新获得或替换进槽位的药水默认智能。策略在主线程捕获进不可变搜索快照，修改后废弃旧路线并立即重算；强制药必须出现在最终路线，且只免除该瓶的 `9 HP` 保留门槛，额外智能用药仍按实际收益审核。
- 设置页删除整场药水策略，生产默认统一为逐瓶智能策略；无人测试仍可用请求级策略覆盖验证 Disabled/Smart/RequireAtLeastOne。新安装和恢复默认的性能预设从 Medium 改为 VeryHigh，即 `20/300 s`、`54/135` Beam、`10,000/50,000` 节点和 `16 GB` 搜索内存预算；用户已有明确预设或自定义参数继续保留。
- 合并 PR #12：多人跑局在所有搜索与回合准备入口保持求解器完全停用；Android/iOS 跳过运行时不支持的 No-GC API，直接使用现有低延迟回退。
- 长线人工优解专项在本版本收口，后续批次复用 `.agents/skills/strategy-replay-iteration/SKILL.md` 的预检、小批量、药水成本和数字记录流程，不继续维护 `docs/PERFORMANCE_FIXTURES.md`。
- 回合开始原生选项统一保留选择前搜索根：页面仍打开时，手动重算按最新性能与逐瓶药水策略立即重跑并替换推荐；玩家已经跳过或手动干预后，重算等待正式进入 Play，再从实际局面搜索。烘焙手套、工具箱和选择悖论均在同一原生页面完成第二次 `PlanReady`，runId `15f1fedfab9f4a15a8cf224afe6b36fd`、`55f9bea8cf014e94a194eaa8280ab1da`、`635775924e2041e5b8348def2a77fef7`；赌博筹码空选后的第 `1` 回合新路线含 `13` 个动作，runId `f87e11a8a9934e4cb0934ee47cadd4c0`。
- PR #13 首版只延迟引用释放后的回收，战斗重置仍会先立即结束 No-GC 区域，因此没有移动真正的击杀帧卡点。合并 PR #19 后，生产战斗结束的区域退出改为延迟 `3–5 s`，后续回收等待该任务完成且不再追加第二段延迟；搜索滚动、预算耗尽和下一次搜索前的释放保持即时。定向实测区域退出为 `3011.4 ms`、`gen2_delta=0`。

## 维护流程：分层快速回归

- 普通问题修复默认停在能证明首个错误状态的最低测试层：单效果严格差分；涉及 Fork、跨回合历史或续用时，增加最小两回合生命周期或最早复用边界。完整自动战斗不再作为每项语义修复的固定尾声。
- 快速 unattended 请求总超时限制为 `120` 秒。搜索型夹具使用短预算并在首个结果、目标动作或最早复用回合停止；超时后缩小 fixture 或记录未验证，不继续把同一场等待放大到 `180/360` 秒。
- 批量问题包按共享根因去重，每个根因保留一个代表。增量搜索核对只用于实际启动搜索的夹具，纯 actual/simulated 差分不再附加无效开关。固定长线、完整自动战斗和可见 Steam 回归留给搜索/部署改动、整场质量结论或用户明确要求的完整门禁。

## 未发布：随机效果与超大附魔牌组搜索性能

- `CombatSolver-AEONGLASS_BOSS-20260831-225816-880.zip` 只作为不受信任的问题证据读取；原始包保留在 Git 忽略目录，没有执行其中内容，也没有提交日志、存档、账户或环境信息。样本为 A10/第三幕（`actIndex=2`）的 Aeonglass 首回合，共 `2305` 张战斗牌，其中 `2187` 张为 `HAVOC+1 + CLONE×4`，形成约 `4499` 个战斗 Hook listener。原日志在 Beam 展开前已记录 `20.25 s` 根捕获、`46.04 s` 主线程长帧、约 `1.95 GB` 分配和 `16.59 s` GC 暂停；因此首要根因不在 Beam 宽度或节点上限。
- 新增去身份化合成 fixture `coverage/unattended/search-performance-ironclad-clone-havoc-huge-deck-cards.json`，用 `26` 条聚合记录精确恢复 `2302` 张牌组牌和 `3` 张战斗生成的 `DAZED`。无人测试单项注入上限从 `20` 提高到 `4096`，并新增 `--clear-run-deck`，使固定 A/B 不再混入角色默认起始牌；这些都只扩展测试协议，不改变生产搜索设置。
- `PredictedCard.MutablePreview` 现在只在首次物化或 Fork 后写时复制导致 preview 身份变化时失效 Hook listener 缓存。普通费用、关键字、回放次数等字段写入继续失效卡牌/牌堆指纹，但不再重建数千项 listener 列表；附魔、苦难、移出战斗和换主人等结构变化改为显式失效。BaseLib `CardModifier` 位于外部侧表：捕获到 modifier 的根会为所有战斗卡启用分支独立预览，在每次枚举时重建 listener，并禁用卡牌/牌堆 fingerprint 与 choice-key 缓存；modifier 的有序类型、`Amount`/`Priority` 和 `StoreSaveData` 的 `IntProperties`/`AdditionalProperties` 进入 fingerprint、选牌键与 continuation。BaseLib listener fork 不再复制永远不会读取的缓存，`EffectivePowers` 仍独立复用；`DirectModifiers` 使用一次编译的委托，空 modifier 卡不再创建临时列表，非空列表则在调用第三方 `StoreSaveData` 前固化快照，保留回调重入时的旧语义。游戏玩法效果生成的卡牌克隆同样复制 modifier、重绑 Owner 并调用 `AfterClonedOnCard`，同时按生成卡语义清空 `DeckVersion` 并将 `HasBeenRemovedFromState` 复位为 `false`；无 modifier 根继续使用原快速路径。最终完整 BaseLib 门禁通过，动态 fixture 在 `StoreSaveData` 回调内修改 live modifier 列表并验证本次完整状态键仍等于回调前快照，runId `06c5235a6d0941f69447180517bce7ab`。
- `CombatRootSnapshot.Capture` 原先前后各调用一次 `LiveCombatStamp.Capture` 和 `ContinuationStamp.CaptureLive`，而前者内部已经生成完整 continuation，导致 `2305` 张牌的 live 状态文本共序列化四次。现在前后各只捕获一次 `ContinuationStamp`，并从同一不可变文本构造 `LiveCombatStamp`；前戳移到 listener 枚举和第三方 Owner 发现之前，后戳仍能拒绝这些回调造成的实机语义漂移，而不增加第五次序列化。
- 原问题日志的战斗结束/失活回收有两次实际释放了大量存活对象，但主停顿为 `1455.6/525.4 ms`；另两次在存活量不降反升时仍停顿 `350.2/150.2 ms`。新策略把“退出 No-GC 区域”与“强制 Gen2”分开：低于 `256 MiB` 的战斗只退出区域并恢复 latency mode；超阈值的搜索、根捕获或取证分配才在后台请求一次非压缩 Gen2，不再每场无条件收集或压缩 LOH。
- 战斗 Reset 在建立引用释放/回收门之前先登记 early No-GC exit：它等活动搜索退出后仅结束区域并恢复 latency mode，不顺带触发 Gen2。Reset 同时取消搜索和部署，并异步等待当前与先前已换下的搜索 worker/主线程 callback、部署 operation、已延迟的根请求、回合开始 visual-setup/回合准备后部署任务、回合准备/自动预出牌原版任务和取证 FIFO 全部释放引用，最后才进入串行回收门。Godot `SceneTreeTimer` 仍服从 pause/time-scale，但外层等待可由生命周期 token 立即取消，Reset 不再被完整动作间隔拖住；Setup 原版阶段返回后会再次核对 active epoch，旧页面不能占住下一场。No-GC 耗尽回收以 reference-release epoch 和 Gen2 mark coverage epoch 配对：引用若在标记边界前释放则 join 同一次回收，若在边界后释放则补一次 Gen2，避免“收集已经发生但上一战斗图尚未解绑”。
- 最终 GC policy 时序门禁 runId `9b4f577a08c84800b219cbcb0bc83310` 在 exhaustion obligation 已登记后确定性覆盖标记前释放 `Gen2=1`、标记后释放 `Gen2=2`；两条弱引用图均死亡，再推进一次正式 release epoch 也不产生第三次回收。生产的三处 No-GC exhaustion 登记入口另经静态调用路径审计。控制器生命周期门禁 runId `ef96c383720b47dbbdcb62075bcb665d` 验证 A→B→Reset、搜索/部署 CTS 和旧 Setup epoch；真实 3 秒 `SceneTreeTimer` helper 取消后引用屏障在 1 秒内完成，正式 `Setup → Resume` 的 token 接线经静态审计。正常 `Setup → AutoPrePlay → 熵选牌 → 第 2 回合复用` 仍由 runId `6f09011103084daaaca36084264eacdb` 覆盖。
- 最近战斗取证的稳态对象不是 live `CombatState` 泄漏，而是最多 6 份序列化检查点；2305 张牌样本按旧 UTF-16 表示外推约 `192.2 MiB`。metadata/replay 现在一次序列化成无 BOM UTF-8 `byte[]` 并直接写 ZIP，删除持久 checkpoint 中未读取的 `StateText`，同输入估算降至约 `101 MiB`，同时保持 current/recent、FIFO 和错误传播语义。current 与 recent 双导出门禁 runId `43e3cc399a6b45e8968da5f7af05556d` 通过严格 UTF-8 与既有 JSON/原生状态/跑局状态检查。
- 完整矩阵首次暴露 `NightmarePower.Card` 与 `ShrinkPower.ApplierName` 的展示文本差分假阳性：这些 `StringVar` 已由 `SemanticStateFieldPolicy` 分类为 `PresentationOnly`，模拟器故意不在 worker 中读取本地化文本。差分归一化现在保留字段名和数值基线、忽略展示字符串，未知字符串仍 fail-fast；最终完整矩阵中的 runId `1fe98254c4d045f7a6d7872ae0f26c6f` 与 `fb2d85cd91724310a3772ebf6204e893` 均通过。
- 清空默认牌组后，固定 DOP1、同一 `1 s` 短搜的上游基线/最终均保持 `expanded=1 / transitions=3 / forks=3 / replays=2`、同一 `ENTROPY` 动作、评分 `-3752001` 与终止边界 `TimeLimit`。根隔离测试阶段由 `4570.556 → 2215.689 ms`（`-51.52%`），搜索时的生产根捕获由 `3971.045 → 2235.620 ms`（`-43.70%`）；搜索由 `5186.2 ms / 912,561,760 B` 降到 `3471.0 ms / 267,554,848 B`（耗时 `-33.07%`、累计分配和每转移分配均 `-70.68%`）。结果时 managed-live 由 `3,989,002,752` 降到 `3,342,816,112 B`（`-16.20%`），工作集由 `5,366,906,880` 降到 `4,723,740,672 B`（`-11.98%`）。最终源码复验仍为同一 `1/3/3/2`、路线、评分和边界：根阶段 `2208.239 ms`（runId `7e8a7512406c44e6bd0bb0fec7a788e0`），搜索 `3717.9 ms / 267,559,992 B`（runId `47c008cb690d4073bfa0781d510d6378`）。
- 新增四个固定随机复杂矩阵：三骑士、女王、实验体和永世沙漏。每组把点名的随机/跨回合牌放在手牌，并从对应角色牌池随机选取一次、随后冻结 `5` 种攻击与 `5` 种防御，每种 `6` 张；所有选择均写入 fixture，A/B 不在运行时重新抽样。四组 `2 s`、DOP1 首回合搜索均在 `120 s` 门限内通过，分别为 `1324.6/2143.5/2062.0/1393.4 ms`，累计分配为 `96,126,192/250,810,968/160,130,880/149,711,016 B`。女王的 choice 分支最多（`1146`），是当前矩阵中优先继续压测的随机分支样本。
- 这些数字来自隔离的 Linux headless 合成首回合，不替代带完整用户 Mod 组合的可见 Steam 帧时间结论，也不覆盖怪物战斗中后段的私有状态。下一阶段优先评估根级卡牌签名/choice 元数据驻留、根 combat preview 复用、按分配压力自适应 DOP 和保序的 typed Hook plan；分页 COW/持久化牌堆、增量哈希与扁平 transposition table 需先拆分分支私有 CardState、OwnerPile、observer 与外部引用解析器，留给独立高风险改造。
- 2026-09-01 最终 Linux headless 全矩阵执行 `246` 条命令：`242` 通过、`3` 个外部用户存档夹具按预期跳过、`1` 个在游戏内结果已为 Passed 后遇到启动器退出身份确认竞态。该项游戏内 runId 为 `50a3d2aaf3d648498666e9d46ad1b2b9`，完全相同命令随后以 runId `08fdef52d7974d9185b255edafd6395e` 独立复验并由启动器返回 `0`；因此 `243` 条可执行命令（`242` 个唯一 ScenarioId）均取得通过结果，没有未解决的行为失败。

## 0.23.0：0.22 系列修复汇总

- `0.23.0` 汇总 `0.22.0` 之后至 `0.22.11` 的玩家可感知改动，合并说明路线与重算、自动执行与选牌、敌人与效果、长线稳定性、路线策略和问题反馈变化，不按补丁版本重复罗列。
- 本次准备发版只同步版本与发布文档；行为源码与 `0.22.11` 相同，沿用 `0.22.1–0.22.11` 已记录的定向证据，不重复运行行为测试。
- `0.23.0` 玩家更新日志见 `docs/0.23.0-RELEASE_NOTES.md`。

## 0.22.11：回合开始选牌只结算一次

- `combatsolver-reports-20260831-141700.zip` 共含 90 份报告，版本跨度为 `0.21.7` 至 `0.22.9`。按首个差异归并后，当前仍可复现的共享根因是回合开始选牌后重复执行通用阵营回合开始钩子；其余当前切片中的同名卡牌部署偏差已由 `0.22.10` 修复。项目没有通用问题包整战导入器，本轮没有宣称逐场重放 90 场战斗。
- 原实现把“选牌回调是否已经触发阵营回合开始”作为调用前求值的布尔参数传入玩家回合开始函数。选牌回调在函数内部把外层标志改为真，但被复制的参数仍为假，导致熵等回合开始选牌完成后，好心情花、覆甲、能量、格挡和敌方状态再次推进。玩家专属回合开始钩子与通用阵营回合开始钩子现已分离，外层在选牌回调返回后读取真实标志并只结算一次。
- 修复前定向夹具在第 2 回合选完熵后产生一次计划外重算，runId `632b60e264064d149ec0e95f45e8a15a`；修复后第 2 回合直接复用、计划外重算 `0`，runId `cf5c0c5b057e4e0a80409e87124c7535`。
- `0.22.11` 玩家更新日志见 `docs/0.22.11-RELEASE_NOTES.md`。

## 0.22.10：同名卡牌自动执行身份

- `combatsolver-reports-20260831-133031.zip` 含五份来自 `0.22.3 / 0.22.4 / 0.22.6 / 0.22.8` 的报告。旧版女王与实验体的回合开始消耗选牌、猫头鹰法官的 Power 动作边界、实验体的后台回收，以及永世沙漏的出牌后 Hook 顺序均已有后续版本覆盖；本轮不重复增加遭遇或卡牌特判。
- 当前仍存在的共享缺陷位于实机自动部署：路线已经保存卡牌完整状态身份，但部署仍按卡牌 ID 和搜索根中的同名序号取牌。前一个同名实例在先前动作中离手后，后一个实例仍携带旧序号，部署因此报找不到手牌。部署现在与搜索回放使用同一状态身份及同状态次序；旧路线没有状态身份时继续使用原 ID 次序。
- 定向身份夹具让两个同名实例具有不同临时状态，并在前一个实例离手后只保留计划实例，部署仍能准确定位，runId `9e733b537f8f4eee98a4df4f79389bb6`。实验体问题包的战前跑局以当前源码自动执行到第 3 回合并直接复用，计划外重算 `0`，runId `7a8d7f88e17b4b05af7e99e2139e1044`。第一次现场请求额外强制必须打出全息影像，但当前短搜选择了另一条合法路线；它同样已到第 3 回合且零重算，仅该测试断言失败，不计通过。
- `0.22.10` 玩家更新日志见 `docs/0.22.10-RELEASE_NOTES.md`。

## 0.22.9：自动出牌、药水选牌与回合边界

- `combatsolver-reports-20260831-123849.zip` 含三份分别来自 `0.22.3 / 0.22.4 / 0.22.6` 的报告。永世沙漏报告在惊逃回合末自动打出头槌时选择了实机弃牌堆中不存在的彼岸咆哮；帝王蟹报告在横祸自动打出虚空形态后仍规划同回合后续动作；女王报告在预知之滴的重复剑柄打击候选上失败。附件只作为取证输入，没有逐场导入完整战斗。
- 模拟 Hook listener 继续保持原版的“生物能力先于遗物、药水和战斗卡牌”顺序。新施加或分支克隆的玩家能力回到所属生物的能力段，不再被统一追加到卡牌之后；惊逃因此会先于放逐区中彼岸咆哮的回合末监听器执行，与实机一致。
- 虚空形态结束回合改为卡牌语义产生的一次性回合请求，外层手动打出和横祸等内层自动打出共用同一路径。搜索动作记录会标记实际导致回合推进的卡牌；续用缓存、下一回合选牌、路线分层和自动部署均按该标记识别边界，不再只识别卡牌 ID 或显式结束回合动作。
- 预知之滴、灰水、赌徒特酿、液态记忆和疯狂之触的既有牌堆候选直接从用药前父状态建立；只有生成随机卡牌候选的药水需要先执行探测。无效药水选择分支与普通卡牌选择一致，归类为该分支不可回放，不再中止整次搜索。
- `FIX-123849-BOUNDARIES` 覆盖能力/卡牌 Hook 顺序、横祸嵌套虚空形态的一次性结束回合请求及预知之滴重复牌跨 Fork 回放，runId `aadd72eb88f0498a8a125e43dd2a3000`。横祸短搜路线只保留一项第 1 回合动作并推进到后续回合，runId `0569fa1dddf5479493aab7fe14a352c6`；预知之滴四张同名升级牌的强制用药短搜完成 `12` 个选牌分支，runId `5babab76d0de43a3bb73861bb7e40726`；横祸全自动执行后第 2 回合直接复用、计划外重算 `0`，runId `9cc7926cb1814abcabad5ad83dee4bfc`。
- 首次横祸部署夹具 runId `27b504a98ee84ae8b3eb4b3288544c7b` 证明实机已停止同回合后续出牌，同时暴露续用缓存只登记显式结束回合节点；统一边界后由上述最终夹具通过。更早的 runId `c1e23e0c1b444ee1ac05ae80800eb14a` 因测试参数误用了必须同时提供标题的断言而失败，修正为首动作断言后通过，不属于生产功能失败。
- `0.22.9` 玩家更新日志见 `docs/0.22.9-RELEASE_NOTES.md`。

## 0.22.8：搜索内存检查点稳定性

- `CombatSolver-KNIGHTS_ELITE-20260831-113335-820.zip` 来自 `0.22.4`。高档 DOP4 搜索累计分配达到 `8 GB` 检查点后，回收过程没有产生已完成的 `GCMemoryInfo`，诊断清理却读取默认结构的 `Concurrent` 属性，最终以 `NullReferenceException` 替换了原始回收结果。异常栈完全位于 Runtime GC 政策，不属于三骑士或储君战斗语义。
- 搜索内检查点现在先完成一次阻塞、非压缩的全代回收，再按原预算重建 No-GC 区并从保留的当前搜索层继续。该路径只用明确的完成标志记录回收结果；真实回收或取消异常会原样传播，不再被诊断日志覆盖。
- 定向 Runtime 夹具实际完成一次搜索内回收并继续，随后保持 No-GC `1 GB → 2 GB` 生命周期与 DOP1/DOP2 固定工作量结果一致，runId `f8764bdbe2594c66920ff428fe6425d6`。本轮没有完整导入或重放问题包中的三骑士战斗。
- `0.22.8` 玩家更新日志见 `docs/0.22.8-RELEASE_NOTES.md`。

## 0.22.7：回合准备页面与路线 UI 同步

- `CombatSolver-THE_INSATIABLE_BOSS-20260831-112541-766.zip` 来自 `0.22.4`，当前战斗是首回合工具箱选牌；其搜索在第 4 回合因待提交力量变化进入 Fork 而失败，这项动作边界已由 `0.22.6` 覆盖。该包不包含烘焙手套跨回合单步复现，不能作为本轮接管结论。
- 当前源码的定向夹具确认：执行本回合进入第 2 回合烘焙手套页面时，求解器没有代选，但路线面板会在原生页面动画与表面锁完成前短暂停留在第 1 回合。既有路线的当前回合视图现在随回合准备事务立即切换；面板从原路线裁出当前及后续回合，并显示等待回合开始选择。
- 既有路线的自动选牌驱动改为在原生页面真正可操作后启动。单步执行到边界时页面交给玩家；只有玩家明确点击执行本回合/全自动，或原本就在全自动模式，才消费计划选择。
- 单步边界验证通过，原生第 2 回合页面保持 `Selected=0` 且 UI 为第 2 回合，runId `64243d425288491198f9a6bf5f415de0`。随后从同一边界明确执行本回合，烘焙手套选择完成、路线复用且计划外重算为 `0`，runId `ae2d5384fa144815b7a250f2442c556c`。首轮新增 UI 断言在旧刷新时机下按预期失败，runId `a3f331c2cf0b48c29f42e6b6c3a56b00`。
- `0.22.7` 玩家更新日志见 `docs/0.22.7-RELEASE_NOTES.md`。

## 0.22.6：搜索快照与回放稳定性

- `combatsolver-reports-20260831-095952.zip` 共含 `19` 份报告，版本分布为 `0.22.3 / 0.22.1 / 0.22.0 = 12 / 2 / 5`。当前版本报告按共享根因归并；两份旧版免费能力层数、旧版受感染棱镜原生选牌已由 `0.22.2` 覆盖，双尾鼠随机初始行动已由 `0.22.5` 覆盖，不重复增加遭遇专用逻辑。
- 搜索根完成物化后，牌堆移动、卡牌公式和 Power 效果只读取分支预览。玩家在后台搜索期间手动打出能力牌或其他牌，不再通过实机原牌的移除标志反向污染冻结快照；产卵飞虫、无厌沙虫和帝王蟹报告中的最终回放多牌、少牌或找不到计划牌归入这一根因。
- 路线动作新增可选的精确卡牌状态身份及同状态出现次序。同 ID、同升级等级但分别来自永久牌组和局内生成复制的卡牌不再互换，外骨骼虫与实验体报告中的 `DeckVersion` 差异由正确实例选择解决；旧路线缺少该字段时继续使用原有 ID 次序回放。
- 卡牌执行在嵌套选择或触发式选择暂停前会结束本次配对出牌事务；每个可 Fork 的动作边界统一提交 Power 层数变化并规范化相关状态。知识恶魔、女王和帝王蟹报告中的待结算配对出牌或力量变化不再跨入下一次 Fork。
- 狂乱逃离在目标身上没有有效流沙坑时按原版完成为空操作，再继续其费用成长；不会为了缺少目标 Power 中止搜索。毒气炸弹、瀑布巨兽等会在当前行动中自我移除的怪物完成该行动后终止意图预测，不再要求不存在的后继行动。
- 运行级 Hook 前缀改为从当前跑局的牌组与附魔一次性捕获；战斗 listener 后缀继续严格验证。虔诚雕刻师报告中，两次独立枚举之间 listener 列表变化造成的错误后缀断言已消除。
- 定向边界与根快照回归通过，runId `0c19ded7f13444fe914499f85924b840`；短预算增量/完整回放一致性通过，runId `dd22d98ed4174af692a27764871314f9`。后者虽然请求 DOP4，但严格增量模式按既有政策强制单线程，不作为并行吞吐量证据。首轮边界请求 runId `52ab5e56bb1744e99aa9b993b3a22306` 因新增流沙坑断言使用未挂接战斗状态的测试夹具失败，修正夹具后通过，不属于生产功能失败。本轮没有逐场重放全部 `19` 场战斗，不把分组归因写成整包完整执行通过。
- `0.22.6` 玩家更新日志见 `docs/0.22.6-RELEASE_NOTES.md`。

## 0.22.5：敌方回合生成怪物的随机初始行动

- `CombatSolver-TWO_TAILED_RATS_NORMAL-20260831-095426-440.zip` 来自 `0.22.3`。首回合根本身稳定；搜索推进到第 3 回合双尾鼠呼叫支援时，新生成的双尾鼠以随机分支作为行动状态机初始节点，因此原版状态日志在正式掷出行动前为空。预测注册把“日志为空”误判为没有初始行动，六次显式重算均在同一结束回合转移失败。
- 敌方回合生成的怪物现在允许以随机分支进入待选初始行动状态：注册阶段选取状态机中已有的可执行行动作为非执行占位，不消费怪物 RNG；统一的下回合行动准备阶段再按原顺序掷出并登记真实行动。已有具体初始行动的幻象等怪物继续保留原行动，死亡/复活边界不变。
- 最小 Fork 边界验证确认生成双尾鼠时怪物 RNG 不变、回合边界才消费 RNG，且结果为抓挠、疫病啃咬或尖啸之一，runId `02120badde45441f99b384437879e517`。同一问题包根修复前 runId `941ffb14c3244100b3285336a61db0eb` 在第 3 回合失败；修复后 runId `5649195a84e342c7b8bc8d70dd5281c0` 完成首轮短搜并给出 10 回合、预计战损 `4` 的路线。
- `0.22.5` 玩家更新日志见 `docs/0.22.5-RELEASE_NOTES.md`。

## 0.22.4：手操优解反馈与策略校准

- 从在线问题后台筛出自动分类为“找到更优世界线”且战损改善至少 `35 HP` 的报告，按遭遇去重后保留 `9` 份代表。旧报告只用于定位候选盲区；当前源码用问题包内战前存档、首个搜索根牌序、敌方生命和行动重新建立短搜基线，不直接把旧版预测当作现行质量。
- `DemisePower` 已在模拟器中按敌方回合末伤害执行，但此前中间排序只统计毒与厄运，消亡粉末路线因此会在实际伤害结算前失去延迟伤害价值。现在把当前破灭层数计入延迟伤害，最终路线仍按真实生存、胜利和整场战损选择。
- 必备工具不再按普通 `1` 层持续能力评价，而按会持续改变后续技能牌序的引擎计入 setup 价值；仍使用普通战斗和 Boss 现有上限，不改变最终路线比较规则。
- 同预算花园幽灵鳗代表从预计 `35` 战损降到 `27`，并找到两瓶药路线，runId `954385d2794049ada20ee0706016486c`；巨斧机器人代表从 `5` 降到 `4`，runId `4595ce39f3df4775b008ea75d46d1f20`。知识恶魔维持 `3`、帝王蟹维持 `0`、双小啃兽维持 `0`，对应 runId `3f4973a3ebe64387af2fca6925797354`、`8b27b0f445b24e669189b855e342358a`、`0d8363df9df44164a0ac8f4267d64e31`。
- 全战斗统一增加未来资源总分、提高早有准备总值、放大破灭重复回合收益和扩大逐目标状态分组均未改善目标与守门样本，其中前三项产生明确战损退化，已全部撤回。保留的两项均有同根 A/B 结果，不把失败实验带入发布。
- `0.22.4` 玩家更新日志见 `docs/0.22.4-RELEASE_NOTES.md`。

## 0.22.3：计算型变量与局内内存检查点

- 继续收口 `combatsolver-reports-20260831-002401.zip`、`002513.zip`、`002652.zip`、`003125.zip` 和 `004356.zip`。五批中按逻辑异常关键词命中的报告共 `105` 份，现行 `0.22.0` 的 `18` 份已经归入 `0.22.1/0.22.2` 的状态戳、选牌身份、结算顺序、死亡/召唤生命周期与怪物行动快照修复；其余旧版样本按共享根因去重，不把手操优解或第三方玩法内容混入本轮错误修复。
- 选牌估值不再通过 `DamageVar/BlockVar/CardsVar` 强类型属性读取基础值，而是按动态变量键读取实际 `DynamicVar.BaseValue`。RitsuLib `ComputedDynamicVar` 或其他合法派生变量可以参与稳定估值，不会因变量名与原版具体类型不一致抛出强转异常，也不会在后台调用第三方实机求值器。
- 后台 Gen2 回收的完成判定改用 CLR 的代际回收计数。首次尚未产生 Background/FullBlocking 分类记录的进程不再读取未初始化的 `GCMemoryInfo`，长线搜索到达局内内存检查点后可以完成回收并继续搜索。
- 定向 `FIX-0223-DYNAMIC-VAR` 验证计算型 Damage 变量读取 `17` 的稳定基础值且没有调用返回 `99` 的实机求值器，runId `adf440d49b424b659d54c2187f4c5953`。`FIX-0223-GC-COLLECTION` 以高血量、多候选固定根验证后台回收、No-GC `1 GB → 2 GB` 切换和 DOP1/DOP2 全字段等价，runId `e8c3222c43d842daa3e2e640155c8d3f`。首次 `1 HP` 输入没有形成并行 wave，被既有门禁拒绝，不计通过也不属于本轮功能失败。
- `0.22.3` 玩家更新日志见 `docs/0.22.3-RELEASE_NOTES.md`。未逐场重放五批中的 `105` 份逻辑报告，不把静态归因写成全部整场通过。

## 0.22.2：连续结算、选牌与怪物生命周期

- 本轮合并分析 `combatsolver-reports-20260831-002401.zip`、`002513.zip`、`002652.zip`、`003125.zip` 和 `004356.zip`。其中现行 `0.22.0` 报告共 `18` 份，按共享根因处理；手操优解只保留为后续策略样本，第三方玩法内容造成的状态字段与手动选择不纳入基础游戏适配。
- 回合准备选牌游标的消费前回调覆盖完整的抽牌与回合开始结算。赌博筹码等开局选牌完成后，先于后续遗物结算的格挡状态会按原版顺序进入搜索根，不再因回调作用域过早结束产生生命或格挡偏差。
- 洗牌触发战略选牌时会先交付该选择，再继续同一回合开始链上的后续选择；卡牌在原生选牌页面打开期间发生费用、伤害、关键词等可变状态更新时，计划令牌仍按卡牌 ID、升级等级和同名出现次序定位同一实体。
- 出牌结束边界会提交待结算的 Power 层数；免费能力牌等遗物在 `BeforeCardPlayed` 阶段消耗的层数也会在卡牌效果开始前提交。力量变化和遗物计数不会再跨 Fork 留在影子状态，最终路线标注与增量分支保持一致。
- 卡牌伤害导致敌人死亡后，模拟会先完成原版死亡 Power 与移除阶段，再执行该牌后续施加效果。永久死亡目标不再接收后续 Power；需要复活的敌人继续保留其复活阶段，修复击杀后错误抽牌、选牌目标缺失及连续行动漂移。
- 敌方回合生成的怪物保留原版已经建立的初始行动记录。它们在首次正式行动前死亡时可以正确进入幻象复活；强制行动同时结束“等待首次掷骰”状态，不再覆盖复活动作。
- 生成牌历史新增实际创建者。超质量体只统计由玩家创建的牌，不再把没有创建者、仅归玩家所有的牌计入额外伤害。
- 怪物行动状态机在主线程根捕获时一并冻结。快速读档或离开旧战斗后，仍在结束中的后台搜索不再回读已拆卸实机怪物上的状态机。
- 定向验证 `FIX-0222-BOUNDARIES` 覆盖选牌身份、Power 提交、免费能力牌遗物消耗、生成牌创建者、死亡后禁止施加、召唤怪初始行动与幻象复活，以及实机怪物状态机被清空后的快照推进；runId `805266b49faa4435abaae7566edaed64`，`22.5` 秒通过。未逐场运行 `18` 份完整战斗，不把问题包静态归因写成整场回放通过。
- `0.22.2` 玩家更新日志见 `docs/0.22.2-RELEASE_NOTES.md`。

## 0.22.1：重算收敛与问题包精简

- `combatsolver-reports-20260830-225649.zip` 共 `47` 份问题包，实际版本分布为 `0.20.0 / 0.21.1 / 0.21.2 / 0.21.3 / 0.21.4 / 0.21.5 / 0.21.7 = 1 / 1 / 5 / 12 / 4 / 12 / 12`。旧版本包只作补充证据；当前修复以 `0.21.7` 的纯求解器控制样本和现行 `0.22.0` 源码为准，手操优解与第三方玩法内容没有混入基础游戏重算根因。
- 后台搜索的实机状态戳改为复用完整跨回合状态文本，覆盖药水、Power、遗物状态、完整牌状态与 RNG。玩家在计算期间喝药或改变这些状态时，旧结果会在展示和执行前直接判为过期，不再执行后到下一回合重算。
- `WHISPERING_EARRING` 的自动出牌严格限制在首回合。此前预测会在后续回合再次执行最多 13 张自动出牌，造成能量、格挡、牌堆、遗物计数和敌方生命同时漂移。
- 回合末临时敏捷回退直接读取仍生效的 `TemporaryDexterityPower` 实例并按所有者汇总，不再依赖“本次模拟中刚施加”的旁路字典。从实机根捕获到已经生效的速度药水、预判、螺旋飞镖或消逝时，也会在回合末正确移除对应敏捷。
- 新召唤怪物的随机生命判重改为读取模拟生物状态中的当前最大生命。直飞产卵虫的蛋孵化后，第二轮产卵不会再拿孵化前的旧生命参与判重；保留原版候选集合和 RNG 调用方式。
- 即时问题包只保存当前战斗；离开战斗后提交时只保存最近结束的一场。常规日志缩为最新 `1` 份尾部 `2 MB`，单场日志片段上限 `4 MB`，内存检查点上限 `12`、归档最近关键 `6` 个；截图、整批磁盘存档、此前楼层和第二份战斗取证不再打包。战前内存跑局存档、结构化状态、原生状态、即时跑局状态、路线、设置、环境和重算审计继续保留。
- 定向验证：产卵生命判重、`WHISPERING_EARRING` 回合边界和 Fork 状态通过，runId `a5104a89fd614a8196c17ea86bc2f042`；速度药水根状态的回合末敏捷回退严格差分通过，runId `bc952a2f52314755b7be8f215e348349`。精简问题包结构断言通过，实际为 `71,470` 字节、`21` 个条目；同一夹具随后在无关的初始求解等待阶段达到 `120` 秒，因此不计整场通过。
- `0.22.1` 玩家更新日志见 `docs/0.22.1-RELEASE_NOTES.md`。

## 0.22.0：搜索性能与界面设置

- 合并贡献者 [ltlly](https://github.com/ltlly) 的 [PR #10](https://github.com/Torch1230/CombatSolver/pull/10)。本轮不改 Beam 宽度、节点预算、评分、候选接受顺序、药水策略或 coordinator 的有序提交，只优化搜索热路径分配与 No-GC 区域生命周期。
- `PredictionForkContext` 在大型映射上改用池化对象身份哈希，小映射继续走线性快路；选牌语义键跨 Fork 缓存，牌堆反向定位、Hook listener 失效观察、稀疏 Power affliction、投影洗牌和 Snapshot 活牌列表减少重复分配。Ritsu 空 capability 快通道按类型与注册 generation 缓存，实例 capability 始终优先。
- 生成牌药水不再克隆全部候选后丢弃未选项，只按原 token/occurrence 顺序定位并克隆实际选中牌；候选集合、顺序、分支上限、评分与药水政策不变。
- No-GC 预算现在逐值执行：低/中/高/极高精确请求 `6/8/12/16 GB`，自定义 `1–16 GB` 按保存值请求且没有隐藏上限。活动搜索使用旧预算时，新预算请求等待旧区域释放，再回收并建立准确的新区域；安全检查点继续保留活动 Beam 后滚动回收。
- PR 固定大牌组 A/B 保持 `576` 展开、`3463` 转移、`1124` 选牌分支以及同一 3 回合、预计战损 `2` 路线：基线 `5116.3 ms / 1,039,502,640 B`，优化后 `3985.0 ms / 528,876,328 B`，累计分配降低 `49.12%`，单次 headless 墙钟缩短 `22.11%`。这是固定工作量单次样本，不替代可见 Steam 性能结论。
- PR 的完整 16 GB 长搜保持 `7018/52644/23196` 工作量、评分、10 回合胜利、预计战损 `56` 与两瓶药策略；仓库新增两份脱敏压力 fixture。合并态定向门禁实际完成 No-GC `1 GB → 2 GB` 切换、DOP1/DOP2 全字段等价、Fork 边界和完整自动战斗，runId `26654574147d4925be016d7295fbee2a`；构建与结构门禁同时通过。
- 合并贡献者 [ClosedFlux](https://github.com/ClosedFlux) 的 [PR #11](https://github.com/Torch1230/CombatSolver/pull/11)，保留其浅色视觉方案和透明度实现，但不把浅色强制为唯一主题。新安装与恢复默认继续使用现有深色主题；常规设置新增“深色（默认）/浅色”选择和 `25%–100%` 覆盖层透明度滑块。
- 主题切换会在下一帧重建覆盖层，重新使用当前主题 token，并恢复最近路线、设置页开关状态、折叠状态和透明度；已缓存的富文本颜色会转换到新主题。浅色主题使用白色内容面、深色文字、主题蓝按钮和适配后的动作胶囊、状态徽章与反馈横幅；深色主题保持原配色与执行高亮。
- 透明度滑块使用固定可见的轨道、强调色已选区段和独立悬停色拖动圆点。原合入样式的轨道只有约 `2px` 边框高度，且已选区段全透明，在深色设置页中难以辨认；现有数值范围、步长和即时保存行为不变。
- 定向 headless 生命周期回归验证默认深色/100%、浅色与透明度设置回读、主题重建后路线/设置页恢复、65% 透明度即时应用，以及既有三页设置和搜索停止/恢复，runId `921055897c4c43ceb773c90c79eac953`。像素观感、拖动和动画尚未在 Steam 可见会话中人工确认。
- `0.22.0` 玩家更新日志草案见 `docs/0.22.0-RELEASE_NOTES.md`，汇总 `0.21.1–0.22.0` 的多核搜索、稳定性、跨回合修复、设置、通知、反馈与界面主题更新。

## 0.21.7：设置页重构与搜索结束通知

- 设置页重组为“常规 / 性能 / 反馈”三页。常规只保留求解器、药水、通知和自动执行；性能集中预设、并行度、搜索内存预算与自定义搜索参数；反馈集中诊断日志、联系方式、上传和导出，上传进度紧跟操作按钮。三页共享固定状态栏，切页与返回都会先提交当前输入，非法数值会留在原页并聚焦对应输入框。
- “禁用求解器”改为正向的“启用求解器”；“本场药水策略”简化为“药水策略”；“No-GC 预算”改为玩家可理解的“搜索内存预算”。底层持久化字段保持兼容，现有设置文件无需迁移。
- 性能页默认折叠四项自定义搜索参数，低/中/高/极高预设会在输入框中显示实际快搜与深搜数值；只有真正修改数值时才切到“自定义”，不再用空白输入代表预设默认值。
- 搜索结束通知的开关和时机合并为“关闭 / 仅游戏不在前台（默认） / 始终通知”三态选项，仍无损读写原有两个持久化字段。

- 新增搜索结束通知。主搜索与开局/回合开始选牌搜索在成功、失败、用户停止或结果因实机状态变化而过期时都会提交一次通知；被新搜索替代的旧回调和没有实际搜索的路线复用不重复通知。
- 新安装和恢复默认均开启通知，并只在游戏不处于前台时提示；设置保存后立即生效。
- Windows 通过原生通知区域接口显示系统通知，不设置静音标志，因此提示音跟随 Windows 通知音量、专注助手和系统通知策略。前台判断使用当前前台窗口所属进程；游戏最小化或切换到其他应用时属于后台。
- 首次可见测试发现 P/Invoke 默认把托管方法名 `ShellNotifyIcon` 当成原生导出名，实际 Windows 入口为 `Shell_NotifyIconW`，五次搜索完成回调均抛出 `EntryPointNotFoundException`，所以没有通知且回调在路线展示后提前终止。现已显式绑定 `Shell_NotifyIconW` 与 `LoadIconW`；独立 Win32 通知探针返回 `shown=True / win32_error=0`。系统提示音仍由 Windows 当前通知设置决定。
- 非 Windows 平台在任何 Win32 调用前直接结束；headless 同样不创建通知区域图标。没有引入仅 Windows 可加载的托管依赖，macOS 不会因空调用 Windows API 报错。
- 定向验证 `NOTIFICATION-SETTINGS-LIFECYCLE-NEXT` 覆盖默认值、关闭/仅后台/始终通知判断、设置 UI 回读、用户停止搜索通知和 headless 原生调用隔离，runId `99e510b870fc4ad6ab0611ce36f8f3b1`，通过。独立 Windows 原生通知探针同时验证正确入口与结构能够提交通知；实际提示音量未程序化检测。
- 重构后的设置生命周期验证 `SETTINGS-TABS-LIFECYCLE-NEXT` 实际创建三页控件，覆盖常规/性能/反馈切换、通知三态对旧字段的无损回读、预设不误判、上传终态和控制器停止/恢复；第 1 回合结束，runId `852a0f03f4724d6598212e309d11a2b4`，通过。该项是 headless 结构与行为验证，不冒充人工视觉检查。
- `0.21.7` 玩家更新日志见 `docs/0.21.7-RELEASE_NOTES.md`。

## 0.21.6：问题包后台记录与版本分类

- 合并 [Moeblack 提交的 PR #9](https://github.com/Torch1230/CombatSolver/pull/9)：战斗检查点继续只在主线程读取并冻结实机状态，元数据整理、字段排序、JSON/原生状态/跑局存档序列化和写盘改由单消费者后台 FIFO 按顺序完成。同一帧、同一战斗状态的重复记录复用冻结结果，减少连续自动执行时的问题包记录开销。
- 导出任务作为后台队列屏障，等待此前检查点和战斗结束信息全部完成后再组包；后台记录失败会阻止生成残缺问题包并返回明确错误。该线程职责已写入架构地图和 Windows/Linux 等价结构门禁。
- 在线提交描述的“CombatSolver 自动分类”固定追加当前 CombatSolver 版本号；即使本场没有检测到异常信号，也能在解压问题包前按版本筛选反馈。
- 定向验证 `PR9-BUG-REPORT-FIFO-VERSION-FINAL` 同时覆盖版本格式、控制器分类、后台检查点排空和活动战斗问题包结构，runId `7065e4ebabd64bc69700ebf70d323e27`，通过。
- `0.21.6` 玩家更新日志见 `docs/0.21.6-RELEASE_NOTES.md`。

## 0.21.5：RNG 身份与选牌重放

- `combatsolver-reports-20260830-193556.zip` 含 `8` 份问题包，其中 `0.20.0 / 0.21.2 / 0.21.3` 分别为 `5 / 1 / 2` 份。女王包的梦魇本地化并发异常已由 `0.21.4` 修复，不重复修改；其余包按开局选牌重放、探寻打击重复选牌和跨回合随机状态三条共享链归并。
- 跨回合续用文本与转置状态键不再只记录 RNG 调用计数。游戏 RNG 的一次调用会因调用方法和参数不同推进不同内部状态，计数相同不代表未来结果相同；九条战斗 RNG 现在同时记录计数器与四段内部状态。实现使用无分配值快照，固定 `250` 节点的 DOP1/DOP2 动作、选择、评分、展开、剪枝和快照继续完全一致。
- 开局原生选牌搜索释放历史候选后，最终候选重建会先按该候选保存的赌博筹码等开局选择恢复 Play 根，再回放出牌动作。此前直接从 Start 根回放，已经被开局选择换走的启动流程、`FALLING_STAR` 等牌会被错误要求仍在手牌。
- 探寻打击的“已有一个未解决选择”改为绑定当前 `CardPlay`。同一张探寻打击在后续回合再次打出时，不再读取上一次出牌留下的选择风险；同一次带重放的出牌仍会等待第一层选择完成后再继续。
- 定向验证：完整 RNG 身份与并行政策等价 runId `8d6ae9a5647240a5846a2549cb06c369`；`SOUL_NEXUS` 问题包战前根在赌博筹码页面完成搜索并返回初始路线 runId `1ec4e296e601422b86674f7f7d3d35df`；跨 `CardPlay` 选择风险隔离 runId `7fcf4d2c016f40a6aeeaf6f314b9cf3e`；探寻打击原版/预测严格差分 runId `cac18e0431be4bc2a06b474da84460dc`。
- 外骨骼虫问题包战前根的完整自动基线 runId `8655f27259fa4d0c822ebef9084f2eac` 在 `120` 秒上限时只到第 `2` 回合，没有走到原第 `5` 回合错误动作，因此不计问题包整场复现或通过。旧版页面缺牌样本中没有被上述根因或后续版本证据覆盖的部分继续保留，等待当前版本再次出现后再关闭。
- `0.21.5` 玩家更新日志见 `docs/0.21.5-RELEASE_NOTES.md`。

## 0.21.4：批量计算失败收敛

- `combatsolver-reports-20260830-184344.zip` 含 `85` 份问题包：`0.20.0 / 0.21.0 / 0.21.1 / 0.21.2 / 0.21.3` 分别为 `24 / 6 / 6 / 41 / 8` 份。当前 `0.21.3` 的八份不是八个独立缺陷，而是三条共享根因：六份本地化格式化器并发、一次 BaseLib 动态变量克隆重复键、一次战略洗牌选牌与花样百出自动出牌选牌重叠。
- 搜索 worker 不再为了模拟提示文本读取怪物名或牌名：缩小的施加者、梦魇的选中牌名属于纯显示字段，战斗语义分别由施加者引用和梦魇选择状态持有；自动策略临时使用稳定卡牌 ID，最终计划再由主线程已捕获的只读名称表补回本地化标题。没有重新引入已证实会让 Harmony 生成非法异常块的 `LocManager.SmartFormat` 补丁。
- BaseLib 的全局弱表现在由原版 `MutableClone` 和内嵌模拟的 `CloneModelForSimulation` 共用同一窄克隆边界；主线程与全部 worker 都参与该边界，避免预测直接调用 `DeepCloneFields` 绕过旧保护。搜索展开、评分、剪枝和串行提交保持并行。
- 自动打出的牌若在自身选牌前因抽牌/洗牌产生另一个选择，模拟会先停在该选择，解决后再继续自动出牌自己的选择。战略洗牌与花样百出自动打出微光的两层原版选择现按发生顺序逐一消费，不再同时占用单个待选槽。
- 老包中另确认两条仍存在于现行代码的共享生命周期问题：新召唤怪物首次掷出的正式行动现在立即进入分支行动历史，利齿之眼和寄生惧魔在完成首个行动前死亡也能进入幻象复活；佩尔军团的临时出牌引用改在整次 `CardPlay` 完成或中止边界统一核销，即使监听器在出牌途中离开枚举也不会污染下一个搜索节点。
- 其余老包按证据处理：知识恶魔敌方选牌、待结算 Power 与配对出牌已有后续版本回归；十份 `RequireAtLeastOne` 无路线是玩家主动选择的硬约束，继续明确失败而不静默放宽。旧版遗物标注回放和卡牌身份错误中，能落入 `0.21.3` 牌入场生命周期或本轮并发根因的部分不再按遭遇重复修改；缺少当前版本复现的部分保留为未独立关闭样本。
- 定向验证：花样百出/战略连续选牌严格差分 runId `0583a87ba9504c54b3b9df92f40398b3`；`FOGMOG` 召唤利齿之眼、首次行动登记后死亡复活严格差分 runId `2c4c0640cedd45bbac812bab31de22ee`；缩小甲虫固定根的 DOP1/DOP2 完整政策等价与 `PAELS_LEGION` Fork 边界 runId `9678ee908aac427e9fbc5a6d7b17e4cf`。三项均在 `120` 秒内完成；未逐包运行 `85` 场完整战斗。
- `0.21.4` 玩家更新日志见 `docs/0.21.4-RELEASE_NOTES.md`。

## 0.21.3：跨回合历史与连续结算

- `combatsolver-reports-20260830-164623.zip` 含 `25` 份问题包，全部来自 `0.20.0` 或 `0.21.0`；其中直升产卵虫两包内容完全相同，三骑士 `150657/150625` 两包内容完全相同。受感染棱镜的刮削牌堆分歧与女王的吹哨眩晕续接已由 `0.21.0` 修复，不重复增加遭遇逻辑。
- 牌在战斗中变换时，现在按原版顺序先登记生成记录、触发“进入战斗”生命周期，再执行变换回调与生成完成钩子。由熵变换出的费用会随本场虚无牌打出次数变化的牌，会在进入手牌时取得正确费用，不再把旧费用写进下一回合路线。
- “本回合已抽取状态牌”和“本回合已开始打出的零费攻击”改为分支状态：根阶段从原生历史捕获，Fork 时复制，并进入搜索指纹与跨回合续用印记。状态牌计数随玩家回合号推进、在抽牌前归零；出牌计数在回合开始生命周期归零。迭代会在每个新回合重新识别第一张状态牌；后施加的野性只读取当前回合的零费攻击，不再把上一回合攻击算入。
- 连续激发同一充能球时，每次激发后立即结算新死亡敌人的死亡 Power，再进入下一次激发。第一击触发的力量、格挡、复活或召唤会即时影响第二击；同一次出牌继续共用既有死亡去重集合。
- 无人夹具的“清空充能球”保留原有球槽容量，只移除队列内充能球；并允许给注入球设置精确的小数内部状态，用于重建问题包里的暗黑球累计伤害。变换牌夹具新增对原版与预测同时执行固定变换的能力。
- 三组定向严格差分均通过并启用增量核对：变换牌进入战斗 `c84456dc9b214cf7a7542579a8cd521f`，跨回合迭代/野性 `98adb2c4b3df4c65aed9cd3fb850512b`，暗黑球连续激发与 CrabRage Power 时序 `99dc41bee83243cf9bad3c6b32826c0b`。
- 母体问题包的完整自动战斗未计通过：原高预算场在第 1 回合搜索中达到 `360` 秒测试总超时；固定 `8` 秒短搜并开启增量核对的场景也在第 1 回合达到 `180` 秒总超时。两次均未产生语义异常，但没有进入第 7 回合，因此不能作为零重算证据。其余 `18` 份只在旧二进制中出现、且未归入上述根因的问题包仍保留为未关闭样本，等待当前版本复现后再判断。
- `0.21.3` 玩家更新日志见 `docs/0.21.3-RELEASE_NOTES.md`。

## 0.21.2：自适应并行默认值与设置修复

- 未显式设置搜索并行度时，Runtime 读取 `Environment.ProcessorCount`：至少 4 个可用逻辑处理器默认 DOP4，2–3 个默认 DOP2，只有 1 个时使用单线程。设置只在捕获新搜索请求时读取，用户显式选择继续优先，搜索内部仍按进程可用逻辑处理器钳制。
- 性能预设误判来自设置页关闭时无条件重新提交全部数值输入；即使文本没有变化，字段 setter 也会把预设写成自定义。整数和小数输入现在先比较当前持久化值，未改变时不写设置；只有实际修改时间、Beam、节点、分支或 No-GC 预算才切换为自定义。设置生命周期回归同时验证 `1/2/3/4/32` 个逻辑处理器解析为 `1/2/2/4/4`，高档预设重进后仍为高档；runId `25fd0bdbeaa9450ca13ca6eebec74d95`。
- `0.21.2` 玩家更新日志见 `docs/0.21.2-RELEASE_NOTES.md`。

## 0.21.1：并行克隆与 Power 显示变量稳定性

- `combatsolver-reports-20260830-150924.zip` 中的五份报告不是五个独立缺陷。四份骇鳗与一份永世沙漏都运行 `CombatSolver 0.21.0 + BaseLib 3.4.5`，在 DOP2/4 克隆带迅捷或螺旋附魔的共享卡牌状态时抛出同一 `ConditionalWeakTable.Add` 重复键异常。
- BaseLib 的克隆扩展在首次读取模型键时使用非原子的“先查后加”。不同 worker 同时写时复制同一张附魔牌，会并发克隆同一个附魔模型并争用该弱表；异常位置随调度落在不同回合和卡牌，但调用链一致。该问题由 `0.21.0` 并行展开暴露，不是骇鳗、永世沙漏或具体卡牌的战斗语义错误。
- Runtime 新增窄并发边界：只在后台模拟作用域且 BaseLib 克隆扩展存在时，串行执行原版 `AbstractModel.MutableClone` 及其第三方扩展；候选物化的其他部分、搜索评估、剪枝和 coordinator 提交继续按原并行设计运行。锁由 Harmony finalizer 释放，原始异常继续传播，不增加吞错或默认值。
- 玩家包的首个 `combat_start` 检查点位于抽牌和补能之前，用它建立的两次夹具只得到 `0` 个动作，明确不计通过。改用同一包的 `001-search_request_AutoTurnStart` 后，DOP4 短搜返回第 8 回合可执行路线，runId `ad3c8c31ef054a459a3f27dc9c45b16f`；同根 DOP1/DOP2 完整政策等价，runId `258ec69dbc1b45c8bb6afa809623f4b6`；DOP4 完整自动战斗在第 8 回合结束且计划外重算 `0`，runId `75cbc36d31d045778a72fa3acf60c080`。
- “似乎 AI 不会打交锋”的同包描述没有形成第二个缺陷：修复并发失败后，最终路线明确在第 9 回合规划交锋；本轮不修改交锋评分或候选政策。
- `combatsolver-reports-20260830-152821.zip` 共含 18 份问题包，其中 10 份构成新的同根主类：6 份受感染棱镜、2 份缩小甲虫和 2 份蔓生爬虫都出现 `Collection was modified`。外层导出还混有 8 份首个异常不同的旧问题，本轮没有展开，也不计为已修。
- 两份代表日志显示，并行 worker 在 `PowerModel.DeepCloneFields` 中首次读取尚未物化的显示变量；活力火花会由 `CanonicalVars` 进入游戏共享的 `LocManager.SmartFormat`，蔓生爬虫也在同一格式化器及对象池链上抛出集合修改或空引用。这是主线程根捕获遗漏了 Power 惰性显示状态，不是三个遭遇的专用语义，也不是上一批 BaseLib 弱表重复键。
- 根捕获现在先在主线程物化所有规范 Power 与当前战斗 Power 的 `DynamicVars`；搜索模拟若仍尝试惰性创建显示变量会立即报告具体 Power，保证 worker 不承担本地化。没有给全局格式化器加锁，也没有为失败格式化提供默认值。
- 第一轮尝试直接给 `LocManager.SmartFormat` 加 Harmony 锁，因原方法含异常过滤器而触发 `Incorrect code generation for exception block`，CombatSolver patch 整体回滚；相关 runId `f15cebdaa235481d970942bbdf5c7232`、`b8f16a2de9ea4779a738604ab8a247c9`、`0e0f3425cc104417bdf083ccf6cdcca0` 均为失败或超时证据，该设计已完全删除。
- 最终实现通过 Release 构建和结构门禁；基础启动 runId `8b4446b6e4a34fa9b68bcdbd971441af`，受感染棱镜玩家包 DOP4 短搜 runId `cb71d23a0baf4046b65dc0348c355041`，固定根 DOP1/DOP2 完整政策等价 runId `614d4ac8af3749cd92b9676662956dae`。按用户要求没有逐份跑完 18 个包，也没有执行本场完整自动战斗。
- `combatsolver-reports-20260830-160209.zip` 的 14 份汇总均报告 same-key。13 份有完整落盘调用栈，所有求解器失败都经过 BaseLib `CloneSpireFields` 与原版 `MutableClone`，已落在现有并发保护边界；永世沙漏包只有自动分类摘要，没有独立调用栈。完整日志另有 5 次商店悬停提示的 UI 重复键，与求解器计算失败无关。本轮只做静态覆盖审计，没有新增行为源码或重复运行既有回归。
- `0.21.1` 玩家更新日志见 `docs/0.21.1-RELEASE_NOTES.md`。本版取代已经撤下的 `0.21.0`，同时包含多核搜索、此前战斗修复和两类并行稳定性修复。
- 设置中的搜索并行度由自由输入改为选择框。“关闭（单线程）”直接使用现有 DOP1 串行路径，数值项为 `2–8`；没有保留与关闭含义重复的 `1` 选项。普通搜索和首回合准备选牌会保存本次请求的 DOP，若并行请求失败，错误信息会要求先上传问题包，再建议切换到单线程重试；已经关闭并行时只显示上传提示。设置面板与错误提示生命周期回归通过，runId `598166aaeb19404fa75603c35a5921fa`。

## 0.21.0：多核搜索、抽牌费用与眩晕续接

- 刮削现在按原版的牌面费用语义判断抽到的牌是否应被弃置。贪婪这类负费用、不可打出的牌不再被预测错误当作 `0` 费牌留在手中；实验体问题包里，刮削后的牌堆、后续抽牌和内存清理原生候选重新一致。
- 吹哨现在建立原版的眩晕行动，并在眩晕结束后恢复怪物被打断前的正式行动。史莱姆狂战士不再从呕吐脓水错误跳到狂怒痛击；佩尔之眼额外回合后的续接路线与实机保持一致。
- 高塔炮手问题包来自 `0.18.0`，其暴政选择被结束回合复核提前消费的根因已由 `0.19.0` 的选牌阶段划分修复。既有环境组装师回归已完整自动执行到第 9 回合且计划外重算为 `0`；本轮没有增加遭遇专用逻辑。
- 千足虫问题包来自 `0.19.0`。旧日志在下一张拆解分支开始前发现 `2` 层力量变化尚未结算；当前卡牌完成流程会在选牌后效果结束时清空这笔变更。升级烙印选牌后立即 Fork 的定向回归通过；同一问题包当前根也完成搜索，没有再抛出旧 Fork 异常，但该次只得到死亡路线，因此不写成完整战斗通过。
- 定向严格差分 runId：刮削负费用 `a5117c6888d0438693c0334775c720e1`，吹哨眩晕后续 `500c0e56d7fb4dd58977040a6dd92610`，升级烙印选牌后 Fork `7ea11c09cf614e28922104cf10875697`。
- 两份玩家包的严格增量部署均通过。史莱姆狂战士实际打出吹哨并复用到第 3 回合，计划外重算 `0`，runId `e6232efae6494f2cb91b14d6790c3c23`；实验体第 2 回合打出刮削，随后内存清理在原生页面成功选中贪婪，复用到第 3 回合且计划外重算 `0`，runId `ec1c11fa2a444eda836520d1fe18e829`。
- Beam 父节点展开新增固定 lane 并行执行，普通搜索默认 DOP2，设置面板和测试入口允许显式选择 1–8；旧设置缺少该字段时继续使用 DOP2。worker 只物化原始候选，transposition、支配剪枝、fallback 和预算接收仍由 coordinator 按原父节点顺序提交；详细诊断与严格增量回放继续强制 DOP1。每条后台 lane 在整次搜索中复用 solver、缓存与节流器，结束时统一 join，不再为每个父节点创建 Task 和 worker。
- Fork COW 的共享标志改为显式跨线程可见；mirror 动态查找缓存改为 `ConcurrentDictionary<Type, Lazy<...>>`，静态 `GetOrAdd` 工厂避免热路径捕获委托分配。搜索结果和阶段日志新增 parallel wave、工作项及实际最大并发遥测；并行阶段耗时按各 lane 累加，表示 CPU 时间而非墙钟时间。
- 固定双小啃兽快照的上游 DOP1、当前 DOP1、当前默认 DOP2 均保持 `573` 展开、`2759` 转移、同一 5 回合零战损/零药路线；独立冷进程 headless 求解时间为 `1424.5 / 1449.3 / 955.2 ms`，当前 DOP2 相对当前 DOP1 缩短约 `34.1%`，累计分配为 `177,425,048 / 177,767,800 / 179,165,392 B`。这是单次迭代 A/B，不替代正常可见 Steam 性能结论。
- 无人协议和 Windows/Linux 启动器、可见基准入口均可显式选择搜索 DOP，并在每次请求结束后清空覆盖值。内置搜索政策断言在带初始力量与弃牌选择的固定根上递归比较 DOP1/DOP2 动作选择、评分、快照、回合标注和全部非时序剪枝结果，并要求真实并发；DOP2 完整自动战斗在第 5 回合结束并从第 3 回合复用，严格增量场景即使请求 DOP4 也确认并行遥测为零。另以低语耳环连续自动出牌场景覆盖首回合多根与嵌套选择，以 DOP4 场景确认四条 lane 的启动、复用和回收。
- 节点预算命中后不再让未展开的父节点和已经剪枝、但不会进入下一层的候选继续保留完整 Simulator；串行、并行 wave 与最后一个 legacy 预算槽共用有序前缀释放，并新增 `node_limit_snapshots_released` 遥测。固定 250 节点门禁同时要求 DOP1/DOP2 结果完全等价且两者都实际释放预算丢弃快照，合并上游 `v0.20.0` 后的最终 runId 为 `c9052d90aa504ae0ba183a5089aa0e07`。
- 固定机甲高分支在同一暖进程中保持 `4319` 展开、`33087` 转移、`18399` 选牌分支和同一第 7 回合/28 战损路线；DOP4/6/8 分别为 `5451.7 / 4281.3 / 3813.0 ms` 与 `4,404,184,848 / 4,412,016,024 / 4,415,450,000 B`，DOP8 相对 DOP4 约快 `30%`，累计分配仅高约 `0.26%`。当前 `v0.20.0` 合并态的独立冷进程 DOP8 为 `6366.8 ms / 4,441,356,192 B`，实际最大并发为 `8`，结束工作集约 `6.01 GB`，GC 与 `>50 ms` 帧均为 `0`；这些仍是 headless 迭代数据，不替代可见 Steam 结论。
- 两项“看似省算”的实验均已撤回。512 槽 lane-local 投影洗牌缓存只在高分支命中约 `7.6%`，累计分配反增约 `48 MB` 且更慢；复用投影洗牌临时列表虽把该嵌套子阶段的归因分配从约 `99 MB` 降到 `76 MB`，整次搜索却只少约 `0.9 MB` 且未提速。阶段日志保留独立 `projected_shuffle` 指标，后续继续从真实热点取证，不保留低收益复杂度。
- 有界流水线原型能改善 DOP4 小场景尾部，却让 DOP2/DOP3 退化约 `9%`，高分支 DOP4 也慢约 `3%`，并扩大失败清理与 CPU 契约风险，已经撤回。并行 Beam/HDA* 的共享 frontier 或哈希分布会改变 tie、预算截断与接受顺序，因此当前继续采用“并发推测物化、coordinator 有序提交”；更多阶段并行只在能保持相同确定性契约时再考虑。
- 本轮两次尝试由现有 Steam 客户端启动可见基准均在 60 秒内未创建游戏进程，没有产生性能数据，也未重启用户 Steam 会话。因此默认值保持 DOP2，DOP3/6/8 的正式帧率结论仍待正常可见会话验证。
- PR #8 合并到本地主线后重新执行最终等价门禁：固定 250 节点下 DOP1/DOP2 的动作、选择、评分、展开、转移、非时序剪枝、快照、continuation 与回合标注完全一致，两档都实际释放节点预算丢弃的 Simulator，runId `c4e1343ad44843229483f97e3d04265a`。合并态默认 DOP2 完整自动部署双小啃兽在第 5 回合结束、第 3 回合精确复用，计划外重算 `0`，runId `21f6375fb9d5499a99d992c2eb7a0878`。

## 0.20.0：在线问题包、首回合选牌与稳定性

- 合并 PR #6 的跨平台开发与无人测试入口。Windows 继续使用 PowerShell，Linux 新增对应的 Bash 构建、本地化、单项无人测试、完整矩阵、可见基准和结构门禁入口；两端按明确的进程身份与隔离数据目录管理无人游戏进程。
- 武装面对唯一未升级候选时，原版会直接选中并立即升级。原生选牌驱动现在保存请求发生时的卡牌语义身份，随后用该身份核销隐式选择，不再拿升级后的状态反查升级前计划。
- 无人建局不再调用按当前 UTC 秒重播种的调试接口；相同 run seed、楼层与遭遇会生成相同敌人组合。无人请求同时跟踪仍在运行的搜索、部署和原生选牌任务，后台失败不能在清理阶段之后把当前请求误报为通过。
- Windows 的临时 RitsuLib 投影先删除两个已知载荷，最后删除所有权标记；进程退出后的短暂文件占用会在有限时间内重试。删除载荷失败时标记仍保留，下一次启动器可以安全识别并继续清理。
- Windows 合并态回归覆盖武装隐式升级、同一 PID 的相同 seed 敌人组合、失败请求退出、成功请求复用、骇鳗活力攻击、毒气炸弹自爆死亡结算和琥珀灰额外回合中的情感芯片历史；对应 runId 与断言保存在 `docs/TEST_MATRIX.md`。Linux 结构门禁在当前 Windows Bash 环境因缺少 `rg` 未执行，保留贡献者原生 Linux 全矩阵证据，不写成本机复跑。
- 花园幽灵鳗问题包实际加载的是 `0.18.0`。旧版在首回合手牌页算出路线后立即确认烘焙手套选择，并在打完本回合后把下一回合的烘焙手套选择错误交给当前结束回合复核，最终以“计划选牌没有触发”中止。`0.19.0` 已包含两处生产修复：计划就绪后保持原生页面并等待玩家启动；实时结束回合复核只消费玩家回合结束与敌方回合选择。
- 新增首回合启动边界断言：必须先观察到原生页面、搜索开始和计划就绪，此时 `Selected=0`、玩家仍处于原生手牌选择页且出牌数为 `0`；无人测试随后才模拟玩家点击启动。基础场景 runId `a2a1fd688e71465d9458c5cbb1c743d4` 在第 2 回合结束，计划外重算 `0`。
- 严格增量校验的完整回放现在先重放同一组首回合准备选择，再重放出牌动作；旧校验从选择前根状态直接找已选择后的手牌，会把正确的烘焙手套分支误报为找不到手牌。花园幽灵鳗问题包的战前牌组、遗物与 RNG 在严格模式下完整自动执行到第 5 回合，runId `0451b77331a94c96ae507e2ccdb4603a`，首回合用户启动前未选牌、未出牌，结束回合复核开启且计划外重算 `0`。
- 求解器设置新增“上传问题包”按钮（位于“导出问题包”左侧），点击后提供可选的问题描述输入框。确认后复用现有导出流程打包，通过接收服务上传，后端字段保持不变。上传时会在玩家原文后追加本场结构化自动分类，覆盖更优世界线、手操或重算后战损上升、计划外重算、续接路线缺失、路线耗尽、搜索初始化/计算/回放/容量错误、第三方 Mod 不兼容、选牌异常、自动执行中止及全自动风险暂停；未出现的类别不会写入。
- 求解器设置新增“反馈联系QQ”文本项，仅需填写一次并持久化到本地设置；后续上传自动带上。QQ 只通过后端既有联系字段发送，不再重复写入 ZIP 的求解器设置。问题包环境信息只保留可执行文件名、用户数据目录名、程序集名称和版本，不再写入本机绝对路径。
- 上传与导出现在共享单实例状态：打开确认框后即锁定其他反馈任务，上传期间显示字节传输进度，“上传问题包”按钮切换为“取消上传”。问题描述、ZIP 和服务端响应均有明确大小上限；成功响应必须包含有效反馈编号并确认匹配的实收字节数才会确认成功，本地 ZIP 删除失败只显示清理警告。失败、取消和超时会保留本地 ZIP，超时明确标为结果未确认，避免玩家立即重复提交。
- 覆盖层“比求解器更好的世界线”提示文案同步改为建议直接上传，不再提示传到群里。上传目标和接收服务协议保持 PR #4 的既有定义；本轮只在玩家问题描述后追加自动分类摘要，不修改朋友维护的后端字段。
- 所有需要玩家反馈的异常统一改为引导玩家在设置中点击“上传问题包”提交日志，不再提示先手动导出再另行提交。搜索初始化/计算失败、回合准备选牌失败、自动执行中止和过期结果会在错误正文中直接显示入口；计划外重算、续接路线缺失、路线耗尽、重算后战损上升、结束回合实机复核偏差和更优世界线会在反馈栏或对应暂停说明中显示入口。用户主动停止计算、按设置在预计死亡回合暂停等正常控制行为不提示报错上传。
- 最新实机日志显示，`1.28 MB` 问题包写完本地网络缓冲后进度立即显示 `100%`，但客户端仍在等待服务器响应；玩家取消后，旧的无取消参数内容序列化入口继续使用空取消令牌，使按钮长时间停在“正在取消”。上传正文现在始终携带本次请求的取消令牌，并在取消时主动终止该请求；文件写出和服务端确认分成两个显示阶段，前者最多显示到 `95%`，同时列出已发送量与总量，只有回执中的反馈编号和实收字节数都有效且大小一致才确认成功并删除本地包；进入成功、失败或取消终态后立即收起进度条。上传弹窗也改到进入 SceneTree 后再等待下一帧居中，不再于构造期间访问空 SceneTree。
- 接收服务使用一份不含日志、存档、截图或个人信息的 `259 B` test ZIP 进行真实连通验证，返回 HTTP `201`、反馈编号 `9264c0f65854423e8254de5ff5e5449f` 并确认实收 `259 B`。本地假服务同时覆盖正文传输中取消、正文完成后等待回执时取消、无效回执和回执大小不一致，取消任务均要求在两秒内结束。
- 后续实机日志记录到两次上传都在连接环境代理 `127.0.0.1:7890` 时立即被拒绝，未进入接收服务；后台失败先把共享状态改为空闲、主线程稍后才刷新仍显示“取消上传”的按钮，玩家在这个窗口点击会按空闲分支重新打开确认框。正式上传现在使用不继承游戏进程代理的专用直连客户端。第一轮把上传状态、取消令牌和按钮文本合并到同一个主线程回调，关闭了“显示取消、实际新建上传”的竞态，但仍依赖搜索用的全局 dispatcher；后续实机确认该回调本身可能不被消费，最终由设置面板完成邮箱取代。
- 在 `HTTP_PROXY / HTTPS_PROXY / ALL_PROXY` 仍指向不可用的 `127.0.0.1:7890` 时，专用直连 test ZIP 于 `427 ms` 内获得 HTTP `201`，反馈编号 `d186e8a6495d4f0291529595667e0a43`，服务端确认实收 `259 B`。无人回归同时演练“仍显示取消上传”到“上传问题包”的单一主线程收尾，runId `ba1c33c4e68946129314dff1a61928cf`。
- 再次实机验证时，`1,340,897 B` 问题包已经获得 HTTP `201` 并记录反馈编号 `254decdf92a94cbba02f5b1fa289c91d`，但设置页仍停留在“等待服务器确认”；约 16 秒后点击取消仍只进入“正在取消”。这证明网络任务已经完成，缺陷在客户端收尾：上传成功、取消和失败回调被送进搜索用的全局 `SolverDispatcher`，而进度条由设置面板自身 `_Process` 驱动，两套生命周期没有同一个终态消费点。上传会话现在由设置面板独占，网络任务只向线程安全完成邮箱发布一次 `Succeeded / Canceled / Failed`；面板每帧先消费终态，再处理进度或取消等待，并在同一次消费中释放取消令牌、收起进度条、替换状态消息和恢复“上传问题包”按钮。
- 面板完成邮箱回归分别覆盖服务端成功和用户取消：终态发布后、面板消费前仍保持活动状态；消费时两条路径都收起进度条、释放取消令牌并恢复空闲按钮，其中取消路径从禁用的“正在取消…”直接结束，不依赖全局 dispatcher。runId `fa5ba87bf06d4dac9c17b052192d0be8`。
- 两个知识恶魔问题包的首个失败点相同：自动执行已经完成本回合出牌，但尚未提交结束回合，实时战损复核就把敌方回合的诅咒计划放进普通选牌游标；知识恶魔的诅咒实际由怪物行动语义按来源与次数消费，因此普通游标必然残留一项并中止。实时复核现在与主搜索使用同一阶段划分：普通回合结束选牌走普通游标，知识恶魔诅咒计划随怪物行动解析。第二个包所见的“愤怒动画卡住”也是该结束回合前异常，不是原生页面已经出现后被动画阻塞。
- 修复前基线 runId `bc12f0e513ec4634b5c2f3dea0f84a66` 精确复现“回合开始仍有 1 个计划选牌没有触发”。修复后，怪物行动夹具在实时战损复核中消费知识恶魔诅咒计划，runId `901066cdbaaa41329876325cd8a06ad5`；开启严格增量校验的完整搜索与原生选牌通过且零重算，runId `b199bf29f0054c1789e1a2c2d2886435`；铁甲战士问题包完整自动执行到第 6 回合，实机打出愤怒并完成两次原生诅咒选择，计划外重算 `0`，runId `28ac269f1be64674976a8d5075965947`；静默猎手问题包也完成首个原生诅咒选择且零重算，runId `9d988afbd8f240e3af519755d35aff3b`。
- `0.20.0` 的玩家更新日志见 `docs/0.20.0-RELEASE_NOTES.md`。游戏内在线上传问题包是本版重大更新，接收服务器和后端由社区朋友提供支持。
- 高塔炮手、实验体、史莱姆狂战士和千足虫的四个新问题包已登记在 `docs/issues/post-0.19.0-pending-bugs.md`。用户要求先暂停修复，因此它们不计入 `0.20.0` 已修内容。

## 0.19.0：重算、中止与选牌边界批次

- 本批次始于 `0.18.0` 发布后，最终发布为 `0.19.0`。原始反馈与逐项状态维护在 `docs/issues/post-0.18.0-bug-batch.md`，实施顺序和验收口径维护在 `docs/issues/post-0.18.0-fix-plan.md`。
- 当前范围只处理战斗语义、原生选牌与自动部署、跨回合续用和计划外重算。更优手操路线、能力牌估值和用药质量已按用户要求暂缓；已有问题包与战损上界继续保留，不写成已经改善。
- 计划选牌现在显式区分动作内、玩家回合结束、敌方回合和玩家回合开始四个时机。实时结束回合复核只消费本次结束回合与随后敌方回合的选择，不再提前要求下一回合必备工具、暴政等准备选择；环境组装师、花样百出、三卷轴怪和知识恶魔共享这一阶段边界。
- 选择悖论、烘焙手套、赌博筹码和工具盒会先显示原生首回合页面，再启动搜索并展示预计行动；求解器等待玩家点击执行或开启全自动后才确认计划选择。执行本回合进入下一回合时，会停在必备工具或暴政页面；此时再次执行或开启全自动会接管已有计划，不进行重复搜索。
- 环境组装师的条件行动按原版把自身计入存活队友。怪物攻击补齐完整的攻击前与攻击后生命周期，骇鳗的活力会在攻击前武装并在攻击后消费，不再于跨回合续用时错位。
- 原生大卡池页面只创建当前可见行的卡牌节点。部署器现在沿原版网格的滚动范围逐段移动并等待目标行分配，再通过真实节点确认；灵魂枢纽的 37 张牌池可正常选择底部的主宰。
- 实验体战利品、永世沙漏原生选择、养蜂人塞入笨拙、千足虫升级和三骑士最终回放在当前源码均越过旧失败点或完整自动结束。蔓生爬虫包记录到搜索期间实机药水栏被改写，但当前源码复跑零重算，暂列证据不足；骇鳗免费能力层数 `3/4` 差异也未在精确牌堆与增量回放中复现，不宣称已经修复。
- 骇鳗增量整包回归在无人测试 `360s` 上限内仍停留于首回合，未形成断言，记为超时而不是通过；同场完整自动执行与怪物攻击严格差分已经通过。
- 主宰会先完整结算易伤施加引起的 Power 层数监听，再读取目标当前易伤并获得力量；凶恶抽牌与地狱狂徒自动打出的攻击因此发生在加力之前。最小原生/预测严格差分通过，runId `ad8f66a8594d436dac6a1c5fafb0e313`。幽灵骑士死亡时移除恶咒的现有补偿也已纳入覆盖声明，三段施加、叠层与施加者死亡回归通过，runId `d53826f1d06a487fbca60ffb740892f7`。
- 无人复现器可直接从问题包回放状态恢复四个有序牌堆、升级、附魔、苦难与疯狂科学的枚举内部状态；自定义性能覆盖会按实际值断言，不再把自定义设置误判成中档。多尼斯异鸟和实验体的精确基线分别保持 `55` 与 `20`，对应玩家上界 `41` 与 `0`；两项只固化为暂缓的搜索质量差距。
- 详细诊断会在最终路线标注回放及实机部署的每个动作后同时记录能量、四个牌堆与敌方生命/格挡，用第一处分歧定位回放失败和计划外重算；最小完整执行 runId `1e83a3bc0d864e278fba5a8a20d66b96` 同时写出两类状态。

## 0.18.0：集中修复批次

- 本节记录 `0.17.2` 发布后的 29 项集中修复；全部问题已结案并进入 `0.18.0` 发布准备。
- 如此甚好的晚置回手钩子现在与原版 `CardPlayFinished` 时序一致：当前正在结算的技能会计入本回合技能数，前两张技能后仍留在弃牌堆，第 3 张后才回手。这修复了储君路线提前规划弃牌堆卡牌、实机部署却找不到手牌的共享根因。逐张牌堆实机差分 runId `5d226c100ea949f1bc498a01a7961106` 通过。
- Power 施加流程现在先建立可变的本次施加实例，再判断效果类型、遗物修正和神器抵消。精神过载不再从不可变的规范 Power 读取运行时类型，47 项卡牌效果实机差分 runId `fa74bbbe3c4245188cde369d7bcf6144` 通过。
- 全自动结束回合前的实时风险复核现在携带路线已确定的回合结束及跨回合选牌。惊逃自动打出头槌时，复核可直接复用“将盛怒置于抽牌堆顶”的计划，不再在无选牌作用域中重跑一次选择；如果路线真的漏了选择，复核会直接报错而不返回不完整的风险值。定向回归 runId `216d8643891f442689074fa5f6f7954e` 通过。
- 原版在回合准备选牌页面暂停时，外层回合启动流程会先执行 `AfterSideTurnStart`，页面确认后才恢复 `BeforeHandDraw` 和正式抽牌。模拟器现在在消费第一项抽牌前选牌时复现这一交错顺序；既定事项与下回合多抽一张同时存在时，后者会在选牌确认前移除，最终手牌数与实机一致。严格差分 runId `a65c0460238c467c803394b5c07a59c5` 通过；猫头鹰法官问题包完整自动执行到第 5 回合且计划外重算为 `0`，runId `cd2f163cd0cd41f5af6183fe9b4fec5c`。
- 最大手牌数不再固定为原版常量。战斗根通过 RitsuLib 的玩家限制接口捕获实际上限，抽牌、生成牌和各类选牌分支统一读取该值，兼容第三方效果改变手牌容量后的搜索与回放。
- 原生选牌会话现在按会话身份结束。敌方回合选择完成后即使下一回合的选牌页面已在同一帧建立，旧会话仍能正常退出，新页面继续由最新会话接管，不再因异步恢复顺序抛出“没有位于活动栈顶”。
- 单实例 Power 的监听顺序现在随真实生命周期变化：Power 归零时退出监听序列，再次施加时作为新实例排到当前序列末尾。知识恶魔长线中，第 1 回合移除、第 11 回合重新施加的既定事项不再错误排到光谱偏移之前；第 12 回合随机生成牌、选中牌和抽牌顺序与实机一致。完整问题包打到第 12 回合结束并保持计划外重算 `0`，runId `bdf89c2bf77341ee8da1c7f68b2d2161`。
- 战斗根现在把 Power 的运行时内部状态同步到预测实例。鬼祟珊瑚群第 2 回合开始时会保留本回合已经受到的 `9` 点伤害，搜索不再多算 `3` 点可造成伤害；原问题包全自动在第 5 回合结束，计划外重算 `0`，runId `b126492a542c4c3d85bc47f0bffe0b1c`。
- 情感芯片触发后会把“上个玩家回合失去过生命”的记录保留到当前回合结束，再按原版时序滚动到下一回合。原问题包全自动在第 5 回合结束，额外触发充能球造成的两次计划外重算归零，runId `e265c0a6887f48ea892c2e5489236721`。
- 搜索中的目标类型现在读取分支 Power 状态。打出刀扇后生成的小刀会按全体攻击规划、结算和部署，不再携带单体目标；永世沙漏问题包完整自动执行到第 7 回合，计划外重算 `0`，runId `e825a7e0fe244d5b8be45086c3f3d7ef`。
- 战斗内存回收改为串行请求链。搜索等待正在执行的回收时只等待当前链；战斗重置在回收期间追加的请求由回收任务自身接续，下一次搜索不会与等待者互相制造新的 `before_next_search` 回收。实验体问题包修复前会约每 `275 ms` 回收一次直至超时，修复后只完成一次 No-GC 滚动回收并正常返回路线。
- 回响形态现在按原版的“已开始出牌系列”决定重放。骚动开始结算后，它自动打出的集中打击能立即看到外层系列，不再被回响形态错误重放；重放自身的后续段不重复增加系列计数。最小原生/预测严格差分 runId `06550cd755b246c7b044865469541422` 通过；实验体原问题包完整自动执行到第 8 回合，计划外重算 `0`，runId `913d3393e919438fbf2d7635ce318b2b`。
- “每个动作的手牌选牌分支数”现在约束整张牌产生的最终选择路线。抉择，抉择把同一技能自动打出三次时，三层原生选择不再各自重复获得完整额度；预算按可选技能平均分配，保证不同技能仍各有候选，并阻止单个动作形成指数级回放。储君实验体原问题包用短搜完成首轮规划，随后连续精确复用 `10` 回合并在第 `11` 回合结束，计划外重算 `0`，runId `c8c9f6f2edda40bd87bc2bf5e6b20520`。
- 活体盾与高塔炮手问题包在当前源码上完成首轮最终遗物标注及完整自动执行，第 `4` 回合结束，计划外重算 `0`，runId `6d1e48b181824f0fbebc43525c959403`。该项由本轮此前合入的共享状态修复覆盖，没有新增遭遇专用逻辑。
- 机甲骑士与静默猎手对灵魂枢纽的两份重算问题包已在当前源码上分别完整自动执行到第 `4`、`5` 回合，计划外重算均为 `0`，runId `cb31ec9272284f579bbf6efa942281e0`、`2b11153f74af4766848d585e8372d62f`。两项均由本轮共享状态修复覆盖，没有新增遭遇专用逻辑。
- 智能用药的独立反事实现在继续复核多瓶路线的边际收益：将 `N` 瓶路线与最多 `N-1` 瓶路线比较，新增药水必须单独达到对应机会成本。瀑布巨兽问题包中，痊愈药水实际降低 `10` 点累计战损并保留；只补回少量生命的再生药水不再借用痊愈药水的收益过线。最终路线由两瓶降为一瓶，runId `dbce824e7919490882bd6022f2ebc394`。
- “至少使用一瓶”在搜索尚未到达终局时会选择当前最优的用药边界路线；只有搜索已经找到完整胜利时，才要求候选本身也是胜利路线。失落之物与遗忘之物问题包中的流动铜液和能力药水均能进入搜索，不再因长战斗只推进到边界而误报“没有可执行的用药路线”，runId `d3fa5886b45e46948093c0d42518f79d`。该回归只证明用药路线可返回，不把死亡边界记作生还。
- 异螨问题包在当前源码上会于第 2 回合使用格挡药水；完整自动执行在第 `5` 回合结束，预计省血 `9/9`，计划外重算 `0`，runId `1959c1dd79c743958a6f921f727d6cae`。该项属于当前共享药水政策已覆盖，不增加遭遇专用规则。
- 女王的稳定血清问题包从战斗开始到结束始终使用“禁用药水”设置，求解器保留稳定血清与固化药水正是该设置的预期行为；玩家手动使用稳定血清后，本局已用药数也正确增加。该项按“当前行为正确”结案，不修改药水语义。
- 实验体问题包在当前源码的同一起点、高档预算下完成成对复跑：智能用药为 `0` 瓶、预计战损 `78`，runId `12dc53ed6dc1469d9e0bae71a1b14b2e`；“至少使用一瓶”选择肌肉药水、预计战损降为 `74`，runId `6a4b981c9c7c4fb7999304980c33f00c`。两次搜索的展开上限和候选分支数一致，未再出现强制用药后战损上升。
- 无厌沙虫问题包在“至少使用一瓶”下会于第 4 回合规划易伤药水；长战斗尚未搜索到完整胜利时仍能返回当前最优用药边界路线，不再误报没有可执行的用药路线，runId `6563e660595d4b35932719281320b5c3`。该短预算结果仍为死亡边界，只证明用药规划可以返回。
- 历史课的跨回合记录现在提交于惊逃的回合末自动出牌之后，因此会复制上一回合最后实际打出的非复制攻击牌。佩尔之眼同时改用“玩家主动出牌数”判断额外回合，历史课与惊逃的自动牌不会错误阻止触发。永世沙漏原问题包从同一起点完整全自动执行到第 5 回合，包含佩尔之眼额外回合，计划外重算 `0`，runId `0b1d38c391d04a36a7a9cc14c5122d5f`；恶魔之焰等回合末自动攻击不再错位到后续回合。
- 孙子兵法路线增加独立的防守与铺垫保留通道，并在仍可触发下回合额外能量时延续近期选牌分支；该改动不改变全局评分，也不扩大无孙子兵法战斗的搜索范围。永世沙漏问题包的固定首手与抽牌堆回归会主动打出灵动步法+，预计战损由包内原路线的 `42` 降为 `27`，runId `0c2c35a7510b4e0f9846fca740aa80c1`。玩家手操后的 `18` 来自第 3 回合后置根状态，只记录为后续质量上界，不写成首回合同根结果。
- 女王“更优解”包的完整日志保留了可比较的首回合：旧求解路线预计战损 `26`；玩家改为先打余像，再通过两次许愿取得肾上腺素和生存者，并打出后空翻、计划妥当与中和，最终实测战损 `5`。按包内首手和 32 张有序抽牌固定同根后，当前路线会主动打出余像与计划妥当，预计战损降至 `9`，runId `598bc5d8e86b4094bbf96c04942e192e`。该项记为显著改善，玩家的 `5` 仍是尚未追平的同根质量上界。

## 0.17.2：手操路线反馈、搜索停止与预算说明

- 求解器现在为手操偏离保存重算前的整场预计战损，并在新路线完成后记录下降、上升或不变。严格下降时，覆盖层显示整行绿色反馈，建议玩家导出完整问题包；比较结果也进入问题包的重算审计。既有“完整路线重算后战损上升”记录和全自动暂停保持独立。
- 搜索期间原“完成后执行”按钮改为红色“停止计算”。用户停止后取消当前普通搜索或回合开始选牌搜索、关闭全自动，并阻止后续回合、部署漂移和全自动入口再次启动搜索；只有“重新计算”解除本场暂停。开局原生选牌搜索停止时会释放原生页面给玩家，不把选择事务卡住。
- 求解器长消息改为独立整行、自动换行的富文本区域；计划外重算与手操改善提示从标题栏移到可换行的整行反馈栏，长错误不再被标题、状态和按钮挤压截断。
- 性能预设新增“极高”，原低、中、高各提升一档。当前四档依次为 `5/60s + 6GB`、`8/120s + 8GB`、`12/180s + 12GB`、`20/300s + 16GB`，并同步提升 Beam、节点、出牌和选牌分支预算。自定义时间上限扩至 `600s`；时间、Beam、节点、单节点出牌分支和 No-GC 预算均增加悬停说明。
- 控制器会话回归 runId `bbbffd3cf1cd4d8686472175a44ed64e` 覆盖停止、后续自动搜索拦截、重新计算恢复、绿色提示与消息换行。四档固定规格回归 runId 为 `c18b796053064ffb89eebde8da49fa69`、`516c303b65d547fb9e60fa34d79ca3b5`、`ce61ed362f5143fc9d69ee8b9763eb2c`、`9791b831f1ac4b6ca296fe28811f81c4`。

## 0.17.1：奇巧自动出牌与奥斯提回合计数

- 本版合入贡献者 `ltlly` 的 PR #1。PR 基于远端 `0.16.0`，合并时保留奇巧嵌套选牌与奥斯提计数修复，排除同一分支误带且未接入运行时的 NOSL 和牌组分析原型，再将结果重放到完整 `0.17.0` 源码上。
- 带奇巧的杂技被其他卡牌弃掉后会由原版自动打出，并在自身 `OnPlay` 内再次请求弃牌。预测此前等自动出牌返回后才补做这次选择，既无法为搜索形成正确的嵌套选择，也让来源牌和被选牌进入弃牌堆的顺序与实机相反。现在自动出牌自身的选择在原版相同的结算位置完成；奇巧杂技严格差分通过，runId `97af4be8cb104d15baac75fe1e4c3701`。
- 既有“战略选择先于杂技自身弃牌”的洗牌时序保持不变。早有准备与升级杂技两项严格差分继续通过，runId `1cd79d4025cd4c5698ec2ac0edc39f4e`；抽牌触发、回合末自动出牌和 32 项卡牌完成生命周期相邻回归分别以 runId `2552b97f8b024ebebf8fbe268377086b`、`03b7d9432aeb4024bf11c2d5cc3a8ed3`、`03944b3c91844c8799965f40166b261f` 通过。
- 奥斯提不独立参加玩家回合开始事件，预测中的本回合攻击次数和命中次数因此曾跨回合残留，使猛晃伤害虚高并可能在敌人实机存活时触发计划外重算。玩家回合开始现在同时重置奥斯提的两类本回合计数；完整结束第一回合后在第二回合打出猛晃，实机与预测严格一致，runId `1d3f201d5c75487f9cf9b70d67635dcf`。

## 0.17.0：策略与长期资源优化

### 策略优化需求与验收口径

- 用户提出的八项原始需求已逐字固化到 `docs/strategy/0.17.0-raw-requirements.md`，整理后的职责划分、实施批次与逐项验收条件维护在 `docs/strategy/0.17.0-optimization-plan.md`。
- 本版以战损和长期资源为质量基线，依次处理战斗语义、终局资源排序和中间保路；不以回合数代替战损，不以扩大搜索预算掩盖候选政策缺失。
- 当前状态：八项策略实现与定向回归完成，已随 `0.17.0` 发布。

### 整场战损、最大生命、复活与开局药水

- 路线现在保存分支累计承伤，而不是用终局当前生命反推战损。治疗与蜥蜴尾巴复活不会抹掉此前损失；跨回合续用继续携带同一累计值。蜥蜴尾巴触发的结束回合胶囊显示“蜥蜴尾巴：复活”。`1 HP + 两张重锤` 的路线首轮与第 2 回合复用均保持整场战损 `1`，计划外重算 `0`。
- 最大生命与当前生命共同进入 Beam 和终局资源比较。无伤可直接击杀时路线不打至亮之焰；必须用它才能避免敌方回合时，路线会使用并保留 `80 -> 78` 的最大生命变化与对应资源成本。
- 力量、敏捷、集中等整场持续增益药水一旦进入采用路线，只能出现在首张牌之前。力量药、敏捷药分别验证为首个路线动作；这条规范不改变必须用药和智能用药的资格判断。

### 事件胜负、合法无限与愤怒扩牌

- `EventDefeat` 明确不是胜利。训练假人三档的可击杀场景均选择击杀；倒计时耗尽继续保留 `EventDefeat`，不能以安全超时替代事件目标。
- 女王问题包来自 `0.14.11`，首个完整异常是计划 `/Exhaust/Hand/` 选择未消费，不是日志直接证明的无限循环报错。当前双卡零费抽牌无限在同一回合执行 `19` 个动作、跨 `18` 次洗牌后击杀，预计战损 `0`，完整自动执行计划外重算 `0`。
- 愤怒的伤害和扩牌副作用分开评价。同样胜利、同样战损时优先不增加愤怒复制；只有愤怒可击杀时仍正常使用。永世沙漏第 9 回合近似重建不再选择愤怒，但该夹具省略完整消耗堆与部分历史，只证明策略取舍，不作为原包战损复放。

### 呼唤与长期成长资源

- 呼唤原有镜像已经覆盖手牌回合末 `6` 点失血、可打性和牌堆移动。本轮补齐策略回归：求解器会先打出呼唤，把首回合损失从 `6` 降为敌人造成的 `4`，第 2 回合自动击杀且零计划外重算。
- 新增长期资源维度，胜利与整场战损仍排在最前；生存结果相同时，遗传算法、巨镰、黏糊防御培养，狂宴最大生命，狩猎卡牌奖励，贪婪之手金币，王国资产金币，禁忌魔典移除奖励，时候未到治疗和炼制药水产物参与保路与终局选择。该维度有独立保路通道，没有扩大 Beam。
- 永久成长只记在原版实际保留跑局版本关系的结算上。华彩重放累计 `+6`，并在第 2 回合继续执行且计划外重算为 `0`；灵魂之力跨回合两次培养累计至少 `+6`。原生差分确认梦魇 `Clone` 的副本没有 `DeckVersion`，这类临时复制不虚构跑局成长。每次真正作用于跑局版本的额外结算独立计数。

## 0.16.0：稳定性与规则修复

### 同类型 Power 多实例根状态与最终路线回放

- 啃咬兽问题包的全自动执行在第 6 回合打出第二张坚韧之环后停止。实机此时同时存在两份 `ToricToughnessPower`，层数分别为 `1` 和 `2`；结束回合前的风险复核重新捕获战斗根时，旧状态表用“持有者 + Power 类型”作为唯一键，第二份实例触发重复键异常。与包内反复出现的第三方动画错误无关。
- 模拟根现在区分普通叠层 Power 与原版允许并存的同类型实例。并存实例逐个克隆，分别保存层数、动态格挡值和预测状态，Fork 后保持各自来源映射；有效监听器按实例替换根对象，归零实例也不会让另一份实例消失。只需要类型总量的根快照改为汇总同类实例，不再假设唯一性。
- 双实例正式搜索覆盖 `8` 回合并正常返回，runId `254746adc3a54c52ad894279e310d1e6`；严格差分使用 `Block=5/8` 的两份坚韧之环，玩家回合开始后实机与模拟均获得 `13` 格挡、剩余总层数 `1`，runId `1eedfd2234a847378a0e79c600fb1012`。问题包战前状态最终在第 `8` 回合全自动结束、计划外重算 `0`，runId `bcd844191a81453d8d21702024aa0fb9`。
- 三骑士问题包的报错发生在最终遗物标注所做的完整路线回放，但首个差异是两张同 ID 的计算牌交换了升级、保留和格挡状态，不是遗物触发记录错误。该包来自 `0.15.0`，当前分支已有的“按分支当前预览参与稳定洗牌”修复覆盖这类身份漂移；用同一战前存档、种子、三敌生命与行动重建后，最终标注回放正常完成并返回三回合路线，runId `dfdb18f2036e42ecb6beda299d808028`。当前路线与包内旧路线不同，因此不记作旧路线逐动作复现。

### 选择悖论的自动选牌与路线首步

- 咬人卷轴问题包中，`0.15.0` 已先显示选择悖论的五选一原生页面并启动回合准备搜索，但搜索线程没有发布计划；玩家随后手选只完成了原生页面事件，被拦截的回合准备事务已经中断，因而表现为页面和战斗都无法继续。包内没有保存该任务的异常文本，无法把旧版退出唯一归因到某个后续改动。当前源码使用同一问题包战前状态已能正常求解并由 Mod 自动完成原生选择，不再建立第二次搜索。
- 首回合路线此前只读取上一回合结束动作携带的选牌，遗漏了搜索结果根上的 `TurnSetupChoices`。现在初次路线把准备阶段选择放在首回合动作胶囊之前，选择悖论显示为“选择悖论：选择 牌名”；跨回合复用结果不会重复显示首回合选择。
- 使用问题包的战前存档、种子 `YS41WKT7ZUXS`、A10、四只咬人卷轴生命与行动重建后，原生页面可见、搜索启动、自动选择的顺序断言通过；短搜 `5891.5 ms` 比较 `5883` 个选牌分支，最终路线通过首胶囊断言，runId `b9fa371a5b29479bb97c19da7980526f`。最小五候选夹具同样通过三项契约，搜索本体 `334.2 ms`，runId `220482eb7fbc4f459c6f970748b0e033`。

### 仪式兽昏眩对嵌套自动出牌的限制

- 仪式兽问题包在第 12 回合先打出破灭，再由破灭翻出重振。原版昏眩按本回合已经开始的出牌判断：破灭是第一张牌，可以结算；重振属于第二次出牌，即使由破灭自动打出也会被阻止。预测此前调用原版 `ShouldPlay` 时读取当前实机历史，搜索未来回合看不到破灭，错误结算重振、消耗手牌并获得 5 格挡，最终把实际会死亡的路线预测成只损失 2 点生命。
- 昏眩的出牌限制现在由精确镜像读取分支内的 `CardPlaysStarted` 计数。该计数从稳定根捕获，在每次出牌的 `OnPlay` 之前递增，随 Fork 复制、进入状态指纹，并在所属方回合开始归零；手动出牌和嵌套自动出牌使用同一规则。旧的“只有打出带昏眩的牌才置位”专用状态已经删除，因为原版限制的是任意已经开始的出牌。
- 最小严格差分修复前稳定得到模拟 5 格挡、实机 0 格挡，runId `c24277b02b414dc08fdcb59fc7cec21e`；修复后破灭正常进入弃牌堆，重振不结算并按破灭的强制消耗结果进入消耗堆，防御仍留在手里，runId `bbc71ec201d34e16a114e2a1769ceb52`。既有昏眩生命周期夹具升级为当前逐实例状态键后，两次施加、第一张手动出牌、后续禁止出牌和回合末清除均通过，runId `0b73e8ca09374b0dbb27e41f6f021ec9`。问题包用于定位第 12 回合首个差异；当前没有通用中途战斗状态导入器，不宣称整包逐动作回放。

### 精确格挡值、怪物攻击钩子与升级牌洗牌复用

- 啪嗒果问题包在前三回合复用后，于第 4 回合只出现一处差异：虚弱状态下打出的坚韧之环，实机 Power 保存 `Block=3.75`，预测保存 `Block=3`。原版保存 `GainBlock` 返回的修正后小数值，预测此前从角色整数格挡差反推，丢掉了小数部分。卡牌执行层现在记录每次 `CardPlay` 实际获得的精确格挡值，坚韧之环直接保存该值；严格差分 runId `e0e74f8d044e4026b9484ae78d03a622` 通过，问题包战前状态到第 4 回合精确复用且计划外重算为 `0`，runId `ed4f9715bde9405bab9655fd83701aba`。
- 实验体问题包在第 5、6 回合都比预测多一张伤口。第二形态的荆棘会按每个穿透格挡的攻击命中向弃牌堆加入伤口；预测怪物攻击此前直接逐段结算伤害，没有进入共享 `AfterAttack` 生命周期。怪物行动现在把整次多段攻击的结果交给统一攻击钩子，荆棘、吸取和活力等效果由同一语义执行。定向严格差分 runId `b89e025cf429450595e4d38f2e603c90`、14 组伤害/攻击钩子回归 runId `50849d4ecff04661a7254b529611c74e` 均通过；问题包战前状态到第 6 回合精确复用、计划外重算 `0`，runId `a267600d977549f1a492d36479394f60`。
- Vantom 问题包第 5 回合的首个差异是普通打击与升级打击交换位置，属于本版已经修复的“分支升级牌仍按根实例参与洗牌排序”。使用该包战前存档、种子、A2、首行动和原 RNG 重建后，第 5 回合精确复用、计划外重算 `0`，runId `e877c8239def4647a36c7d5102c940f3`。

### 跨角色召唤奥斯提与暗淡蓝点第五张牌

- 较新的无厌沙虫问题包并非被“狂乱逃离”插牌打断。静默猎手打出 `INVOKE` 后，搜索在结束回合并结算 `SUMMON_NEXT_TURN_POWER` 时要求实机已经存在奥斯提；原版 `OstyCmd.Summon` 会为任何尚无奥斯提的角色现场创建宠物，因此预测提前抛出“没有可供模拟的奥斯提实例”并停止全自动。
- 预测分支现在可以创建独立的奥斯提身份，分配稳定 CombatId、加入分支阵容并施加初始“为你而死”。奥斯提生命、最大生命、可命中性、伤害转移、卡牌公式、遗物/Power 钩子、搜索评分、指纹和续用戳都读取这一分支实例；亡灵契约师已有的实机奥斯提继续沿原生命周期复用。
- 较早问题包的计划外重算来自另一条确定性偏差：第三回合恰好打出第五张牌时，暗淡蓝点的预测钩子尚未把当前 `CardPlay` 写入历史，因而只看到四张牌并漏施加下回合抽二。第四回合实机为 `hand=10/draw=0`，预测为 `hand=8/draw=2`。阈值判断现在把正在完成的当前牌计入本回合出牌数。
- 静默猎手跨角色召唤的原生/模拟严格差分通过，runId `ec2f0a77e09a424fad6b8f78f2460c7e`；无厌沙虫短搜越过原失败点并正常返回 5 回合候选，runId `0765ed5133604dcb9fab017fa8e30f42`。暗淡蓝点第五张牌后推进到下一回合，双方均抽到基础 5 张加额外 2 张，严格差分通过，runId `cc6470a2161c417bbf64e5a672a67367`。亡灵契约师既有奥斯提卡牌和“为你而死”伤害回归分别以 runId `037a7a3ec6bc48f797913d398f0dfde1`、`c09e18e9f28b47e1a5c528495c62c124` 通过。

### 战斗中升级卡牌的稳定洗牌

- 帝王蟹问题包在第 2、3 回合精确复用，第 4 回合首次差异是抽牌堆中的普通防御与升级防御错位；第 11 回合又出现抽牌堆、弃牌堆之间的普通/升级防御反转。首个差异不在怪物数值或 Shuffle RNG，而在同名卡牌的稳定排序。
- 原版洗牌前使用 `CardModel.CompareTo` 稳定排序，其中包含卡牌当前升级等级。预测卡牌此前始终按战斗根的原始实例排序，分支中已经升级的预览仍携带旧等级参与排序；现在改为按分支预览的当前状态比较，使原生与模拟在相同 RNG 下得到相同牌序。该修复直接恢复精确状态，不放宽续用比较，也不容忍牌序差异。
- 定向夹具修复前在第 2 回合稳定得到 `hand[0] expected DEFEND_IRONCLAD+0 / actual DEFEND_IRONCLAD+1`，runId `1718b01532d94f34b65acc79a246482a`；修复后原生/模拟严格差分通过，runId `854065893bc742c5ac04e3d6f59e8cdf`。同类啃咬者问题包完整自动战斗在第 5 回合结束、计划外重算 `0`，runId `ddebe062128845f9a3f73fbb6992e3ff`；增量/完整前缀回放覆盖 12 回合、3 次洗牌并保持一致，runId `0c32515cb2944570a6a748febf928737`。

### 洗牌选牌先于卡牌自身选牌

- 实验体 Boss 问题包的两次搜索都在第 6 回合回放升级准备充足时失败。准备充足抽牌触发洗牌，战略选择先请求从抽牌堆拿一张；模拟器没有在这张前置选择页暂停，继续发起准备充足自己的弃两张请求，于是同一个 pending 槽同时收到 `STRATAGEM_POWER/MoveToHand/Draw/1` 和 `Discard/Hand/2`，直接触发不变量异常。
- 卡牌执行现在遇到 OnPlay 内产生的未解决选择就停在原版异步边界。路线动作同时记录卡牌自身选择之前已经发生的嵌套选择数量，搜索回放与真实部署统一按执行顺序消费；普通“卡牌自身选择在前”的动作保持原结构，Void Form 的跨回合选择继续排在动作选择之后。
- 最小严格场景 `STRATAGEM-PREPARED-CHOICE-ORDER-FINAL-0160` 固定为空抽牌堆、三张弃牌、战略选择和升级准备充足：增量/完整前缀回放一致，原生页面依次完成战略选牌与弃两张，随后留下打击并在首回合斩杀，计划外重算 `0`，runId `d305379b208841b68e25f8987e2e1967`。问题包精确 32 张牌根的强制短搜越过原失败动作并返回 7 回合候选、未镜像项 `0`，runId `d4903310604044ae8fa0c689a82f8b8d`；整包增量与普通深搜都在无人夹具 180 秒上限结束，因此不记为通过。
- 千足虫与永世沙漏问题包分别把同一缺陷推进到早有准备和杂技：`0.15.0` 在第 17/6 回合为卡牌自身选择登记第二条 pending 后失败。新增双场景严格差分让战略选择先取走打击，再由早有准备置顶或杂技弃掉，原生/模拟完整状态一致，runId `8da0adbda1484b8f8131cadd30e60d2c`。两个问题包战前根也分别返回搜索候选且不再发生动作回放失败，runId `967571405a5c4a78851c5310fcdd303a`、`d85101a57dcc4d4d910a2e159e02e6c0`；重搜路线与旧失败分支不同，不记作逐动作回放。

### 跨回合无进展循环终止

- Bowlbugs 问题包的深搜在内存回收后收缩为 `2` 个 frontier、`0` 个 ended，但仍从回合层 `2688` 推进到 `4140`。这些状态没有继续伤害敌人或建立获胜条件，只因绝对回合与 RNG 计数持续变化而无法命中精确置换；现有保护只识别同回合零费牌循环，不能终止跨回合循环。
- 每条搜索路线现在携带历史最佳战斗进展：逐敌人耐久/存活数、新敌人击杀、可兑现的进攻与持久成长、未来资源、回复与控制、牌组净化、被盗资源追回及特殊战斗目标推进。只有这些值刷新历史最佳才重置窗口；回合号变化、RNG 前进、单纯维持格挡和临时状态波动不算进展。窗口为 `max(16, 两个完整抽牌循环所需回合)`；其中 `16` 只是跨回合最小观察期，不是回合内重复上限或总回合上限。
- 这不是总回合数上限。路线只要持续造成伤害、积累力量等可兑现进攻、压缩牌组或推进目标，就可以继续搜索任意回合；稳定无进展分支才会被淘汰。最小场景修复前耗满短搜约 `22` 秒并搜索 `54` 回合，修复后搜索本体 `175.3 ms` 结束并剪掉 `18` 条循环分支，runId `5e3fa09b18094a77a07492098e204785`。
- 问题包战前存档近似重建 runId `33c814b90b6b4bdda47fe5b9c98961f9` 仍找到第 `6` 回合胜利、预计战损 `3`、零药水；该工具不能导入包内原生战斗状态，因此只作为相同战前状态的路线质量回归，不宣称逐字节回放原现场。

### 重放选牌卡的空候选结算

- 百足虫问题包当前场次在首轮搜索中导出，尚未走到报错；同包全局日志保留了两次完整同类故障。爆发与复制让生存者结算三次，前两次分别弃掉一张牌，第三次手牌为空。原版仍发出 `0` 候选、`0..0` 必选的手牌请求，部署器却在识别空请求前强制读取下一条计划，因而报“计划外选择”。
- 原生选牌驱动现在直接完成这类确定性的空请求；搜索显式生成空选择计划时同步核销一条空计划，重放后自然出现的空请求不要求虚构选择。所有存在候选的页面仍必须有严格计划，候选身份、数量或页面时序不一致继续失败。
- 生存者三次结算回归 runId `207cdd4927f74188948ec903574a3c7c`：前两次实际弃牌，第三次空请求不消费计划，首回合正常结束且计划外重算为 `0`。相邻拾荒重放 runId `c73b5302e55e4b06bf56dc35169f3e20` 验证两次原生空请求各自核销一条显式空计划，同样首回合结束且零重算。修复前基线 runId `026a459931b24b58be85d644d3778d25` 稳定在第三次请求报错。
- 瀑布巨兽问题包中的头槌在弃牌堆为空时同样产生 `options=0 / select=0..0` 的原生牌堆请求，`0.15.0` 部署因此误报计划外选择；宇宙冷漠共享这一原版行为。当前通用空请求处理无需卡牌专用分支。头槌与宇宙冷漠均以空弃牌堆完成实际出牌并进入第 2 回合精确复用，计划外重算为 `0`，runId 分别为 `53d9a793c14040d790183727ab0a88cd`、`b09e6ef038604469b73680803c6916e7`。

### 重放卡牌的逐次出牌生命周期

- 猫头鹰法官问题包第二回合的首个且唯一续用差异是怀表：首回合路线显示 `10` 个出牌动作，原版历史却有 `11` 次 `CardPlay` 完成。投掷斧让首张猛扑的 `PlayCount=2`；路线动作数保持 `10` 是正确的，错误在于预测把整组重放只记作一次出牌生命周期，令怀表预测为 `10`、实机为 `11`。
- 出牌完成后的怀表、攻击/技能/小刀/格挡出牌计数及同类生命周期现在按每次实际 `CardPlay` 更新；整张卡牌动作结束边界只保留整组重放后的死亡结算。路线与 UI 仍把这组结算显示为一个动作，并沿用重放次数标注。
- 修复前最小夹具稳定得到怀表 `expected=1 / actual=2`，runId `89792141b109409aa2aa5adcc7d2a846`。修复后 `POCKETWATCH-REPLAY-REUSE-0150` 在开启增量/完整前缀核对时于第 2 回合精确复用、计划外重算为 `0`，runId `93d934679e8746de95bebd9dd5ce58e2`；完整两回合自动战斗同样为 `0` 次重算，runId `106f0b2966dd4225ac9ce1213e123712`。
- 知识恶魔问题包为同一缺陷的华彩路径：首回合路线有 `15` 个动作，带华彩的升级后空翻实际 `PlayCount=2`，令 `0.15.0` 的怀表续用戳为预测 `15`、实机 `16`。当前源码定向夹具使用相同的华彩后空翻并推进到第 2 回合，原生/模拟均记录怀表 `2`，runId `c82c4fcbadda41dc96df6d65cf0e0d63`。两次问题包战前跑局分别在 180 秒和 120 秒上限内没有完成首搜，因此不宣称原场次已完整回放。

### 第三方玩法 Mod 的明确不兼容提示

- 尸蛞蝓问题包在首轮根快照阶段拒绝了 `Watcher` 注册的未知战斗订阅器；敌人、牌堆和搜索本身尚未开始执行，不属于尸蛞蝓语义缺口。
- 未经适配的第三方玩法 Mod 订阅器继续被明确拒绝。初始化失败面板现在显示 Mod 名称和标识，并建议玩家卸载该 Mod 后重启游戏；订阅器类型与完整异常只写入日志，便于开发排查。
- 本项目不再为这类未知第三方玩法 Mod 主动增加专用模拟适配。非玩法 Mod、现有 BaseLib CardModifier 与已经明确支持的 Loadout 边界保持原行为。

### 训练假人第二档的限时失败边界

- 第二档问题包的搜索在第 3 回合结束动作回放时重复让假人逃跑，抛出“逃跑的生物不在当前模拟战斗中”。更早的分支已经把倒计时耗尽当成敌人全数离场并授予胜利分，因此异常只是错误事件语义的后续表现；第一、三档没有该限时 Power，所以不进入这条路径。
- 倒计时耗尽现在保留仍存活的假人，并以明确的事件挑战失败边界终止该分支，不再把逃跑当成击杀。假人已经死亡时不再触发倒计时；模拟逃跑与原版命令一致，对已经登记为逃跑的生物重复调用保持幂等，其他不在战斗中的生物继续显式报错。
- 问题包完整牌序在开启增量/完整回放对照后通过，runId `7fbd338febef40668a4980555cc51971`：未击杀候选为 `won=False / EventDefeat`，最终击杀候选为 `won=True / None`；第 2、3 回合精确复用，完整自动执行成功，计划外重算为 0。独立 1 层倒计时边界 runId `1b1d321d7ac941adb5d515efa861d6ee` 同样通过。修复前当前源码与 `0.14.12` 均在第 3 回合结束动作稳定复现同一重复逃跑异常。

### 瓶中精灵的生存路线与自动用药

- 女王问题包的首轮根已捕获瓶中精灵，但威胁评分在精确结束回合结算之前直接扣减预报伤害，把本可以交掉精灵药继续战斗的铺垫路线当成死亡节点。同时，精灵药由死亡钩子自动消耗，旧路线只统计显式 `UsePotion` 动作，因而把实际用药记为 `0` 瓶。
- 威胁投影现在按原版阶段先结算瓶中精灵，再结算未使用的蜥蜴尾巴；多段攻击会在每次致死后消耗对应的防死来源并继续投影。药水库存变化会立即重建分支监听器：已消耗精灵药不会再触发，熵酿等途径新获得的药水也能进入当前分支。
- 自动复活现在进入路线总用药数、战略成本、Beam 分组、置换支配和跨回合剩余路线统计。“禁用药水”只禁止求解器主动喝药，不会否定原版强制触发的精灵药；自动消耗仍如实显示在预计用药中。定向回归以 `1 HP + 精灵药 + 两张重锤`强制验证第 1 回合复活、第 2 回合击杀、路线记 `1` 瓶且计划外重算为 `0`；runId `bbddfcc1e1e54be2a4405e58cd7f557e`。精灵药严格生命周期 runId `e601bec430ea49318ef57a550d8284f8` 与无药反向死亡 runId `53ad9b78496646b196aa4844794766ef` 同时通过。问题包用于确认根状态与错误分类；当前没有通用中途战斗导入器，不将定向夹具写成原女王整战回放。

## 0.15.0：稳定性与跨回合执行修复

### 注能冰川后的臂铠稳定状态

- 地道虫问题包的两次首轮搜索都在第 3/4 代分叉时失败，异常为 `Cannot fork Vambrace during card-play resolution`。开局注能冰川已经由原版自动打出并进入弃牌堆，玩家获得 `12` 格挡；问题不是地道虫行动、冰川牌效或搜索策略，而是根快照中的臂铠状态被错误判为仍在出牌事务中。
- 游戏 `0.111.0` 的臂铠在第一次格挡牌结算后只把 `_blockGainedThisCombat` 设为真，并将 `_triggeringCard` 保留到战斗结束。预测状态现在按同一生命周期保存两者，允许这份稳定状态参与 Fork；`AfterCardPlayed` 也不再提前清除触发卡引用。
- 修复前定向 Fork 基线 runId `0e1c56fe922c4b4a87109dbd1c06acd0` 在相同异常处失败；修复后 Fork runId `31793a0e83df4656aa0ea3b9182c4c29`、注能冰川组合搜索 `a9ebb7b36f554948a8de0f658385aa34`、增量/完整回放 `689793cd6343465393fdd567a4a7c41e` 和遗物严格差分 `eddeca24b4544e918299e4b4bb2a401b` 均通过。严格差分首次复跑的 `43` 格挡来自同批前项遗留的 `28` 格挡，修正夹具隔离后两张防御严格得到 `10 + 5 = 15`，该次夹具失败不计为功能失败。

### 巨斧机器人两连击的逐段死亡

- 巨斧机器人问题包在前四回合精确复用，第 4 回合敌人以 `2 HP` 执行两连击。第一段攻击触发玩家 `3` 点荆棘并杀死当前实例，原版中止剩余攻击、消耗一层库存并生成新的巨斧机器人；预测继续结算第二段，凭空记录 `5` 点战损，导致第 5 回合出现 `hp expected=70 / actual=75` 的计划外重算。
- 怪物多段行动现在每一击后立即处理反伤造成的死亡及死亡 Power。攻击者死亡后不再执行剩余攻击段；行动函数在攻击命令之后明确声明的尾部效果仍继续结算。库存重生继续沿既有死亡/召唤语义生成新实例，后续敌人和回合从更新后的阵容推进。
- 两连击与上勾锤击的反伤致死严格差分均与实机一致；后者仍正确施加虚弱和脆弱。问题包战前状态以 Low/Instant 完整自动结束于第 11 回合，计划外重算为 0；增量分叉与完整前缀回放覆盖 11 回合、3 次洗牌并保持一致。当前搜索路线与原包不同，不作为原包逐动作回放。

### 跨回合出牌历史与 FTL 抽牌

- 多尼斯异鸟和蛇行扼杀者问题包都在前几回合精确复用后重算。两条路线的首个共同分叉是 FTL：它作为本回合第 3 张牌时，原版仍会抽 1 张；预测却把此前回合的已完成出牌也算入 `PlayMax`，少抽一张后令下一回合手牌、抽牌堆及后续 Shuffle RNG 错位。
- FTL 和 Make It So 现在直接读取随分支重置、随 Fork 复制的本回合总出牌数和技能数。Fetch 另行保存本回合已经打出的具体卡牌实例，并把该集合纳入状态指纹；搜索 worker 不再为这些条件读取全局实机历史。FTL 恢复为第 3 张抽牌、第 4 张不抽，Fetch 与 Make It So 也不再跨回合累计。
- 多尼斯战前跑局重建已在第 3 回合精确复用，蛇行扼杀者重建已在第 4 回合精确复用，两场计划外重算均为 0；多尼斯路线同时通过增量分叉与完整前缀回放一致性。

### 方柱构装体搜索初始化

- `0.14.12` 问题包在首轮根快照中读取 `CubexConstruct.ChargeUpStrengthGain`，但游戏 `0.111.0` 的方柱构装体不存在该成员；它的蓄能和两次重复轰击回调都直接获得固定 `2` 点力量。多余的静态值捕获因此在 Beam 启动前抛出 `MissingMemberException`。
- 方柱构装体已经从静态成员表移除，三个增力动作继续由现有固定 `2` 点语义结算。根快照、正常搜索和四个方柱构装体动作的实机/模拟累计差分均已通过。
- 同批蠕虫问题包来自 `0.14.11`，首个错误是 Loadout 的免费卡牌监听器被拒绝，属于 `0.14.13` 已完成的 Loadout 兼容问题，不新增蠕虫专用逻辑。

### 帝王蟹火箭蓄能回放

- 帝王蟹问题包的三次搜索都在第 3 回合结束动作回放时失败；最内层异常是 `Rocket` 执行蓄能时找不到分支静态值 `ChargeUpStrengthGain`。`EndTurn` 只是推进敌方行动的入口，不是事务边界错误。
- 火箭在游戏 `0.111.0` 中确实拥有该成员，怪物行动语义也已经从分支状态读取它；缺口是静态参数表只登记了同名但实际不存在该成员的方柱构装体，却遗漏了真正使用它的火箭。根快照现在在主线程捕获火箭的蓄能力量值，并随怪物静态快照在 Fork 间不可变共享。
- 从问题包战前存档重建后，搜索越过原第 3 回合失败点并覆盖 7 回合；低档 Short 的增量/完整回放对照覆盖 9 回合。帝王蟹 10 个行动的实机/模拟累计差分也全部通过，火箭蓄能获得 `2` 点力量并正确保留到激光与重新充能。

### 三骑士降级后的计算牌根快照

- `0.14.11` 问题包在前两回合成功部署并复用。魔法骑士第 2 回合施加抑制后，第 3 回合手牌与 Shuffle RNG 不再匹配既有路线，Runtime 正确请求重算；重算根把耗尽堆 `EXPECT_A_FIGHT` 的 `CalculatedBlock` 预览缓存从 `16` 重建为基础值 `15`，随后被根稳定性校验拒绝，界面因而停在搜索初始化失败。
- 原版 `CalculatedVar` 的 `BaseValue` 是随克隆、预览和 UI 更新而变化的派生缓存；真正结算由 `CalculationBase`、`CalculationExtra` 以及当前力量、牌堆、Power、历史等分支输入计算。43 个原版计算字段现在统一标记为 `Derived`，不再进入续用文本和状态指纹；基础变量及所有公式输入仍完整保留。
- 当前源码从问题包战前存档重建时还暴露了破灭自动出牌的嵌套随机选择分支：完整前缀重放后，计划牌可能不再属于当前选项。此类精确的计划分支失效现在进入既有 `InvalidPlannedChoiceBranchException` 通道，只淘汰该候选；牌堆缺失、事务错误和其他执行异常仍然中止搜索。
- 计算牌根快照、5 点力量下的 `EXPECT_A_FIGHT` 实机/模拟格挡、魔法骑士全牌堆降级/死亡恢复、问题包首轮搜索、增量/完整回放及完整全自动均已通过。当前源码重建的首抽路线与原包不同，因此整场结果只证明相同战前存档可正常求解和部署，不作为原包逐动作回放。

### 原生手牌固定数量选择

- 啃咬机问题包在第 2 回合按既有路线打出燃烧契约并选择升格者之灾。导出状态已经显示燃烧契约进入弃牌堆、升格者之灾进入耗尽堆，说明卡牌匹配和原版选择均已完成；部署随后仍检查确认按钮，页面先自动结束时便以“原生手牌选择完成后确认按钮仍不可用”中止。
- 原生选牌请求现在保留 `CardSelectorPrefs.RequireManualConfirmation`。固定数量的手牌选择和手牌升级在选满后允许原版自动结束；页面尚未结束时，驱动器使用当前已启用的确认按钮完成事务。可变数量选择仍要求手动确认，已完成页面不会再被误判为按钮故障。
- 燃烧契约固定单选与武装升级的原生页面均完成真实部署，页面期间没有搜索；问题包战前存档重建也已越过燃烧契约页面并在第 5 回合结束。该整场重建另有一张防御升级状态不一致并触发 1 次重算，因此只作为本问题路径证据，不记为整场通过。

### 自动触发选牌的跨阶段接管

- 异螨问题包中的 `0.14.11` 实际已经在首回合接管烘焙手套：原生手牌页显示后搜索，随后自动选择并耗尽 `GATHER_LIGHT`，状态严格匹配。当前源码从该包的战前跑局状态重建后，仍按 `Visible → SearchStarted → Selected` 自动选择 `REFLECT`；首回合手套、工具盒、选择悖论、赌博筹码等继续由 Mod 接管，不交还玩家。
- 知识恶魔的诅咒选择发生在求解器提交结束回合之后。路线虽然已把 `MIND_ROT` 等选择记录在结束回合动作上，旧部署却在敌方行动开始前结束原生选牌会话，导致二选一页面无人接管并永久等待。
- 部署现在把 `ApplyKnowledgeCurse` 从下一回合准备选择中分离，在结束回合后保持专用原生选牌会话，直到路线中的敌方回合选择全部完成；下一玩家回合只重放真正的回合准备选择。知识恶魔首轮自动选择 `MIND_ROT` 后获得对应 Power，第 2 回合精确复用且计划外重算为 0；同时持有烘焙手套的组合场景也依次完成首回合手套、敌方诅咒和第 2 回合手套边界。

### 跨平台 No-GC 预算边界

- macOS ARM64 问题包中的两次首轮搜索都在进入 worker 后、建立求解器之前失败。默认 `6 GB` 总预算和 `1 GB` LOH 预算使 `GC.TryStartNoGCRegion` 抛出 `ArgumentOutOfRangeException(totalSize)`；同一战斗的手动全自动重试在相同调用点再次失败，战斗根本身已成功捕获。
- No-GC 启动现在明确区分成功、内存不足和当前 CLR 不支持该 SOH 区域大小三种结果。区域大小不受支持时，Runtime 使用现有 `SustainedLowLatency` 与 Beam 安全检查点回收继续搜索；参数错误、嵌套 No-GC 等其他异常仍然直接失败。首轮进入和检查点回收后的重建共用同一入口。
- Windows headless 以 `16 GB` 预算完成首轮求解并产出可执行动作，证明正常 No-GC 路径未退化；该平台实际接受了 `16 GB` 区域，因此不能替代 macOS ARM64 对拒绝分支的实机复测。

## 0.14.13：Loadout 战斗费用兼容

- 最新实机日志在创建首回合搜索根时拒绝了 Loadout 新增的 `LoadoutEveryCardFreeCombatHook`，因此界面显示“搜索初始化失败”。Legacy RNG 兼容层和 CombatSolver 本身均已成功初始化，首个错误只发生在未知 gameplay subscriber 的根快照校验。
- 根快照现在按玩家捕获 Loadout 的“所有牌免费”状态，搜索分支从该不可变值修正能量费用。开关关闭时不会再因订阅者存在而拒绝搜索；开关开启时，当前卡牌和后续生成卡牌都按 Loadout 的 0 费规则计算，worker 不读取 Loadout 的实时静态状态。其他未知 gameplay subscriber 仍保持显式拒绝。
- 实际 Loadout `0.4.10` 与 BaseLib `3.4.5` 的第一轮定向运行已经成功创建并 Fork 根快照，原 `SEARCH_SETUP_FAILURE` 未再出现；运行随后暴露测试对 ModHelper 运行级订阅者的旧计数口径并已修正。修正后的第二轮在 Loadout 的 headless 战斗房间资源预载处超时，未进入断言，因此不记为完整场景通过。

## 0.14.12：牌堆压缩后的连锁路线

- 同族小队极限存档的生还路线依赖怀表控牌、灰水一次耗尽六张手牌，以及下一回合按“愤怒复制 → 放血回费 → 燃烧契约抽牌 → 多段攻击”完成斩杀。旧搜索会把这些中间状态分别压回即时伤害最高的代表，导致完整连锁在伤害兑现前被淘汰。
- 多张选牌现在为每个合法选择数量保留代表，选牌历史按来源、效果、具体选项和当前回合事件隔离；错误的选择分支直接淘汰。牌堆状态同时区分精确敌方分布、无序牌堆构成和有序洗牌前缀，避免不同灰水/燃烧契约选择被错误合并。
- 怀表边界显式保留出牌节奏、目标集火和若干精确洗牌状态。大规模回合末牌堆压缩会建立持续到后续回合的专用路线血统，并分别保留回费、攻击复制增长、牌堆整理、集火、进攻和下一步动作种类；这让零费铺垫、回费和抽牌可以在终结伤害出现前连续存活。
- 易伤的中间价值改为按可利用的攻击窗口非线性计算，当前能量作为能否继续出牌的独立 Beam 资源；两者只影响中间保路，最终路线仍按真实胜负、战损、药水和卖血规则选择。
- 精确回归 `kin-boss-route-clean` 从 56 HP 开始，第 5 回合以 3 HP 获胜，战损 53，计划外重算为 0。最终路线第 4 回合使用灰水压缩牌堆，第 5 回合通过愤怒、放血、燃烧契约和连续攻击击杀三名敌人。

## 0.14.11：创意工坊首发元数据

- Mod 作者统一为 `Torch`，准备首次 Steam 创意工坊公开发布；本版不改变战斗模拟、搜索、部署或 UI 行为。

## 0.14.10：敌方攻击压制保路与 X 牌顺序

- 最新实机日志中，无厌沙虫首轮正在使用无伤害的“液化地面”。`0.14.9` 初始搜索没有选择萎靡+，预计整场掉血 `19`；玩家手动用 4 能量打出后，实机根准确捕获敌方 `力量 -5 / 虚弱 5`，同一搜索预计掉血降为 `4`。X 值捕获和 Power 结算均正确，问题发生在进入下一次敌方攻击前的 Beam 保路。
- 搜索快照现在分别记录活跃敌人的力量压制和虚弱剩余层数。卡牌或药水提升任一维度时进入 Control 通道；每个药水组显式保留力量压制最强与虚弱最强的候选，并把两项加入小型 Pareto。相对根状态新增的压制按普通战/Boss 的有限未来攻击窗口参与 Beam 排名，终局选择仍只比较真实模拟出的胜负、战损、药水和卖血。
- 沙虫固定首手回归在 High 预设下记录首个可执行动作 `MALAISE`，runId `d46f2d8c7adb4bbc89b1b5c8a6b8cdeb`。双小啃兽非退化运行仍为 `0` 战损、`0` 计划外重算、两次洗牌；实际第 `6` 回合结束，与请求中的旧第 `5` 回合断言不同，因此 runId `a6ef1d75a48d46f2be7b98ce7ef4def5` 保留为“战损通过、回合断言失败”，不记为整场通过。

## 0.14.9：Tender 出牌完成结算与跨回合复用

- 猎人杀手问题包在第 3 回合拒绝续用，唯一差异为敌方生命：预测 `110`、实机 `113`。第 2 回合的后空翻、中和+、打击在 Tender 下实际只造成 `7` 点伤害；模拟仍按未降力量的 `10` 点计算。复用检查正确拦截了这份错误预测，重算是上游出牌完成语义偏差的结果。
- `CardPlayFinished` 历史和原版 `AfterCardPlayed` 钩子完成后，模拟器现在立即结算该次实际出牌新产生的历史补偿及 Power 变化。Tender 的力量/敏捷下降会进入下一次出牌；带重放的牌按每个 `CardPlay` 分别结算。整张卡牌执行完成边界仍只负责卡牌生命周期。
- 修复前最小严格差分稳定复现 `expected E0.hp=111 / actual=114`。修复后目标差分 runId `79929fef88b3495cbe60e4d529594a31`、Tender 增量/完整回放 runId `bb70d9239f78495e988682b40bda9bec`、猎人杀手跨回合复用 runId `c242e1f6287c484cbae5925b36a995f5` 和旧 Tender 回合末恢复 runId `5da676be79aa45e7b4f6cff40b353fa4` 均通过；完整场景第 2 回合精确复用且计划外重算为 `0`。
- 问题包战前存档已尝试交给现有无人入口重建，但原版在 headless 的 `NOverlayStack` 初始化阶段空引用，未进入战斗；该次尝试不计作问题包回放证据。

## 0.14.8：回合首张牌出牌间隔

- 全自动的新回合会等待原版抽牌和回合准备动作全部完成；手牌稳定进入 `Play` 状态后，再等待设置中的“牌间额外停顿”，随后才恢复路线或开始部署第一张牌。
- 普通抽牌回合、回合开始选牌重放和首回合选牌搜索统一使用同一等待入口。玩家未开启全自动时，自动搜索仍按原时序开始，不额外拖慢结果展示。
- 按用户要求，本版不运行行为测试；只执行最小发布所需的 Release 构建和 ZIP 内容检查。

## 0.14.7：Beam 检查点回收与跨战斗后台回收

- 实机历史日志确认战斗 Reset 曾同步执行强制、阻塞、压缩式全代 GC，单次暂停约 `177-382 ms`；高水位时还调用 `EmptyWorkingSet`，把整个游戏工作集从约 `3.28 GB` 降到 `101 MB`，直接制造后续房间的重新缺页。该路径已经删除，战斗结束只登记后台非压缩全代回收，不再清空进程工作集。
- 回收请求现在与真实 worker 生命周期绑定。取消或异常的搜索即使晚于战斗 Reset 退出，也会在最后一个 worker 退出后完成已登记回收；下一场搜索等待回收任务完成，避免压力状态跨战斗遗留。
- Runtime 依据 No-GC 的 SOH 可用额度注入分配边界。搜索只在一层出牌扩展完成、当前 Beam 已剪枝的安全点响应：保留活动 Beam、已完成候选和路线父链，丢弃转置表、已展开表及可重建评分缓存，结束本轮 No-GC，等待后台非压缩全代回收完成，建立新 No-GC 配额，再从原 `turn_layer/play_depth` 继续。该机制不提前返回路线，也不从战斗根重算。
- 1 GB 压力场景 `GC-CHECKPOINT-RESUME-0147`（runId `cd3b633d60754b5cad509fe3c175bbc7`）触发 `5` 次检查点；每次保留 `30` 个活动节点，回收后托管存活量从约 `659-806 MB` 降到 `100-205 MB`，GC 暂停为 `3.1-4.0 ms`。最终搜索到完整 `6` 回合路线并获胜，`UnexpectedReplans=0`，没有 No-GC 耗尽、`>50 ms` 主线程帧或提前内存边界。

## 0.14.6：动作选牌事务与部署高亮时序

- 永世沙漏日志在第 3 回合通过完整 continuation 后，杂技原生弃牌页找不到路线中的凋零。首个差异不是牌堆或 RNG：原版杂技先完成抽牌与弃牌，再运行 `WitheringPresencePower.AfterCardPlayed` 生成凋零；模拟器原先完成整张牌和 `AfterCardPlayed` 后才枚举主选择，因而把未来生成的凋零错误加入当前候选。手动出牌现在在 `OnPlay` 效果之后、附魔/苦难与 `AfterCardPlayed` 之前消费动作选择；无计划探测停在该边界，已选分支在同一事务继续结算，旧的事后补选和来源牌二次搬移已删除。
- Overlay 路线行现在单独维护可执行动作胶囊。必备工具等回合开始选择仍作为已完成的上下文显示，但不再占用部署动作索引；当前牌只在真实动作准备入队时高亮，结算后到下一动作启动前保持无活动胶囊。纯结束回合单独显示活动/完成状态，因此必备工具和佩尔之眼额外回合切换后不会整体错一张或留下最后一张常亮。
- 沙漏选择边界严格差分 runId `4e3576a6ffc546c6979c768ea6f46f60` 断言杂技候选不含尚未生成的凋零；生存者、杂技、早有准备和燃烧契约相邻事务 runId `bcdaab927b3d405c8b8d20e3d4de4c93` 全部通过。沙漏完整自动部署 runId `3ad4ff73aefd4136b51d7596741a7795` 由搜索规划杂技弃防御、原生页面只请求一次选择，随后打击结束战斗，增量/完整回放一致且计划外重算为 0。必备工具 UI 时序 runId `268f83840ffc40fdb182edf1c03ff2f3` 与佩尔之眼/必备工具组合 runId `5e0966d1ce114496b2c4292a60d3871b` 均精确复用第 2 回合、原生页面不搜索、计划外重算为 0。

## 0.14.5：佩尔之眼结束回合复核与路线胶囊

- 搜索已经正确规划“第三回合直接结束并触发佩尔之眼”，但全自动提交结束回合前的实机风险复核重新用影子历史推导额外回合，误把下一阶段当成敌方回合，虚构三个千足虫行动与 32 点掉血后暂停。实机复核现在用原版 `PaelsEye.ShouldTakeExtraTurn` 读取当前真实遗物与 History 判定，再在影子状态中结算佩尔之眼耗尽手牌；确认额外玩家回合后不再预测敌方行动。
- 路线 UI 现在把 `EndTurn` 投影成只读动作快照。没有出牌的回合仍显示“直接结束”胶囊；该结束动作触发佩尔之眼时，胶囊内以现有遗物联动配色显示“佩尔之眼：额外回合”。卡牌计划动作同时保存该实例的附魔后重放次数，动作胶囊在牌名后显示“重放×N”，同名普通牌保持无标注。
- 定向佩尔场景 runId `ee7607c122bf4623a785daa13a3dc993` 在开启实机结束回合风险复核时直接结束首回合、显示佩尔之眼联动，随后进入额外玩家回合并精确复用第 2 回合，计划外重算为 0。螺旋打击场景 runId `77cc4df7265640d9b78f93768a333f15` 同时核对计划与 Overlay 快照均为 `重放×1`，并完成实际部署。

## 0.14.4：单步选牌页接管与跨会话出牌间隔

- 单步执行结束后，下一回合的必备工具、暴政等计划选牌仍先显示原生页面并等待玩家。运行时现在保留该页面的既有路线会话；玩家在页面上点“执行本回合”或开启“全自动”时，会按上一轮 `EndTurn.TurnStartChoices` 接管当前选择，随后精确复用 continuation 并继续部署，不从 `Start` 中间态新建搜索。
- “牌间额外停顿”现在覆盖回合开始选牌到本回合第一张牌的会话边界。原生选择事务完全结束并进入稳定 `Play` 状态后，控制器先等待当前设置的间隔，再恢复路线和部署；出牌动作内部选择及普通相邻出牌的既有间隔保持不变。
- 定向 PUNCH Construct 夹具先断言第 2 回合必备工具页面停住且求解器选择数为 0，再分别由“执行本回合”和“全自动”接管。runId `48ec35fe5d4e45e38ed6fbed3fc012e4` / `f82172f5aa264f5d9d73b6c59e79a2a9` 均直接复用第 2 回合、计划外重算为 0；设置 500 ms 时，选牌完成到下一张牌实测间隔分别为 610 ms / 609 ms。增量/完整回放核对整战 runId `cfb96d6f67b74b7797dec580983b9bbb` 连续完成第 2、3 回合必备工具页面，`visible=2 / selected=2 / search=0`，逐回合复用且计划外重算为 0，于第 3 回合结束。

## 0.14.3：部署动作完成边界

- 瀑布巨兽实机日志中的首次错误发生在第 12 回合：回响斩击已把巨兽转入蓄爆，随后杂技页面选中升级战术大师；部署器却读取了动作执行器上一批已经完成的队列任务，在战术大师嵌套自动打出尚未结算时检查下一张防御。此时能量仍为 0，因而误报 `card_unplayable`、从中间牌堆状态重算；第 13 回合又因该不稳定根产生一次状态失配重算。
- 卡牌和药水部署现在捕获本次实际入队的 `PlayCardAction` / `UsePotionAction`，并等待该动作实例的 `CompletionTask`。原版入队动画、药水 `BeforeUse`、原生选牌页及嵌套自动打出时序保持不变；部署不再依赖可能指向上一批动作的全队列完成快照。
- 定向双怪 runId `cabb121a5a1544a196dd1bda013884b2` 精确覆盖“回响斩击先触发瀑布巨兽死亡/蓄爆，再同回合杂技弃升级战术大师”：杂技完成后能量为 2，战术大师与杂技均已进入弃牌堆，第 2 回合直接续用且 `UnexpectedReplans=0`。重建长线 runId `24ecb35d75b4417aa6cd7a44652dcc65` 于第 10 回合结束，1 次搜索、9 次续用、完整经过蓄爆/爆炸，计划外重算为 0。药水部署 runId `dfed9e7bbf884d7f8fdf07960831bef4` 使用弱化药并打出攻击，于首回合正常结束。

## 0.14.2：单步回合边界与重放卡牌身份

### 单步与全自动的控制边界

- “执行本回合”只部署当前回合路线。上一回合结束后，后续 `SetupPlayerTurn` 不再消费计划中的必备工具、暴政等回合开始选择；原版页面保持打开并等待玩家。只有全自动模式会在该页面按上一轮 `EndTurn.TurnStartChoices` 播放既有计划。
- 全自动完成回合开始选牌后，`PlayerTurnSetupCoordinator` 先结束并释放当前原生选牌会话，再恢复 continuation 或启动剩余路线部署。这样嵌套部署不会压在旧会话之上，消除了 `turn_setup:N` 不是活动栈顶的异常；页面仍是动画展示，不触发新搜索。

### 同名卡牌的语义身份与 Beam 保路

- `PlanCardToken` 记录卡牌语义状态键。部署与预测回放按 ID、升级、费用、重放次数、临时标志、关键词、动态变量、附魔和苦难状态匹配具体实例；带螺旋附魔的重放防御与普通防御不再被当成可互换的同名牌。
- `BeamRetentionPolicy` 的选牌保路覆盖动作选择、嵌套选择和 `EndTurn.TurnStartChoices`，并纳入弃牌/弃牌再抽。不同来源、上下文和卡牌语义状态的路线各自保留候选，避免必备工具的弃牌分支在后续出牌价值显现前被同一 Beam 层淘汰。
- 定向 headless runId `07d96613e313442a99275ee969e1c02b` 证明单步在第 2 回合手牌页停住、求解器选择次数为 0，并计划弃掉普通防御、保留带螺旋附魔的防御。全自动 runId `80552c268daf450cbce05aeb9314b844` 在同一牌组上记录 `visible=1 / selected=1 / search=0`，第 2 回合直接续用且计划外重算为 0，日志中没有原生选牌会话栈异常。
- 玩家问题包只保存了必备工具选择完成后的检查点，且没有通用逐实例战斗状态导入器，因此不能从包内状态精确还原选择前的第 3 回合手牌；上述定向夹具隔离验证了同名普通/螺旋防御的计划身份和两种控制模式，不把非精确复现写成原问题整战复现。

## 0.14.1：原生选牌界面与路线复用

### 原生选牌界面与回合准备边界

- 求解器接管的选牌不再在生产部署中安装隐藏 `ICardSelector`。`NativeChoiceRuntime` 观察原版 `ChooseCard`、简易网格、奖励网格、战斗牌堆、手牌和手牌升级页面，锁住玩家输入后按路线逐项点击并确认；单候选和原版明确自动选择仍遵循原逻辑。无 UI 严格差分保留测试专用 `PlannedCardSelector`。
- 工具盒保持原版 `BeforeHandDraw` 时序；选择悖论、烘焙手套和赌博筹码在抽完首手后打开页面。首回合没有既有路线，页面可见后才从 `Start` 根搜索，并把进度送回 Overlay。必备工具、暴政、熵、烘焙手套及其他后续回合选择继续由上一轮 `EndTurn.TurnStartChoices` 规划；下一回合只打开原生页面并按该计划播放选择动画，不建立第二次搜索。
- `PlayerTurnSetupCoordinator` 同时管理 `SetupPlayerTurn` 与 `RunAutoPrePlayPhase`。后续回合页面完成后，控制器用既有 continuation 与真实 `Play` 状态做完整状态戳核对：一致则直接显示剩余路线并继续部署，只有候选或战斗状态失配才进入普通重算。出牌和药水产生的生存者、早有准备、杂技、全息影像等选择在对应动作事务内显示并执行。
- Headless 验收覆盖工具盒、选择悖论、烘焙手套、赌博筹码、助能生存者、必备工具第 2 回合、出牌手牌选择、战斗牌堆选择及横祸嵌套选择。首回合工具盒断言 `Visible → SearchStarted → Selected`；第 2 回合必备工具断言 `visible=1 / selected=1 / search=0`、`SEARCH_REUSED turn=2` 和零计划外重算。推断选牌严格差分为 12/12。开发期曾分别暴露双入口重复搜索、`TurnStarted` 早于 `Play`、助能来源标识与原牌不一致、可见 trace 只在回合准备记录，以及把后续页面错误绑定到新搜索五个根因，均由所有权、生命周期和统一观察边界直接修正；失败样本不计为通过证据。
- Steam 可见机甲最终 runId `a070eac1fbd548c09b2cc398a4a5b176` 在第 7 回合结束，必备工具原生手牌页共 `visible=6 / selected=6 / search=0`，第 3 回合 continuation 复用和 `UnexpectedReplans=0` 均通过。此前错误设计的可见 runId `31f5bc33de31409c8238334a9dbaeea8` 虽显示页面并结束战斗，却因每回合另起搜索而没有第 3 回合复用，明确保留为失败证据。

## 0.14.0：求解器职责边界重构

### CombatBeamSolver 阶段边界

- Beam 快照新增存活牌堆的重放潜力：按卡牌额外执行次数与基础牌值计算并封顶，只参与中间保路，不进入战斗状态键、续用文本或终局胜负排序。获得更多重放的路线归入成长通道，修复未掘宝石把后续灵体变为三次结算后仍被即时伤害路线淘汰的问题。
- 猫头鹰法官问题包固定根在原 High 规模下从“仅死亡路线”改为主动打出未掘宝石并于第 8 回合获胜；实际执行后第 2-8 回合全部精确复用，零计划外重算。低预设的增量/完整前缀核对也找到第 10 回合胜利；双小啃兽长线保持第 5 回合、两次洗牌、0 药、0 战损。
- `CombatBeamSolver` 从约 4,133 行单文件拆为 `Entry/Models/Phases/Expansion/Retention/Terminal/StateEvaluation` 七个同类型 partial 文件；随后增加 `BeamRetentionPolicy` 与 `FinalPlanOrdering` 两个具体策略边界。预算/进度仍留在 `Solve`，不在纯移动批次假装完成 run-local context。
- 结构门禁固定分片文件集，并在所有 Beam 文件继续检查宽泛异常与 live 根回读。固定机甲第 5 回合、完整动作序列、4624 展开、33432 转移、17735 选牌分支及各剪枝计数精确不变；本轮 `11.35 s / 3.55 GB / 0 ms GC / 17.2 ms 最大帧`，零非预期重算。Defect 球与 Synchronize 严格差分通过。
- 单次搜索的计数器、性能指标、帧节流、转置表及 stand-pat/威胁/coverage/路由缓存进入具体 `SearchRunContext`；不可变根配置仍由 solver 持有。本批不复用 scratch 或集合。相同机甲为 `11.52 s / 3.55 GB / 0 ms GC / 17.4 ms`，路线和计数不变。
- Beam 状态去重、分数排序、多样性通道、药水配额和 Pareto 保留进入具体 `BeamRetentionPolicy`；模拟器仍由 solver 拥有，政策只通过窄 stand-pat 委托取一次延迟伤害评估。终局候选由只读 `SearchFeatures` 投影后交给 `FinalPlanOrdering`，胜负、药水、偷窃、卖血和边界排序不再留在 `Solve`。固定机甲最终仍为第 5 回合、同动作序列，`4624/33432/17735` 展开/转移/选牌分支和各项剪枝计数精确不变；本轮 `11.51 s / 3.55 GB / 0 ms GC / 18.8 ms 最大帧`，零非预期重算。

### 剩余编排与单一事实源

- 无人值守入口按 `ProtocolHost → ScenarioBuilder → Executor → Assertions → Writer` 建立具体边界；协议字段、stage 名、Passed/Held/Failed 语义和既有战斗夹具不变。同一 headless 进程中的成功、故意失败、失败后恢复、Held 释放、严格球/药水差分和实际自动部署均通过。
- 控制器在结果交给 UI 前一次性生成只读 `SolverOverlaySnapshot`。Overlay、回合行和动作胶囊不再读取 `SolverResult`、`PlanAction`、`PlanCardChoice` 或 `ModelDb`；钢笔尖两动作首回合击杀验证了路线文本、遗物标注、部署状态和速度恢复。人工布局检查仍按原测试项保留。
- 两类 `MethodMirrorRegistry` 统一输出 `MethodMirrorRegistryDescriptor`，CoverageCatalog 不再反射 registry 私有字段推断支持状态。切换前后全量目录均为 3035 项，所有 verify 门禁和生成文件保持不变；钢笔尖严格增量搜索与真实部署通过。
- 双小啃兽长线原失败定位为怪物从活动 roster 移除时过早删除 AI/静态参数，而同一敌方行动尾部仍需读取这些根数据。AI 状态现在跟随已知怪物与分支生命周期保留，不增加默认值或 live 回读；`REFACTOR-FINAL-NIBBITS-549` 第 5 回合结束、跨两次洗牌、0 药、0 战损并逐回合精确复用，`UnexpectedReplans=0`。

### 第三方 ModHook subscriber 根边界

- `ModHelper` 的 run/combat subscriber 现在与原版 listener 分段捕获并验证顺序。只在开战前生效或来自非 gameplay Mod 的 subscriber 不进入 worker；其余未知 gameplay subscriber 直接报告不支持，不再保留 live 模型继续搜索。
- Loadout 的 `IMaxHandSizeModifier` 在主线程计算每名玩家的最大手牌，模拟加入手牌时消费该根值。BaseLib CardModifier 以拥有它的 live 卡牌为捕获键；每次预测卡牌克隆时复制 Modifier、重绑 Owner，并从当前分支卡牌重建 listener。没有 CardModifier 的战斗不进入反射枚举热路径。
- 完整 Mod 栈首次运行 `07f3093e18044030821d7f6a260ce080` 在机甲第 8 回合的 `KNIFE_TRAP` 嵌套自动出牌暴露单槽 Power 事务。事务现改为卡牌源栈，并按 `UnsettlingLamp → triggering card` 关联结算；自动出牌 `d71206dca40347acb4a474d4dbab4edc` 与升级版 `8164a323631f467bb4e6e2afdb359f08` 严格差分通过。
- 根隔离与双小啃兽增量整战 `9d431748477e49969df981103321d551` 通过：第 5 回合、两次洗牌、0 药、0 战损、零计划外重算。
- EventPipe 确认 No-GC 中途退出由约 `4.48 GB` 小对象分配触发 `InducedLowMemory`，LOH 只有约 `20.9 MB`，所以 LOH 比例试改保持撤回。求解隔离域为空 Ritsu capability 卡牌跳过属性贡献空遍历；Fork 继承有效 listener/Power 缓存，只在 Power/Orb 重映射时复制变化项。机甲 worker 分配由约 `4.98 GB` 降至 `3.57 GB`。
- 完整 Mod 栈机甲连续两次独立进程为 `12.24/12.19 s`、`0 ms GC`、最大帧 `8.6/16.5 ms`，均第 5 回合胜利。实际 BaseLib CardModifier 夹具验证根克隆、Amount 保留、Owner 重绑和卡牌写时复制；带夹具的最终整战仍为 `11.87 s / 3.57 GB / 0 ms GC / 13.5 ms`。
- Ritsu 快通道的反向夹具给预测卡牌临时挂接 CardType contributor，确认 capability 非空时保留 Ritsu 原属性贡献；快通道只覆盖确认无已挂接或默认 capability 来源的卡牌。
- Orb listener 改由当前分支 `SimOrbQueue` 重建；药水/复活最大生命、Doom 遗物与 Power、Bound Phylactery 遗物清单、Entropic Brew 槽位容量均不再回读 live Player/Creature。
- 怪物行动图在根阶段捕获攻击模板、随机权重、条件选择和 35 类行动参数；Living Shield/Bowlbug Rock 条件使用分支 roster/事务，Encounter 槽位使用根数组。根物化后 Creature、Player、Monster AI 或意图缺项直接失败。相关 48 项怪物行动差分、环境组装师预测召唤和 Defect 根快照通过。

## 0.13.43：原版 Badge 与单人缩放 listener 边界

- 两个原版 Badge listener 现在以根克隆进入预测；`CccComboModel` 的回合计数不再通过 live 模型身份进入 worker，`DebufferModel` 的进度统计仍不属于求解语义。
- `MultiplayerScalingModel` 克隆后清除其 live `RunState`/`CombatState` 引用；格挡倍率由单人专用 mirror 明确返回 1，遇到多人状态直接拒绝，符合项目只支持单人战斗的边界。
- 本轮修正“worker 不持有任何 live 对象”这一错误目标。Player/Creature/Monster/Encounter 可以作为稳定身份或只读模型元数据；完成标准是 worker 不读取它们的分支可变值，且所有可变状态进入根快照、分支影子或 StateStore。
- 本机已安装 Mod 静态清单确认 Loadout 会注册 run/combat 单例，BaseLib 会从 live 牌堆返回 CardModifier。它们不能靠通用浅拷贝处理：BaseLib listener 必须重映射到预测卡牌上的克隆 Modifier，Loadout 的全局/伴侣状态也要按具体 Hook 分类。该第三方适配仍是下一批，不计为本版完成。
- 最终 0.13.43 DLL 的根隔离与双小啃兽增量整战 `8e380d6cdb0c4f54abb312f3afdb8393` 通过：第 5 回合、两次洗牌、0 药、0 战损、`UnexpectedReplans=0`。

## 0.13.42：Monster/Modifier listener 状态边界

- 16 类原版 Modifier 已逐类核对：根快照现在复制 Modifier 并让 Hook 前缀引用克隆；`Hoarder` 的可变跳过集合只服务跑局牌组变更，`Murderous` 的战斗语义则在预测召唤敌人时施加 3 力量。
- Monster listener 中会读取战斗内可变模型字段的 `Aeonglass.AfterCardGeneratedForCombat` 改为读取分支怪物计数；Queen 死亡监听只依赖 Creature 稳定身份与分支阵容，不复制整套怪物行动图。召唤入场遗物也改用根 Relic 清单。
- 首次永世沙漏两步差分 `2f2928b9533f445e9cb8bcfa340ecf02` 在第二步失败：直接构造的测试模拟器没有物化根，预测力量为 4、实机为 7。模拟器构造现统一完成主线程根物化，消除“半个根”API；最终 0.13.42 DLL 的永世沙漏 `7360f01e3e3f427a9373df736192c646`、Murderous 根克隆与召唤 `a80224403eea47e48375581ed34fe376`、双小啃兽增量整战 `bbebff8bc2314752aa4d3502bf7f8bcf` 均通过。
- 本版关闭已确认的原版 Monster/Modifier listener 可变读取；mod subscriber 与 Player/Creature/Monster 稳定 identity 仍待字段级审计，因此不宣称 worker 已经不持有任何 live 对象。

## 0.13.41：运行级根快照与 Hook 前缀

- 主线程根新增当前地图坐标、起始玩家回合和运行级 Hook 前缀；预测 RNG、幕/房间/地图/多人约束、抽牌回合修正和卡牌生成不再从 worker 回读 `IRunState`。
- 运行级监听器保持原版“run 前缀 + combat 后缀”顺序。根捕获验证后缀身份与顺序，复制牌组 Card/Enchantment；worker 将冻结前缀与当前分支 combat listener 组合。
- 根快照 `a8b69594be7d4066a21e46a4f4849ec4`、攻击药生成差分 `a3b2629aef3b4459b417a8af38c76939` 和双小啃兽增量整战 `a12d3a1d4d8b466a8b1652eee23a85b8` 通过；后者第 5 回合、两次洗牌、0 药、0 战损、零计划外重算。CoverageCatalog 全门禁与结构检查通过。
- 本版只关闭已确认的 `IRunState` worker 读取；Monster/Modifier/mod subscriber identity 仍按字段级证据继续审计。

## 0.13.40：搜索根模型清单与 listener 克隆

- Beam 不再读取 live 最大生命、Relic/Potion 列表；模拟根快照玩家回合、金币、完整卡牌注册、Osty、初始 Power、Run RNG、幕/房间/卡池约束和已知怪物私有字段。`SimulatedCombatState(CombatState)` 自身拒绝 worker 调用。
- Relic、Potion、Card/Affliction/Enchantment listener 使用主线程克隆。卡牌 Original/Preview 的 `ContainsCard` 由 floating identity 与分支注册表回答，进入、移出和 Fork 同步维护。
- Relic 的 `AfterCloned` 会重置战斗计数；11 项差分首次复跑准确抓到钢笔尖 9 层被重置。根捕获现把 25 类受跟踪 Relic 私有状态从原件迁入克隆对应的 `PredictionStateStore`，随后释放原 Relic source 映射。
- 最终根隔离 `9f1a11e8be0749268419d73eb500c41d`、遗物 Hook 11/11 `f642afb8250c44ab8b2f8399714b5b06`、钢笔尖 `d7f95d11b2934c7399658d0932a6c992`、Knowledge Demon `0526ed3711604287a692d035c20f11e6`、Smart 救命药 `ee3907732ea147088387209c1fa178ae` 和双小啃兽增量 `bfb8b3fa03ca4c39a2b8902857f10da1` 通过。
- RunState 接口、Player/Creature/Monster identity 和怪物行动图仍共享；它们进入下一段字段级审计，不把稳定 identity 机械深拷贝。

## 0.13.39：主线程搜索根第一阶段

- 正式搜索和首回合搜索在主线程创建 `CombatRootSnapshot`，捕获前后比较完整 live/continuation 文本，并将 live 状态与根模拟投影逐项比较。Beam、药水反事实与最终标注回放只从同一根 Fork，不再在 worker 重建 live 根。
- 根捕获会物化生物生命/格挡、能量/星能、五个牌堆的卡牌副本、球、Power、Hook listener 列表、怪物 AI/私有状态、遗物计数、药水槽、RNG 和当前战斗历史。本轮同时修正 Fork 构造先读 live 再覆盖的路径。
- 定向根隔离 `091c600ef65b47d79dac265d1bd7054d`、钢笔尖增量 `589a0ec232044d6996c268a7a64c881a`、双小啃兽普通 `f5ccd0953e9c4fabb277aaa4cc2f5334` 与增量 `695323f49a9f4b2d94312db1faa6fb3e` 通过。
- 这不是整棵游戏对象图的深拷贝：RunState 与模型 identity 仍共享，Relic/Monster 稳定元数据、`ContainsCard` 和少量模型私有字段 fallback 继续列在第四轮剩余项中。

## 0.13.38：Fork 静止边界

- 模拟器在分配任何克隆前统一预检 trace、战斗事务、Hook 私有状态、延迟历史和遗物效果记录器。`PredictionStateStore` 通过 `IPredictionForkBoundary` 自动检查已物化的瞬时 Hook 状态；各状态自身的 Fork 仍执行同一断言。
- 补齐动作选择游标、嵌套出牌死亡集合、钢笔尖待翻倍攻击和蜷身受击来源等瞬时边界。当时把臂铠触发卡也归为瞬时引用并在 `AfterCardPlayed` 后清除；`0.15.0` 已依据原版源码与问题包纠正为持续到战斗结束的稳定状态。
- 定向 Fork runId `c2eaca32dedc4280ac94c8b7dee6cc6a` 覆盖 trace、选择、出牌、遗物记录、钢笔尖、臂铠、蜷身、延迟历史和稳定分支隔离。配对出牌中途死亡 `2fc6f92950f14078b7cef48d5d5d767c`、钢笔尖增量路线 `c835e618bf9d49c9860efac6392b22c4`、臂铠 11 项 `e38faf739afc49ffb787de83ecb26a70`、Power 卡牌 Hook 12 项 `9ef90837a332410fa0811801c13d9ab1` 均通过。
- 双小啃兽增量整战 runId `f72e7839614e46b098c3ea7ccad20527` 第 5 回合、两次洗牌、0 药、0 战损、零非预期重算。

## 0.13.37：控制器会话生命周期

- `SolverController` 保留静态入口，但战斗、单次搜索和单次部署的可变字段分别进入 `SolverCombatSession`、`SolverSearchSession`、`SolverDeploymentSession`。战斗重置直接替换会话；搜索进度、帧/GC 指标和完成后部署不再靠逐字段清零。
- worker 进度与完成 callback 绑定创建时的搜索会话实例，取消后即使旧任务稍后返回也不能写入新搜索。部署完成同样只清理创建它的部署会话；每次部署固定使用启动时捕获的详细诊断设置。
- 会话取消组合测试发现战斗结束异步 GC 回收与下一搜索存在竞态，新搜索可能收到 `Cannot start search while combat-end GC reclaim is active`。GC 所有者现用完成信号同步两者：worker 等待当前回收真正结束再建立低延迟搜索区，不跳过回收或使用替代策略。
- 最终策略快照/会话取消/回收后重搜/完整部署 runId `128c54b5da8b4c61973524ed222d253a` 通过。双小啃兽普通 `21b29399fc294d28bd721378248b167c` 与增量 `0f85f6bd05ce484b83691eec0787a74f` 均第 5 回合、两次洗牌、0 药、0 战损、零非预期重算。

## 0.13.36：预测失败边界与搜索策略快照

- 推断 OnPlay 和搜索动作回放不再吞掉异常继续产出候选。无法分支内计算的 `IComputedDynamicVar` 明确终止预测；搜索转移失败统一携带动作、目标、父状态和动作序号，后台搜索由既有控制器故障路径统一上报。
- 搜索启动时在主线程一次性捕获性能档、药水策略、诊断开关、测试覆盖和偷窃策略。`Search/` 不再从工作线程读取全局设置、测试运行器、控制器或入口日志；帧压力通过独立信号对象传入，进程级 GC 所有权移到 `Runtime/`。
- 新增失败传播和策略快照定向回归。双小啃兽普通与增量整战均在第 5 回合结束、两次洗牌、0 药、0 战损并成功复用；药水引擎 17/17、推断选牌 12/12、推断卡 43/43、CalculatedVar 25/25 通过。Smart 反事实保持 0 药 0 战损，RequireAtLeastOne 在两瓶药中只用一瓶，两项均零重算。
- 回归同时修正了测试夹具本身：自动生成的 X 费卡固定使用 1 能量，避免以 99 能量杀死低血宿主后比较已清场状态；`HELIX_DRILL` 的前置打击场景给敌人高额格挡，在不结束战斗的前提下保留 99 能量的数值验证。提交前基线 `4117eb0` 已证明原夹具会在同一位置失败。

## 0.13.35：持久化禁用求解器

- 设置页新增“禁用求解器”开关并持久化。开启后取消当前后台搜索和自动部署，关闭全自动，清空旧路线与续用基线；手操期间不再产生自动搜索、路线复用校验或计划外重算记录。
- 禁用状态覆盖普通回合开始、延迟搜索回调、手动搜索/执行、全自动和首回合遗物/能力选牌搜索。首回合搜索在开关切换时被取消后交回原版选择流程；新战斗仍显示精简的禁用状态和设置入口，避免持久化后无法重新启用。
- 关闭开关后立即基于当前真实战斗状态发起一次新搜索，不沿用禁用前路线。按用户要求本版不运行 headless、Steam 可见或 CoverageCatalog 回归，只执行 Release 编译与产物校验，功能验证登记为 Pending。

## 0.13.34：千足虫同回合死亡后的重接推进

- `0.13.27` 亡灵契约师千足虫问题包在当前源码仍稳定出现三次计划外重算。首个差异发生在肢节已经执行正常行动、随后于同一敌方回合死亡时：原版在下一玩家阶段已从可直接过渡的 `DEAD_MOVE` 进入 `REATTACH_MOVE`，模拟额外要求死亡动作必须实际执行一次，因此停在 `DEAD_MOVE`。
- 怪物行动推进现在统一服从原状态机的 `MustPerformOnceBeforeTransitioning`。`DEAD_MOVE` 可在回合切换时直接进入重接；`REATTACH_MOVE`、幻象复活和其他明确要求至少执行一次的动作仍由通用状态机条件保留，不增加千足虫路线特化或新分支状态。
- 严格差分夹具补上“正常行动后死亡，再滚下一行动”的时序，并修正死亡但仍留在阵容中的复活怪物 Power 捕获；`ReattachPower` 继续按原版保留并进入完整状态比较。
- 语义纠正后，中档与高档都在第 3 回合耗尽候选，只留下死亡路线，证明不是时间或节点预算不足。复活保留标签此前只在出牌/用药动作直接制造复活窗口时更新；本局的 Doom 在结束回合触发肢节死亡，节点未得到标签。结束回合状态转换现在同样登记通用 `RevivalWindow`，已有的按用药数量分组复活通道因此能保住长线。
- 最终严格差分 runId `b481833954f741a68faf86c45584755f` 通过。用户问题包整战 runId `40de641e3af44100a83c63734ffd3544` 在 Medium、Smart、Instant/0 秒下第 `6` 回合胜利，使用药水形状的石头和速度药水，预计/实测均掉 `43`，第 `2-6` 回合全部精确复用，`UnexpectedReplans=0`。上一份千足虫 runId `e4039058025849c893710f917770550e` 仍为第 `4` 回合、零重算；双小啃兽普通/增量 runId `07285eb2edf340a8bb3b6347e7722466`、`2b1b50c7a668463a905af6b04e49c654` 均为第 `5` 回合、`0` 药、`0` 战损、零重算。

## 0.13.33：路线预计用药显示

- “本局扣血”左侧的用药文字改为 `预计用x瓶药`，数量为本战已经使用的药水加当前路线后续计划使用的药水。它从方案形成时就显示预计整场用量，跨回合执行掉前面的药水后仍保持同一总数；状态详情继续分别展示已喝数量和路线剩余数量。
- 本次按要求只执行 Release 编译检查，不运行 headless 或 Steam 可见 UI 回归；可见文字、间距与跨回合显示已登记到人工 UI 待测项。

## 0.13.32：千足虫死亡动作到重接动作的推进

- 千足虫问题包的第 3、4 回合首个差异均为复活肢节行动：模拟停在 `DEAD_MOVE`，实机已经进入 `REATTACH_MOVE`。原版 `ReattachPower.AfterDeath` 会先强制死亡动作，该动作真正执行后再进入必做一次的重接后继；模拟此前只要肢节仍为 0 HP 且处于 `Reviving` 就无条件禁止 AI 推进，因此把已经执行的死亡动作也永久冻结。
- 怪物 AI 回合推进现在区分“刚被其他行动杀死”和“本轮已经执行当前强制复活动作”：前者继续保留强制动作，后者按原状态机进入后继。现有死亡阶段、当前行动、行动历史、RNG、Fork、指纹和续用文本已经完整持有所需状态，没有增加千足虫路线或药水特化。
- 修复前同一反馈包重建 runId `0d98fc4597024091a217f52adfa04b37` 完成战斗但出现 `1` 次计划外重算；最终仓库夹具 runId `5e716ee686214e46bc1157459129ffa9` 在 Smart、Instant/0 秒下第 `4` 回合结束，第 `2-4` 回合全部精确复用，`UnexpectedReplans=0`。该重建首轮计划使用 `3` 瓶药，实机也只按首轮计划使用这 `3` 瓶，没有因回合间重搜改变策略。

## 0.13.31：Smart 药水独立反事实

- 异螨问题包中，同一初始状态的 Smart 搜索只保留了掉 `31 HP` 的无药代表，于是把三瓶普通药掉 `1 HP` 错算成省 `30 HP`，超过 `27 HP` 门槛并选择三药；切换 Disabled 后独立搜索实际找到掉 `11 HP` 的无药路线。两条路线均全程精确复用，问题属于统一 Beam 内无药对照被药水候选挤压，不是模拟偏差。
- Smart 主搜索只要最终准备使用药水，现在固定追加一次同预算的 Disabled 反事实搜索，并只用独立结果计算真实省血。报告场景重新得到三药仅省 `10 HP`、低于 `27 HP`，因此选择 0 药掉 11；无药不胜时仍保留救命药。额外搜索只发生在主搜索选中药水时，时间、分配和 GC 合并计入总指标。
- 最终 `0.13.31` 固化存档完整自动回归 runId `925ec6ace4174dd2b6799f6319a336f0`：High、Smart、Instant/0 秒下首轮从三药改为 0 药，预计与实际均掉 `11`，第 `8` 回合结束，第 2-8 回合精确复用，`UnexpectedReplans=0`。救命格挡药 runId `dfa6548b170542b3a5156ebcc128e3e0` 和既有低损无药 runId `a1c32d908b7e43238a72c8da448446fb` 均保持原策略。

## 0.13.30：双尾鼠随机行动权重

- `0.13.27` 双尾鼠问题包首轮预计累计掉血 `7`，第 2 回合却因第一只存活双尾鼠的后续行动预测为 `SCREECH_MOVE`、实机为 `SCRATCH_MOVE` 而重搜，后续路线预计累计掉血升到 `19`。当前 `0.13.29` 源码基线同样稳定出现两次计划外重算。
- 原版 `TwoTailedRat.GenerateMoveStateMachine` 中 `AddBranch(SCREECH_MOVE, 3, CannotRepeat, weight)` 的 `3` 是冷却回合数，尖啸与抓挠、疾病啃咬的基础权重相同。求解器此前把它误读成尖啸三倍权重；现由 `MonsterRandomBranchResolver` 继续统一处理三回合冷却，双尾鼠分支权重只镜像原版动态权重。
- 修复前一步差分 runId `11544c6afa594da8848b04d3f827574c` 稳定得到预测尖啸、实机抓挠；最终 `0.13.30` 严格差分 runId `fd954cb637824f8d87a02d71f6c45e68` 通过。最终用户存档完整自动回归 runId `edf81e2f78f24b4fac9fbb44861a5faa` 在 Medium、Smart、Instant/0 秒下第 `6` 回合结束，首轮预计与实测均掉 `9`，第 2-6 回合全部精确复用，`UnexpectedReplans=0`。不设搜索边界的完整增量验证在 300 秒超时；限定 500 ms 短搜的增量与第 2 回合续用回归 runId `a394a24ca7d3496c91955d8d92431b24` 通过且零重算。

## 0.13.29：瀑布巨兽长线终止性与蒸汽喷发阶段

- 两个 `0.13.27` 瀑布巨兽问题包暴露了路线终止性错误：首轮深搜在 `6000` 节点处只规划到第 11 回合，敌人仍有 `106 HP`，但末尾 `EndTurn` 之后的空状态仍被缓存成第 12 回合续用点。全自动因此以 `action_count=0` 空过第 12 回合、实吃 `13` 点，再到第 13 回合才因 `continuation_missing` 重搜。
- 续用缓存现在只保留确实存在下一回合动作的边界；续用结果必须包含当前回合计划。部署器只在路线明确含有当前回合 `EndTurn` 时过回合，预算在回合中耗尽时执行已有动作后以 `plan_exhausted` 重新搜索，不再替搜索器擅自结束回合。
- 蒸汽喷发的死亡阶段此前只保存爆炸伤害，没有完整执行原版 `SetMaxAndCurrentHp(999999999)`、`SetMoveImmediate(ABOUT_TO_BLOW_MOVE)`、普通 Power 死亡清理和“不移出战斗”。现在首次致死会保留巨兽在敌方阵容中，同步无限最大/当前生命、AI 当前行动与 Power 生命周期，之后严格经历即将爆炸和爆炸自灭。
- 用户第二包修复验证 runId `6f63b7f0581b4e16acd975b1a4280400`：固定种子 `CFA9VQGH9YQJ`、250 HP、中档、Smart、Instant/0 秒，第 13 回合结束、计划外重算 `0`。第一包 runId `4191777f0eb4419a96b605b2a2c17714`：固定种子 `LHBRXZBSXYTB`，第 16 回合结束、计划外重算 `0`。定向两回合阶段回归 runId `cbd0f82f1d3740aa8dc3df4dc7641365` 同样通过；最终 `0.13.29` 增量等价 runId `9da2a97a941c492caf53262e0321e925`、第二包最终版本整战 runId `da44fc55138d4bedaec12623953a52af` 均通过。

## 0.13.28：贪食即时眩晕与怪物行动状态机

- 最新实机尸蛞蝓日志中，第 2 回合能够复用方案，但第 3 回合出现生命预测 `26/29` 偏差，第 4 回合又出现预计 `STUNNED`、实际 `GOOP_MOVE` 的偏差。根因不是 RNG，而是求解器把 `RavenousPower` 的眩晕错误延后，同时在怪物回合开始前一次性缓存了全部怪物意图。
- 按原版 `RavenousPower.AfterDeath` 语义，同伴死亡后立即用 `STUNNED` 替换持有者当前意图，并把被替换的行动保存为眩晕后的后继行动；不再区分玩家回合或敌方回合，也不再维护延迟眩晕状态。
- 怪物回合现在固定本轮参与行动的怪物名单，但每只怪物在真正行动前重新读取当前意图。行动结束后以实际执行的 `MoveState` 身份决定是否推进状态机，避免前一个怪物的死亡效果改变后一个怪物意图后仍执行旧快照。
- 回归证据：玩家侧击杀严格夹具 `d6062ab6930041cc95d44a7591f14abe`、敌方 Doom/Ravenous `bd685e1cff8e4871a7486a2d42f267c`、Bowlbug 眩晕循环 `6b5377af10c6454e8d12a8c90f44af71`、尸蛞蝓完整战斗 `bf0470bd8c7f44f99c93903e11e38480`（第 7 回合结束，计划外重算 `0`）、增量/完整前缀等价 `42f86b7f43c141f5ac26addebc7fc00e`。

## 0.13.27：睡眠甲虫配对出牌与湮灭数值快照

- 用户睡眠甲虫问题包运行的是 `0.13.25`，两次搜索都在结束回合候选分叉时抛出 `Cannot fork while paired card-play state is pending`。当前配对事务已绑定到整次 `CardPlay` 完成边界：即使卡牌结算移除监听 Power，After 阶段结束后也会核销本次瞬时状态，Fork 的非静止状态检查继续保持 fail-fast。
- 用问题包开战前存档验证时又发现下一处首回合偏差：路线连续打出两次湮灭，第二次叠加会把 `OblivionPower` 从 `3` 增至 `6`；旧补偿在出牌后读取可变 Power，错误地把出牌前应记录的 `3` 当成 `6`，导致灾厄预测比实机多 `3`。湮灭的出牌后效果现在直接消费 BeforeCardPlayed 捕获的数值，由 Hook 镜像成为唯一权威实现，旧的卡牌生命周期重复补偿已删除。
- 最小严格差分修复前为预测灾厄 `9`、实机 `6`；0.13.27 最终严格差分 runId `1e7e56073fca4324aba5bf69d7e9be9a` 通过。原问题存档完整自动回归 runId `7906b6deb0fc487da9b7ddb969dba4a5` 在 Medium、Smart、Instant/0 秒下第 `7` 回合结束、`UnexpectedReplans=0`；开启增量等价的 runId `a2ecdb4f3dc545b298892babcb1f88b2` 同样第 `7` 回合结束且零重算。
- 后续收到的 0.13.25 啃咬机问题包属于同一配对事务故障：generation `95` 在首轮搜索约 `2.8` 秒后于 `CardPlayPairPredictionState.Fork` 失败，包内没有进程级崩溃栈。0.13.27 恢复该包开战前存档后，普通整战 runId `fec516087d0645b28f1a0770201f0d7e` 与增量整战 runId `b9bc305045e945149c8bd18cf029f896` 均在第 `11` 回合结束、`UnexpectedReplans=0`，确认现有根因修复覆盖该场景。

## 0.13.26：卡牌身份克隆、振翅眩晕与配对事务收口

- 偷窃草蜢的首次偏移来自卡牌身份而不是 RNG 克隆。模拟卡牌首次写入时沿用了原版“创建新卡”的克隆收尾，清空了 `DeckVersion`；已经打出的摧残因此被偷牌过滤器排除，三张候选缩成两张，同一 RNG 原始值从第 2 张变成第 1 张。卡牌状态克隆现在保留 `DeckVersion` 与移除标志，生成新卡/复制牌继续使用原版清空语义；两项身份同时进入卡牌指纹、跨回合状态和严格差分。
- 振翅此前只递减层数，没有在归零时按原版把偷窃草蜢当前行动替换成 `STUNNED`。现在归零会读取分支行动历史、消费同一 Monster AI RNG 并建立正确后继；连续攻击不会在 Power 已失效后重复安排眩晕。
- 外骨骼问题包的 `Cannot fork while paired card-play state is pending` 来自配对状态绑定在监听器生命周期：湮灭结算中移除监听器后，出牌后 Hook 不再枚举它，瞬时配对残留。配对完成现在绑定整次 `CardPlay`，无论监听器是否在结算中移除，动作完成边界都能清空；Fork 的 fail-fast 保持不变。
- 仪式兽的削弱增幅此前从真实怪物读取 `DebilitatePower`，看不到分支中新施加的层数。虚弱与易伤乘区现在读取分支 Power，并保留纸鹤、纸蛙原版规则。原版 `0.111.0` 全量反编译源码固定在本地 `.local/decompiled/sts2-v0.111.0` 作为语义参考，后续不再为单个类型反复局部反编译。
- `0.13.26` 最终严格差分：削弱增幅 `424b21ac5fed47459ccc51693ad306ae`、已打出牌参与偷牌 `95c5b6570a444004b4422a4cde5e2902`、振翅归零眩晕及后继 `fbc61230c114421e9b7f8753588c2bde`。用户开战存档整战：偷窃草蜢 `25c6621068c6498b9bf7fcdd86b462e6` 第 5 回合结束，外骨骼 `3dd661ab8f3b4657a228fb84a914fbad` 开启增量等价后第 5 回合结束，仪式兽 `7132508e4aa24d4f9865198529938e76` 第 9 回合结束；三场均为中档、Smart、Instant/0 秒且 `UnexpectedReplans=0`。

## 0.13.25：女王长线选牌、威胁集火与能力铺垫

- 女王旧路线达不到手操的原因不是模拟语义或节点预算：中档和高档都稳定把首回合 `编译冲击 → 内存清理删除防御+` 候选生成出来，但 Beam 的能力组合与集火槽位先占满必留空间，破坏性选牌代表排在后面被截断。现在幕末 Boss 深搜把一半必留位明确分给抽牌、捞牌、消耗、变牌和修改牌等路线选择，普通深搜使用 40%，短搜使用更小配额；代表选择优先保留完整能力铺垫和关键攻击。
- Beam 新增通用的高威胁目标压力、未打出的持续能力、下回合资源和能力组合签名。目标压力同时考虑已造成伤害、目标剩余生命和当前行动伤害；它由显式集火通道保留。全局 Pareto 只增加潜在能力这一项，避免集火与资源维度让普通状态互不支配。幕末 Boss 和多敌战使用激进全局权重，单敌普通/精英使用 4 层持续能力权重并依赖专用通道。所有新值只影响候选保留，不改变最终真实战损排序，也不进入模拟状态或续用指纹。
- 最终女王中档完整自动回归 runId `20a18e92f9734d54a123f988981c6684`：首轮使用 1 瓶敏捷药、删除打击并保留两张编译冲击，第 11 回合获胜，预计/实测均为 `0` 战损，`UnexpectedReplans=0`；关闭药水后无药路线死亡，因此 Smart 记录的 26 HP 用药收益成立。搜索 `17.07s / 4.29GB`，No-GC 全程保持，GC `0`，最大帧 `18.2ms`。
- No-GC 总预算保持用户设置值不变，SOH/LOH 分区由固定 `2/3 + 1/3` 改为 `5/6 + 1/6`。机甲实机证明旧分区会在约 4.4GB 时因 SOH 先耗尽并产生 700-800ms 暂停；新分区在默认 6GB 下保持到战斗重置。发布 ZIP 干净安装后的 Steam 可见 runId `fbdf140c23e340fdb5f5abc17990939e`：首搜 `12.23s / 3.82GB`，第 6 回合、0 药、预计战损 30，GC `0`，最大帧 `9.4ms`，无 `>50ms` 帧。
- 回归：双小啃兽 `0a3936c2b0ad4c229e7112eef07bbf14` 保持第 5 回合、0 药、0 战损、零重算，首搜约 `2.01s / 405MB`；增量/完整前缀等价 `3d05fa2444054fb1bf1df1d9112774e5` 通过。智能低损 `8655716bc37b41f7bff67e45fb63c1f2` 改善为 0 药 0 战损；致死救药 `6c83e4446cd54c73a88745b2934fb5e4`、至少一瓶 `205c54860d77415aa424e4be91078b79`、卖血约束 `d464c47dbb9544c68d1a9d3f0eb33aee` 均通过。完整自动战斗统一使用 `Instant / 0 秒`。

## 0.13.24：Ravenous 死亡时序与长线药水铺垫

- 噬尸蛞蝓的 `RavenousPower` 在本版本改由统一死亡生命周期结算，但当时将玩家回合击杀误实现为保留当前意图。`0.13.28` 已按原版纠正为立即替换当前行动，并以被替换行动作为眩晕后继。
- 修复 Buffer 只记录风险却不施加 Power、生成牌加入战斗后监听器缓存不失效、Void 抽牌固有效果漏触发、Focus 下球数值仍用原始数值、Echo Form 错把历史所有回合出牌数当本回合计数，以及内存清理从出牌后牌堆生成非法候选的问题。Buffer 严格生命周期 runId `608a90ce60344a5b8b1d1ed495eb46b0` 通过。
- 统一 Beam 不恢复独立药水搜索，但最终截断强制保留约 `2/3` 尚未用药路线与 `1/3` 已用药路线；消耗/变牌/修改等破坏性选牌按选择对象保留攻防铺垫代表。持续能力按 Curious、Echo Form、Buffer、Focus、Thunder 和递增能力形成组合签名；下回合能量、抽牌、星能和留牌进入资源维度及 Beam 排序。
- 从后续沙漏问题包日志重建女王开局：固定 8 张首手、20 张抽牌、疯狂科学 `Power + Curious` 私有状态、附魔、两敌生命/行动和敏捷药。最终 runId `f94871fa225c4d51b86fa8927d6b265e` 首轮不再判死，智能模式使用 1 瓶敏捷药，第 13 回合自动获胜且零计划外重算。该重建预计战损仍为 `25`，没有达到用户手操 `4` 战损，因此只证明“可生还路线不再丢失”，不宣称达到手操最优。
- 回归：智能无药低损 runId `05cc3d1e0a4e4b3f9675ed62e29d89c0` 保持 0 药、预计掉 2、零重算；致死救药 `e7d84de22210444ca3e3e3f052804132` 保持格挡药生还；双小啃兽 `acc440b2ec7e42f9bc151ca2ade1fee1` 保持第 5 回合、0 药、0 战损、零重算。完整自动战斗均使用 `Instant / 0 秒`。

## 0.13.23：佩尔之眼额外回合、幕末路线与计划外重算反馈

- 熟睡甲虫的偏移并非普通人工制品问题。佩尔之眼额外回合仍属于第 1 轮，原版会再次触发敌方 `PlatingPower` 的初始镀层；模拟现同步补发。佩尔之眼的弃牌判定只在额外回合准备阶段执行一次，不再在历史提交后重复耗尽手牌。炮台操作员的 `RampartPower` 同步遵守原版“玩家额外回合不加格挡”的条件。精确整战回归：熟睡甲虫 runId `7f747823b2b54e6fa2e13b2fe78f2405`、虫术师 `67e7384eb8e54b3ca8b9f87cbbcee0ae`、炮台操作员 `fde9f00bb6f8415cb051d43034bac0f9`，三场均 `UnexpectedReplans=0`。
- 路线侧补齐壁垒的真实翻倍格挡，给“拒绝佩尔之眼额外回合”、肉身妙法反伤、活跃灵魂牌和非线性 Doom 铺垫独立保留位。幕末 Boss 允许搜索所有非致死卖血路线，并在中档深搜宽度不足时仅对该场提升到 `45`，最终比较仍优先生存和真实最低战损。凯撒蟹 runId `520796f6c561477480b8190e0e27bef7` 第 9 回合生还；永世沙漏中档 runId `01d32a37623e454695850b4f433a487c` 第 8 回合、剩 `16 HP`；两场均零非预期重算。
- 标题右侧新增常驻的计划外重算告警：`出现计划外重算，可能是模拟或算法问题。请在设置中导出完整问题包并反馈`。只有求解器完整执行后的状态偏移和部署漂移会触发，普通手操偏离不触发；但若前面手操、后面由求解器完整接管并再次偏移，仍会告警。
- 问题包的 session、每个检查点和 export-context 新增 `controlMode` 与 `lastSolverDeployedTurn`。`solver_only` 表示整场纯求解器，`manual_plus_solver` 表示本场曾由玩家操作后再交给求解器。混合接管告警/当场导出 runId `ae8da00ed14b4b6d8842a9e2292ff889` 通过，纯求解器活动战斗与战后双导出 runId `6e1a6e1095614b29a2253de911e10a1f` 通过且零重算。

## 0.13.22：偷窃遭遇策略、幻象复活顺序与啃咬机回归

- 偷窃草蜢和偷钱地精战斗的底栏新增上下文按钮“保牌/保钱”和“放走”，其他遭遇不显示。默认保资源：先比较未追回牌/金币，再比较生命，允许为追回资源卖血，并在全局禁用药水时仍可为追回资源评估普通药水；龙涎香的 40% 门槛不豁免。放走模式不为被盗资源付生命或药水成本。分支内显式记录被盗牌/金币，击杀追回、逃跑保留，不再从易失 Power 反推结果。
- 偷钱地精保资源 runId `21b28cc55f2d47e3bac79fbe7937e3a2` 在全局禁药时使用火焰药水追回全部资源；放走 runId `7fcf8e932fef48199d18447a393f9f10` 不用药并允许带走 40 金币；偷窃草蜢 runId `f1e60d4674b849df8ebe48fdb58d62e4` 追回被盗牌。三场均零非预期重算，按钮仅在对应遭遇显示。
- 幻象若在敌方回合末被灾厄杀死，旧模拟会先设置 `REVIVE_MOVE`，随后又在同一轮的统一意图推进中提前改成后继攻击，造成下一回合 `0 HP` 幻象错误攻击、战损少算和连续重搜。现在刚进入 `Reviving` 且仍为死亡状态的怪物保留复活意图；实际执行复活并恢复生命后才推进后继。佩尔之眼是否触发改为读取分支内回合历史，额外回合在动作胶囊中标注为遗物效果。
- 原版幻象复活并不清除 Buff：`IllusionPower` 保留 Buff 和临时 Power，只移除普通 Debuff。模拟按原版这一语义清理，且只对死亡幻象自身生效。胧光怪问题包整战 runId `95e0cfc833c54965830b44ad2c5dc681` 第 7 回合结束，佩尔之眼标注存在，`UnexpectedReplans=0`。
- `ShrinkPower` 在所属一方回合末衰减一层；甲虫汁继续经过通用 Artifact 入口。差分 runId `16d2b2829bec409eae114eff8adf3d41` 验证两层人工制品变一层且不生成缩小。啃咬机问题包的战前跑局快照是在初手已抽出后保存，不能单独重建原首手，因此从导出日志固化 5 张初手和 17 张有序抽牌堆；精确回归 runId `790ea769327841229ccc058d4ebcdd76` 第 6 回合获胜，预计/实际均掉 21，后续回合全部复用，零非预期重算，也不再出现 `VEILPIERCER` 回放找不到手牌。

## 0.13.21：可重放取证检查点与实际用药显示

- 问题包取证格式升级到 schema 3。当前和最近一场战斗的每个检查点现在都写出四份同名材料：含当时设置、结果摘要与全部 RNG 的 metadata；含有序手牌/抽牌/弃牌/消耗/打出区、逐牌存档与动态字段、Power、遗物、Orb、怪物私有状态和完整行动历史的 replay-state；游戏原生 `NetFullCombatState` 二进制包；该检查点时刻的内存跑局存档。即使玩家在战后或下一场战斗中点击导出，也能从 `forensics/recent` 取得问题战斗，而不再只能依赖已经变化的磁盘存档。
- 导出回归 runId `b6266146d6094db39846e5cbe08c8216` 在活动战斗与返回主菜单后各创建一份 ZIP。测试实际打开 current/recent 检查点，逐项解析 metadata、replay-state、即时跑局存档，并确认原生战斗状态包非空；Run RNG 流、玩家 RNG/odds、五个牌堆、阵容、历史和当时设置均存在。
- 路线标题栏在“本局扣血”左侧新增实际用药数，只在本战已经喝过药时显示 `喝x瓶药`。它读取真实 `PotionUsedEntry` 历史，不把路线未来计划提前计入。runId `a5a3148d2c71484f808bdc4cc7ccd597` 使用 Instant/0 秒完整执行两回合救命药路线；第 2 回合复用结果记录实际已喝 `1`、未来计划 `0`，零非预期重算。
- 已按用户要求删除不再需要的知识恶魔问题包 `CombatSolver-KNOWLEDGE_DEMON_BOSS-20260824-194812-127.zip`；删除操作进入系统回收站。

## 0.13.20：辐射星能历史与整场强制用药

- 女王问题保存在后续永世沙漏导出包的 `forensics/recent`。首轮路线预计第 10 回合结束、累计掉血 54，实机却在第 2 回合按同一序列打完星尘后结束。根因不是女王死亡逻辑：`Venerate`、`RoyalGamble`、Genesis 等共用入口直接增加星能，却没有写入“本回合获得星能”历史，导致 `Radiate` 严重少算连击数。共用入口现在统一经过模拟器的星能获得与历史记录。
- 四牌严格差分 runId `e92f1af969e14d138f374e5ee44ad455` 覆盖“崇拜 → 胜券在王 → 辉光 → 辐射”，完整资源、牌堆、伤害与状态一致。问题包战前存档不能精确重建原首手，因此不拿混合还原结果冒充通过；改从包内完整第 2 回合检查点固定两敌、行动历史、四牌堆、附魔和 Power。runId `b761d379fcd248ae8b8dd68618d09a2d` 在 648.6 ms 内找到本回合斩杀，预计战损 0，实机第 2 回合结束、剩 51 HP、零重算。
- 同族殖群的“至少一瓶”路线实际规划了能力药、熵酿、瓶装潜力和速度药共 4 瓶，只是玩家在第二瓶附近观察到异常。旧策略先按生命排序，且统一 Beam 的无药代表被截断成死亡线，因此把 4 瓶总共只省 7 HP 误判为救命路线。现在多瓶候选会触发纯无药反事实；无药能赢时，以真实无药战损重跑最多 1 瓶的搜索，第一瓶满足强制要求，额外药仍按每瓶 9 HP 机会成本。无药确实死亡时仍允许多瓶救命。
- `RequireAtLeastOne` 改为整场约束。战斗历史已经记录过药水后，后续重算按 Smart 继续，不再要求“从当前状态再喝一瓶”，因此药水用尽不会抛 `PotionPolicyUnsatisfiedException`。速度药另修复临时敏捷被重复登记两次、回合末留下 `DEXTERITY_POWER:-5` 的错误。
- 固定原同族首手回归 runId `51231fed1247449fa1a2449ade6a85bd`：只用 1 瓶、预计累计掉血 17、完整战斗零重算。注入 1 HP 偏差强制在喝药后重算的 runId `72158b5e4c58447dbe8a84e418c77e69` 完成战斗且不再要求第二瓶。速度药严格生命周期 runId `508252a0bcc248f1a45c694231c9b265`、Smart 无药必死救药 runId `4645935ed8bd42e19ca0deb95bfb497e` 均通过。

## 0.13.19：首回合奥斯蒂状态与致死出牌分叉

- 亡灵契约师持有赌博筹码和绑定护命匣时，首回合预搜把奥斯蒂从 `1 HP` 错算成 `2 HP`，选择提交后记录 `INITIAL_SETUP_STATE_MISMATCH field=osty.hp`，导致预搜路线作废并从弃牌后的状态重搜。原版 `BoundPhylactery.AfterEnergyResetLate` 明确排除第 1 回合；模拟现按同一条件执行。
- 棘刺蟾蜍问题包中的 `Cannot fork while paired card-play state is pending` 来自预览分支在出牌中途被荆棘反伤致死：原版不再触发 `AfterCardPlayed`，模拟却留下了出牌前/后的瞬时配对记录。致死中断现在只撤销该次 `CardPlay` 的配对记录，仍保留其他状态的 fail-fast 分叉校验。
- 亡灵契约师赌博筹码回归 runId `9ad6213caf7841289551a8cf37c821c4`：开局选择实际提交后完整状态一致，预搜结果继续可用。独白 Power、99 荆棘和 1 HP 的定向分叉回归 runId `fc424ddf95ce4ad18105b2cea77a3c6d`：搜索正常完成、识别第 2 回合死亡并暂停，没有 `SEARCH_FAILURE` 或 `cannot fork`。
- 问题材料目录从临时名称改为 `bug-reports`；有明确修复与回归证据的材料移入 `archive`，缺少可重放战前状态的永世沙漏和无日志误报继续留在 `open`。

## 0.13.18：快速导出战斗取证包

- 旧导出器只读取点击时的 `CombatManager` 状态。离开战斗后 `combat-state.json` 只剩 `combatActive=false`，磁盘 `current_run.save` 也可能已经是战后状态，无法还原开战 RNG。
- 新增有界取证缓存，只保留当前与最近结束两场，每场最多 32 个检查点。战斗开始、搜索请求、缓存复用、搜索完成、导出点击和战斗结束分别记录完整 Run RNG、每位玩家 RNG/odds、精确 ContinuationState、可读状态、当时路线和重算审计。超过 32 项时保留开战点并滚动淘汰最旧中间项。
- 开战时始终调用原版 `RunManager.ToSave` 保存内存跑局快照；磁盘 `current_run.save/progress.save` 已写出且种子匹配时另行原样缓存。磁盘匹配最多尝试三次。日志记录本场开始/结束偏移，导出本场专属片段并将普通日志尾部上限从 8 MB 提高到 16 MB。
- ZIP 新增 `export-context.json`、`environment.json`、`forensics/manifest.json`，以及 `forensics/current` 和 `forensics/recent` 的 session、checkpoints、pre-combat、last-route、replan-audit、logs。环境文件包含 OS、.NET、进程架构和已加载程序集版本/路径。文件名增加毫秒，连续快速导出不再碰撞。
- headless runId `d749d1e1cb7544968c2ef0b6842bf306` 在同一场 1 回合战斗中先活动导出、再回主菜单导出；两份包均验证可打开，战后包只标记 `combatActive=false`，但 `forensics/recent` 仍含 4 个检查点、完整 RNG 与约 49 KB 内存跑局快照。5 回合两次洗牌回归 runId `954c2d4a2c17494f926224b8f489a59a` 第 2-5 回合全部精确复用，证明取证序列化不推进 RNG。

## 0.13.17：Smart 药水反事实审计

- 最新亡灵契约师淤泥旋螺日志中，Smart 首搜的无药代表只推进一回合、敌人剩 35 HP，却把三药胜利当成“无药不胜、药水救命”无条件放行。结果同时记录 `potion_count=3`、`potion_hp_saved=0`、`potion_hp_required=27`。同状态切换 Disabled 后立即找到第 5 回合无药胜利、战损 2，证明无药反事实在统一 Beam 中被截断。
- 不恢复常驻独立药水 Beam。Smart 只有在准备选择用药路线、但当前无药基线未胜且报告省血低于战略成本时，才追加一次同预算的纯无药审计。审计找到无药胜利后重新计算真实省血，不达门槛就选择无药；审计仍找不到无药胜利则保留用药生还线。两次搜索的时间、分配和 GC 统一计入总指标。
- 用户状态完整回归 runId `322b6901b1cd48428daf8f9b326bd953`：三药路线要求 27 HP，审计找到无药战损 2，记录 `selected=potion_free`；最终第 5 回合结束，第 2-5 回合精确复用、零重算。
- 致死反向回归 runId `178e099396d94aeebee708ad2b4dfcb8`：1 HP 玩家无药必死，格挡药使其第 2 回合击杀；虽然普通省血只有 1、低于 9，审计确认无药不胜后仍保留救命药，完整战斗零重算。

## 0.13.16：奥斯蒂复活与噬尸蛞蝓吞食眩晕

- 最新亡灵契约师噬尸蛞蝓实机日志首个差异为第 3 回合 `DIE_FOR_YOU_POWER`：预测 `2`、实机 `1`。原版 `OstyCmd.Summon` 只在首次创建奥斯蒂时施加“为你而死”；复活战斗中已经存在的奥斯蒂只重新加入宠物、重设生命并触发复活 Hook。模拟此前每次复活都再次施加 1 层，现已删除该重复施加。
- 同一日志第 5 回合的行动差异不是前项的同一字段。原版 `RavenousPower.AfterDeath` 会立即以 `CreatureCmd.Stun` 把幸存蛞蝓当前行动替换为 `STUNNED`，并把被替换行动保存为后继；模拟此前仅登记“跳过下一行动”，消费后又从原行动图重新滚动，导致 `GLOMP_MOVE/WHIP_SLAP_MOVE` 偏移。现在直接建立相同的 `STUNNED` 状态和后继。
- 定向差分 runId `f26d95bf947b489188343c369aed296f`：3 HP 奥斯蒂被 `WHIP_SLAP_MOVE` 打死后用护卫复活，“为你而死”保持 1；击杀一只蛞蝓后，幸存者获得 5 力量并立即进入 `STUNNED`，两项完整状态一致。
- 用户开局完整战斗 runId `2b090c725e804f1db2a1d420c5570e4e`：恢复日志中的 `GOOP_MOVE / WHIP_SLAP_MOVE` 首轮行动，搜索第 5 回合结束、预计/实际掉血 `7`，第 2-5 回合全部精确复用，`UnexpectedReplans=0`。

## 0.13.15：次要敌人终局与生成选择越界

- 最新储君雾菇实机日志中，主怪死亡后仍有 `6 HP` 的幻象次要敌人。原版战斗已经满足结束条件，模拟器也在结束回合提交了 `IsInProgress=false`，但搜索快照额外要求全部次要敌人的有效生命归零才承认胜利。结果首搜错误扩展到 `486` 回合、`470` 次洗牌和 `15000` 节点，后续又从已结束快照继续扩展带生成选择的动作。
- 单张生成牌接口在已结束战斗中会从批量接口得到空结果，却无条件访问 `[0]`，因此稳定抛出 `ArgumentOutOfRangeException`。现在单张接口与牌堆接口使用同一失败语义，战斗已结束时返回 `Success=false`；搜索快照同时承认模拟器已经提交的终局，以及只剩次要敌人时的原版结束条件。
- 用户 `profile2` 当前存档回归 runId `7e27030aafee4994905d8a6c02260fc1`：相同 Fogmog 开局从 `15000` 节点/`486` 回合收敛为 `2289` 节点/第 `3` 回合，完整自动战斗结束，第 2-3 回合精确复用且 `UnexpectedReplans=0`，没有 `SEARCH_FAILURE`。
- 定向次要幻象终局 runId `260dab8461bf4be78617ed4f6dc68853` 只展开 `2` 个节点并在首回合结束；生成选择 `4/4` runId `da51c916376047a69fc95502a569d1bb` 和实验体三形态 runId `c562482ad1934635ad1b01abc2b526bd` 通过，后者仍为第 `6` 回合与零重算。

## 0.13.14：生成牌选择保护窗与储君印牌爆炸

- 最新实机缩小甲虫日志中，储君牌组含类星体、彰显威权和光谱偏移。首搜展开 `15000` 节点、`16136` 个选牌分支、`61665` 次转移并分配 `6.22 GB`，最终仍只保留当前回合未击杀路线。固定日志牌序的基线 runId `5a537b5142c14ceda23de5c2d1832fea` 同样吃满 `6000` 节点，产生 `12519` 个选牌分支、`37061` 次转移和 `4.09 GB` 分配。
- 根因不是某一张储君卡，而是生成牌选择被当作永久路线身份：每个具体候选会长期申请综合、进攻和防守保留位，使生成卡再生成卡时持续挤大 Beam。现在具体生成候选仍全部展开，但额外路线保护只持续当前及后续两回合；足够覆盖抽到、打出和影响下一次战斗 RNG，又不会永久积累。普通牌堆选择仍保留原来的一回合窗口，永久免费牌继续永久保护。
- 固化回归 runId `947ce5ac5b2647c18de08b9d5e627333`：节点 `3182`、选牌分支 `5426`、转移 `18371`、分配 `2.07 GB`、搜索 `6.52 s`，均约为基线的一半；路线从第 5 回合改善为第 4 回合，预计/实际掉血 `3`，第 2-4 回合精确续用且零非预期重算。
- `Fisticuffs` 的格挡来自实际攻击伤害，不存在独立 `BlockVar`。通用 IL 推断器此前既推断攻击又误推断普通格挡，搜索日志因此重复数百次 `has no block var`。该卡现在显式使用普通攻击 Mirror，再由既有伤害后补偿获得等量格挡；15 项推断卡差分 runId `a31f73b5f9004cd79873392a6f1a5a5a` 通过，最终回归日志中该警告为 `0`。
- 生成三选一四张牌差分 runId `069490896dc24e4ebf95374fb3960e56`、双小啃兽 runId `57f00bf6956f429b9f3eeb02195e1f0e` 和机甲 runId `8b67b67472bd4c9eaab470f54e2f38a6` 均通过；双小啃兽保持 0 药 0 战损，机甲保持第 7 回合与零重算。

## 0.13.13：Power 数量影子事务与全量收口回归

- 当前 DLL 的机甲收口回归首次推翻旧基线：首轮预测保留 `BURST_POWER:2`，实机回合末已移除，导致第 2 回合 `state_mismatch`，错误路线声称第 4 回合结束、预计掉血 28，实机重算后才在第 9 回合结束。根因是回合末清零了模拟 Power，却没有同步清零 Hook 使用的 Power 数量影子。
- 爆发、复制、借用时间、连环拳、狂怒、回弹等一次性 Power 的回合末清理现在同时消费影子数量。所有 Power 数量影子在一次 Hook 批次同步到模拟状态后立即从 `PredictionStateStore` 删除，后续 Hook 从当前模拟 Power 重建，不能把旧层数写回；分叉也不再携带已经结算的影子对象。
- 定向场景 runId `b5d4bff21934430baa8caf847b654e03` 在 1 HP 下用两层爆发重复打出升级防御，第 2 回合精确续用且 `UnexpectedReplans=0`。重复出牌 Power `11/11` Hook runId `aa11595630ba40f4803719f33892c0b8`、伤害 Power 十四场 runId `3a05fcb0d68347ce9c6dfa785384909c` 均通过。
- 当前最终 DLL 的机甲 runId `c28627123f6241cfa3e9f75fac740ea9` 第 7 回合结束，首轮预计整场掉血 36，第 2-7 回合全部精确复用，零非预期重算；首搜 14.73 秒、4.03 GB。相同代码的上一轮可保持 No-GC 到搜索结束并得到 13.34 秒/0 ms GC，最终轮则出现 1.12 秒 GC 暂停，说明 6 GB 固定 SOH/LOH 分区仍有进程级波动，未把零 GC 写成保证。
- 同版收口回归继续通过：双小啃兽 runId `7cb23a651ece4a5294e0b3d575e5403a` 第 5 回合、两次洗牌、0 药、0 战损、零重算；首回合三段选择 runId `483f8f61eaba4e82a852eb387bcedacb`；横祸嵌套选择 runId `479c30bda2d94561b3b770c2dcd958c7`；千足虫复活 runId `1fe4daa1d7ad43b4ba21c59c27556d30`，均通过。

## 0.13.12：助能自动牌嵌套选牌

- 审计首回合选牌后确认，旧版只证明助能的普通防御会自动打出；`EnchantmentLifecycleSupport` 没有把自动牌产生的选牌请求交给有序选择游标。因此“助能附在生存者、宇宙冷漠等选牌技能上”并不属于完整支持。
- 助能自动牌现在复用 Mayhem/历史课的 `AutoPlayWithChoice` 事务：自动出牌后识别弃牌、消耗、生成、移动牌堆及嵌套自动出牌选择，缺计划时形成搜索分支，重放时以牌 ID、升级和助能序号校验上下文，不默认选第一张。
- 新增独立差分夹具：助能生存者与一张打击在手，计划选择打击；原版实际记录 `Player chose cards [STRIKE_IRONCLAD]`，模拟与原版完整生命、格挡、能量、四牌堆、逐牌状态和 RNG 一致。runId `efeb53600fff4f2296ef47c2b8295d89` 通过。
- 首回合选择已从 `PlayerTurnPhase.Start` 接管：求解器模拟能量重置、`BeforeHandDraw`、首手抽牌、`AfterPlayerTurnStart`、玩家方开始 Hook 和 `AutoPrePlay`，递归展开整串选择，再让原版 `SetupPlayerTurn + RunAutoPrePlayPhase` 在同一个 `PlannedCardSelector` 事务中消费。完成后比较完整 Play 状态戳，不一致即拒绝预搜结果。
- 工具盒、选择悖论、烘焙手套首回合、赌博筹码和助能生存者分别通过 `INITIAL-*-394..398` 原版生命周期回归；五场均实际记录 `DEPLOY_CHOICE`、没有玩家界面，并命中 `INITIAL_SETUP_STATE_MATCH validation=exact_state_text`。工具盒、烘焙手套和助能生存者同场的三段选择链另以 runId `64acbb3e98ec41c1a35f07d3cad6d746` 通过。`coverage/pre-play-choice-gaps.json` 的玩家干预欠账现为 `0`；低语耳环继续使用原版确定性 Vakuu 选择器。
- 复查静态闭环时发现首回合根状态还遗漏宝石面具、节庆礼炮、烦人谜盒与低语耳环。宝石面具按 `CombatCardSelection` 随机挑选非先天能力牌并设为本回合 0 费；谜盒按 `CombatCardGeneration` 生成；礼炮结算全体无倍率伤害；低语耳环在普通 AutoPrePlay 后按 Vakuu 固定策略支付费用、选择最左目标并处理嵌套选牌，最多 13 张。力量电池、扭曲漏斗和石化蟾蜍发生在 Setup 接管点之前，继续从 Start 状态精确继承。
- 八遗物组合 runId `82fb0ef9c424473d92c6d5d6c7e77ce6` 通过完整状态与 RNG 校验；低语耳环在首回合自动完成击杀，`CombatEndedTurn=1`、`Unmirrored=0`。这也纠正了旧目录把宝石面具写成“玩家选择”的错误：原版实际使用 RNG 自动选择，不需要玩家操作。
- 低语耳环的 Vakuu 嵌套选择另以高密度生存者牌组强制覆盖。runId `8426e2573f71427fb03e31a3b46aecab` 中原版连续两次记录 `Player chose cards [SURVIVOR]`，模拟按行优先选择同一候选，最终仍命中精确状态。
- 新增原版 `CardSelectCmd` 调用点反向门禁。CoverageCatalog 递归扫描正式模型和异步状态机 IL，当前识别 `85` 个调用点：`60` 个单人战斗来源由求解器支持，`24` 个只在获得遗物时执行，`Tutor` 为多人专属，未知来源 `0`。该检查并入普通 `--verify`，以后新增未登记选牌会直接失败。
- 反向扫描 `CardCmd.AutoPlay/AutoPlayFromDrawPile` 时发现只有 Beat Down 显式继续处理自动牌选牌；横祸、骚动、破灭/瀑流/蒸馏混沌、Stampede、Hellraiser、Eidolon 等路径会漏掉自动牌的嵌套选择。自动出牌现在返回是否真正打出，并在每张成功自动牌后进入统一嵌套选择解析；多张链在待选时暂停，补计划后按原顺序重放。
- 横祸、破灭、骚动完整自动战斗 runId `5c7ebc3043544c729618408bd608dc8d`、`9e923f232ece4fa680a5f687cfb2d272`、`9dd66522ea4e416db06f2231593d6398` 全部零重算并跨回合精确复用；蒸馏混沌 runId `7187663997164f0785e87987c3af2fcb` 原版/模拟完整差分通过。新增 AutoPlay IL 门禁共识别 `19` 个调用点，`18` 个单人来源受支持、`1` 个多人专属、未知 `0`。
- 新增战斗阵容变化来源门禁，递归扫描 `CreatureCmd.Add/Escape`、`PlayerCmd.AddPet` 和 `OstyCmd.Summon`。当前 `51` 个调用点中 `47` 个正式单人来源由敌方召唤、死亡 Power、Osty、逃跑或战前快照处理，`3` 个 Mock、`1` 个多人专属，未知 `0`。测试对象三形态、幻象、千足虫重接等自定义复活状态机继续由既有逐行动差分和整战续用证明。

## 0.13.11：熟睡甲虫与盛碗虫受击状态机续用

- 最新静默猎手实机日志在 `SLUMBERING_BEETLE_NORMAL` 第 `2` 回合首先出现 `BOWLBUG_ROCK` 行动差异：首轮头槌被完全格挡后，实机下一行动为 `STUNNED`，模拟仍保留 `HEADBUTT_MOVE`。原版 `ImbalancedPower` 对盛碗虫（石）会在本次头槌结算中立即安排一个可见眩晕行动；旧模拟只保存“下次不执行”标记，状态文本和后继行动均不完整。
- 盛碗虫（石）现在在完全格挡后立即进入带 `HEADBUTT_MOVE` 后继的 `STUNNED`，并阻止本敌方回合末提前越过该一次性行动；下一敌方回合消费眩晕后再恢复头槌循环。
- 第 `3` 回合的第二个差异来自熟睡甲虫：敌方回合开始的毒伤已扣血，却没有经过求解器的受伤后补偿链，漏减 `SlumberPower`，因此模拟仍预测 `SNORE_MOVE`，实机已醒来进入 `ROLL_OUT_MOVE`。敌方与玩家方中毒结算现在都立即执行受伤后 Power/遗物补偿，实时结束回合风险复核同步使用同一顺序。
- 固化用户种子 `S1EELFRXEZQ0`、进阶 `10`、`46/42/89 HP` 三敌和原牌组/遗物/RNG。最终 runId `b66aa13ef94d4cd78ee1a75be9803ec5` 在 Custom `5/60s`、Instant/0 秒下第 `6` 回合结束、预计战损 `0`；第 `2-6` 回合全部 `SEARCH_REUSED`，`UnexpectedReplans=0`。

## 0.13.10：千足虫跨回合续用与自动出牌选牌稳定性

- 最新千足虫实机日志的四次重算分为两类：已有 `WeakPower` 被怪物再次施加时，模拟错误地再次设置“跳过下次衰减”，导致预测层数比实机多 `1`；一节千足虫完成 `REATTACH_MOVE` 后，预测命中判断仍读取原始 `ReattachPower` 的“复活中”内部标记，导致复活段漏吃毒雾并不能稳定成为目标。
- 怪物施加持续减益现在只在 Power 首次创建时跳过一次衰减，重复叠加按原版在该敌方回合末正常衰减。命中判断提升为预测战斗状态语义，以分支死亡/复活阶段为准，并统一供出牌目标、药水、毒雾、Orb 和死亡联动使用。
- 两个 BOWLBUGS 问题包分别暴露了回合开始选择的集合修改和 Mayhem 重放不稳定。选牌改为从牌堆快照结算；Mayhem 按原版先把本次自动牌全部移到打出区，再逐张执行。自动牌选择额外绑定牌 ID、升级和本次序号，重放身份不一致的候选按无效搜索分支淘汰，不再终止整次搜索，部署校验仍保持严格。
- BOWLBUGS 问题包最终 runId `c6f172b96f684795ab91120d77d585db` 在 Custom `5/60s`、Instant/0 秒下第 `2` 回合结束，`UnexpectedReplans=0`，没有未计划选择、集合修改或玩家界面。千足虫复活命中 runId `2885486eb59740c4aea0717d6f9b56f3` 第 `2` 回合精确续用；连续两回合施加 Weak 的 runId `b0d82b2e8e3849fea30887808e62723b` 第 `3` 回合精确续用，二者非预期重算均为 `0`。

## 0.13.9：同族零重算与状态写入全量门禁

- 同族小队的三次重算定位为 `TrackingPower` 调用了实机目标的 `HasPower<WeakPower>()`：首轮未来分支少算追踪增伤，重搜后又可能在虚弱消失后多算。追踪之环、幻影之刃、巨像、致命和不死印记改为只读分支状态；用户进阶 10 存档从满血敌人开局，第 16 回合结束且 `UnexpectedReplans=0`。
- 新增 `branch-state-read-risks.json`、`state-fields.json` 与 `state-mutations.json`。字符串动态字段必须显式分类为行为或纯显示；状态写入扫描递归跟进模型私有辅助方法，并按静态行动图、初始快照、模型变换和未来生命周期分组。门禁最终为分支实机读取 `0`、字段未分类 `0`、搜索期必需状态写入缺证据 `0`；首回合根状态补齐后剩余 `22` 个只在求解接管前执行的快照写入，另有 `115` 个静态行动图构造器单独列出，不冒充已做模拟差分。
- 跨回合续用与 Beam 指纹忽略 11 个只用于名称/说明的字符串变量，但完整测试快照继续保留它们用于诊断。新版本出现未知字符串字段会直接使覆盖门禁失败，不使用默认分类。
- 补齐局内生成卡的完整监听生命周期：卡牌进入战斗时立即执行女妖哀嚎、精准打击、践踏和 Flatten 的历史费用初始化，并登记为后续 Hook 监听者；移出战斗时注销，搜索分叉时重映射到分叉卡实例。生成牌同时立即结算 Hex、Tangled、Ringing、Vital Spark、Sword Sage、Phantom Blades 和 Ghost Seed 等入场联动。
- 能量/星费监听恢复到原版“扣费后、卡牌效果前”顺序；空手检查移到牌进入结果牌堆后；药水统一执行槽位移除、使用前监听、效果/选牌、使用后监听和空手检查；死亡阻止后仍为 0 HP 时按原版递归并限制十次。
- 运行证据新增 98 张状态写入升级卡、8 张降级/恢复卡、5 种附魔、4 张入场历史卡、17 个药水、11 个死亡/受伤遗物和 57 个怪物行动。覆盖目录仍为 `3035` 项，未分析、待实现、运行证据缺口和原生重扫边界均为 `0`；范围继续排除多人模式、多人专属牌与战斗外流程。

## 0.13.8：缩小甲虫 Power 身份续用修复

- 最新实机日志的第 2 回合重算只有一个字段差异：`SHRINK_POWER` 的层数和 `DamageDecrease=30` 均一致，但预测 `ApplierName` 为空，实机为“缩小甲虫”。原因是分支内通用 Power 施加保存了 `Applier` 引用，却没有执行 `ShrinkPower.AfterApplied` 写入的显示动态变量。
- 预测施加缩小时现在按原版从最初施加怪物写入本地化 `ApplierName`。已经实现的“施加者死亡时移除缩小”同时登记为运行时补偿，击杀甲虫不再产生错误的 `UnsupportedEffect` 终局边界。
- 受控 `SHRINKER_BEETLE_WEAK` 回归 runId `f2b1d05a9594487987021327e24209fd`：41 HP 甲虫执行无限缩小后，第 `2-5` 回合全部 `SEARCH_REUSED`，`UnexpectedReplans=0`、`Unmirrored=0`、`unsupported_intent=false`、`modeled_damage_exact=true`。

## 0.13.7：Vigor 组合攻击续用与龙涎香保血门槛

- 修复 `VigorPower` 在分支中同时维护原 Power 层数和旧影子层数的问题。攻击消费活力后再由星星点点获得的新活力不再被旧影子 `0` 覆盖；致死攻击即使让模拟器提前进入战斗结束态，也会完成配对的 `AfterAttack` 清理，不再留下禁止分叉的攻击引用。
- 万向斩与回声斩按原版 `AttackContext` 建立组合攻击生命周期。万向斩现在会在整组伤害完成后消费活力，后续陨星不会重复获得同一份 `2` 点加伤；问题包原先第 11 回合敌方生命 `40/42` 的偏差由此关闭。
- 幻象复活后的行动来源改为 `IllusionPower.FollowUpStateId` 或怪物状态机最后一条正式行动记录，不再把临时 `REVIVE_MOVE` 自己写成后继；复活期间的重复死亡通知保持幂等。
- 龙涎香新增独立珍贵药水门槛：路线相对最佳无药路线最终至少多保住最大生命的 `40%` 才可入选，向上取整；其他同时使用的普通药水机会成本继续叠加。该门槛在“至少用一瓶”模式下同样生效，缺少无药反事实基线时不放行。
- 用户 Obscura 开战前存档在进阶 10、第二幕、中档 `5/60s`、Instant/0 秒下完整通过：runId `46370a747ad6495da4732dcef1067ede`，第 `10` 回合结束，`UnexpectedReplans=0`。龙涎香门槛另以 `9` 组边界计算验证 `100→40`、`67→27`、低一血拒绝与多药成本叠加。

## 0.13.6：战略跨洗牌选牌作用域修复

- 审计问题包 `CombatSolver-VANTOM_BOSS-20260823-235009.zip`。错误与光谱偏移无关：新生之喜生成战略，战略打出后留下 `STRATAGEM_POWER`；未来回合手牌抽取跨洗牌时，Power 要求从抽牌堆选择牌，但旧模拟只在单次出牌期间开放选择作用域，因此稳定抛出“在动作选择作用域外请求选牌”。
- 有序选择游标现在覆盖完整的“结束玩家回合 → 敌方回合 → 下回合抽牌与自动出牌”事务。洗牌 Hook、回合开始效果和自动出牌共用同一游标，知识恶魔的敌方行动选择仍保留独立通道；未触发、数量错误或顺序错误继续 fail-fast。
- 模拟抽牌在洗牌 Hook 产生待选请求时立即暂停，不再继续抽空牌堆后才枚举候选。补入分支选择后从父快照重放，选择发生时的抽牌堆和原游戏一致。
- 问题包存档最终 runId `f0d0382c558945f4a63c896b8acfc098` 首搜正常完成：`7` 回合、`2` 次洗牌、`2623` 个选择分支、未镜像 `0`，无 `SEARCH_FAILURE`。中档预算下仍只找到第 `7` 回合死亡线，本修复只关闭确定性异常，不宣称已经找到生还路线。
- 受控整链 runId `e559a66c35da41639c6cf111fb402620`：第 1 回合计划 `STRATAGEM_POWER → STRIKE_REGENT`，实机洗牌自动提交该牌，第 2 回合 `SEARCH_REUSED`，非预期重算 `0`，没有玩家选择界面。

## 0.13.5：结束回合战损复核误报修复

- 修复全自动在提交结束回合前只用“当前格挡 + 敌人攻击”估算战损的问题。即时复核现在先执行与主搜索相同的完整玩家回合末和敌方回合开始流程，包括山铜、仿制山铜、斗篷扣、充能球、手牌弃置、临时 Power、敌方格挡清理、回合开始 Power 与中毒，再判断当前路线是否真的变差。
- 用户实验体日志中的误报口径为路线预计掉 `11`、旧即时复核掉 `20`；缺少的正是回合末遗物格挡。最终 `0.13.5` 在“战损变差时暂停”开启、药水策略“至少一瓶”时完整自动执行到第 `6` 回合，实际剩 `66 HP`，第 `2-6` 回合全部精确续用且没有 `FULL_AUTO_STOP`，runId `426e770715144974b09680af5bc13c38`。
- 真阳性没有被关闭：最小场景原路线以 `5` 格挡承受 `6` 点攻击、预计掉 `1`；测试在提交结束回合前强制清空格挡后，即时复核仍识别致死并暂停，runId `ce41712e126c419ca9630e81fd19b174`。
- 原有墨宝真阳性夹具复跑时被当前 `STRATAGEM_POWER` 洗牌选牌作用域问题提前中止，没有进入结束回合复核，因此不把该失败算成本修复证据；本版本用不含计策的最小场景替代真阳性验收。该计策问题是独立适配欠账。
- 启动日志的版本号改为读取程序集版本，不再长期显示旧的硬编码 `0.13.2`。

## 0.13.4：实验体三形态整战修复

- 实验体的搜索生命从“当前形态生命”改为“当前生命加全部尚未出现的形态生命”。击杀第一、第二形态后，Beam 中的敌方生命不再从接近 `0` 反向跳到 `212/313`，阶段击杀由此成为单调进展，不会被当作路线倒退剪掉。
- 补齐实验体的 `SKULL_BASH_MOVE` 易伤、第二形态连击次数逐回合增长、第三形态灼烧咆哮生成灼伤与增加力量，以及复活行动的支持登记；三形态路线现在为 `unsupported_intent=false`、`modeled_damage_exact=true`。
- 临时力量回收改为统一处理全部 `TemporaryStrengthPower`，不再维护容易漏项的类型清单。爬虫饰品饮药后的 `REPTILE_TRINKET_POWER` 与力量会在原版时点一并移除，修复首回合后续用状态差异；同时覆盖同源的 Feeding Frenzy、Coordinate 等临时力量。复制药加爬虫饰品的原版/模拟回合末完整状态差分由 runId `b89d4b4a269b4b739c39fba099117515` 验证。
- 以用户 `profile3/current_run.save` 固化实验体二 Boss 回归。中档 `5/60s`、智能药水、瞬间出牌下，最终 `0.13.4` runId `da895fb550344e6f99c7b6030cc10402` 首轮一次搜索得到第 `6` 回合击杀、预计/实际战损 `11`、剩余 `66 HP`；第 `2-6` 回合全部精确续用，三次形态均实机结算，非预期重算与未镜像均为 `0`。求解器最终采用“迅捷药水整理牌序，复制药复制华丽收场”终结，属于比手操“开局复制灵动步法”更快的可执行生还线。
- 无人存档还原同时补上 `max_potion_slot_count`，带药水腰带或其他扩槽来源的真实存档可按原槽位恢复，不会在夹具准备阶段越界。

## 0.13.3：问题包修复、三态药水策略与复活窗口

- 修复腐臭药水在击杀并移除首个目标后继续遍历原集合导致的异常；所有可能改变阵容的群体药水先固化目标列表。修复 Flyconid 等怪物全部 RAND 权重为零时额外抛错，预览与模拟统一复刻原版首分支和 RNG 消耗语义。
- 变牌补齐生成历史与 `AfterCardGeneratedForCombat` 时序，武器库、创世之柱、Regalite 和 Soulbound 等生成监听者可响应 Charge/Begone。`PlanAction` 支持有序嵌套选牌，狠揍和 Decisions Decisions 自动打出的选牌卡由求解器继续展开；部署器使用动作级事务核销显式与隐式选择并保持 fail-fast。
- 新增持久化药水策略“禁用 / 智能 / 至少用一瓶”。智能策略在统一 Beam 内按精确用药数量保留防守、进攻和铺垫代表，Beam 不预扣药水机会成本，最终仍要求普通药每瓶至少省 `9 HP`；Token 药水战略成本为零。固定同族小队在默认智能策略下第 `13` 回合两药生还，预计战损 `36`、未镜像 `0`、非预期重算 `0`；双小啃兽仍为第 `5` 回合、`0` 药、`0` 战损。
- 删除幕末 Boss 的低生命权重和放宽卖血特化；最终统一按生存、胜利、真实整场战损、药水资格和更早击杀比较。Beam 改从模拟状态提取新增持续 Buff、毒、流沙坑剩余回合、牌组污染和复活窗口，能力/毒/逃离/洁牌路线跨回合保留；虚无牌不会被当成长久污染优先变掉。
- 搜索续用区分 `manual_divergence` 与求解器严格部署后的 `state_mismatch`，手操偏离不再触发“预计战损变差”暂停。帧百分位最后桶返回实际最大间隔；No-GC 记录本战与上次搜索分配量，在下一次搜索预计无法装入剩余区域时，于两次搜索之间回收并重新进入。4 GB 定向回归实际记录一次轮换，手操偏离 `1`、非预期重算 `0`。
- 千足虫从中途 0 HP 状态开始搜索时会导入复活阶段，`REATTACH_MOVE` 正确恢复 `25 HP`；有效敌方生命计入可兑现复活债，同时独立保留同回合连续击杀窗口。受控整战在第 `2` 回合结束、预计/实际 `0` 战损且无重算。问题包旧“再掉 10”预览来自漏算复活生命，修正后不再作为真实可达基线；精确恢复的第 3 回合样本目前没有验证出生还路线。
- 无人夹具支持恢复中途回合号、能量、星能、逐怪行动历史、0 HP 复活敌人、逐牌动态变量及注入后跑局 RNG；后台搜索异常会立即结束断言，不再表现为测试卡死。完整战斗基准统一使用瞬间出牌和零额外停顿。
- 虔诚雕刻师第 `4` 回合和知识恶魔第 `11` 回合问题包状态完成精确恢复，最终 DLL 均在当前回合 `0` 战损、`0` 药、未镜像 `0`、零重算击杀。知识恶魔的 `MIND_ROT` 分支搜索和自动提交另由首回合选择回归覆盖。
- 永世沙漏目录只保留战后 `current_run.save` 与开局可读日志，没有开战前 RNG 状态；战后存档显示 `pre_finished_room=AEONGLASS_BOSS` 和 `12 HP`，不能直接重进同一战斗。用开局牌序配战后 RNG 的混合夹具判死，不作为算法回归结论；`12 HP` 基线仍缺可重放的战前快照。

## 0.13.2：回合末单一时序与全量证据审计启动

- 修复玩家回合结束时充能球被动被 `OrbLifecycleSupport` 与 `SimulateEndPlayerTurn` 各触发一次的问题；新增 `PlayerTurnEndLifecycle` 作为唯一阶段一入口。闪电、黑暗、冰霜和玻璃球均完成原生/模拟完整状态差分。
- 卡牌参数化补偿改在原牌进入结果牌堆之前执行，修复高速脱离先进入弃牌堆、生成晕眩后到达所造成的跨洗牌顺序偏差。11 类生成牌通过有序四牌堆与完整续用状态差分。
- 花园幽灵鳗日志场景完成首轮到战斗结束回归：第 2-9 回合全部 `SEARCH_REUSED`，非预期重算为 0。力量强化本身原已正确；日志中的每回合 3 点生命偏差来自重复闪电球。
- 自动搜索原因拆为初搜、状态偏差、缓存视野结束、执行漂移和用户主动请求。战斗结束输出 `REPLAN_SUMMARY`；详细诊断可输出全部字段差异，问题包新增 `replan-audit.txt`。
- 无人差分开始硬比较牌堆顺序、充能球顺序与数值、完整续用文本；死亡怪物不再被后续夹具误选。通过场景只写单行摘要，失败才保留完整三方快照。
- 新严格差分发现并补齐旋转机施加 `SPINNER_POWER`，并发现拆解对易伤目标必须攻击两次，后者改为显式镜像，不再依赖宽松推断。
- CoverageCatalog 新增 `--verify-runtime-evidence`、`runtime-evidence-gaps.json` 和简单推断卡自动夹具生成。`0` 未分类只表示登记完整，不再等价于全量运行正确；本版本开始按机器欠账逐批闭环。
- 全量运行证据审计闭环：卡牌、药水、充能球、附魔、怪物特殊 Hook、Power 和遗物均已归零；`EngineExact` 无运行证据从 `264` 降至 `0`。目录 `3035/3035` 项现为未分析 `0`、待实现 `0`、无运行证据 `0`、持久遗物状态缺口 `0`、`NativeAutoRescan=0`，完整门禁通过。范围仍明确排除多人模式和多人专属内容。
- 药水 `17/17` 严格差分补齐自用空目标解析、`ClarityPower` 与瓶中仙女自动消费槽位；充能球 `10/10` 覆盖五种球的 `Passive/Evoke`；附魔 `11/11` 覆盖出牌、抽牌随机费用和真实洗牌置顶。
- 女王现在在火炬头聚合体死亡时写入分支内 `_hasAmalgamDied` 并从 `BURN_BRIGHT_FOR_ME_MOVE` 立即切换到 `ENRAGE_MOVE`；其两个后续条件分支读取预测状态。其余十二个怪物残余 Hook 经源码审计确认只涉及音乐、语音、音效或动画。
- 卡牌 replay 的求解器补偿改到每个原生 `OnPlay` 次数内部执行，修复 `SignalBoost + Inflame` 只获得一半力量的问题；46 张参数化卡牌严格回归通过。该批同时发现并修复 `EnragePower` 被误忽略、`CurlUpPower` 消费后残留，以及 Panache/Withering/Automation 的分支计数状态缺口。
- `PredictionStateStore` 支持模型重映射：Power 首次变为分支可写克隆时，预测状态保持同一身份并随分叉映射，避免跨行动内部计数重新从克隆默认值开始。无人夹具新增逐项清牌堆、清 Power 及充能球直接 `Passive/Evoke` 差分入口。
- Power 剩余行为分为出牌计数、抽牌/牌堆、受伤、跨回合和最终生命周期批次完成严格原版差分；修复炸弹最后一层不爆炸、战略能力在嵌套抽牌且唯一候选时错误要求动作选择作用域，以及 Power 数值状态未同步。战后奖励或纯表现 Hook 按反编译源码单独静态闭环，不用空跑测试冒充行为验证。
- 遗物 `74` 个旧证据缺口全部重新审计。测试夹具支持逐项清空并通过原版 `RelicCmd` 注入遗物，覆盖受伤/防死、出牌成对 Hook、洗牌、耗尽、弃牌、回合末、充能球和私有计数；只影响闪烁、状态提示或战后奖励的九项按源码静态关闭。石日历改读分支玩家回合号，不再把实机当前回合号带入未来路线。
- 显式 `OnPlay` 补偿目录补齐全部生产处理类型。严格差分确认碎屑、隐藏储藏、粒子护盾和共鸣的模拟本已正确，原先同族路线中的 `Unmirrored=4` 来自登记遗漏；补齐后同一完整战斗为 `Unmirrored=0`。
- 最终开局到结束回归：双小啃兽保持 `0` 药、`0` 战损、`0` 非预期重算，但当前路线由旧证据第 `6` 回合变为第 `7` 回合并跨 `3` 次洗牌；花园幽灵鳗第 `9` 回合结束，第 `2-9` 回合全部精确复用；同族小队使用攻击药和癫狂之触，第 `13` 回合生还，预计掉血 `37`，第 `2-13` 回合全部复用且未镜像为 `0`。

## 1. 项目目标

CombatSolver 只解决一场已经开始的单人战斗：读取当前战斗状态和可预测 RNG，在不修改游戏 RNG 的前提下，以时间和节点预算搜索较优行动路线，并允许玩家查看或自动执行当前回合方案。

当前不讨论整局路线、选事件、商店、地图和构筑。开发回归通过隔离的塔 2 `--headless` 进程执行原版规则，不另建自有 CLI。

设计优先级：

1. 路线能在原版战斗中执行。
2. 已知效果尽量与游戏结算一致。
3. 未镜像内容必须显式记录，不能用“高置信度”掩盖。
4. 搜索在后台运行，不阻塞游戏主线程。
5. 性能优化不得悄悄改变 RNG 或把近似搜索描述成穷举。

## 2. 当前功能基线

### 搜索

- 两阶段 Beam Search：默认中档短搜统一宽度 `18`，深化统一宽度 `45`；药水不再拥有独立配额。
- 普通搜索按可用逻辑处理器自适应选择 DOP1/2/4，新安装在至少 4 个逻辑处理器时默认 DOP4；设置可在 `1–16` 之间调整。候选仍由协调线程按原顺序提交，详细诊断与严格增量验证固定使用单线程。
- 默认中档短搜预算 `8 秒 / 2400` 节点；短结果存在未来掉血、用药或未确认胜利时，同一搜索会话继续使用总计最多 `120 秒 / 12000` 节点的深化预算。
- 回合内行动数不设上限。费用、星能、可出牌性、击杀、精确状态置换、通用有界周期调度、时间和节点预算共同构成终止条件。
- 不设固定回合数或洗牌次数上限；时间、节点、确认胜负、跨回合无进展窗口和有界周期探测共同构成搜索边界。周期证据不是无限性的数学证明。
- 每节点默认中档短搜最多保留 Top-14 出牌分支，深化最多 Top-24；深化同时扩大牌堆/手牌选项预算。
- 子节点保存生成时的模拟快照；后续展开、最终复核和路线标注复用快照。
- 最优路线会保存后续玩家回合的预测起点；真实状态逐项文本一致时直接复用剩余路线，状态不一致则重算。点击“重新计算”始终从当前实机状态发起新搜索。
- 相同状态下的等价重复卡牌只模拟一次。
- 每次 Beam 剪枝除评分最高路线外，还保留一条当前威胁下预计剩余生命最高的防御路线，使回合结束时的最低可达到战损基线不会被输出评分提前挤掉。
- 深化搜索额外保留成长、资源、控制和帕累托路线，并对非即时效果执行一次缓存的结束回合探测；不按具体牌名特化毒伤或蛇咬。
- 同一搜索会话复用“父状态 + 动作”的模拟转移。完全相同状态继续由置换表剪除；控制形状在最多 `8` 个动作内重现时，以动作相位和一致转移建立通用周期证据。无即时进展的族按节点档位获得有限动作租约，并对各相位的非周期出口做最多 `8` 动作、`2` 次回合转换的收益探测；伤害、逐敌人耐久、回复、资源、持久成长、控制、牌库净化或特殊目标进展会阻止无进展淘汰。生产实现不包含按卡牌、Power、遗物或敌人名称放宽预算的分支。该机制不限制普通回合行动数，也不承诺找到所有长周期或晚兑现组合。
- UI 在滚动框中展示本次搜索得到的全部回合；完整路线同时写入日志。

### 选牌

- 支持从手牌、抽牌堆和弃牌堆选择已有牌，以及移动、弃置、消耗、升级等已列入支持表的选择。
- 正在结算的头槌等卡牌不会提前进入自己的弃牌堆候选。
- 富足、发现、类星体和飞溅的生成三选一已进入搜索分支；部署器可直接驱动原生“选择一张牌”界面。药水、回合开始选择和嵌套自动出牌使用同一计划令牌，不等待玩家干预。
- 现有牌选择统一由 `CardChoiceSpec` 描述；必备工具等回合开始选择、普通出牌选择、生成选项和嵌套自动出牌共用计划令牌与部署选择器。
- 当前选牌不是全部穷举：单卡捞取枚举所有不同结果；默认中档多牌组合的牌堆选择最多保留 `6/12` 个启发式优先分支，手牌选择最多保留 `8/16` 个。
- 这个限制会提高速度，但可能漏掉面板价值较低、后续联动较强的候选，属于明确的精度取舍。

### 战斗状态与效果

- 已知牌序、能量、星能、手牌和各牌堆状态。
- 玩家与怪物的生命、格挡、力量、敏捷、虚弱、易伤、中毒、脆弱等核心状态。
- 怪物意图、逐段攻击伤害和一批常见怪物 Buff / Debuff。
- 武装的单牌升级与升级后全手牌升级。
- 噬尸蛞蝓的饥饿死亡联动：同伴死亡后获得力量，并立即将当前意图替换为带原行动后继的 `STUNNED`；最后一只死亡能确定结束战斗。
- 愤怒、战斗专注、绯红披风和上勾拳的基础 Mirror 缺口由求解器补偿。绯红披风在每个玩家回合开始触发自伤与格挡；自伤数值只在再次打出该牌时增加，不会随回合数自动增长。
- 瀑布巨兽的常规行动已补偿蒸汽积累、虚弱、治疗、压力炮成长、蓄爆与爆炸；`0.13.29` 补齐提前击杀后的阵容保留、无限最大/当前生命、强制行动和普通 Power 死亡清理。
- 缩小、人工制品的负数减益抵挡、缠结、昏眩和无实体已补齐完整确定性生命周期；卡牌附魔随新牌进入和 Power 移除同步，昏眩的回合内首张牌限制进入搜索状态指纹。
- 力量、敏捷、虚弱、易伤、脆弱、中毒、残影、双倍伤害、不可格挡、覆甲和缓慢的核心数值及回合生命周期已补齐；持续减益保留原版首次跳过规则，不可格挡按源码例外无条件递减，缓慢计数与重置进入模拟状态。
- 下回合能量、抽牌与格挡已按原版结算时点进入搜索；预借时间、征服者、融入暗影、不可抽牌、无法获得能量、保留手牌及一批一次性状态的数值或回合末生命周期已补齐。手牌清空发生在临时状态递减之前。
- 未镜像死亡效果仍可成为搜索边界，但已由求解器补偿的效果会单独标注。
- 确定性 `OnPlay` 不再依赖宽松 IL 猜测：常见攻击/格挡/抽牌由严格 `EffectRecipe` 证明完整后执行，Power、资源、生成和永久牌面修改由参数化规格表补全；未知游戏调用、条件效果或模型写入不会被伪装成精确支持。
- 所有原生 `CalculatedVar` 卡牌都必须登记分支内公式。公式读取预测牌堆、Power、Osty、回合内事件和 RNG 状态，不再调用绑定真实 `CombatState` 的委托；覆盖工具会在新增卡牌缺公式时失败。
- 结构性召唤、复活、替换、逃跑、额外回合和怪物行动状态均由分支自有阵容、稳定 CombatId、AI 状态日志与克隆 RNG 继续推进；覆盖门禁要求 `NativeAutoRescan=0`。
- 怪物攻击统一进入内置引擎的完整伤害管线，原生伤害加算/乘算/上限、格挡、奥斯蒂“为你而死”转移与溢出、受伤后钩子按游戏顺序结算；多段攻击中模拟态奥斯蒂死亡后会临时屏蔽仍指向实机存活对象的转移钩子，确保后续段落到玩家；行动专属动态基础伤害和特殊后处理仍由求解器维护。
- 精准、钙化、勒紧、腐化、好奇、吊杀、难以杀灭、领袖气质、致死性、一心化万、幻影之刃、翱翔、跟踪和不动已完成独立可见游戏差分；依赖本回合历史的首张攻击、小刀和格挡次数使用预测计数并进入状态指纹。
- 来生、护卫、流星锤、轰击、洁净、挽歌、幻景、执迷、刀刃陷阱、独白、亡灵精通、夜魇、凡庸、死者苏生、增生、炸弹、无休手斧和虚空形态的卡牌入口已关闭：模拟状态现在跟踪奥斯蒂当前/最大生命与复活、独立 Power 实例、夜魇选择、跨回合回手牌和回合内出牌数；抽牌前与自动预出牌阶段接入搜索推进。前 `17` 项完成 `12` 条生产模拟/原生差分，虚空形态另用全自动闭环确认实际打出后立即推进回合。炸弹倒计时、亡灵精通和虚空形态后续 Power 生命周期仍按独立目录条目适配，不借本批提前标记。
- `INVOKE` 等来源允许非亡灵契约师在未来回合创建奥斯蒂；新实例完整进入分支阵容、伤害管线、卡牌/Power/遗物语义、状态指纹与跨回合续用，不依赖实机预先存在宠物。
- 甲虫汁、熔炉的祝福、格挡药水、鲜血药水、骨头酿、敏捷药水、复制药水、能量药水、爆炸安瓿、火焰药水、肌肉药水、集中药水、固化药水、罐装幽灵、铁心药水、流动铜液、幸运补剂、马萨雷斯的赠礼、毒药水、扩容药水、灾厄药水、消亡粉末、明耀酊剂、镣铐药水、速度药水、稳定血清、星星药水、力量药水、易伤药水和虚弱药水已作为独立搜索动作接入。模拟状态跟踪已消耗槽位并纳入指纹与续用校验；自动部署按路线调用原生药水队列。`30` 条即时差分、`3` 条临时属性生命周期差分、`2` 条无法获得能量交互差分及火焰药水全自动斩杀闭环通过；当时留下的选牌、动态生成和自动复活入口由下一条第 045 批关闭。
- 龙涎香、狡诈药水、尸鬼瓮、果汁等生成、额外回合和最大生命变化均在分支内继续结算。药水槽本身是可分叉状态；熵酿会按原版 RNG 补满空槽，添水、腰带扣与后续同路线用药读取同一分支库存。龙涎香只有相对最佳无药路线至少多保住最大生命 `40%` 时才允许入选，其他药水仍按普通机会成本累计。
- 伶俐、腐化、华彩、黏糊、注能、本能、动量、灵巧、锋利、沉眠精华、涡旋、特兹卡塔拉的余烬和活力附魔，以及等离子球回合开始能量，已完成 `14` 条真实可见游戏差分。附魔 ID、层数、启用状态和影响未来路线的私有成长进入状态指纹与跨回合续用戳。
- 怪物行动中的召唤、逃跑、替换、牌库改写和私有 AI 状态修改已进入分支模拟；一步差分额外比较整个阵容、行动日志、私有状态和 RNG，环境组装师、Living Fog、Fogmog、Obscura、Ovicopter/Tough Egg、双尾鼠及两种逃跑完成原生闭环。
- 抽牌数、最大能量、跨回合保留格挡和整手保留改为直接调用游戏原生纯计算钩子，并向钩子提供当前分支的有效模拟 Power 监听器。领域、心灵腐化、友谊、薪火之源、衰朽、必备工具、暴政、壁垒、埋地和计划妥当已完成真实态/模拟态差分；必备工具、暴政、熵和烘焙手套的回合开始选牌现由 Beam 规划并自动部署。
- 损毁头盔首次正力量翻倍与不安油灯首张有效减益牌翻倍已进入统一 Power 数值管线；一场一次状态、当前触发牌和临时 Power 内部类型分别按长线/瞬时语义维护。宾邦、五轮书、黑石护符和招财异鱼的 `AfterCardChangedPiles` 只监听永久跑局 `Deck`，与战斗 `Draw` 不同，按单场战斗范围静态关闭。覆盖目录现为 `3035/3035`，未分析项归零。
- 腰带扣、书签、舵盘、情感芯片、星系尘埃、幽灵种子、船夹板、金纸、锁镰、月亮糕点、抱抱先生、招架盾、爬行动物饰品、闪亮口红、不休陀螺和不死符文的响应式遗物语义已进入生产模拟；佩尔之眼额外回合、历史课 Dupe 自动出牌和烘焙手套选牌均可跨回合精确复用。
- 律动残余、艳丽围巾、恶魔之舌、苦无、开信刀、迷你储君、音乐盒、精致折扇、佩尔的士兵、佩尔之泪、彩虹戒指、君王矿石、手里剑和天鹅绒颈圈的 RF 私有预测状态现在按分支独立重置、推进并进入状态指纹。击剑指南、无界命匣、符文电容器、钗、黄金印、共生病毒和烫嘴可可的确定性回合资源也已接入；苦无、手里剑和彩虹戒指原先只有 RF 风险记录而没有实际数值效果，本批已纠正。
- 赤牛、孙子兵法、弹珠袋、风箱、两种血瓶、骨茶、轰鸣海螺、面包、硫磺、烛台、吊灯、破损核心、钻石头冠、天命所归、两种开心小花、两种古茶具套装、灯笼、水银沙漏、红面具和王室猛毒的回合开始/能量重置/回合结束语义已进入生产模拟。大帽子、十字弓和橙色团块的随机生成牌按原生 RNG 顺序精确推进。
- 冰淇淋、三角铃鼓和坚固钳子的未来回合生命周期改为读取模拟回合号并保留原生监听器否决顺序；异蛇头骨的中毒加算进入模拟施加 Power 的人工制品判定之前。化学物X、符文金字塔、注能核心、带刺手甲以及九项纯伤害/格挡/生命损失回退完成真实游戏差分；已验证的 RF 原生纯函数回退不再显示为未补偿风险。
- 摆动球、怀表和花粉核心的私有回合/出牌计数改为分支内独立维护，并进入节点指纹与跨回合完整状态文本。怀表 `AfterCardPlayed` 在 RF `0.13.8` 中实际是 ignored 登记，现已纠正假精确分类；连续六回合周期和 `4/0/3` 张打牌阈值完成原生差分。首回合生成遗物由游戏结算后再采集初始牌堆与 RNG，未来回合不重复生成。
- 遗物纯抽牌/最大能量 Hook 继续复用游戏原生实现，但不能直接信任遗物读取的实时 `PlayerCombatState.TurnNumber`：RF 模拟玩家状态没有回合号。求解器现在独立推进分支回合，并校正准备背包、大蘑菇、轰鸣海螺、长蛇戒指、蛇之戒指、面包和佩尔之肉的回合条件；第 2、3 回合原生差分已通过。
- Power 回合末语义补齐摧残、魔法炸弹、独白、湮灭、神气制胜、紧追不放、胆小、紧勒和幽冥之界；独白与湮灭不再沿用 RF `0.13.8` 仅记录风险的配对钩子，而是按出牌开始前快照结算。模拟状态可以按精确 Power 实例创建可变副本并移除本回合新施加的实例，避免实例型 Power 被旧的“所有者 + 类型”状态表漏掉。
- 回合开始流水线补齐 `BeforeSideTurnStart → AfterEnergyResetLate → BeforeHandDraw → AfterPlayerTurnStart → AfterSideTurnStart → AfterSideTurnStartLate`。`AmountOnTurnStart` 进入分支与指纹；虚空之唤、创造性 AI、既定事项、你好世界、光谱偏移、好勇斗狠和熵均按原生生成/选择 RNG 继续搜索。
- 暗影步、惊逃、岩石铠甲、计策、子程序、征召上前、超临界态、剑圣、同步、战术大师、地形改造、封印王座、铸剑者、雷霆、必备工具、化废为宝、内核加速、暴政、坚定不移、袖里乾坤、崇拜、凶恶、计划妥当和鬼火的 `OnPlay` 已在同一可见游戏 PID 中完成 `6` 个场景、`7` 条最终差分；同步按模拟 Orb 种类施加临时集中并在玩家回合结束回收，征召上前处理跨牌堆君王之剑后再锻造，内核加速保持能量先于虚无生成的原版顺序，袖里乾坤同时镜像小刀和本场费用变化。运行时将这 `24` 个 RF 不支持的 `OnPlay` 风险标为求解器已补偿；后续响应式 Power 钩子继续独立登记。肉盾、指导和幽冥之界按原版多人专属约束静态排除。
- 自动化、禁忌魔典、湮灭、环绕轨道、抢占先机、书页风暴、暗淡蓝点、神气制胜、招架、幻影之刃、创世之柱、生产制造、延伸、薪火之源、死神形态、滚石、胜券在王、王国资产、追踪之刃、融入暗影、厄运之衣、信号增强、血肉戏法、烟囱、蛇咬、光谱偏移、速行者和灰烬之灵的 `OnPlay` 已在同一可见游戏 PID 中完成 `6` 组最终差分；延伸读取模拟态当前格挡，胜券在王保持支付后获得的顺序，追踪之刃同步生成并锻造君王之剑。运行时将这 `28` 个 RF 不支持的 `OnPlay` 风险标为求解器已补偿；后续 Power 生命周期仍逐钩子登记。筹划、鬼祟和灵魂绑定按原版多人专属约束静态排除。
- 神化、武器库、刀刃之舞、墨之刃、放血、黑暗镣铐、致命毒药、碎屑、吞噬生命、双倍能量、弱化之触、开悟、熵、暴露、刀扇、既定事项、迷雾、隐秘藏品、热修复、唤起、连环拳、准备时间和钢铁风暴的 `OnPlay` 已完成同一可见游戏进程差分；逐牌局部费用、附魔和升级等级纳入无人差分快照，临时力量与集中力在对应方回合结束精确回收。刀刃交响曲、消影和彼岸一瞥按原版多人专属约束静态排除。
- 磨蚀、灵体、暴涨、绯红披风、主宰、灵动步法、友谊、燃烧、何人僭越、萎靡、尖啸、非凡技艺、腐败、淬炼刀刃、共鸣、命运同担、震荡波、侧步、战栗和幽魂形态的 `OnPlay` 已完成可见游戏差分；X 值从模拟钩子状态结算，充能球容量进入差分快照，下回合能量统一使用原生 Power，尖啸包含敌方回合结束恢复。
- 触媒、精准、余像、好勇斗狠、预判、壁垒、偏差认知、缓冲、铁蒺藜、扩容、腐化、创造性 AI、黑暗之拥、碎片整理、恶魔形态、回响形态、涂毒、无惧疼痛和熔炉的 `OnPlay` 已完成可见游戏差分；预判的临时敏捷与回收进入状态指纹，扩容直接修改 RF 充能球队列容量。希望灯塔按原版多人专属约束静态排除。
- 星位序列、黑洞、预借时间、咕嘟冒泡、子弹时间、爆发、钙化、虚空之唤、征服者、汇流、冷却剂、腐蚀波、倒数计时、残酷、死亡使者、野性、冰雹风暴、你好世界、无尽刀刃、势不可当、杂耍、致死性、循环、机器学习、乱战和毒雾的 `OnPlay` 已完成同一可见游戏进程差分；野性与杂耍中途施加时会从本回合已有出牌初始化私有计数并进入状态指纹。炽焰按原版多人专属约束静态排除；这些能力后续的随机生成或生命周期仍按独立钩子登记，不借本批提前标记。
- 劫难、群星之子、死亡之舞、领域、永恒铠甲、勒紧、疯狂进食、被遗忘的仪式、纠缠、地狱狂徒、迭代、谋划专家、王之凝视、中子护盾、怀旧、时候未到、狂怒、撕裂、群蛇形态、哨卫模式、雷暴、燃料、创世纪、冷光和无处可逃的 `OnPlay` 已完成同一可见游戏进程差分；时候未到包含显式生命恢复断言，无处可逃按目标当前灾厄的十层档位计算。`11` 张多人专属卡牌完成静态范围审计。实例化 Power 卡仍保留待分析，不用普通层数 Power 冒充精确支持。

### 执行与 UI

- 每个玩家新回合自动后台搜索。
- “重新计算”：从当前实机状态发起新搜索。
- “执行本回合”：只执行当前回合推荐路线。
- 搜索期间主操作按钮为红色“停止计算”。停止会取消普通搜索或回合开始选牌搜索、关闭全自动，并暂停本场后续主动搜索；“重新计算”恢复。
- 绿色“全自动”按钮：连续搜索并执行每个回合；“死亡回合暂停”和“完整路线重算后战损增加时暂停”默认开启，“战斗结束回合暂停”默认关闭。
- 行动使用不会拆开中文卡名的胶囊流；完整显示怪物名称。
- 选牌信息位于对应卡牌胶囊内部；击杀动作使用整颗绿色胶囊。
- 每回合显示预计掉血、剩余费用、击杀和战斗结束。
- 本局扣血以无边框文字显示在“推荐路线”标题右端；常态界面不再显示主动卖血摘要。“状态详情”移到“方案就绪”状态行右端，详情内容仅在展开时占用高度。
- 覆盖层可拖动并按 Viewport 约束位置；标题栏去掉副标题，状态描述卡始终存在。路线窗口固定为三条未换行回合的高度，当前回合换行时优先完整显示当前回合。
- 顶部状态卡只保留一行 `14px` 原生粗体信息；底部只保留紧凑按钮行，战损栏与下分隔线之间不留伸缩空白。
- 自动部署时已完成动作变暗，当前动作以黄色高亮并显示执行序号。
- UI 的颜色、间距、圆角、字号和按钮样式集中为设计 Token；文字统一使用游戏当前语言字体，标题/指标/正文/辅助文字采用 `15/14/13/12px` 四级字号，不再出现难读的 `11px`，标题、回合标签、卡牌名和关键结果使用原生粗体。回合行拆成固定“回合 / 行动 / 结果”三列，单行按 `28px` 胶囊加上下各 `8px` 收束为 `44px`，三列内容统一垂直居中。行动胶囊按攻击、技能、能力、负面牌和药水使用克制的类型色。完整怪物名、胶囊内选牌和整颗击杀高亮沿用既定规则。
- 展开面板不再使用固定最小高度，按当前可见内容收紧；路线、状态详情或内部最小尺寸变化后执行两次延迟布局，先完成胶囊与底栏换行，再按新最小高度收紧底边，避免面板偶发多出一段空白。结果栏紧贴下分隔线，按钮区在分隔线下留 `8px`、面板底部保留 `12px`。
- 标题栏在“收起”左侧提供“设置”按钮。设置页可持久化三类全自动暂停、详细诊断日志、快搜/深搜时间、统一 Beam 宽度、节点上限、单节点出牌分支、牌堆/手牌选择分支、No-GC 预算（默认 `8 GB`）和 UI 位置；文件为 `user://combat_solver_settings.json`。性能预设为低、中、高、极高和自定义，复杂预算项带悬停说明。详细诊断默认关闭，只在下一轮新搜索中启用高频分支日志。数值框留空时不保存覆盖值，以灰色占位文字显示代码默认值；无效输入保持红色并阻止离开设置页。拖动只在松开时保存坐标，分辨率变化后仍按 Viewport 约束。
- 设置页提供“一键导出问题包”：按游戏问题包口径在后台收集最近 `8 MB` 日志、截图、当前档案和版本信息，并追加当前战斗完整续用状态、可读摘要、当前路线与求解器设置；ZIP 写入桌面 `CombatSolver-BugReports` 并打开所在目录，不自动上传。
- 面板拖动统一使用 Viewport 绝对鼠标坐标，避免标题栏局部坐标随面板移动而形成反馈漂移；移动帧只更新并约束现有矩形，完整响应式重排和位置持久化均在松手时执行。
- “收起”只隐藏方案摘要、路线和详情，保留重新计算、执行本回合、全自动三个操作按钮；紧凑状态隐藏“战斗结束时暂停”开关。
- 回合开始的强制选牌不再停在动态边界等待玩家。必备工具、暴政、熵和烘焙手套会进入 Beam 选牌分支，弃牌、耗尽或随机变换结果进入牌堆、RNG、状态指纹与跨回合续用校验；自动部署把同一组选牌保持到新回合结算完成，路线行显示选择来源与牌名。目录内效果若发起计划外战斗选牌会明确失败并停止自动执行，不再用运行时猜测掩盖搜索缺口。
- 战斗结束、胜利结算、放弃或退出时自动隐藏并取消后台任务。
- 后台模拟中的动态费用等预测变量可能触发从原卡牌克隆过来的 `CombatStateTracker` 事件。求解作用域会抑制这类伪状态通知，避免工作线程启动真实手牌 UI 刷新；正常游戏线程上的战斗状态通知不受影响。

## 3. 当前评分语义

最终路线不再只按线性 `Score` 选取，而是依次比较：玩家存活、确认胜利、整场预计掉血、药水数量、累计主动卖血、敌方剩余生命和行动数量。动态重搜或未支持边界不能冒充确认胜利。普通药水路线若能取得无药路线做不到的确认胜利可直接进入比较；否则仍须相对最佳无药路线每瓶至少省下 `9 HP`。龙涎香不适用胜利直通：无论药水模式如何，都必须相对最佳无药路线最终多保住至少 `ceil(最大生命 × 40%)`，同时使用的其他药水成本继续叠加。

Beam 中间排序与最终选择分离。稳健预设把 `1 HP` 约视为 `3` 点即时伤害，用于避免长线保血路线在到达终点前被即时输出挤掉；能力牌铺垫奖金只在当前回合内帮助展开，结束回合后清零，后续价值必须由模拟出来的 Power、牌序和伤害证明。未用能量不再单独扣分。普通出牌与药水共同竞争统一 Beam，快搜/深化默认宽度为 `24/60`；剪枝仍强制保留最佳无药、无药防御、无药进攻、无药策略、战术启动和具体药水身份代表，因此药水不会抹掉最终收益比较所需的无药反事实基线。

默认中档使用 `8` 秒短检查点和 `120` 秒总预算。短结果仍会新增战损、使用药水或没有确认胜利时，同一搜索会话继续深化；长预算是上限，搜索空间提前耗尽会立即返回。

扣血与主动卖血按整场战斗累计，不会在新回合归零。对同一个回合起始状态，搜索器先比较所有已经完整结算该回合、且没有停在动态或未支持边界的候选，以其中最低可达到战损为基线；某条路线超过该基线的实际掉血才算主动卖血。这样既包含少出防御，也包含放弃击杀、虚弱等其他可用减伤手段。直接击杀且未掉血不计卖血，根本防不住的伤害也不计卖血。主动卖血阈值为普通 `5`、精英 `10`、Boss `15`；阈值是整场战斗硬预算，搜索每完成一个回合就剪掉累计超额路线。

重要规则：战斗已经结束后，能力牌不再获得未来回合收益。`0.5.8` 曾因此错误选择第三回合先打能力牌再斩杀，而不是第二回合直接斩杀；`0.5.9` 已修正。

## 4. 代码结构

- `src/Search/`：搜索循环、计划与快照、选牌、模拟状态、预算和后台步调。
- `src/Prediction/`：怪物意图、牌与 Power 补偿、怪物行动效果和覆盖分类。
- `src/Engine/`：内置并独立命名的最小战斗模拟核心；只保留状态、牌堆、RNG、分叉、历史和战斗 Mirror，不含 RF 的入口、补丁、UI、设置、遥测与局外功能。
- `src/Runtime/`：Mod 入口、异步生命周期、状态文本、自动部署与结构化日志。
- `src/Testing/`：原版游戏进程中的一次性无人测试协议、场景执行器与进度隔离补丁。
- `src/UI/`：Godot 覆盖层 UI。
- `docs/`：持续开发笔记与外部审计材料。
- `tools/run-unattended-test.ps1`：直接启动使用独立用户目录、关闭 Steam 的 `--headless` 游戏进程，并等待同一 `runId` 的结构化结果；检测到玩家进程时拒绝启动。
- `tools/CoverageCatalog/` 与 `coverage/`：稳定编号的战斗钩子目录、分类和完整性检查。
- `local.props.example`：本机依赖路径配置示例；真实 `local.props` 不进入版本控制。

## 5. 性能现状

已完成：

- 搜索节点复用模拟快照。
- 节点去重与等价卡牌判断使用双 `ulong` 状态指纹，避免为每个状态创建长字符串；跨回合续用仍使用完整状态文本，不依赖指纹。
- 跨回合复用已选路线：逐项比较生命、格挡、费用、各牌堆、Power、怪物意图与相关 RNG 计数，不做哈希；完全一致时下一回合为 `0` 个新展开节点。
- 后台线程使用较低调度优先级；每 `16` 次回放检查一次连续工作时间，超过 `4 ms` 时调用 `Thread.Yield()`；搜索进度改用普通 `Label` 并限制为每 `200 ms` 最多刷新一次。
- 自动搜索至少延迟三个渲染帧，并等待游戏的回合开始动作队列执行完成，避免与发牌、状态结算的峰值直接重叠。
- 模拟状态中未使用的 Power/回合资源/怪物专项集合按需创建；状态剪枝、动作支配与 Top 队列避免构造 LINQ 中间分组。
- 怪物模型的反射数值访问器按类型和成员缓存为编译委托。
- 最终候选、战损、击杀和战斗结束标注不再重复完整回放。
- 合并等价重复卡牌与重复选牌组合。
- 选牌使用独立局部分支预算。
- 日志记录 `expanded`、`replays`、`snapshot_reuses`、`duplicate_cards_pruned`、`choice_branches`、后台线程分配量、每次回放平均分配、GC 次数、搜索区间 GC 暂停和线程让步次数。GC 次数与暂停是进程级区间数据，不能单独归因给求解线程。
- `0.7.6` 的真实跑局快照路线质量回归需要 `2542` 次完整回放、约 `5.99 GB` 工作线程分配和 `22.4 s`；结果正确但性能不合格。扩大 Beam 不是 GC 解决方案，下一阶段仍需 RF 提供可靠的模拟器分叉/深拷贝，或由本模组实现可分叉模拟内核。
- `0.8.0` 已在本地 RF fork 中加入完整模拟状态分叉 API：根状态只构造一次，每个搜索候选从父节点复制 RNG、牌堆、逐牌状态、Orb、生物、历史、RF StateStore 以及求解器自有 Power/遗物状态，再只执行当前动作。搜索节点改为父链保存单个动作，不再为每个分支复制整条动作列表。长线固定快照的 `2541` 个分叉逐一与旧完整前缀回放比较，状态文本、指纹、边界、风险和 RNG 全部一致；实机仍为第 `6` 回合结束、`0` 战损、`0` 药损。三次干净进程性能中位数为 `605,129,120 B / 2.394 s / 505.6 ms GC 暂停 / gc2=0`，相对 `0.7.6` 的 `5.99 GB / 22.4 s / 10.42 s GC 暂停` 分别降低约 `89.9% / 89.3% / 95.1%`，通过 `0.9 GB / 5.6 s / 暂停降低 80%` 门槛。原始三次指标和计算口径见 `docs/RF_FORK_PERFORMANCE_RESULTS.md`。
- `0.9.0` 在 RF fork 上继续加入卡牌预览写时复制、StateStore 按键惰性复制、共享历史前缀、求解器私有集合写时复制、原生 Hook 监听器分支缓存和无序列化 RNG 克隆。短搜/深化预算改为 `3/20` 秒，深化使用 `24+8` Beam、扩大选牌预算、帕累托路线和通用延迟收益探测；短结果有战损、用药或未确认胜利才触发。回合内行动上限删除，完全相同的无进展状态负责循环终止。最终长线差分比较 `2540` 个实际转移全部一致；8 张零费小刀在同回合全部执行并击杀。
- 用户在两轮最终独立进程性能样本通过后要求停止继续统计并定版。两轮均保持长线第 `6` 回合 `0/0` 和逐回合续用，中点为 `208,448,400 B / 1.550 s / 218.7 ms GC 暂停 / gc2=0`，约 `198.8 MiB`；相对 `0.8.0` 中位数，分配、耗时和 GC 暂停分别降低约 `65.5% / 35.3% / 56.7%`。详见 `docs/RF_FORK_PERFORMANCE_RESULTS.md`。
- `0.10.0` 将上述已验证的战斗分叉与写时复制实现移入 CombatSolver 自身程序集并改为 `CombatSolver.Engine` 命名空间；搜索热路径不再跨程序集调用 RF，也不再要求加载本地 fork。迁移不改变搜索预算、目标函数和分叉语义。
- `0.10.0` 的隔离 headless 回归中，完整长线在 RF 本地版共同加载时继续通过 `2540` 个增量/完整回放差分及第 `2-6` 回合精确续用；移除 RF 后，必备工具和熵分别完成无人选择、真实 RNG 与多回合续用闭环。`VerifyIncrementalSearch` 会刻意同时保留新旧两套回放，分配量不能当作生产性能样本。
- `0.10.1` 复现用户机甲骑士 `5s/60s` 长搜：实机日志为总分配 `6,008,388,896 B`、总耗时 `65.43 s`、GC 累计暂停 `46.94 s`，搜索后进程约 `3 GB`。隔离同状态基线为托管堆 `1.84 GB`、工作集 `3.08 GB`。根因有三层：无界转移缓存保存全部完整模拟快照；回合结果表强引用已被 Beam 淘汰的节点；每个模拟 CardModel 克隆都会进入 RitsuLib 的模型克隆通知和免费出牌弱表，令全局表容量随分支数膨胀。
- `0.10.1` 将转移缓存限制为短搜前 `128` 项并在深化阶段只读；回合结果随幸存节点保存；预测历史改为持久化分段；模拟模型使用等价的游戏原生深复制步骤但不广播消费者克隆通知；求解作用域内的 Ritsu 免费绑定查询改由内置费用状态负责。搜索期间使用 `SustainedLowLatency`，长搜后先触发非阻塞 Gen2，战斗 Reset 再对高碎片堆执行一次压缩。相同机甲骑士状态最终为 `16.61 s / 4,198,511,760 B / 5.12 s GC`，单次 GC 最大 `30.5 ms`，主线程最大帧间隔 `43.9 ms` 且无 `>50 ms` 帧；战斗结束压缩耗时 `145.5 ms`，最终托管堆 `110.8 MB`、碎片 `0.16 MB`、工作集约 `2.04 GB`。路线仍在第 `9` 回合胜利并逐回合精确复用。
- `0.11.0` 将短搜与深化改成一次单会话搜索：深化宽度从根状态开始运行，短预算成为进度检查点，未达到无药零战损确认胜利时原地继续，不再从根重复计算。最终结果在返回前压平为动作、标量摘要、续用戳和覆盖摘要，不再跨阶段或跨回合持有 `SearchNode`/模拟器链；跨阶段完整快照缓存改为不再参与生产搜索，卡牌指纹改为分支本地缓存，PredictionGap 按历史签名复用。牌堆有序指纹、Hook 监听器和关键词查询复用分支/线程本地状态，历史的按 Type 字典计数改为三个固定计数器；精确转置同时覆盖短搜并保存不互相支配标签。完整 RF/RitsuMetrics 栈 A/B 后采用 `SustainedLowLatency`，搜索中不主动强制 Gen2，只在战斗 Reset 回收；后台检测到 `>33 ms` 帧后等待一个主线程心跳。固定机甲骑士 headless 当前为第 `9` 回合、`0` 药、预计掉血 `40`，约 `11.7 s / 3.14 GB / 3.26 s GC`，单次 GC `27.7 ms`、无 `>50 ms` 帧；双小啃兽继续第 `6` 回合 `0/0` 并逐回合精确复用。实机带完整 Mod 组合仍必须以最新可见日志单独验收。
- `0.11.1` 根据 Steam 可见实机而不是 headless 定版。深化 Beam 从 `24+8` 收敛为经质量回归通过的 `22+7`；`20+6` 会把机甲路线从 `40` 战损退化到 `41`，已排除。长搜索按战斗维持可配置 No-GC 区域（默认 `6 GB`），初始化时预热，搜索结束不立即触发全量回收；战斗 Reset 后结束区域、压缩托管堆，并在 Windows 上仅当压缩后托管堆低于 `512 MB` 且工作集仍超过 `3 GB` 时清空陈旧工作集页。Steam 正常会话、完整用户 Mod 组合、默认 `6 GB` No-GC 下的固定机甲首轮为 `9.62 s / 2.32 GB` 求解线程分配、`0 ms` GC、最大帧间隔 `39.4 ms`、无 `>50 ms` 帧；路线为第 `7` 回合、`0` 药、预计掉血 `40`，第 `2-7` 回合精确复用。战斗结束后托管堆约 `359 MB`、工作集约 `2.78 GB`、私有提交约 `6.04 GB`。
- `0.11.2` 取消药水独立 Beam，改为快搜/深化统一 `12/30`；药水按 `18 HP` 等价值的稀缺资源机会成本参与中间排序，并只保留一个最佳药水安全候选，无药反事实路线继续由策略必保分支维护。开发中曾试删卖血硬剪枝：双小啃兽由 `0` 战损退化到 `6`；把统一深化宽度增至 `40` 虽恢复 `0/0`，却使 Steam 可见机甲首轮升到 `22.67 s`。因此最终恢复普通/精英/Boss `5/10/15` 的累计卖血硬剪枝，并把深化宽度定为 `30`。最终双小啃兽为 `2.25 s / 439 MB`、第 `6` 回合 `0/0`；Steam 可见机甲为 `9.57 s / 2.90 GB`、GC `0 ms`、最大帧间隔 `88.7 ms`、第 `6` 回合 `0` 药且预计掉血 `43`。这比旧分池路线多掉 `3 HP`，但仍满足 `<20 s` 与无 `>100 ms` 帧门槛。No-GC 在每场战斗首次搜索时总会尝试启动，预算进入持久化设置，默认 `6 GB`。修复噬尸蛞蝓同伴死亡后饥饿力量变化未完成生命周期结算便分叉导致的计算异常；补齐束缚锁链每回合已附魔牌数与已打出束缚牌状态的重置和指纹，女王首回合施加后可跨后续回合精确续用；自动部署在最后一颗行动胶囊变暗后等待一个渲染帧，避免必备工具回合开始选择衔接结束回合时吞掉末胶囊动画。
- `0.11.3` 按真实 `SEARCH_PHASE` 拆账优化长线，而没有扩大 No-GC 或收窄搜索。机甲旧基线每转移约 `217 KB`，其中 Fork 仅约 `17 KB`；真正的大头是回合推进和牌堆指纹。根因是回合末对全部约 `60` 张牌无条件调用 `MutablePreview.EndOfTurnCleanup()`，令没有瞬时状态的牌也完整深克隆，并使全部卡牌指纹失效。现在按原版四类瞬时字段精确判断，只对确实需要回合末清理的牌分叉；第一次用公开组合属性判断的实验反而升到 `3.85 GB / 13.05 s`，已撤回并改为无分配私有字段访问器。预测 History 的抽牌、生成、选牌和附魔事件改存紧凑不可变卡牌摘要，单牌牌堆移动、单目标伤害和空结果集合不再走批量容器。StateStore 改为 eager fork，使 ForkContext 不再逃逸保活父图；Context 的映射数组在 Fork 返回前清空并归还池。搜索展开后释放历史节点 Simulator，击杀改用敌人存活位图，跨回合续用戳只给幸存 frontier 捕获；最终候选会断言祖先模拟器均已释放。流式动作支配和单次 Beam 策略选取保持原比较顺序。固定机甲 headless 保持 `1453` 展开、`13338` 转移、第 `6` 回合、`0` 药和预计掉血 `43`，降至 `7.02 s / 1.73 GB / 0 ms GC`；双小啃兽保持第 `6` 回合 `0/0`，为约 `1.76-1.83 s / 368-380 MB`。两轮 Steam 可见实机为 `7.80-8.49 s / 1.806-1.818 GB / 0 ms GC`，最大帧间隔不超过 `23.6 ms`、无 `>33/50/100 ms` 帧；战斗 Reset 后托管堆约 `259 MB`、工作集约 `2.51-2.56 GB`。

仍可研究：

- 继续降低怪物攻击 Hook、玩家 Flush 和回合开始 Power 展开的分配；最新机甲拆账中它们分别约为 `283/120/147 MB`。
- 在完整模拟前增加不改变结果的等价分支识别。
- 基于真实日志调整 Beam 宽度、节点预算和选牌预算。
- 继续从可见 Steam 会话验证不同并行度的帧时间、CPU 占用和高分支收益，再决定是否调整默认并行度。

## 6. 已知限制

- 牌堆选牌为 Top-K 近似，不是穷举。
- 搜索不再因洗牌次数、空牌堆或固定回合数停止；极端无胜负循环最终由时间或节点预算终止。
- 多人牌、多人模式和局外效果仍不在本项目范围；游戏版本新增而目录尚未收录的行为必须重新审计。
- 部分动态怪物伤害仍为近似值。
- 当前 `0.111.0` 目录内单人战斗钩子已完成分类与证据闭环；游戏更新后的新增行为仍需重新生成目录和审计。
- 瀑布巨兽被提前击杀后的无限生命显示、蓄爆与爆炸自灭已加入模拟，并通过 `MONSTER-WATERFALL-001` 可见游戏场景验证；其普通行动中的动态伤害仍按现有近似策略标注。
- 状态键和等价卡牌键需要随着新实例状态的支持持续补充。
- 当前只支持单人战斗。

## 7. 未来构想：RitsuLib 路径预设

搜索预算和 UI 位置已经由模组自身 JSON 设置页实现。这里保留的未来项只指改变最终目标权重的“路线风格预设”；此功能尚未实现。

初步预设：

### 短期保血优先

- 提高近期生命与格挡权重。
- 降低主动卖血容忍度。
- 降低为了未来回合而提前打能力牌的收益。
- 适合普通战斗、低血量或只关心眼前一至两回合的情况。

### 长线能力优先

- 提高能力牌与持续状态在剩余已知回合内的价值。
- 允许在阈值内承受少量战损换取后续收益。
- 仍必须在终局清空未来收益，不能为了“已经不存在的后续回合”延迟斩杀。

### 均匀伤害优先

- 提高 AOE 和敌方总生命下降的权重。
- 降低对单个低血目标的过量伤害。
- 可能适用于需要同步压血或后续有群体斩杀的场景，但面对高威胁单体时未必合理。

### 减员优先

- 增加存活敌人数惩罚和提前击杀奖励。
- 优先消灭能行动、成长或造成高威胁的目标。
- 避免把伤害平均铺开却让所有敌人继续行动。

### 建议的配置结构

不要直接在运行时修改 `SolverWeights` 常量。更合适的结构是：

1. 定义不可变 `SolverProfile`，包含评分权重、卖血阈值、搜索预算、选牌预算和边界策略。
2. 通过 CombatSolver 自身的持久化设置保存 `PresetId`，并允许高级用户覆盖少量关键参数。
3. 每次发起搜索前在主线程捕获一份完整配置快照，后台线程只读取该快照。
4. 日志写入 `preset`、配置版本和所有覆盖项，保证结果可以复现。
5. UI 中显示当前预设，但不要让预设名称代替实际权重日志。

预设不是“难度档位”，而是不同优化目标。它们应共享同一套模拟规则，只改变评分、预算和剪枝偏好。

## 8. 其他未来方向

- 可选“选牌穷举模式”，同时明确显示预计组合数和成本。
- 展示 Top-N 条不同风格路线，而不只显示单一最优路线。
- 给每条路线显示关键取舍原因，例如“多掉 2 HP，提前击杀 1 个敌人”。
- 为怪物成长、召唤和死亡效果建立统一注册表。
- 增加可复现的离线战斗快照测试，减少完全依赖实机日志的回归成本。
- 对 UI 增加缩放配置。

## 9. 原版规则验证与无人闭环

- 开发流程通过 `tools/run-unattended-test.ps1` 直接启动塔 2 自带的 `--headless` 进程；它仍运行原版游戏程序集和动作队列，不是自建 CLI 或重写规则。
- 无人进程使用 `%LOCALAPPDATA%/CombatSolver/headless-runtime` 下的独立 `APPDATA/LOCALAPPDATA`，通过 `--force-steam=off` 避免 Steam 会话，并在隔离设置中显式允许 Mod 加载，绕过无界面的首次 Mod 警告确认。由于关闭 Steam 后游戏不扫描创意工坊，启动器只在 headless 生命周期内把当前游戏版本的 RitsuLib 投影到带所有权标记的本地临时目录，进程退出后删除；不会复制 RF 或其他创意工坊 Mod。脚本只复用 marker 明确记录的测试 PID；发现任何其他塔 2 进程时立即拒绝启动，不与玩家进程并发。
- 一次性请求先写入同目录临时文件，再原子发布为 `user://combat_solver_test_request.json`，避免游戏读取半写入或被占用的请求。Mod 建立 `shouldSave:false` 的单人跑局，进入指定遭遇，通过原版 `CreatureCmd`、`CardPileCmd` 和动作队列注入状态，再调用求解器全自动。
- 测试结果写入 `user://combat_solver_test_result.json`，必须以同一 `runId` 匹配；成功场景会清理跑局并回到主菜单，默认保留进程供下一场复用，只有最后一场显式要求时才退出。
- 无人测试期间跳过原版 `UpdateProgressAfterCombatWon`，避免把测试遭遇和击杀写入玩家进度。首轮验证在加入隔离前已经把一次毛绒伏地虫胜利写入当前 Modded Profile；这次历史修改无法在没有事前快照的情况下可靠回滚。
- 独立 headless 档案第一次进战斗时，原版“战斗基础”教学会创建缺少场景字段的无窗口节点并在 `NCombatRulesFtue.Start()` 空引用。无人请求活动期间将该纯 UI 教学的 `Create()` 直接返回空，与游戏自身的 `TestMode` 路径一致；普通玩家进程不受影响。
- `SMOKE-001` 和隔离后的 `SMOKE-002` 已在塔 2 `0.111.0`、RitsuLib `0.5.13`、RandomForeseer `0.13.7` 的可见游戏进程中通过：铁甲战士进入 `FUZZY_WURM_CRAWLER_WEAK`，敌人设为 `1 HP`，手牌注入 `STRIKE_IRONCLAD`，求解器搜索并真实自动打出打击结束战斗。
- `MONSTER-WATERFALL-001` 已通过两次，第二次带严格断言：给 `1 HP` 瀑布巨兽注入 `SteamEruptionPower:10`，求解器第一回合预测第二回合结束；实际依次执行 `ABOUT_TO_BLOW_MOVE`、`EXPLODE_MOVE` 并在第 `2` 回合结束，第二回合路线通过完整状态文本以 `0` 个新展开节点复用。
- Godot 在进程退出时仍报告 `RID/resources still in use at exit`；第二轮已消除首轮的重复搜索、脚本注册错误和第三方 UI 清理错误，退出资源统计暂不作为场景失败条件。
- 不对构建产物进行哈希验证；搜索内部允许使用双 64 位状态指纹。
- 每批实现先通过 Release 编译，再运行相关无人场景；无法自动表达的 UI、交互和长线策略仍进入人工待测清单。
- 同一版 DLL 中能共享场景的验证项必须合并为一次 headless 游戏进程和一份批量请求；项目之间重置必要战斗状态，不退出游戏。只有重新编译后需要让游戏重新加载 Mod 时才重启进程。
- 全自动场景可以在注入前清空当前手牌，并在战斗进行中锁存指定牌的真实完成记录；不能在战后历史已经清理后再推断该牌是否实际打出。
- 适配批次默认按同一机制族聚合约 `20` 个以上覆盖目录条目；只有真实依赖或测试隔离要求才拆成更小批次，避免每批只关闭少量条目。
- 版本号必须同时更新项目、Mod 清单和 `INIT` 日志。
- 自动与人工清单见 `docs/TEST_MATRIX.md`；完成语义适配时必须更新对应覆盖条目的 `testId` 和清单状态。
- 重点日志：`START`、`PLAYER`、各牌堆、`ENEMY`、`RESULT`、`TURN_OUTCOME`、`COVERAGE` 和 `ACTION`。
- 报告“搜索结果与实际不符”时，优先比较上一回合预测的状态与下一回合真实 `START` 状态。

## 10. 维护规则

后续改动至少同步以下内容：

- 新支持的牌、Power、怪物行为：更新“当前功能基线”和“已知限制”。
- 权重或剪枝变化：更新“当前评分语义”或“性能现状”。
- 新配置或预设：把“未来构想”移动到已实现章节，并记录默认值。
- 已复现并修复的错误：在下面追加一条简短记录，写清原因，而不只写“已修复”。

## 11. 变更记录

- `0.13.8`：补齐 `ShrinkPower.AfterApplied` 的施加者名称状态，并把已实现的缩小施加者死亡移除语义登记为运行时补偿。缩小甲虫受控战斗第 `2-5` 回合全部精确复用，非预期重算与未补偿项均为 `0`。

- `0.13.7`：修复 Vigor 攻击分叉残留、旧层数覆盖新层数和万向斩漏掉原版组合攻击生命周期；同源回声斩一并接入。幻象复活后继改读原版 Power/状态机来源，避免 `REVIVE_MOVE → REVIVE_MOVE`。Obscura 用户存档完整自动战斗第 `10` 回合结束且非预期重算为 `0`。龙涎香新增相对最佳无药路线至少保住最大生命 `40%` 的硬门槛，多药成本继续叠加。

- `0.13.2`：修复充能球回合末双重触发和生成牌晚于结果牌堆的通用时序错误；花园幽灵鳗固定场景第 `2-9` 回合全部复用且非预期重算为 `0`。重算原因、完整差异和战斗汇总进入日志/问题包；差分开始硬比较有序牌堆、充能球数值与完整续用状态。新增主动 Hook 运行证据门禁和机器欠账，审计过程中补齐旋转机 Power、拆解双击、先导打击生成小刀及 Maul 双击/全牌成长。

- `0.13.1`：修复储君 4 HP 墨宝战把致死路线误报为零战损。根因不是墨宝或下砸：涟漪盆 `BeforeSideTurnEnd` Mirror 在模拟未来回合时读取实机当前回合 History；首轮搜索期间实机尚未出攻击牌，导致每个未来回合凭空获得 `4` 格挡。Mirror 改为读取分支自己的攻击牌计数后，同构牌序会在第 `2` 回合补打一张防御，`b5189bd32ece46ecb7093caadba5477f` 第 `2/3` 回合均精确复用、零战损并第 `3` 回合结束。原有“死亡回合暂停”和“重算后战损增加暂停”都只读取搜索结果；旧错误把该回合标成安全，玩家又在敌方回合直接死亡，因此没有下个玩家回合触发重算。部署器现于全自动提交结束回合前，从执行后的实机状态重新演算当前怪物行动；实机预计掉血高于路线或将死亡时，关闭全自动并保留玩家回合。回归 `6ad80b16f8b14a479ddf2803cdecc02a` 将第 `2` 回合计划产生的 `5` 格挡测试性清零，复核发现路线预计 `0`、实机 `4` 且致死，未提交结束回合。
- `0.13.0`（同族小队、旧日雕像与诊断收尾）：修复最终候选和预算中断候选提前释放模拟快照、候选淘汰未及时释放、无药基线为空时崩溃等生命周期问题；攻击药水等生成牌药水进入精确选项分支，单卡捞取枚举全部不同结果并保留跨回合选择身份，宇宙冷漠、碎屑清理和迫降前后手牌差异进入路线状态。补齐星界脉冲双段伤害、永久移除怪物后的残留 Power 清理与 `AmountOnTurnStart` 语义归一。同族小队精确跑局 `65ec62d2e2f84e7685874f730d4849b5` 在第 `14` 回合用两瓶药结束，第 `2-14` 回合全部精确复用；骑士团伙 `fcc9d9f0d589462b86a4f55306a96105`、金纸洗牌 `fde89a871a4f496bb795566b75cdeeb8` 与攻击药选牌 `f972054ac47542b6bc8e987dbaf5282c` 通过。用户旧日雕像日志加载的是 `0.12.5`，首轮把受弱化与力量影响的攻击少算，导致第三回合开始生命差异和连续重算；当前 `0.13.0` 同牌序回归 `14eb7f26d040422b9974b235b563d12d` 从第 `2-9` 回合全部精确复用，旧版 `11 HP` 是错误预估，不作为质量基准。新增默认开启的“完整路线重算后战损增加时暂停”：回归 `fa8b26ada7e44a6583e18e1ca8e36fca` 人为制造生命漂移后从 `32` 升到 `38 HP`，界面标记并在第 `2` 回合暂停。详细诊断日志改为默认关闭的持久化开关；问题包在后台将日志、档案与当前战斗/路线/设置打包到桌面，回归 `b355b76a59b54eb3b3354f4a7dd59dea` 验证 ZIP 内容完整。
- `0.13.0`：移除单人战斗搜索中的固定 `16` 回合、两次洗牌和空牌堆终止条件，视野改由时间/节点预算与确认胜负约束。模拟器新增分支自有阵容、稳定 CombatId、怪物 AI 状态日志与 `MonsterAi/Niche` RNG，完整处理召唤、替换、逃跑、死亡、复活、额外回合和动态目标；环境组装师、Living Fog、Fogmog、Obscura、Ovicopter/Tough Egg、双尾鼠、实验体、幻象、百足虫、寄生、库存和意外等完成原生差分或正常回合复用。回合开始随机生成与选择统一进入分支，修正 `AmountOnTurnStart`、Call of the Void/Creative AI 单张抽取 RNG、History Course Dupe 去向；必备工具、暴政、既定事项、熵、Mayhem、Pael's Eye、Ambergris 均在第 `2` 回合精确复用。药水槽升级为完整分支状态，Entropic Brew、Sozu 与 Belt Buckle 的槽位、Power 和 RNG 差分通过。覆盖目录 `3035` 项的未分析、待实现、缺证据和 `NativeAutoRescan` 全部归零，新增 `--verify-no-rescan` 门禁。修复召唤槽位误按字符串排序、环境组装师重算未导入 `_lastSpawned`、佣兵偷钱未扣模拟金币、复活怪被误判胜利、逃跑未清 Power，以及搜索线程把共享实机 Creature 的 `CombatState` 写为 null。双小啃兽普通搜索 `1.957s / 360.7MB`，第 `6` 回合、两次洗牌、`0` 药、`0` 战损并第 `2-6` 回合精确复用；增量/完整前缀差分通过。机甲固定基准 headless 为 `8.207s / 2.212GB`，Steam 正常可见完整 Mod 栈为 `9.208s / 2.291GB`，均第 `8` 回合、预计战损 `31`；可见会话 GC `0ms`、最大帧间隔 `11.0ms`。默认 `6GB` No-GC 总预算按 `4GB SOH + 2GB LOH` 分配，避免任一子堆提前耗尽。
- `0.12.5`（历史记录，现已被下一版本通用机制取代）：当时为阻止回手零费牌耗尽单回合节点，引入了固定 `16` 次重复窗口和少数已知兑现方式的命名放宽；同构回归从 `1200` 节点、`2` 回合、`NodeLimit` 收缩到 `619` 节点并找到第 `3` 回合零损胜利。该方案后来证明会漏掉隐藏相位、卖血/烧牌启动和未知内容组合，因此撤回，不再代表当前生产设计；现在使用上文的控制形状、精确动作相位、通用收益向量和有界出口/跨回合探测，历史 runId 仅保留为问题来源，不作为现行算法说明。
- `0.12.4`：审计外部试玩的 `93,565,475` 字节、`751,272` 行 `godot.log`。乐加维林族母睡眠阶段没有逐回合重算：原日志第 `2/3` 回合均 `SEARCH_REUSED`，新无人回归亦命中第 `2` 回合；攻击唤醒时的同回合 `DynamicResolution` 是睡眠 Power、行动状态和私有 AI 切换后的保守重搜。真正的求解器异常是第 `3` 回合计划牌 `BEAT_INTO_SHAPE` 被原版 `TryManualPlay` 拒绝后直接终止。部署器现在记录 `target_valid`、原版 `UnplayableReason`、阻止模型、能量/星能与费用，清除旧续用并从已执行后的真实状态自动重搜；手动执行会继续完成本回合，全自动仍经过死亡/结束暂停策略。搜索根节点额外调用实际卡牌的 `CanPlayTargeting`，保证重搜不会重复采用同一个实机非法动作，深层节点仍使用 Fork Mirror。`BEAT_INTO_SHAPE` 正常出牌与睡眠复用均通过无人实机。超大日志另有独立外因：`AncientWaifus 0.2.3` 在全局输入钩子调用当前游戏已删除的三参数 `MegaAnimationState.SetAnimation`，产生约 `41,467` 次 `MissingMethodException` 和 `41,211` 次日志器二次异常；CombatSolver 仅 `1,845` 行，无法从本模组侧修复该外部程序集。
- `0.12.3`：性能配置由散列数值重构为低档、中档、高档和自定义四种模式。低档固定 `2/20s、10/24 Beam、1000/4000 节点、8/12 出牌分支、4GB No-GC`；默认中档固定 `5/60s、12/30、1200/6000、10/16、6GB`；高档固定 `8/120s、18/45、2400/12000、14/24、8GB`。选择固定预设会一次写入整套参数，修改任一时间、Beam、节点、出牌分支或 No-GC 数值会切换到自定义；旧设置未记录预设时按解析后的完整参数自动归入匹配档位，否则保留为自定义。恢复默认现在采用中档，死亡回合暂停默认开启，战斗结束暂停默认关闭。击杀动作胶囊维持绿色边框、背景和文字，并将 `击杀` 扩展为 `击杀：目标名`，多目标按实际击杀列表展示。隔离游戏无设置文件启动日志确认默认 `Medium/5s/60s/6GB/结束暂停关/死亡暂停开`；低档和高档分别由无人实机断言全部固定参数并首回合结束战斗。
- `0.12.2`：知识恶魔“知识的诅咒”不再用固定低伤害兜底或原生结算后重搜；搜索按私有诅咒计数构造两个候选，分别施加瓦解、腐化心智、懒惰或日渐衰弱的持久 Power，并把选择写入结束回合计划交给原生界面执行。未来行动预测同步推进三次诅咒计数，第三次后的 AI 循环不再误判。第一、二幕 Boss 与第三幕第二 Boss 标记为战后回血战斗：保持死亡硬惩罚，但降低剩余生命/卖血权重、把卖血预算放宽至当前生命减一，并让无药确认胜利优先；第三幕第一 Boss 不启用。新增持久化“死亡回合时暂停”，当前路线预计本回合死亡时全自动停止并交还操作权。主界面删除全自动右侧重复暂停开关；“方案就绪”胶囊取消固定宽度，删除“计划 x 个动作”；设置页拆分为基础执行区与仅含快搜/深搜的四行搜索表格。知识恶魔搜索选择、二幕 Boss、三幕首/第二 Boss、普通战斗、死亡回合标记与暂停均由无人实机回归通过。
- `0.12.1`：修复合法重复药水使 `SolverDisplayNames.ToDictionary` 抛重复键、首回合 UI 与搜索一同消失的问题；药水显示名按类型去重，实际计划仍按槽位区分，搜索初始化异常现在进入面板和 `SEARCH_SETUP_FAILURE`。胜利快照不再计算敌方意图，结果新增“当前搜索未找到生还路线”；UI 在累计扣血左侧显示该状态，搜索中状态/回合/秒计时/详情使用固定列。自动执行新增持久化的跟随游戏/正常/快速/瞬间档位与 `0–3s` 牌间停顿，所有完成、取消、错误和动态重搜路径恢复原游戏速度；无人测试的速度覆盖只写内存并在 `finally` 恢复，不污染隔离或用户设置文件。最终路线只额外精确回放一次并记录真正生效的遗物来源，胶囊可显示 `钢笔尖×2`、音乐盒复制等金色后缀，不给 Beam 每个节点增加标注集合。钢笔尖、百年积木、金纸、铁棍、双节棍、音叉、永冻土、投掷斧、燃烧木棍、节拍器、蜥蜴尾巴和臂甲等跨动作状态统一进入搜索指纹与续用文本；覆盖工具反射检查专用持久遗物状态，当前缺口 `0`。实机新增故障中，原版在唯一候选时绕过 selector，部署器现只在虚无、抽牌堆顶等可观察结果确实符合计划时记录 `DEPLOY_CHOICE_IMPLICIT`；雕琢打击与宇宙冷漠均已完成隐式核销和后续精确复用；知识恶魔不可跳过但传 `minSelect=0` 的界面强制选一张并按低伤害顺序首选腐化心智；感染棱柱活力火花改为按其真实“敌方 Power 污染玩家技能牌”语义保持并同步逐牌污染，不再错误按同阵营清除；同时删除旧生命周期层与统一 AfterCardPlayed Mirror 的重复补偿，打出污染牌不再把 `2` 层错误施加为 `4` 层。验证通过重复药水女王、雕琢打击第 `2` 回合复用、知识恶魔 `MIND_ROT_POWER`、棱柱两步完整差分、钢笔尖第十击、百年积木、金纸、仅死亡路线与速度恢复。双小啃兽为 `2.288s / 386MB`、第 `6` 回合 `0/0`；机甲 headless 为 `9.820s / 2.266GB`，Steam 可见完整 Mod 栈为 `9.818s / 2.350GB / 0ms GC / 8.4ms 最大帧间隔`，保持第 `8` 回合、预计战损 `31`、`0` 药和第 `3` 回合复用。
- `0.12.0`：重新审计“目录已分类但实际未完全适配”的差距。覆盖目录升级为有效支持状态，区分 `Exact`、`NativeAutoRescan` 与 `OutOfScope`，并对 `43` 张含 `CalculatedVar` 的卡牌强制分支公式登记；`--verify-effective` 当前为 `0` 缺口。引擎新增严格 `EffectRecipe`、参数化卡牌效果、生成三选一、五类手牌选择、嵌套自动选择与原生结算后重搜。补齐击杀奖励、Osty、永久牌面成长、最大生命、Power/遗物/附魔触发、毁灭处决、眩晕边界和一次性 Power 同步。修复选择后抽牌的来源牌过早进入弃牌堆、可能参与同次洗牌的顺序错误。真实游戏差分最终通过 `46 + 32 + 25 + 13 + 11 + 6 + 4` 项；双小啃兽固定长线保持第 `6` 回合、两次洗牌、`0` 药、`0` 战损、`0` 卖血并在第 `3` 回合精确复用，首轮 `1880.2 ms`、Unmirrored=`0`。Steam 可见机甲因精确 Mirage/历史状态找到预计战损 `31`（旧版 `43`）的更长第 `8` 回合路线，搜索空间从 `13338` 增至 `16812` 个转移；新实机门槛按该更高质量搜索调整为 `2.6 GB`，最终为 `14.736 s / 2.438 GB`、GC `0 ms`、最大帧间隔 `42.5 ms`、无 `>50 ms` 帧。
- `0.11.3`：按实测拆账修复回合末无变化卡牌仍被全量深克隆的问题；预测历史改用紧凑卡牌摘要，单牌牌堆与单目标伤害使用无批量容器路径，StateStore eager fork 后立即释放并池化 ForkContext 映射。搜索中的祖先快照在展开或淘汰后释放 Simulator，击杀标注改用存活位图，续用戳在幸存回合 frontier 捕获；最终候选断言不存在历史 Simulator 保活。流式支配和 Beam 必保路线选择移除重复 LINQ 排序，不改变 `12/30` Beam、权重、卖血或药水政策。固定机甲 headless 为 `1.73 GB / 7.02 s`；两轮 Steam 可见实机为 `1.806-1.818 GB / 7.80-8.49 s`、GC `0 ms`、最大帧间隔不超过 `23.6 ms`，保持第 `6` 回合、`0` 药、预计掉血 `43`。
- `0.11.2`：取消药水独立 Beam，改为统一 `12/30`，恢复普通/精英/Boss `5/10/15` 累计卖血硬剪枝；修复噬尸蛞蝓死亡力量结算、束缚锁链跨回合状态和必备工具末胶囊动画。双小啃兽保持第 `6` 回合 `0/0`，Steam 可见机甲为 `9.57 s / 2.90 GB`、第 `6` 回合、`0` 药、预计掉血 `43`。
- `0.11.1`：以 Steam 可见游戏进程完成机甲长线严格跑分。默认深化 Beam 改为 `22+7`，保留 `0` 药、`40` 战损并将路线提前到第 `7` 回合；长搜索使用跨整场战斗的预热 No-GC 区域，避免搜索结束时立刻产生一次数百毫秒全量回收。战斗退出后统一结束 No-GC、压缩托管堆并有条件回收 Windows 陈旧工作集页。默认 `6 GB` 下可见实机首轮 `9.62 s`、GC `0 ms`、最大帧间隔 `39.4 ms`、`>50/100 ms` 均为 `0`。
- `0.11.0`：针对实机长线搜索重构生命周期、搜索调度和热点数据。短搜/深化合并为单次 anytime Beam，消除机甲骑士重复根搜索；结果对象不再保留模拟器节点链，风险与指纹缓存不再成为全阶段强引用。牌堆指纹、Hook 列表、关键词集合、遗物读取和历史计数减少热路径分配；转置表保存精确状态的非支配标签。完整 RF/RitsuMetrics 栈 A/B 后使用 `SustainedLowLatency`，慢帧触发一次主线程心跳让步，日志新增 p95/p99 帧间隔和主动等待时间；战斗进行中不再强制回收，战斗 Reset 后统一压缩。headless 机甲基准从 `17.35 s / 4.20 GB` 降至约 `11.7 s / 3.14 GB`，路线仍为第 `9` 回合、`0` 药、预计掉血 `40`；双小啃兽仍为第 `6` 回合 `0/0`。
- `0.10.1`：修复长搜索的内存高水位与 GC 主线程冻结。限制跨阶段转移快照缓存，消除已剪枝节点的回合结果强引用，将预测历史改为共享分段；模拟模型克隆绕过 RitsuLib 消费者通知，并在求解作用域内隔离 Ritsu 免费出牌全局状态，避免数万个临时卡牌把弱表扩到数百 MB。搜索采用持续低延迟 GC，结果后异步回收，战斗退出时只在高碎片情况下压缩。机甲骑士 `5s/60s`、子弹时间费用差分、长线增量/完整回放及第 `9` 回合全自动闭环通过；最终单次 GC 最大 `30.5 ms`、主线程最大帧间隔 `43.9 ms`、托管堆回落到约 `111 MB`。
- `0.10.0`：将本地 RF fork 中已经过增量/完整回放差分和性能回归的战斗模拟核心移入 CombatSolver，统一改名为 `CombatSolver.Engine`。只内置预测状态、牌堆、RNG、分叉、历史和战斗 Mirror，删除 RF 入口、Harmony 补丁、UI、HoverTip、设置、遥测、集成与局外预测；项目引用、Mod 依赖、启动校验和构建链均不再要求 RandomForeseer。覆盖目录改为直接检查内置 Mirror。无人测试启动器改为独立用户目录的原版 `--headless` 进程，关闭 Steam、只复用测试 marker PID，并在玩家进程存在时拒绝启动；隔离档案的首次 Mod 警告和纯 UI 战斗教学由无人模式显式跳过。完整长线在 RF 本地版共同加载时通过，移除 RF 后烟雾、必备工具和熵回归继续通过；本地 RF 已移到工作区归档。创意工坊原版当前未订阅，因此其实包共存仍需用户订阅后做最后启动确认。
- `0.9.4`：修复收起后操作按钮消失，紧凑界面保留重新计算、执行本回合和全自动。将必备工具、暴政、熵、烘焙手套的回合开始选牌纳入搜索分支和自动部署，消除新回合抽牌后的玩家选牌干预；选择结果参与牌堆、RNG 和续用校验，计划外动态选牌也由部署守卫自动决策并触发重搜。修补斗篷与匕首生成小刀、闪躲翻滚下回合格挡，并把最新日志中已实际补偿的致命毒药、带毒刺击、狂乱逃离、萎靡、早有准备和压制登记为已补偿，避免伪“未镜像”提示。
- `0.9.3`：压缩求解器结果区。本局扣血改为“推荐路线”标题右端的无边框文字，删除常驻主动卖血摘要；“状态详情”按钮移入顶部方案状态行，详情面板只在展开时出现。由此将路线滚动视口从 `96px` 扩到 `148px`，常态可同时显示三条未换行回合路线。拖动改用 Viewport 绝对坐标，并移除移动过程中的完整布局重算，修复不跟手和反馈漂移。
- `0.9.2`：修复猎人杀手 `TENDERIZING_GOOP_MOVE` 施加 Tender 后搜索崩溃。旧顺序先清空卡牌 Power 变化，再执行历史补偿；Tender 补偿随后追加力量/敏捷变化却没有再次清空，下一层 Fork 因 pending Power 队列拒绝。历史补偿现在循环处理新增历史并清空 Power 变化，直到动作边界稳定；卡牌、药水、抽牌和 Orb 入口共享该保证。猎人杀手 + 1 层 Tender + 8 张零费小刀场景在增量/完整回放差分下规划并真实执行 3 张后首回合击杀；主长线差分与第 `6` 回合 `0/0` 续用回归通过。
- `0.9.1`：标题栏新增“设置/返回”按钮和独立滚动设置页。用户可覆盖快搜/深搜时间、两类 Beam、节点、单节点分支和选牌预算，并持久化“战斗结束时暂停”与 UI 坐标。空输入使用灰色默认占位并清除覆盖值；非法数字显示错误且不关闭设置页。配置在主线程捕获为不可变搜索快照，进行中的搜索不受中途修改影响。自定义文件跨进程加载、位置恢复、文件哈希不被程序同步误写和无配置文件 smoke 均通过。
- `0.9.0`：RF 模拟分叉改用卡牌预览写时复制、StateStore 惰性复制、共享历史前缀、求解器集合写时复制、Hook 监听器缓存和直接 RNG 状态克隆；加入按阶段耗时/分配诊断。搜索改为 `3 秒`短搜与独立 `20 秒`深化，触发条件为未来掉血、用药或未确认胜利；深化使用 `24+8` Beam、Top-16、扩大选牌预算、精确状态置换、帕累托多目标保留和通用结束回合探测。删除回合内 `7` 次行动上限，以费用、可出牌性、击杀、完全状态循环、节点和时间预算终止；8 张零费小刀同回合击杀闭环通过。最终差分 `2540` 个转移全部与完整回放一致，长线仍为第 `6` 回合 `0` 战损、`0` 药损并逐回合精确续用。两轮最终性能样本中点为 `198.8 MiB / 1.550 s / 218.7 ms GC / gc2=0`；用户随后要求停止继续统计并定版。
- `0.8.0`：CombatSolver 改为依赖本机 `StS2.RandomForeseer` fork，启动时强制校验 fork API `1` 与上游提交标识，不再接受普通创意工坊 DLL。RF 分叉显式复制全部预测对象图，StateStore 可变值通过泛型约束强制声明复制语义；活动中的卡牌、攻击或延迟历史结算禁止分叉。Beam 搜索由“每个候选从战斗开头完整回放前缀”改为“父快照分叉后执行单个动作”，并将节点动作改为父链。首次差分发现求解器保存的脱离牌堆卡牌没有 RF 映射，改为显式克隆后，长线场景 `2541` 个分叉全部与旧回放一致。创意工坊版本已取消订阅，游戏只加载本地 fork；三次性能中位数为 `605.1 MB / 2.394 s`，GC 暂停 `505.6 ms` 且无 Gen2。三条卖血策略、跨两次洗牌续用和瀑布巨兽两回合结束回归全部通过。
- `0.7.8`：顶部状态卡压为单行，状态、回合上下文和方案摘要统一使用更大的 `14px` 原生粗体；搜索进度在同一文字行内切换。重新计算与执行按钮只保留按钮文字，状态详情移除说明数量，只保留按钮文字和展开箭头。路线标题只显示“推荐路线”，删除右侧搜索回合副标题；路线滚动框由缓存前三回合改为缓存并展示求解所得的全部回合，最多 `16` 行，视口高度仍为两行。每个非结束及结束回合都固定显示剩余费用，包括“余 0 费”。全自动开启时恢复绿色正向按钮底色，关闭时仍为暗色次级按钮。Release 构建零警告、零错误并已部署；最终视觉仍由玩家实机验收。
- `0.7.7`：统一给普通文本、富文本、按钮和 CheckButton 加入 `2px` 深墨色描边，解决半透明暗色面板上的小字发灰发虚。状态概览移除重复的“自动搜索已开启”第三行，最小高度从 `88` 收紧到 `64px`，置信度文案只保留高/中/低，不再向玩家展示 RNG 建模黑话。右上角收起按钮改为透明轻量胶囊，仅悬停显示底色；行动目标使用与正文一致的 `13px` 高对比浅色文字和独立箭头。模型说明数量并入带展开箭头的状态详情按钮。界面移除用于拼接信息的中圆点：状态区拆成状态 Badge 与回合上下文，概览指标改用细竖分隔，路线标题拆成主标题和括号副标题，行尾 HP 与剩余费用使用固定双列。执行按钮保持唯一蓝色主操作；全自动改为暗色状态胶囊，运行中只将文字变绿，不再使用整块绿色与主按钮抢焦点。Release 构建零警告；最终视觉由玩家实机验收，不登记为已完成视觉闭环。
- `0.7.6`：最终路线改为按生存、确认胜利、整场战损、药水和主动卖血分层选择；Beam 改为无药 `9`、药水 `3` 的独立配额，并保留通用保血、进攻、成长/资源/控制分支。稳健中间排序将生命权重提高到即时伤害的三倍，能力牌铺垫只在当前回合生效，取消未用能量惩罚。修复 RF 在交互选牌返回前就把来源牌放入结果牌堆造成的顺序错误：生存者丢弃的牌现在先于生存者本体进入弃牌堆；跨第一次洗牌后的第 `3` 回合由 `SEARCH_REUSE_MISS` 变为精确复用。续用失败日志新增首个不同字段和值，并忽略已从实机列表移除的死亡敌人。无人测试可从只读跑局快照恢复完整牌组、遗物和全部 RNG；种子 `BJCZX3J13PZJ` 的 `19` 张牌双小啃兽场景首轮找到第 `6` 回合结束的 `0` 战损、`0` 药损路线，第 `2-6` 回合全部零节点复用。原先要求固定卖血 `4` 的夹具在新目标下改为验证“可直接击杀威胁时不故意卖血”；另两条硬预算与不可避免伤害回归保持通过。Release 与完整可见游戏闭环通过；该场景首轮性能为 `2542 replays / 5.99 GB / 22.4 s`，性能优化仍未完成。
- `0.7.5`：针对需要“低压回合铺蛇咬、高压回合全力防守”的长线，暂时把普通可预测洗牌上限从 `1` 提高到 `2`，第三次洗牌前停止。Beam 宽度、节点预算和评分保持不变，用于单独验证原先漏解是否主要来自搜索视野不足；本项只完成 Release 构建，路线质量等待同一实机战斗手动复测，不登记为已通过策略闭环。
- `0.7.4`：修复高输出节点在回合内提前挤掉连续防御节点，导致卖血基线把可避免伤害误判为不得不掉血的问题。Beam 现在额外保留当前威胁下预计生命最高的防御分支；两只小啃兽合计来袭 `22`、玩家 `3` 敏捷且手中三张防御的固定场景最终选择 `24` 格挡、`0` 掉血、`0/5` 卖血。后空翻已核对原版 `CardsVar(2)` 与 RF 无条件抽牌推断路径，搜索路线也能继续使用其抽到的牌。生成牌显示名改为从全卡池按当前语言缓存各升级等级标题，实机中的 `SHIV` 已显示为官方简中“小刀”。UI 统一游戏原生常规/粗体字体层级，最小字号提高到 `12px`，修正胶囊、回合与结果文字的垂直对齐。Release 构建及两条可见游戏无人回归通过，最终 UI 观感仍由玩家实机验收。
- `0.7.3`：补齐中和的弱化与墨影附魔的弱化模拟，并把后空翻、墨之刃、灵动步法、中和、毒性爆发、穿透哀嚎、小刀、生存者及紧缠死亡移除登记为求解器已补偿，消除已适配效果仍显示“未镜像”的误报。中和与墨影完成原生动作/预测状态差分，均精确施加 `1` 层虚弱。药水路线加入反事实战损门槛：与同批最佳不使用药水路线比较，每瓶药必须至少少掉 `9 HP`，多瓶按瓶数累加门槛，否则不允许入选；卖血基线按本回合消耗的药水槽位隔离，避免“保留药水”被误算为主动卖血。低收益火焰药水被拒绝，高收益格挡药水真实省下 `12 HP` 并由全自动使用。搜索范围从首次洗牌前延长为最多跨过一次洗牌，第二次洗牌前停止；双 64 位状态指纹加入七组战斗 RNG 游标。固定牌序实机从第 `1` 回合搜索到第 `3` 回合并跨过一次洗牌，第 `2/3` 回合均以完整状态文本零节点复用，第三回合按预测结束战斗。UI 高度改为跟随最小尺寸变化执行两次延迟收敛，修复换行完成前读取旧高度造成的偶发底部空段；Release 构建与创建日志通过，最终视觉仍由玩家实机验收。
- `0.7.2`：按 UI/UX 审计拆出设计 Token、动作胶囊和回合行组件；统一中性深色层级、紧凑字号与按钮规格，路线改为稳定三列布局。单行回合收束为 `44px` 并将胶囊垂直居中，路线窗口保持两条单行高度；面板按当前可见内容计算展开高度，并在路线或详情显隐后的下一帧重排，结果栏继续紧贴分隔线，只在分隔线与按钮之间保留 `8px`。修复蛇形附魔预测费用变化引发的闪退：预测卡变量事件会从工作线程泄漏到真实 `CombatStateTracker`，继而在非主线程刷新 `NCard` 字体对象；现仅在求解作用域抑制这类伪通知。相同蛇形附魔静默猎手场景修复前出现多次 `sd is null` 并退出，修复后完整运行 `7` 回合，日志连续记录 `SIMULATION_NOTIFICATION_SUPPRESSED caller=OnCardValueChanged`，无字体服务错误并返回 `Passed`。UI 已通过 Release 构建；本轮最终视觉排版仍由玩家实机验收。
- `0.7.1`：重写主动卖血判定。旧逻辑只在已展开节点中记录最高格挡，并到最终选路时用软惩罚处理阈值，胜利奖励可以压过惩罚；跨回合复用还会把已发生回合的逐回合卖血记录过滤掉。现改为同一起始状态下“路线实际掉血 - 最低可达到掉血”，在每个完整回合边界携带累计卖血，并把普通/精英/Boss 的 `5/10/15` 设为整场硬预算。固定牌组完成三条真实可见游戏闭环：主动卖血 `4/5`、有防御路线 `0/5` 且剪掉超额分支、无防御实际掉血但卖血为 `0`；跨回合精确复用后历史 `turn=1 sold_hp=4` 保持不变。
- `0.7.0`：完成游戏 `0.111.0`、RandomForeseer `0.13.8` 的单人战斗钩子全量分类，机器目录共 `3035` 项且未分析与待实现均为 `0`；求解器补偿、RF 推断核对、搜索策略边界和原生状态均纳入逐项记录。手工维护的独立闭环共 `1091` 项，其中 `897` 项完成真实可见游戏闭环、`194` 项完成源码与构建静态闭环。最终 Release 实机回归通过瀑布巨人阶段转换、严格第 `2` 回合结束、跨回合零计算复用和性能基线，首轮为 `116 replays / 74.65MB`，复用轮为 `0 replays / 0 bytes / 0ms`。
- `0.6.3`（适配批 058）：关闭最后 `8` 个未分析遗物条目。损毁头盔与不安油灯 `4` 个 Power 数值钩子在同一可见游戏 PID 完成两场完整差分，覆盖一场一次状态、同牌多减益和临时 Power 内部防重复翻倍；宾邦、五轮书、黑石护符和招财异鱼 `4` 个永久牌组钩子完成 `PileType.Deck` 全调用点静态审计。覆盖目录达到 `3035` 项、`0` 项未分析。
- `0.6.3`（适配批 057）：关闭 `38` 个未分析遗物条目，其中 `21` 项以最终 Release DLL 在同一真实可见游戏进程完成十一份场景，`17` 项完成源码、搜索边界与构建静态闭环。补齐药水响应、格挡清空、手牌清空、星能、回合结束、空手抽牌、伤害倍率、遗物私有状态及充能球生命周期；额外回合、自动出牌和选牌走明确动态边界。差分定位并修复生产搜索遗漏普通充能球回合末被动、金纸时点、情感芯片历史滚动等问题。覆盖目录未分析从 `46` 降至 `8`。
- `0.6.3`（适配批 056）：关闭 `24` 个未分析遗物回合生命周期条目，并纠正苦无、手里剑、彩虹戒指和佩尔之泪的 `4` 个 RF 风险/ignored 假精确条目。`26` 项在真实可见游戏中完成八个最终请求、十五条完整状态差分，覆盖 RF 私有计数重置、攻击/技能/能力触发、金币与星能、跨回合能量、奥斯蒂、充能球、生成牌、格挡冷却和受伤上限；橙色团块与历石 `2` 项按动态生成边界及纯表现源码完成静态闭环。覆盖目录未分析从 `70` 降至 `46`。
- `0.6.3`（适配批 055）：关闭 `26` 个未分析遗物回合生命周期条目，并纠正孙子兵法 `AfterCardPlayed` 的 `1` 个 RF ignored 假精确条目。`25` 项在同一可见游戏 PID 中完成五个最终请求、六条完整差分，覆盖首回合资源/Power/升级/伤害、孙子兵法攻击历史、第 2/3 回合能量、真假开心小花和真假古茶具套装私有状态、破损核心充能球及精英房条件；大帽子和十字弓 `2` 项按随机生成牌策略完成静态边界闭环。覆盖目录未分析从 `96` 降至 `70`。
- `0.6.3`（适配批 054）：关闭 `20` 个遗物条目。冰淇淋、三角铃鼓和坚固钳子补齐未来回合能量、整手保留和格挡封顶；异蛇头骨补齐中毒加算；化学物X、注能核心、带刺手甲及九项 RF 原生纯数值回退完成七场真实可见游戏组合差分。`18` 项计实机闭环，异蛇头骨表现钩子与添水药水获取边界 `2` 项计静态闭环；覆盖目录未分析从 `116` 降至 `96`。
- `0.6.3`（适配批 053）：关闭 `15` 个未分析遗物条目并纠正 `1` 个 RF ignored 假精确条目。补齐摆动球、怀表和花粉核心的分支私有计数、抽牌周期、怀表出牌历史、状态指纹与跨回合复用文本；四件确定性首回合生成遗物完成原生快照/后续回合差分，宝石面具、工具箱和怀表两个纯表现钩子完成静态闭环。覆盖目录未分析从 `131` 降至 `116`。
- `0.6.3`（适配批 052）：关闭 `31` 个遗物条目。`20` 个抽牌/最大能量纯 Hook 通过真实可见游戏差分，包含原生精英房轰鸣海螺及连续第 2/3 回合阈值；`11` 个金币、商店、地图、战后或纯表现钩子完成源码与构建静态闭环。新增跑局前/战斗建立后遗物注入夹具和模拟玩家回合计数，修复未来路线仍按实时第 1 回合计算遗物效果的问题。覆盖目录未分析从 `162` 降至 `131`。
- `0.6.3`（适配批 051）：关闭最后 `31` 个未分析 Power 钩子，并纠正 `6` 个 RF `RegisterIgnored` 或空处理被目录误计为精确支持的条目。补齐灾厄/减益触发、星能与能量实例余数、卡牌入场苦难、暗淡蓝点私有开关、回合末抽牌/死亡、集中变化边界，以及药水目标引起的包围朝向；唤醒、逃跑、额外回合、随机自动出牌和原生选牌统一在不可精确延续前返回动态边界。`31` 项完成真实可见游戏闭环，`6` 项只记源码与构建静态闭环。覆盖目录未分析从 `193` 降至 `162`，Power 类归零，剩余全部为遗物。
- `0.6.3`（适配批 050）：关闭 `37` 个 Power 死亡、移除与清格挡条目，其中 `33` 项在真实可见游戏中完成闭环，`4` 项完成反编译源码与构建静态闭环。补齐蟹之怒、自成型黏土、坚韧之环与饥饿的确定性结算；复活、召唤、换位、控制权和私有 AI 状态等无法静态延续的死亡效果统一在原生结算前返回动态边界，避免把死亡误判为战斗结束。开发期向错误宿主注入幻象和饥饿分别触发其原生宿主约束，改用原生怪物后通过，未增加兜底。覆盖目录未分析从 `230` 降至 `193`。
- `0.6.3`（适配批 049）：关闭 `24` 个 Power/遗物回合开始条目，其中 `22` 项完成最终 Release DLL 的真实可见游戏闭环，`2` 项虚空形态叠加瞬时时序完成源码与构建静态闭环。补齐固定生成、动态生成/选牌边界、奥斯蒂与缚魂命匣原生晚阶段顺序、充能球被动、随机目标 RNG、RF 私有计数重置与状态指纹、沙坑递减和强制致死；覆盖目录未分析从 `254` 降至 `230`。
- `0.6.3`（适配批 048）：新增 `27` 项独立适配证据，其中 `14` 个 Power 配对、回合末与死亡钩子完成四场 `10` 项真实差分，`13` 项完成源码与构建静态闭环。补齐独白力量回收、湮灭施加灾厄、魔法炸弹两种移除路径，以及神气制胜/胆小的 RF 私有计数重置；新增精确实例可变副本，修复本回合新施加实例无法在模拟回合末移除。覆盖目录未分析从 `277` 降至 `254`。
- `0.6.3`（适配批 047）：关闭 `21` 个 Power 条目。生产模拟改走游戏原生 `ModifyHandDraw`、`ModifyMaxEnergy`、`ShouldClearBlock` 与 `ShouldFlush` 纯钩子，覆盖 `11` 项数值和保留语义；补齐绯红披风完整玩家回合开始及野性中途施加历史，另静态关闭 `8` 个说明、VFX 或多人专属钩子。开发期资源回归发现 `DrawCardsNextTurnPower.AmountOnTurnStart` 时点缺失，修正后第 031 批资源与手牌生命周期全部复测通过；覆盖目录未分析从 `298` 降至 `277`。
- `0.6.3`（适配批 046）：关闭 `57` 个条目，覆盖 `13` 种附魔、等离子球、污染附着资格和最后 `42` 个怪物行动；新增附魔私有状态指纹、自动预出牌/手牌清空前附魔生命周期、等离子球回能，以及结构性怪物行动的敌方结算后动态边界。`14` 条附魔/Orb、`22` 条确定性怪物行动和 `3` 类正式搜索边界在真实可见游戏中通过；覆盖目录未分析从 `355` 降至 `298`。
- `0.6.3`（适配批 045）：关闭剩余 `19` 个药水 `OnUse` 和再生生命周期钩子；完成确定性数值、手牌/抽牌堆/弃牌堆选择、自动复活、最大生命、锻造、整副打击实例修改及动态生成后同回合重搜。`19` 个真实可见游戏场景与狡诈药水全自动闭环通过；差分发现并补齐 RF `0.13.8` 再生只治疗不递减。编译和覆盖基线同步到 RF `0.13.8`。
- `0.6.3`（适配批 043）：关闭最后 `30` 个 Card 未分析条目：`18` 个战斗入口完成真实可见游戏闭环，补齐奥斯蒂生命模型、选牌/生成、独立 Power、跨回合回手牌、出牌限制、抽牌前与自动预出牌阶段，以及虚空形态强制推进；`4` 个进入战斗费用初始化登记为原生运行时状态，`8` 个战后/描述/多人/VFX/地图钩子完成静态范围审计。开发期差分定位并修复独白漏触发下一张牌；无人脚手架补齐真实回合号推进和战斗中出牌记录锁存。
- `0.6.3`（适配批 042）：关闭 `24` 个卡牌 `OnPlay` 条目；在同一可见游戏 PID `46700` 中用最终 Release DLL 连续完成 `21` 个场景，逐字段比较全部敌人、四个牌堆、逐牌费用/升级/实例状态及 `6` 组战斗 RNG。补齐随机目标/插牌、击杀递归、群体伤害、毒触发、私有计数，以及选牌、可选空选择、变形、复制、局部费用和重复次数；新增长线实例状态同步加入搜索与跨回合指纹。开发期夹具常量和死亡敌人快照规范化问题均修正后整批从头复跑，最终 `21/21` 通过。
- `0.6.3`（适配批 041）：关闭 `27` 个卡牌 `OnPlay` 条目；`24` 个确定性牌效在同一可见游戏 PID 中完成 `6` 个场景、`7` 条最终差分，补齐 Power、能量/星能、临时集中及回收、Orb 种类计数、跨牌堆君王之剑、锻造、虚无、小刀、局部费用和整手弃牌；`3` 个多人专属条目完成静态范围审计。Power 分派按字母段拆出 `CardPowerOnPlaySupport.SToZ.cs`，避免继续把单文件堆长。开发期同步请求先暴露旧式 Orb JSON 错误，随后差分定位临时集中漏施加，增加生命周期检查后又修正无人脚手架的 `SynchronizePower` 回合末白名单；最终整批从头复测。
- `0.6.3`（适配批 040）：关闭 `31` 个卡牌 `OnPlay` 条目；`28` 个确定性牌效在同一可见游戏 PID 中完成 `6` 组最终差分，补齐 Power、能量/星能、延迟格挡、目标 Power 和追踪之刃生成/锻造，并将对应 RF 风险标为求解器已补偿；`3` 个多人专属条目完成静态范围审计。首次第二组夹具因注入 `11` 张手牌超过上限而失败，拆为 `10+1` 后重新通过，失败 run 未计入证据。
- `0.6.3`（适配批 039）：关闭 `26` 个卡牌 `OnPlay` 条目；`23` 个确定性牌效在两个可见游戏进程中完成 `13` 组差分，补齐临时力量/集中力、资源顺序、多层人工制品移除、双敌全体目标、生成牌及附魔、全牌堆升级、费用持续时间和整手弃牌替换；`3` 个多人专属条目完成静态范围审计。无人差分快照新增逐牌局部费用、附魔与升级等级。
- `0.6.3`（适配批 038）：关闭 `36` 个卡牌 `OnPlay` 条目；`25` 个确定性牌效在同一可见游戏 PID 中完成 `7` 组差分，补齐 Power、治疗、能量/星能、临时力量双 Power 和灾厄分段计算；`11` 个多人专属条目完成静态范围审计。简单 Power 施加从卡牌特殊效果分派中拆成独立文件，测试协议增加初始生命和最终生命断言。
- `0.6.3`（适配批 037）：关闭 `27` 个卡牌 `OnPlay` 条目；`26` 个确定性牌效在同一可见游戏 PID 中完成 `6` 组差分，补齐资源、条件 Power、锻造、手牌本回合零费，以及野性/杂耍中途施加的私有历史计数；`1` 个多人专属炽焰完成静态范围审计。
- `0.6.3`（适配批 036）：关闭 `20` 个卡牌 `OnPlay` 条目；`19` 个确定性能力/技能完成同一可见游戏进程的 `3` 组差分，补齐原生 Power、扩容槽位和预判临时敏捷生命周期；`1` 个多人专属希望灯塔完成静态范围审计。
- `0.6.3`（适配批 035）：关闭 `20` 个 RF 未镜像卡牌 `OnPlay`；补齐荆棘、槽位、施加顺序、友谊/幽魂形态 Power、锻造、震荡波动态值、萎靡 X 值与尖啸生命周期，并以同一可见游戏进程完成 `5` 组最终差分。
- `0.6.3`（适配批 034）：关闭 `33` 个覆盖目录条目；`22` 个单人 Power 钩子完成 `13` 组可见游戏差分，怪物攻击改走 RF 完整伤害管线并补齐奥斯蒂承伤、溢出、死亡保留与不可选中状态；另静态关闭 `11` 个多人专属钩子和 `1` 个纯表现钩子。无人测试请求改为原子发布，消除同进程连续投递的文件占用竞争。
- `0.6.3`（适配批 031）：同一可见游戏进程集中完成三场、十一条生产预测/实机差分；一次关闭 `22` 个覆盖目录条目，补齐 `21` 项下回合资源、费用/伤害/格挡修正、保留手牌时序和一次性 Power 生命周期，另登记 `1` 个纯表现钩子；同时删除搜索推进中重复触发敌方中毒的调用，该修正仍待独立完整 Beam 回归。
- `0.6.3`（适配批 030）：同一可见游戏进程集中完成五场、十一条生产预测/实机差分；补齐核心伤害、格挡、中毒、残影、双倍伤害、不可格挡、覆甲和缓慢共 `18` 项求解语义，并关闭 `2` 个纯表现钩子和 `1` 个求解开始前已进入原生状态的钩子。
- `0.6.3`（适配批 029）：一次可见游戏进程集中完成五场、九条生产预测/实机差分；补齐缩小、人工制品负数减益、缠结、昏眩和无实体共 `20` 项求解语义，并关闭 `3` 个纯表现钩子。
- `0.6.3`：卖血改按每回合最高可起防与路线实际起防的差额计算；加入连续自动搜索/执行的全自动按钮和战斗结束回合暂停开关；标题栏压缩且状态卡固定显示，路线窗口保留两条未换行回合的高度，状态详情合并到本局战损栏；编译依赖同步到 RandomForeseer `0.13.7`。
- `0.6.2`：本局扣血和主动卖血改为跨回合累计；普通、精英、Boss 阈值改为 `5/10/15`，并降低 Boss 为少量即时伤害卖血的优先级；节点状态改用双 64 位指纹，收紧 Beam、节点和选牌预算，补充累计战损与 GC 诊断日志。
- `0.6.1`：修复已有 mutable Power 被错误调用 `ToMutable()` 导致瀑布巨兽搜索失败；根据外部审计加入响应式布局、路线滚动、折叠收窄、部署步骤高亮、后台批次让步与怪物成员访问器缓存；自动搜索错开发牌动作队列，进度改用普通文本，减少搜索热路径临时分配，并增加分配/GC/让步遥测；整理 Git 仓库目录。
- `0.6.0`：加入严格状态文本匹配的跨回合路线复用；后台搜索降优先级、分时让出 CPU，并限制进度 UI 刷新；补偿愤怒、战斗专注、绯红披风、上勾拳和瀑布巨兽常规行动。
- `0.5.9`：终局不再保留能力牌未来收益；恢复胶囊内选牌和整颗击杀高亮；胶囊圆角增大。
- `0.5.8`：补偿噬尸蛞蝓饥饿死亡联动；重排本局扣血、主动卖血和计算详情。
- `0.5.7`：行动流改为独立胶囊，加入固定战损列和折叠模型说明。
- `0.5.6`：复用搜索快照，合并等价重复分支，增加真实回放计数。
- `0.5.5`：逐张标注击杀，绿色标注战斗结束回合。
- 更早版本：完成异步搜索、自动回合搜索、路线执行、动态洗牌边界、选牌支持、卖血权重、UI 覆盖层和模型覆盖诊断。
