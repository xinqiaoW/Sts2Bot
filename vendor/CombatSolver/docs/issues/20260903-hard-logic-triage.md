# 2026-09-03 硬逻辑问题包分诊

## 范围与结论

本次分诊覆盖 `D:\Download\Edgedownload` 中 2026-09-03 打包的四个 CombatSolver 报告包。四个外层包共包含 `309` 个内层问题包，提交编号和日志包名称均唯一，`反馈汇总.csv` 中全部标记为未解决。

这些问题集中在计划外重算、搜索计算失败、自动部署中止和回合准备选牌失败四个硬逻辑入口。当前 `0.28.3` 只有两条同一根因的计算失败：女王 Boss 中回放找不到 `MANGLE#0`；其余问题来自 `0.22.9–0.28.2`，不能直接当作当前版本仍然复现。

本文件是问题包分诊和后续修复排期，不代表问题包已经被完整回放，也不代表某项已经修复。本轮只整理证据，没有修改 CombatSolver 源码和测试。

## 0.29.0 修复回填

以下修复已经合入当前源码并分批推送：

- 搜索回放的卡牌身份、费用/选牌缓存和 MANGLE 的动作镜像：修复抽牌 Hook 的 COW 身份错位，并让回放找牌失败留下完整状态上下文。
- 部署实时漂移：计划手牌消失、回合所有权变化、原生页面候选/计划顺序/选择数量/页面生命周期不一致，统一关闭旧事务并按 `DeploymentDrift` 重捕获。
- 原生选牌事务：确认按钮等待页面完成布局；洗牌 Hook 在已有待处理选择时暂停后续监听器，分支消费后继续，避免同一事件写入冲突选择。
- 模拟生成敌人：生成新敌人前固定现有敌人快照，避免集合修改异常。

当前版本的回归证据见 `docs/TEST_MATRIX.md` 的 `0.29.0（开发中）` 区域。历史报告中由第三方模组 `ActsFromThePast.ConditionalBranchState` 提供但当前工作区没有该模组和条件语义，仍保持显式不支持；强制药水策略无法满足也属于用户策略约束，不是模拟器可自动修复的状态。

## 证据处理

- 原始 ZIP 保留在 `D:\Download\Edgedownload`，没有覆盖或改写。
- 外层包安全解压到 `.local/issue-bundles/combatsolver-reports-20260903-*/raw/`；已检查相对路径、路径穿越、符号链接/重解析点和加密条目，未发现异常。
- 每个内层包的文件清单和大小记录在对应目录的 `inventory.csv`；四个汇总表合并为 `.local/issue-bundles/20260903-aggregate.csv`。
- 分析优先使用外层 `反馈汇总.csv`、内层 `environment.json`、`replan-audit.txt` 和 `logs/00-godot.log` 的首个异常窗口。没有执行问题包内脚本或程序。
- 仓库没有通用的 `replay-state` / `native-state` 一键导入器，因此本文只写“包内日志显示”“包内状态可供后续 fixture 使用”，不写“已回放”。

已直接核对当前版本代表包 `CombatSolver-QUEEN_BOSS-20260903-211021-926.zip`：`environment.json` 记录 CombatSolver `0.28.3.0`、游戏 `0.111.0`、RitsuLib `0.5.18.0`、.NET `9.0.7`。其他版本数量以各包的 `反馈汇总.csv` 自动分类字段统计，尚未逐包读取全部环境文件。

## 外层包清单

| 外层包 | 内层问题包 | 主分类 | 原始大小 | 解压后内层总大小 | 接收时间范围 |
| --- | ---: | --- | ---: | ---: | --- |
| `combatsolver-reports-20260903-211919.zip` | 174 | 计划外重算 | 183,794,101 B | 216,031,569 B | 2026-09-02 09:53 至 2026-09-03 21:10 |
| `combatsolver-reports-20260903-212029.zip` | 65 | 计算失败 | 33,793,707 B | 35,843,542 B | 2026-09-02 10:15 至 2026-09-03 21:10 |
| `combatsolver-reports-20260903-212500.zip` | 48 | 自动执行中止 | 40,208,764 B | 44,242,300 B | 2026-09-02 10:44 至 2026-09-03 20:50 |
| `combatsolver-reports-20260903-212549.zip` | 22 | 回合准备选牌失败 | 11,266,253 B | 11,642,807 B | 2026-09-02 13:36 至 2026-09-03 19:36 |
| **合计** | **309** |  | **269,062,825 B** | **307,760,218 B** |  |

版本分布如下。版本号来自报告自动分类，不把文件名当作版本事实。

| 版本 | 计划外重算 | 计算失败 | 自动执行中止 | 回合准备选牌失败 | 合计 |
| --- | ---: | ---: | ---: | ---: | ---: |
| `0.22.9` | 1 | 0 | 0 | 0 | 1 |
| `0.23.0` | 1 | 0 | 0 | 0 | 1 |
| `0.24.0` | 0 | 2 | 0 | 0 | 2 |
| `0.25.0` | 1 | 0 | 0 | 0 | 1 |
| `0.25.1` | 16 | 7 | 7 | 0 | 30 |
| `0.25.2` | 24 | 1 | 2 | 0 | 27 |
| `0.25.3` | 10 | 22 | 1 | 2 | 35 |
| `0.26.0` | 0 | 2 | 0 | 0 | 2 |
| `0.27.1` | 16 | 3 | 6 | 2 | 27 |
| `0.27.2` | 42 | 3 | 10 | 6 | 61 |
| `0.28.0` | 21 | 5 | 5 | 6 | 37 |
| `0.28.1` | 18 | 6 | 10 | 3 | 37 |
| `0.28.2` | 24 | 12 | 7 | 3 | 46 |
| `0.28.3` | 0 | 2 | 0 | 0 | 2 |

## 分类一：计划外重算

### 数量和边界

计划外重算共有 `174` 条，报告中的首层分类全部为“状态不一致”，不是执行漂移。`replan-audit.txt` 的首个差异字段按族统计如下。每条只按首个字段计数，字段后续可能还有多个差异。

| 首个差异字段族 | 条数 | 说明 |
| --- | ---: | --- |
| 敌人生命、格挡或行动 `E*` | 56 | 敌人 HP、格挡、行动序列、MoveState 或怪物静态快照首先不同 |
| 手牌/选牌堆 `H[*]` | 50 | 手牌、抽牌堆或选牌后的卡牌身份/状态首先不同 |
| 怪物行动/AI | 18 | `AI*`、`MS*` 或 `R.monster_ai` 首先不同 |
| 其他战斗字段 | 18 | 能量、星能、目标、洗牌、基础 HP/格挡等首先不同 |
| Power 状态 `P[*]` | 15 | Power 层数或实例集合首先不同 |
| 卡牌集合/状态 `C[*]` | 10 | 牌堆卡牌状态、升级、保留或动态字段首先不同 |
| 无可读差异 | 4 | 审计只有计数，没有留下差异字段 |
| 遗物计数/镜像 | 2 | 遗物计数或镜像快照首先不同 |
| 耗尽/生成卡 `X[*]` | 1 | 耗尽堆或生成牌状态首先不同 |

高频遭遇是 `QUEEN_BOSS`（25）、`TEST_SUBJECT_BOSS`（23）和 `DECIMILLIPEDE_ELITE`（23）。这些是报告数量，不代表三类遭遇共享一个已经证明的根因。

### 重算伴随信号

下面是同一条报告中可能同时出现的附加信号，数字不互斥：

- `56` 条记录重算后预计战损上升，`53` 条因此触发全自动暂停。
- `11` 条明确记录手操偏离原路线后重算；另有 `6` 条记录计算期间状态变化导致过期结果丢弃。
- `5` 条同时记录回合准备计划与实机状态不一致。
- `10` 条记录找到更优世界线；这描述的是重算后的结果改善，不等于首个状态差异已经定位。
- `11` 条同时出现自动部署中止，另有各 `1` 条同时出现计算失败或选牌页面执行失败。

### 代表性首差异

以下样本来自 `0.28.2` 的 `replan-audit.txt`，只记录证据，不下根因结论：

| 遭遇 | 首个差异 | 提交编号 |
| --- | --- | --- |
| `MECHA_KNIGHT_ELITE` | `E0.hp` `272/274`，并伴随 `P[4]` 毒层 `3/1` | `5dd4ec811cc74be1a07fa1c9d38376c5` |
| `AXEBOTS_NORMAL` | 遗物计数 `UNSETTLING_LAMP` `1/0` | `5c15e69a6cb744d79adf79d40e75553e` |
| `KNIGHTS_ELITE` | `X[5]` 的 `SHIV` 耗尽/状态标志不同 | `6cbb9ad333954653a5c466edb39ad37d` |
| `LAGAVULIN_MATRIARCH_BOSS` | 敌人格挡、睡眠行动序列、MoveState 和 `ASLEEP_POWER` 同时不同 | `143da3c8cab344f3864e36c1d18323d1` |
| `TEST_SUBJECT_BOSS` | 手牌与抽牌堆位置同时错位 | `73b6d9d84a3a432093d9b8ceb52ab00c` |
| `PHROG_PARASITE_ELITE` | 手操偏离后 `H[2]` 从防御牌变成感染牌 | `b86d3aedbd9947e7a9215a7a7f30de04` |

后续应按首个差异字段族各选一个代表，而不是为 `174` 条报告逐包新增特判。手操偏离的记录还要先确认是否属于外部操作，不能直接归为模拟器偏差。

## 分类二：计算失败

`65` 条计算失败按首个明确异常分成以下互斥族：

| 根因族 | 条数 | 版本/现象 |
| --- | ---: | --- |
| 怪物行动 `EXPLODE_MOVE` 没有后继状态 | 21 | `0.25.3` 为 19 条，`0.26.0` 为 2 条；当前源码已有活体雾/瀑布巨兽终局行动回归记录，但本批旧包不作为当前版本证据 |
| 回放找不到手牌实例 | 16 | `UNLEASH` 6、`MANGLE` 2，其余为 `BLUDGEON`、`DEATH_MARCH`、`DEFEND_DEFECT`、`EXPECT_A_FIGHT`、`GLASSWORK`、`KINGLY_KICK`、`PROTECTOR`、`REFRACT` 各 1 |
| 最终路线遗物标注回放与选中状态不一致 | 13 | 其中 `0.28.2` 有 6 条，常见差异为 HP 或卡牌动态状态 |
| 回合开始计划选牌未触发 | 7 | 主要是 `KNOWLEDGE_DEMON/ApplyKnowledgeCurse`，版本集中在 `0.24.0–0.25.1` |
| 集合被修改或动态变量字典缺键 | 4 | `Collection was modified`、`Repeat`、`WeakPower`、`PlatingPower` 各 1 |
| 搜索内存/Beam 容量错误 | 2 | `OutOfMemoryException` 1 条，策略必需分支无法保留 1 条 |
| 强制药水策略无法满足 | 1 | `PotionPolicyUnsatisfiedException` |
| 未支持的怪物状态 | 1 | `ActsFromThePast.ConditionalBranchState` |

### 当前版本 P0：MANGLE 回放找不到

两条 `0.28.3` 报告完全相同：

| 遭遇 | 日志包 | 提交编号 | 自动分类 |
| --- | --- | --- | --- |
| `QUEEN_BOSS` | `CombatSolver-QUEEN_BOSS-20260903-211021-926.zip` | `63999e55c1e845afad6c6e600cac043a` | 计算失败 2 次：回放找不到 `MANGLE#0` |
| `QUEEN_BOSS` | `CombatSolver-QUEEN_BOSS-20260903-211002-831.zip` | `67da331be7224e5fbb239d03533c70c9` | 计算失败 2 次：回放找不到 `MANGLE#0` |

包内 `logs/00-godot.log` 的调用链是搜索 worker 完成候选后进入最终路线回放，在 `CombatBeamSolver.Replay` 抛出“回放时找不到手牌 MANGLE#0”。同一日志还出现“未注册 `Mangle` 的 `OnPlay` mirror，尝试 best-effort 推断”的信息。这是当前最强的修复线索，但仍需用包内战前状态构造 fixture，确认是未注册动作镜像造成的卡牌实例/牌堆身份偏差，还是最终路线释放后回放索引失效；目前不能把日志提示直接写成已证实根因。

建议边界：根捕获的 `MANGLE+1` 实例 → 搜索动作回放 → 终局候选物化 → `Replay` 卡牌 occurrence 匹配。验收应包含两次搜索代际和最终路线回放，不只断言“搜索没有抛异常”。

## 分类三：自动执行中止

`48` 条自动部署中止按异常入口分为：

| 根因族 | 条数 | 具体证据 |
| --- | ---: | --- |
| 部署找不到计划中的手牌状态 | 38 | `35` 个不同卡牌名，重复最多的是 `ACCELERANT`、`MACHINE_LEARNING`、`SQUASH`；样本包括 `HAILSTORM`、`STONE_ARMOR`、`STRIKE_SILENT`、`FOOTWORK` |
| 原生选牌页面找不到计划卡牌 | 5 | `GREED` 2，`ADRENALINE`、`ACROBATICS`、`STRIKE_REGENT` 各 1 |
| 原生选牌计划没有被游戏请求或页面无法完成 | 2 | `KNOWLEDGE_DEMON/ApplyKnowledgeCurse`、`COSMIC_INDIFFERENCE` |
| 原生手牌选择完成后确认按钮不可用 | 1 | `BYGONE_EFFIGY` |
| 部署途中已经不是玩家回合 | 2 | 部署时序/控制权边界 |

该类问题的入口在自动部署和原生选牌会话，不应与搜索质量或卡牌评分混为一谈。`0.28.2` 有 `7` 条，`0.28.3` 没有；最接近当前版本的样本是 `GLOBE_HEAD_NORMAL` 中 `HAILSTORM@0`、`AXEBOTS_NORMAL` 中 `GREED+0#0`，以及知识恶魔的未请求计划。

建议边界：保存路线后的 live 手牌身份匹配、同名卡 occurrence、原生选牌候选观测、确认按钮可用性和玩家回合所有权。每种入口选一个代表，不按缺失卡牌名称分别修复。

## 分类四：回合准备选牌失败

`22` 条回合准备选牌失败分为：

| 根因族 | 条数 | 代表现象 |
| --- | ---: | --- |
| 回放找不到手牌 | 6 | `CLOAK_AND_DAGGER`、`HOWL_FROM_BEYOND`、`MEMENTO_MORI`、`PROTECTOR`、`REAP`、`REND` 各 1 |
| 模拟状态已有待处理选牌 | 2 | `pending=STRATAGEM_POWER` 后又创建 `TOASTY_MITTENS` 等新选择 |
| 原生选牌计划数量与页面要求不一致 | 2 | 计划选择 `1` 张，页面要求 `2..2` 张 |
| 原生选牌页找不到计划卡牌 | 2 | `HOWL_FROM_BEYOND`、`DEBT` |
| 最终路线遗物标注回放不一致 | 2 | 选牌准备阶段触发终局标注回放失败 |
| 强制药水策略无法满足 | 1 | 回合准备阶段直接暴露药水政策失败 |
| 收到计划外原生选择 | 4 | `HAND_TRICK` 2、`PREPARED` 1、`SURVIVOR` 1 |
| 原生手牌确认按钮不可用 | 1 | 页面完成选择后确认仍不可用 |
| 回合准备页面等待超时 | 1 | 页面完成后 30 秒未进入可复用状态 |
| `SearchNode` 重复键 | 1 | 同一搜索节点键重复加入字典 |

`0.28.2` 有 `3` 条：`MEMENTO_MORI` 回放找不到、`SURVIVOR` 计划外选择和确认按钮不可用；`0.28.3` 暂无该类报告。这里既有模拟回放错误，也有原生页面/控制器错误，后续应先按入口拆开。

## 当前修复排期

### P0：先复现当前版本

1. `0.28.3` Queen/MANGLE 两包：同根、当前版本、搜索失败后没有路线，是第一优先级。

### P1：验证近期版本是否已被当前源码覆盖

1. `0.28.2` 遗物标注回放不一致（6 条），先用 Queen、Scrolls 或 Haze/Fight Me 样本确认当前终局回放是否仍有状态差异。
2. `0.28.2` 自动部署找不到计划手牌（4 条）和原生选牌页找不到 `GREED`（2 条），分别覆盖状态身份匹配和原生页面候选。
3. `0.28.2` 的 `Collection was modified`、`OutOfMemoryException`、未支持怪物状态和 `0.28.2` 的 2 条卡牌回放失败，先确认当前源码的失败入口是否仍然出现；不因旧日志直接扩大预算或静默跳过。

### P2：历史共享回归

1. `EXPLODE_MOVE` 无后继状态、Knowledge Demon 计划选牌未触发、旧版动态变量缺键等已有较新代码和测试记录的项目，作为共享回归哨兵复核，不按旧版本数量重复开新问题。
2. 其余 `0.22.9–0.27.2` 计划外重算，按 `E* / H[*] / AI / P[*] / C[*]` 首差异族各选一个代表；手操偏离样本单独保留，不能混入模拟器偏差统计。

## 后续 fixture 入口

当前只整理入口，尚未执行以下 fixture：

| 根因族 | 建议 fixture 边界 | 必须断言 |
| --- | --- | --- |
| MANGLE 回放失败 | Queen 战前根、`MANGLE+1`、最终候选物化和卡牌 occurrence | 搜索 worker、路线回放和第二次搜索均不抛异常，卡牌实例身份一致 |
| 遗物标注回放 | Queen/Scrolls 的选中路线与终局标注回放 | 标注回放与选中状态的首个字段差异为空，不只比较 HP |
| 怪物 `EXPLODE_MOVE` | 活体雾或瀑布巨兽终局行动 | 怪物离开活动阵容后不解析不存在的后继行动，战斗可正常结束 |
| 回合开始计划选牌 | Knowledge Demon 的 `ApplyKnowledgeCurse` | 选择只在正确的玩家回合开始阶段消费，不能被结束回合复核提前要求 |
| 自动部署手牌找不到 | `HAILSTORM`/`STONE_ARMOR` 这类同名或状态变化卡牌 | live 手牌匹配使用路线保存的实例状态和 occurrence，失败时保留上下文并停止部署 |
| 原生选牌页面 | `GREED`、`HAND_TRICK`、`SURVIVOR` 代表页 | 候选观测、计划令牌、选择数量和确认按钮状态一致 |
| 模拟待处理选牌 | `STRATAGEM_POWER` 与后续 `TOASTY_MITTENS` | 已有 pending choice 时拒绝创建冲突选择，错误落在正确的回合准备边界 |
| 计划外重算 | 每个首差异字段族一份短根状态 | 找到最后正确检查点和第一个差异字段；不以“重算后路线更好”掩盖前置状态偏差 |

## 未执行与限制

- 未修改源码、测试、版本号或发布文档。
- 未执行问题包内任何脚本或可执行文件。
- 未运行通用问题包导入器；当前仓库没有该能力，因此没有声称完整回放、实际修复或当前版本全部复现。
- 未为 `309` 条报告分别启动完整战斗；后续按共享根因去重，每类先选一个最小 fixture。
- 原始问题包、完整日志、状态文件和本次 `.local/issue-bundles/` 证据不进入源码提交。
