# 2026-09-04 19:00 后问题包分诊

## 范围与证据

本次检查的是 `D:\Download\Edgedownload` 中 19:07:00 至 19:09:08 下载的 5 个汇总包：

- `combatsolver-reports-20260904-190700.zip`
- `combatsolver-reports-20260904-190758.zip`
- `combatsolver-reports-20260904-190820.zip`
- `combatsolver-reports-20260904-190848.zip`
- `combatsolver-reports-20260904-190908.zip`

5 个包中的 `反馈汇总.csv` 共包含 173 条互不重复的日志记录，接收时间覆盖 2026-09-03 21:25 至 2026-09-04 18:50。包名的下载时间是筛选依据；CSV 内的日志接收时间早于下载时间，不把两者混为一谈。

当前版本记录：32 条为 CombatSolver `0.29.0`，其余来自 `0.28.3`、`0.28.2`、`0.27.2` 和 `0.27.1`。问题包没有通用的一键状态回放器，本次结论来自汇总描述、问题包内 `replan-audit.txt`、`godot.log`、回合检查点和原始玩家备注；没有把静态取证写成完整战斗回放。

## 全量归类

下表按首个硬错误互斥归类，因此每条记录只计一次；同一条描述中的附加标签不重复计数。

| 首个硬错误 | 数量 | 处理方向 |
| --- | ---: | --- |
| 计算失败 | 26 | 先按异常签名分组，修复根因后用一个代表包验证 |
| 回合准备选牌失败 | 10 | 回合准备原生选牌会话、计划消费和时序 |
| 回合准备状态不一致 | 16 | 回合开始 Hook、能量、手牌和敌方状态快照边界 |
| 自动执行中止 | 22 | 部署前状态重核对、药水槽位和原生选牌漂移 |
| 计划外重算 | 99 | 只在真实状态变化时重算，先排除上面各类状态漂移根因 |

`计划外重算`、`重算后战损上升`、`找到更优世界线` 等是结果标签，不代表每次都有独立模拟错误；修复顺序以首个状态差异为准。

## 0.29.0 当前版本重点

32 条当前版本记录的主要签名如下。计数允许一条记录同时带有重算和下列附加故障，因此这里用于确定代表夹具，不作总数相加。

| 签名 | 数量 | 代表日志/证据 |
| --- | ---: | --- |
| 计划外重算或执行漂移 | 22 | `CONSTRUCT_MENAGERIE` 的 `TRACKING` 原生选牌漂移；`DECIMILLIPEDE`、`THE_INSATIABLE`、`SOUL_FYSH` 等重算后战损上升 |
| 回合准备状态不一致 | 2 条明确首报，另有重算记录附带该标签 | `ENTOMANCER` 工具箱后 `H[4]` 预期 `DEATHBRINGER`、实机为 `SOUL`；`KNOWLEDGE_DEMON` 能量预期 3、实机 4 |
| 回合准备选牌/原生选牌失败 | 1 条首报，另有附带记录 | `ENTOMANCER` `turn_setup:11` 收到计划外 `SURVIVOR/Hand`；`KAISER_CRAB` 选牌页找不到 `FINISHER` |
| 回放时找不到手牌 | 3 | `NO_ESCAPE#0`、`UNLEASH#0`、`COMET#0`，均发生在后续回合动作回放 |
| `Repeat` 动态变量缺失 | 2 | `KAISER_CRAB`、`THE_OBSCURA` 报 `KeyNotFoundException` |
| 部署药水槽位为空 | 1 | `WATERFALL_GIANT` 部署时槽位 3 为空 |
| 原生/第三方环境噪声 | 多条日志出现 | `MegaSpine.SetAnimation`、`MegaLabel` 字体覆盖和 KitLib patch 警告；除非能证明改变 CombatSolver 状态，否则不作为本批逻辑修复目标 |

## 玩家备注逐条归档

以下保留玩家原话，并按首个可验证方向归类。原话是证据，不是已经确认的根因。

### 回合开始选牌与时序（最高优先级）

这些记录直接覆盖烘焙手套、工具箱、选择悖论、必备工具、暴政以及同一套 `turn_setup` 生命周期。

- `ENTOMANCER_ELITE-20260904-173037-704`：**“工具箱选牌后会重新计算”**。日志：`TURN_SETUP_STATE_MISMATCH`，手牌槽 `H[4]` 预期 `DEATHBRINGER`、实机 `SOUL`。
- `KNOWLEDGE_DEMON_BOSS-20260904-171204-677`：**“不自动选知识恶魔的选项”**。日志：第 2 回合能量预期 3、实机 4；说明知识恶魔选项完成与下一回合根捕获的边界仍有空窗。
- `QUEEN_BOSS-20260904-152308-189`：**“应该是没有烘焙手套和必工具的顺序问题导致”**。原生回合准备确认按钮不可用。
- `QUEEN_BOSS-20260904-153031-648`：**“无限后，计策不选牌了”**。需要检查重复/无限出牌后的计策选牌请求是否仍登记并消费。
- `KNIGHTS_ELITE-20260904-010339-923`：**“回合准备选牌失败”**。旧版本 `turn_setup` 失败，作为跨版本回归样本。
- `DEVOTED_SCULPTOR_WEAK-20260904-131448-373`：**“出现算法之外的情况，系统崩溃”**。该包没有开始搜索，需先确认是否是原生页面/外部模组异常，不能直接归因搜索。

### 选牌后动作与卡牌实例回放

- `TEST_SUBJECT_BOSS-20260904-164954-001`：**“复制药水两层计策后的全部捞2次牌都没有反应”**。当前版本出现两次状态不一致；回合开始选择和捞牌动作的实例身份、连续选择消费需要一起核对。
- `QUEEN_BOSS-20260904-161303-019`：**“ai显示里打出欺凌，但实际历史课在下回合重放了头槌，捞牌界面需要让我自己选择”**。实机与预测在 `D[0]`、`C[0]` 交换，涉及历史课记录、头槌前置弃牌和下一回合原生捞牌控制权。
- `ENTOMANCER_ELITE-20260904-133457-519`：**“在‘怀旧’将第一张使用的‘耸肩无视’置于牌堆顶后无法被头槌选中，报错”**。需要核对历史牌置顶后的牌堆实例是否仍可被头槌候选匹配。
- `TEST_SUBJECT_BOSS-20260904-164128-704`：**“无伤通关”**。这是玩家手操质量反馈，不是失败；日志记录手操后预计战损 `97 → 0`，应作为路线质量基线，不把它当作异常修复完成。

### 重算、战损与复活状态

- `LAGAVULIN_MATRIARCH_BOSS-20260904-154128-137`：**“应该是重放碎骨导致的bug”**。状态差异为 `osty.hp` 预期 21、实机 15。
- `BOWLBUGS_NORMAL-20260904-150227-355`：**“计划外重算”**。首个差异为 `C[6]` 的 `ASCENDERS_BANE` 与 `BLADE_DANCE` 交换。
- `THE_LOST_AND_FORGOTTEN_NORMAL-20260904-144242-340`：**“出现计划外重算”**。抽牌堆与弃牌堆出现 `DEFEND_SILENT`/`ASCENDERS_BANE` 位置交换。
- `TEST_SUBJECT_BOSS-20260904-100803-460`：**“出现计划外重算”**。同时出现牌堆数量变化和 `R.shuffle` 计数差异。
- `QUEEN_BOSS-20260904-021815-155`：**“重算增加战损”**。状态差异是卡牌 `BOUND` 标记丢失。
- `MECHA_KNIGHT_ELITE-20260904-020138-239`：**“重算战损增加”**。能量预期 6、实机 7，需检查回合结束/回合开始能量结算顺序。
- `DECIMILLIPEDE_ELITE-20260904-014839-108`：**“重算后战损增加，怀疑处理复活出现问题”**。复活敌人的 HP、行动序列、死亡标记和怪物 RNG 同时分叉。
- `DECIMILLIPEDE_ELITE-20260904-014256-811`：**“出现计划外的重算，可能是忽略了加强的蛇戒或者纸鹤的影响”**。复活对象行动预期 `DEAD_MOVE`、实机 `REATTACH_MOVE`。
- `DECIMILLIPEDE_ELITE-20260903-220418-123`：**“还是没有解决打千足虫复活之后会需要重新计算的问题”**。与上一条共享复活后 roster/AI/RNG 根因，合并为一个代表夹具。
- `INFESTED_PRISMS_ELITE-20260904-000307-670`：**“计划外重算”**。玩家 HP 预期 81、实机 82。
- `AXEBOTS_NORMAL-20260903-235907-074`：**“巨斧机器人容易触发计划外重算”**。敌人 HP 预期 82、实机 61，需先确认是否为动作回放失败后的派生差异。
- `PHROG_PARASITE_ELITE-20260903-221237-284`：**“冰雹风暴杀大蛆导致？”**。四个敌人的 HP 均出现差异，重点检查群体伤害和死亡/召唤结算顺序。
- `CORPSE_SLUGS_WEAK-20260903-214355-675`：**“出现计划外重算”**。敌方 `STRENGTH_POWER` 预期 10、实机 5。
- `INFESTED_PRISMS_ELITE-20260903-213731-867`：**“计划外重算”**。首个差异是 `CALCULATED_GAMBLE` 与 `FOOTWORK` 的牌堆位置交换。

### 最终路线和系统错误

- `QUEEN_BOSS-20260904-114318-068`：**“遗物标注回放与选中状态不一致”**。属于最终路线二次回放状态一致性，不能用 UI 文本修补。
- `KNIGHTS_ELITE-20260904-151145-156`：**“求解器记录到异常”**。需要以包内实际异常为准，不以玩家概括替代堆栈。
- `BOWLBUGS_NORMAL-20260904-150227-355`、`THE_LOST_AND_FORGOTTEN_NORMAL-20260904-144242-340`、`TEST_SUBJECT_BOSS-20260904-100803-460`、`QUEEN_BOSS-20260904-021815-155`、`MECHA_KNIGHT_ELITE-20260904-020138-239`、`INFESTED_PRISMS_ELITE-20260904-000307-670`、`AXEBOTS_NORMAL-20260903-235907-074`、`PHROG_PARASITE_ELITE-20260903-221237-284`、`CORPSE_SLUGS_WEAK-20260903-214355-675`、`INFESTED_PRISMS_ELITE-20260903-213731-867`：玩家只写了“计划外重算/重算增加战损”等概括，统一按上面的首个状态差异处理，不重复建立夹具。

## 修复顺序与验证边界

1. **先修回合开始时序**：统一烘焙手套、工具箱、选择悖论、必备工具、暴政、知识恶魔和生存者的原生选牌请求登记、计划消费、确认回调、下一回合根捕获和单步/全自动控制边界。重点保证选牌页面尚未完成时不提前建立错误 continuation，也不把已经完成的选择再次当成计划外选择。
2. **再修卡牌实例与牌堆回放**：覆盖捞牌、历史课、头槌、怀旧、计策和复制药水后的连续选择；动作回放必须按实例身份和合法牌堆位置匹配。
3. **修复硬异常签名**：补齐 `Repeat` 动态变量来源，修复 `NO_ESCAPE`/`UNLEASH`/`COMET` 等手牌回放，恢复复活敌人的 roster、死亡标记、行动队列和 RNG 一致性，修复药水槽位及遗物标注二次回放。
4. **最后处理重算与战损结果**：前述状态差异消失后，重新判断剩余的计划外重算和战损上升；不以放宽比较或吞异常降低数量。

同一根因只保留一个代表问题包做 L1/L2 语义验证；回合开始/Fork/跨回合状态涉及 L2。当前文档只记录分诊结论，尚未声称任何一项已经修复。
